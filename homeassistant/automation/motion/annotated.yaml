###
# homeassistant/automation/motion/annotated.yaml
###

## counter 
- id: motion_annotated_counter
  alias: motion_annotated_counter
  initial_state: on
  trigger:
    platform: state
    entity_id: binary_sensor.motion_annotated
  condition:
    - condition: template
      value_template: >-
        {{ is_state('binary_sensor.motion_annotated','on') }}
  action:
    - service: counter.increment
      entity_id: counter.motion_annotated_counter
 
## snapshot
- id: motion_annotated_snapshot
  alias: motion_annotated_snapshot
  initial_state: on
  trigger:
    platform: state
    entity_id: sensor.motion_annotated_camera
  condition:
    condition: and
    conditions:
      - condition: template
        value_template: >-
          {{ is_state('input_boolean.motion_snapshot_save','on') }}
      - condition: template
        value_template: >-
          {% set ep = state_attr('camera.motion_event_annotated','entity_picture') %}
          {{ ep is not none and ep|lower != 'unavailable' and ep|lower != 'none' and ep|lower != 'unknown' }}
      - condition: template
        value_template: >-
          {% set c = states('sensor.motion_annotated_camera') %}
          {% if c is not none and c|lower != 'unavailable' and c|lower != 'unknown' and c != 'null' %}
            {% set ep = state_attr('camera.motion_event_annotated_' + c,'entity_picture') %}
            {{ ep is none or p|lower != 'unavailable' or ep|lower != 'none' or ep|lower != 'unknown' }}
          {% else %}false{% endif %}
  action:
    # annotated
    - service: camera.snapshot
      data_template:
        entity_id: >-
          {{ 'camera.motion_event_annotated' }}
        filename: >-
          {% set c = states('sensor.motion_annotated_camera') %}
          {{ '/config/www/images/motion_annotated' + c + '.jpg' }}
    - service: local_file.update_file_path
      data_template:
        entity_id: >-
          {{ 'camera.motion_annotated' }}
        file_path: >-
          {% set c = states('sensor.motion_annotated_camera') %}
          {{ '/config/www/images/motion_annotated' + c '.jpg' }}

- id: motion_annotated_camera_composite
  alias: motion_annotated_camera_composite
  initial_state: on
  trigger:
    platform: state
    entity_id: sensor.motion_annotated_camera
  condition:
    condition: and
    conditions:
      - condition: template
        value_template: >-
          {{ is_state('binary_sensor.motion_annotated','on') }}
      - condition: template
        value_template: >-
          {{ is_state('input_boolean.motion_snapshot_save','on') }}
      - condition: template
        value_template: >-
          {% set ep = state_attr('camera.motion_end_composite','entity_picture') %}
          {{ ep is not none and ep|lower != 'unavailable' and ep|lower != 'none' and ep|lower != 'unknown' }}
      - condition: template
        value_template: >-
          {% set c = states('sensor.motion_annotated_camera') %}
          {% if c is not none and c|lower != 'unavailable' and c|lower != 'unknown' and c != 'null' %}
            {% set ep = state_attr('camera.motion_event_annotated_' + c,'entity_picture') %}
            {{ ep is none or p|lower != 'unavailable' or ep|lower != 'none' or ep|lower != 'unknown' }}
          {% else %}false{% endif %}
  action:
    - service: camera.snapshot
      data_template:
        entity_id: >-
          {{ 'camera.motion_end_composite' }}
        filename: >-
          {% set c = states('sensor.motion_annotated_camera') %}
          {{ '/config/www/images/motion_annotated_composite' + c + '.jpg' }}
    - service: local_file.update_file_path
      data_template:
        entity_id: >-
          {{ 'camera.motion_annotated_composite' }}
        file_path: >-
          {% set c = states('sensor.motion_annotated_camera') %}
          {{ '/config/www/images/motion_annotated_composite-' + c + '.jpg' }}

- id: motion_annotated_camera_animated
  alias: motion_annotated_camera_animated
  initial_state: on
  trigger:
    platform: state
    entity_id: sensor.motion_annotated_camera
  condition:
    condition: and
    conditions:
      - condition: template
        value_template: >-
          {{ is_state('binary_sensor.motion_annotated','on') }}
      - condition: template
        value_template: >-
          {{ is_state('input_boolean.motion_snapshot_save','on') }}
      - condition: template
        value_template: >-
          {% set ep = state_attr('camera.motion_end_animated','entity_picture') %}
          {{ ep is not none and ep|lower != 'unavailable' and ep|lower != 'none' and ep|lower != 'unknown' }}
      - condition: template
        value_template: >-
          {% set c = states('sensor.motion_annotated_camera') %}
          {% if c is not none and c|lower != 'unavailable' and c|lower != 'unknown' and c != 'null' %}
            {% set ep = state_attr('camera.motion_event_animated_' + c,'entity_picture') %}
            {{ ep is none or p|lower != 'unavailable' or ep|lower != 'none' or ep|lower != 'unknown' }}
          {% else %}false{% endif %}
  action:
    - service: camera.snapshot
      data_template:
        entity_id: >-
          {{ 'camera.motion_end_animated' }}
        filename: >-
          {% set c = states('sensor.motion_annotated_camera') %}
          {{ '/config/www/images/motion_annotated_animated-' + c + '.gif' }}
    - service: local_file.update_file_path
      data_template:
        entity_id: >-
          {{ 'camera.motion_annotated_animated' }}
        file_path: >-
          {% set c = states('sensor.motion_annotated_camera') %}
          {{ '/config/www/images/motion_annotated_animated-' + c '.gif' }}

- id: motion_annotated_camera_animated_mask
  alias: motion_annotated_camera_animated_mask
  initial_state: on
  trigger:
    platform: state
    entity_id: sensor.motion_annotated_camera
  condition:
    condition: and
    conditions:
      - condition: template
        value_template: >-
          {{ is_state('binary_sensor.motion_annotated','on') }}
      - condition: template
        value_template: >-
          {{ is_state('input_boolean.motion_snapshot_save','on') }}
      - condition: template
        value_template: >-
          {% set ep = state_attr('camera.motion_end_animated_mask','entity_picture') %}
          {{ ep is not none and ep|lower != 'unavailable' and ep|lower != 'none' and ep|lower != 'unknown' }}
      - condition: template
        value_template: >-
          {% set c = states('sensor.motion_annotated_camera') %}
          {% if c is not none and c|lower != 'unavailable' and c|lower != 'unknown' and c != 'null' %}
            {% set ep = state_attr('camera.motion_event_animated_mask' + c,'entity_picture') %}
            {{ ep is none or p|lower != 'unavailable' or ep|lower != 'none' or ep|lower != 'unknown' }}
          {% else %}false{% endif %}
  action:
    - service: camera.snapshot
      data_template:
        entity_id: >-
          {{ 'camera.motion_end_animated_mask' }}
        filename: >-
          {% set c = states('sensor.motion_annotated_camera') %}
          {{ '/config/www/images/motion_annotated_animated_mask-' + c + '.gif' }}
    - service: local_file.update_file_path
      data_template:
        entity_id: >-
          {{ 'camera.motion_annotated_animated_mask' }}
        file_path: >-
          {% set c = states('sensor.motion_annotated_camera') %}
          {{ '/config/www/images/motion_annotated_animated_mask-' + c + '.gif' }}
