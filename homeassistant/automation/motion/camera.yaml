###
## homeassistant/automation/motion/camera.yaml
###

## set options for cameras
- id: motion_camera_name_options
  alias: motion_camera_name_options
  initial_state: on
  trigger:
    platform: state
    entity_id: sensor.motion_camera_name_list
  condition:
    - condition: template
      value_template: >-
        {% set v = states('sensor.motion_camera_name_list') %}
        {{ v|lower != 'unavailable' and v|lower != 'none' and v|length > 0 }}
  action:
    - service: python_script.input_select_set_options
      data_template:
        entity_id: input_select.motion_camera_name
        options: >-
          {{ states('sensor.motion_camera_name_list') }}

###
## autoselect current camera when detected_entity
###

- id: motion_camera_autoselect_detected_entity
  alias: motion_camera_autoselect_detected_entity
  initial_state: on
  trigger:
    platform: state
    entity_id: counter.motion_detected_entity_counter
  condition:
    - condition: template
      value_template: >-
        {{ is_state('binary_sensor.motion_camera_autoselect','on') }}
  action:
    - service: input_select.select_option
      data_template:
        entity_id: input_select.motion_camera_name
        option: >-
          {{ states('sensor.motion_detected_entity_camera') }}
