###
# homeassistant/automation/motion/person_occupancy.yaml
###

- id: motion_person_occupancy_previous
  alias: motion_person_occupancy_previous
  initial_state: on
  trigger:
    - platform: state
      entity_id: sensor.motion_person_occupancy
  condition:
    condition: and
    conditions:
      - condition: template
        value_template: >
          {% set s = states('sensor.motion_person_occupancy') %}
          {# s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' and s|lower != 'pending' #}
          {{ s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' }}
  variables:
    latitude: >-
      {% set s = state_attr('sensor.motion_person_occupancy','latitude') %}
      {% if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' %}
        {{ s }}
      {% else %}null{% endif %}
    longitude: >-
      {% set s = state_attr('sensor.motion_person_occupancy','longitude') %}
      {% if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' %}
        {{ s }}
      {% else %}null{% endif %}
    id: >-
      {% set s = state_attr('sensor.motion_person_occupancy','id') %}
      {% if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' %}
        {{ s }}
      {% else %}null{% endif %}
    movement: >-
      {% set s = states('sensor.motion_person_occupancy_previous') %}
      {% if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' %}
        {% set s = state_attr('sensor.motion_person_occupancy_previous','latitude') %}
        {% if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' %}
          {% set plat = s|float %}
          {% set s = state_attr('sensor.motion_person_occupancy_previous','longitude') %}
          {% if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' %}
            {% set plng = s|float %}
            {% set s = state_attr('sensor.motion_person_occupancy','latitude') %}
            {% if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' %}
              {% set clat = s|float %}
              {% set s = state_attr('sensor.motion_person_occupancy','longitude') %}
              {% if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' %}
                {% set clng = s|float %}
                {% set d = distance(plat, plng, clat, clng) %}
                {% if d|int is number and d|int > 0 and d|int < 40075 %}
                  {{- '%0.2f'|format(d * 1000) -}}
                {% else %}
                  {{ range(0,state_attr('sensor.motion_person_occupancy','gps_accuracy'))|random }}
                {% endif %}
              {% else %}null{% endif %}
            {% else %}null{% endif %}
          {% else %}null{% endif %}
        {% else %}null{% endif %}
      {% else %}null{% endif %}
    interval: >-
      {% set t = state_attr('sensor.motion_person_occupancy_previous','timestamp') %}
      {% if t|lower != 'none' and t|lower != 'unknown' and t|lower != 'unavailable' and t|lower != 'null' and t|float is number and t|float > 0 %}
        {{ utcnow().timestamp() - t|float }}
      {% else %}null{% endif %}
    velocity: >-
      {% set d = movement %}
      {% if d|lower != 'null' and d|int is number and d|int < 40075 %}
        {% set t = interval %}
        {% if t|lower != 'null' and t|float is number and t|float > 0 %}
          {{- '%0.2f'|format(d|float/t|float) -}}
        {% else %}null{% endif %}
      {% else %}null{% endif %}
  action:
    - alias: 'known sensor?'
      condition: and
      conditions:
        - condition: template
          value_template: >-
            {{ id|lower != 'null' }}
    - alias: 'set sensor.motion_person_occupancy_previous'
      service: python_script.set_state
      data_template:
        entity_id: sensor.motion_person_occupancy_previous
        latitude: >-
          {{ latitude }}
        longitude: >-
          {{ longitude }}
        id: >-
          {{ id }}
        movement: >-
          {{ movement }}
        interval: >-
          {{ interval }}
        velocity: >-
          {{ velocity }}
        timestamp: >-
          {{ utcnow().timestamp() }}
        distance: >-
          {{ state_attr('sensor.motion_person_occupancy','distance') }}
        sensor: >-
          {{ state_attr('sensor.motion_person_occupancy','sensor') }}
        area: >-
          {{ state_attr('sensor.motion_person_occupancy','area') }}
        state: >-
          {{ states('sensor.motion_person_occupancy') }}

- id: motion_person_occupancy
  alias: motion_person_occupancy
  initial_state: on
  trigger:
    - platform: state
      entity_id: sensor.motion_shellies_occupancy
  condition:
    condition: and
    conditions:
      - condition: template
        value_template: >
          {% set s = states('sensor.motion_shellies_occupancy') %}
          {# s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' and s|lower != 'pending' #}
          {{ s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' }}
  variables:
    location: >-
      {% set s = states('sensor.motion_person_location') %}
      {% if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' %}
        {{ s }}
      {% else %}null{% endif %}
    sensor: >-
      {% set s = state_attr('sensor.motion_shellies_occupancy','sensor') %}
      {% if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' %}
        {{ s }}
      {% else %}null{% endif %}
    id: >-
      {% if sensor|lower != 'null' %}
        {% set s = state_attr(sensor,'id') %}
        {% if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' %}
          {{ s }}
        {% else %}null{% endif %}
      {% else %}null{% endif %}
    ago: >-
      {% if sensor|lower != 'null' %}
        {% set s = state_attr(sensor,'last') %}
        {% if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' %}
          {{ utcnow().timestamp()|int - s|int }}
        {% else %}null{% endif %}
      {% else %}null{% endif %}
    duration: >-
      {% if sensor|lower != 'null' %}
        {% set s = state_attr(sensor,'duration') %}
        {% if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' %}
          {{ s|int }}
        {% else %}null{% endif %}
      {% else %}null{% endif %}
    latitude: >-
      {% if sensor|lower != 'null' %}
        {% set s = state_attr(sensor,'latitude') %}
        {% if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' %}
          {{ s|float }}
        {% else %}null{% endif %}
      {% else %}null{% endif %}
    longitude: >-
      {% if sensor|lower != 'null' %}
        {% set s = state_attr(sensor,'longitude') %}
        {% if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' %}
          {{ s|float }}
        {% else %}null{% endif %}
      {% else %}null{% endif %}
    distance: >-
      {% if latitude|lower != 'null' and longitude|lower != 'null' %}
        {% set s = distance('zone.home', longitude, latitude) %}
        {% if s|lower != 'none' and s|lower != 'null' and s|lower != 'unknown' and s|lower != 'unavailable' and s|int is number and s|int < 40075 %}
          {{- '%0.2f'|format(s|float * 1000) -}}
        {% else %}null{% endif %}
      {% else %}null{% endif %}
    gps: >-
      {% if latitude|lower != 'null' and longitude|lower != 'null' %}
        {% set s = state_attr('zone.home','radius') %}
        {% if s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and s|lower != 'null' and s|int > 0 %}
          {{ latitude|lower != 'null' and longitude|lower != 'null' and distance|int < s|int }}
        {% else %}null{% endif %}
      {% else %}null{% endif %}
    area: >-
      {% set s = area_name(sensor) %}
      {% if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' %}
        {{ s }}
      {% else %}null{% endif %}
    probability: >-
      {{ state_attr('binary_sensor.motion_person_occupancy_active','probability') }}
  action:
    - alias: 'choose: notify on id or gps'
      choose:
        - alias: 'condition: id unknown and gps bad?'
          conditions:
            - condition: template
              value_template: >
                {{ id|lower == 'null' and gps|lower != 'true' }}
          sequence:
            - alias: 'notify id and gps bad'
              service: persistent_notification.create
              data_template:
                title: >-
                  INVALID: Occupancy sensor
                notification_id: >-
                  {{ sensor|string + '_person_occupancy' }}
                message: >-
                  Occupancy sensor id and location invalid; id: {{ id -}}; GPS: {{ latitude,longitude -}}; distance: {{ distance }} 
        - alias: 'condition: gps bad?'
          conditions:
            - condition: template
              value_template: >
                {{ gps|lower != 'true' }}
          sequence:
            - alias: 'notify gps bad'
              service: persistent_notification.create
              data_template:
                title: >-
                  NOTICE: Occupancy location
                notification_id: >-
                  {{ sensor|string + '_person_occupancy' }}
                message: >-
                  Occupancy sensor location invalid; id: **{{- id -}}**; GPS: {{ latitude,longitude -}}; distance: {{ distance }} 
        - alias: 'condition: id unknown?'
          conditions:
            - condition: template
              value_template: >
                {{ id|lower == 'null' }}
          sequence:
            - alias: 'notify id bad'
              service: persistent_notification.create
              data_template:
                title: >-
                  INVALID: Occupancy identifier
                notification_id: >-
                  {{ sensor|string + '_person_occupancy' }}
                message: >-
                  Occupancy sensor identifier invalid; id: **{{- id -}}**
    - alias: 'choose: update based on location'
      choose:
        - alias: 'condition: id is good and gps is bad'
          conditions:
            - condition: template
              value_template: >-
                {{ id|lower != 'null' and gps|lower != 'true' }}
          sequence:
            - alias: 'service: set_state sensor.motion_person_occupancy'
              service: python_script.set_state
              data_template:
                entity_id: sensor.motion_person_occupancy
                location: >-
                  {{ location }}
                id: >-
                  {{ id }}
                sensor: >-
                  {{ sensor }}
                latitude: >-
                  {{ state_attr('zone.home','latitude') }}
                longitude: >-
                  {{ state_attr('zone.home','longitude') }}
                area: >-
                  {{ area }}
                ago: >-
                  {{ ago }}
                probability: >-
                  {{ probability }}
                state: >-
                  {{ id }}
        - alias: 'condition: id is good and gps is good'
          conditions:
            - condition: template
              value_template: >-
                {{ id|lower != 'null' and gps|lower == 'true' }}
          sequence:
            - alias: 'service: set_state sensor.motion_person_occupancy'
              service: python_script.set_state
              data_template:
                entity_id: sensor.motion_person_occupancy
                location: >-
                  {{ location }}
                id: >-
                  {{ id }}
                sensor: >-
                  {{ sensor }}
                area: >-
                  {{ area }}
                ago: >-
                  {{ ago }}
                latitude: >-
                  {{ latitude }}
                longitude: >-
                  {{ longitude  }}
                distance: >-
                  {{ distance }}
                probability: >-
                  {{ probability }}
                state: >-
                  {{ id }}

- id: motion_person_occupancy_off
  alias: motion_person_occupancy_off
  initial_state: on
  trigger:
    - platform: state
      entity_id: binary_sensor.motion_shellies_occupancy
      to: 'off'
  action:
    - service: python_script.set_state
      data_template:
        entity_id: sensor.motion_person_occupancy
        state: >-
          {{ 'none' }}
