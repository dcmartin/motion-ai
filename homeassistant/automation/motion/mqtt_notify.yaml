###
# homeassistant/automation/motion/mqtt_notify.yaml
###

## notify

- id: motion_mqtt_notify
  alias: motion_mqtt_notify
  initial_state: 'on'
  trigger:
    - platform: state
      entity_id: binary_sensor.motion_mqtt_active
  condition:
    condition: and
    conditions:
      - condition: template
        value_template: >
          {{ trigger.to_state.state != trigger.from_state.state }}
      - condition: template
        value_template: >
          {{ is_state('binary_sensor.motion_mqtt_active','on') }}
  action: 
    - service: persistent_notification.create
      data_template:
        title: >-
          MQTT DOWN
        notification_id: >
          motion_mqtt_down
        message: >-
          MQTT inactive for {{ states('sensor.motion_status_mqtt_ago')|int }} seconds
