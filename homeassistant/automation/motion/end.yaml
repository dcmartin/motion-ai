###
# automation/motion/end.yaml
###

- id: motion_end_counter
  alias: motion_end_counter
  initial_state: on
  trigger:
    platform: state
    entity_id: binary_sensor.motion_end
  condition:
    - condition: template
      value_template: >
        {{ is_state('binary_sensor.motion_end','on') }}
  action:
    - service: counter.increment
      entity_id: counter.motion_end_counter

- id: motion_end_snapshot
  alias: motion_end_snapshot
  initial_state: on
  trigger:
    platform: state
    entity_id: camera.motion_event_end
  condition:
    condition: and
    conditions:
      - condition: template
        value_template: >-
          {{ is_state('input_boolean.motion_snapshot_save','on') }}
      - condition: template
        value_template: >-
          {{ state_attr('camera.motion_event_end','entity_picture') is not none }}
  action:
    - service: camera.snapshot
      data_template:
        entity_id: >-
          {{ 'camera.motion_event_end' }}
        filename: >-
          {{ '/config/www/images/motion_end.jpg' }}

- id: motion_end_composite_snapshot
  alias: motion_end_composite_snapshot
  initial_state: on
  trigger:
    platform: state
    entity_id: camera.motion_event_composite
  condition:
    condition: and
    conditions:
      - condition: template
        value_template: >-
          {{ is_state('input_boolean.motion_snapshot_save','on') }}
      - condition: template
        value_template: >-
          {{ state_attr('camera.motion_event_composite','entity_picture') is not none }}
  action:
    - service: camera.snapshot
      data_template:
        entity_id: >-
          {{ 'camera.motion_event_composite' }}
        filename: >-
          {{ '/config/www/images/motion_end_composite.jpg' }}

- id: motion_end_animated_snapshot
  alias: motion_end_animated_snapshot
  initial_state: on
  trigger:
    platform: state
    entity_id: camera.motion_event_animated
  condition:
    condition: and
    conditions:
      - condition: template
        value_template: >-
          {{ is_state('input_boolean.motion_snapshot_save','on') }}
      - condition: template
        value_template: >-
          {{ state_attr('camera.motion_event_animated','entity_picture') is not none }}
  action:
    - service: camera.snapshot
      data_template:
        entity_id: >-
          {{ 'camera.motion_event_animated' }}
        filename: >-
          {{ '/config/www/images/motion_end.gif' }}

- id: motion_end_animated_mask_snapshot
  alias: motion_end_animated_mask_snapshot
  initial_state: on
  trigger:
    platform: state
    entity_id: camera.motion_event_animated_mask
  condition:
    condition: and
    conditions:
      - condition: template
        value_template: >-
          {{ is_state('input_boolean.motion_snapshot_save','on') }}
      - condition: template
        value_template: >-
          {{ state_attr('camera.motion_event_animated_mask','entity_picture') is not none }}
  action:
    - service: camera.snapshot
      data_template:
        entity_id: >-
          {{ 'camera.motion_event_animated_mask' }}
        filename: >-
          {{ '/config/www/images/motion_end_mask.gif' }}
