###
# homeassistant/automation/motion/device/safety.yaml
###

- id: motion_device_safety_automation_enable
  alias: motion_device_safety_automation_enable
  initial_state: on
  trigger:
    platform: state
    entity_id: group.motion_device_class_safety
    attribute: 'entity_id'
  condition:
    - condition: template
      value_template: >-
        {% set s = state_attr('group.motion_device_class_safety','entity_id') %}
        {{ s|lower != 'none' and not s is string and s is iterable and s|count > 0 }}
  variables:
    automations: >-
      {% set automations = ['motion_device_safety_activity_notify','motion_device_safety_alarm_activity','motion_device_safety_alarm_bad','motion_device_safety_alarm_high_off','motion_device_safety_alarm_high_on','motion_device_safety_alarm_low_off','motion_device_safety_alarm_low_on','motion_device_safety_bad_notify','motion_device_safety_good_dismiss','motion_device_safety_good_notify','motion_device_safety_high_dismiss','motion_device_safety_high_notify','motion_device_safety_low_dismiss','motion_device_safety_low_notify','motion_device_safety_mute','motion_device_safety_restart','motion_device_safety_self_test','motion_device_safety_status','motion_device_safety_unmute','motion_device_safety_update'] %}
      {{ automations }}
  action:
    - alias: 'enable automations for device_safety'
      repeat:
        while:
          - condition: template
            value_template: >-
              {% set s = automations[repeat.index-1]|default('') %}
              {{ s|string != '' }}
        sequence:
          - variables:
              automation: >-
                {% set s = automations[repeat.index-1] %}
                {{ 'automation.' + s|string }}
          - alias: 'test if automation is off'
            condition: template
            value_template: >-
              {{ states(automation)|lower == 'off' }}
          - alias: 'enable automation'
            service: automation.turn_on
            data_template:
              entity_id: >-
                {{ automation }}
          - condition: template
            value_template: >
              {{ states('input_select.motion_log_level') in ['debug'] }}
          - service: persistent_notification.create
            data_template:
              title: >-
                {% set s = automations[repeat.index-1] %}
                DEBUG: enabling {{ s }}
              notification_id: >
                {{- 'debug-automation-safety' -}}
              message: >-
                Automation enabled: {{ automation }}

- id: motion_device_safety_automation_disable
  alias: motion_device_safety_automation_disable
  initial_state: on
  trigger:
    platform: state
    entity_id: group.motion_device_class_safety
    attribute: 'entity_id'
  condition:
    - condition: template
      value_template: >-
        {% set s = state_attr('group.motion_device_class_safety','entity_id') %}
        {{ sensors|lower == 'none' or sensors is string or not sensors is iterable or not sensors|count > 0 }}
  variables:
    automations: >-
      {% set automations = ['motion_device_safety_activity_notify','motion_device_safety_alarm_activity','motion_device_safety_alarm_bad','motion_device_safety_alarm_high_off','motion_device_safety_alarm_high_on','motion_device_safety_alarm_low_off','motion_device_safety_alarm_low_on','motion_device_safety_bad_notify','motion_device_safety_good_dismiss','motion_device_safety_good_notify','motion_device_safety_high_dismiss','motion_device_safety_high_notify','motion_device_safety_low_dismiss','motion_device_safety_low_notify','motion_device_safety_mute','motion_device_safety_restart','motion_device_safety_self_test','motion_device_safety_status','motion_device_safety_unmute','motion_device_safety_update'] %}
      {{ automations }}
  action:
    - alias: 'disable automations for device_safety'
      repeat:
        while:
          - condition: template
            value_template: >-
              {% set s = automations[repeat.index-1]|default('') %}
              {{ s|string != '' }}
        sequence:
          - variables:
              automation: >-
                {% set s = automations[repeat.index-1] %}
                {{ 'automation.' + s|string }}
          - alias: 'test if automation is on'
            condition: template
            value_template: >-
              {{ states(automation)|lower == 'on' }}
          - alias: 'disable automation'
            service: automation.turn_off
            data_template:
              entity_id: >-
                {{ automation }}
          - condition: template
            value_template: >
              {{ states('input_select.motion_log_level') in ['debug'] }}
          - service: persistent_notification.create
            data_template:
              title: >-
                {% set s = automations[repeat.index-1] %}
                DEBUG: disabling {{ s }}
              notification_id: >
                {{- 'debug-automation-safety' -}}
              message: >-
                Automation disables: {{ automation }}

- id: motion_device_safety_rescan
  alias: motion_device_safety_rescan
  mode: single
  max_exceeded: silent
  initial_state: on
  trigger:
    - platform: time_pattern
      minutes: '/5'
    - platform: homeassistant
      event: start
  variables:
    all: >-
      [{%- for state in (states.sensor|list + states.binary_sensor|list) if 'device_class' in state.attributes and device_id(state.entity_id)|lower != 'none' and state.attributes.device_class=='safety' and not '.motion_' in state.entity_id -%}
        {%- if not loop.first -%},{% endif -%}
        "{{- device_attr(device_id(state.entity_id),'name') -}}"
      {%- endfor -%}]
    ids: >-
      {%- if all != [] and all is iterable and all|count > 0 -%}
        {{- all|sort|unique|list -}}
      {% else %}none{% endif %}
    total: >-
      {% set s = ids %}
      {% if s|lower != 'unknown' and s|lower != 'none' and not s is string %}
        {{ s|count }}
      {%- else -%}0{%- endif %}
    update: >-
      {% set s = ids %}
      {%- if s|lower != 'unknown' and s|lower != 'none' and not s is string -%}
        [{%- for i in s -%}
          {% set o = loop %}
          {%- for state in (states.button|list + states.switch|list) if device_attr(state.entity_id, 'name') == i and state.attributes.device_class|default('null')|string == 'update' -%}
            {%- if not o.first or not loop.first -%},{%- endif -%}
            {%- set v = state.state -%}
            {"id":"{{- i -}}","entity_id":"{{- state.entity_id -}}"}
          {%- endfor -%}
        {%- endfor -%}]
      {%- else -%}null{%- endif %}
    restart: >-
      {% set s = ids %}
      {%- if s|lower != 'unknown' and s|lower != 'none' and not s is string -%}
        [{%- for i in s -%}
          {% set o = loop %}
          {%- for state in (states.button|list + states.switch|list) if device_attr(state.entity_id, 'name') == i and state.attributes.device_class|default('null')|string == 'restart' -%}
            {%- if not o.first or not loop.first -%},{%- endif -%}
            {%- set v = state.state -%}
            {"id":"{{- i -}}","entity_id":"{{- state.entity_id -}}"}
          {%- endfor -%}
        {%- endfor -%}]
      {%- else -%}null{%- endif %}
    controls: >-
      {% set s = ids %}
      {%- if s|lower != 'unknown' and s|lower != 'none' and not s is string -%}
        [{%- for i in s -%}
          {% set o = loop %}
          {%- for state in (states.button|list + states.switch|list) if device_attr(state.entity_id, 'name') == i and state.attributes.device_class|default('null')|string == 'null' -%}
            {%- if not o.first or not loop.first -%},{%- endif -%}
            {%- set ctl = state_attr(state.entity_id,'friendly_name') -%}
            {%- set ctl = ctl|replace(i+' ','') -%}
            {"id":"{{- i -}}","entity_id":"{{- state.entity_id -}}","control":"{{- ctl -}}"}
          {%- endfor -%}
        {%- endfor -%}]
      {%- else -%}null{%- endif %}
    test: >-
      {% set s = ids %}
      {%- if s|lower != 'unknown' and s|lower != 'none' and not s is string -%}
        [{%- for i in s -%}
          {% set o = loop %}
          {%- for state in states.sensor if device_attr(state.entity_id, 'name') == i and state.attributes.self_test|default('none')|string != 'none' -%}
            {%- if not o.first or not loop.first -%},{%- endif -%}
            {"id":"{{- i -}}","entity_id":"{{- state.entity_id -}}","state":"{{- state.state -}}","updated":{{- state.last_updated|as_timestamp(None) -}}}
          {%- endfor -%}
        {%- endfor -%}]
      {%- else -%}null{%- endif %}
    areas: >-
      {% set s = ids -%}
      {%- if s|lower != 'unknown' and s|lower != 'none' and not s is string -%}
        [{%- for i in s -%}
          {% set o = loop %}
          {%- for state in (states.sensor|list + states.binary_sensor|list) if device_attr(state.entity_id, 'name') == i and state.attributes.device_class|default('null')|string == 'safety' -%}
            {%- if not o.first or not loop.first -%},{%- endif -%}
            {%- set v = state.state -%}
            {"id":"{{- i -}}","area":"{{- area_id(state.entity_id) -}}","state":"{{- state.state -}}"}
          {%- endfor -%}
        {%- endfor -%}]
      {%- else -%}none{%- endif %}
    status: >-
      {% set s = ids %}
      {%- if s|lower != 'unknown' and s|lower != 'none' and not s is string -%}
        [{%- for i in s -%}
          {% set o = loop %}
          {%- for state in (states.sensor|list + states.binary_sensor|list) if device_attr(state.entity_id, 'name') == i and state.attributes.device_class|default('null')|string == 'safety' -%}
            {%- if not o.first or not loop.first -%},{%- endif -%}
            {"id":"{{- i -}}","entity_id":"{{- state.entity_id -}}","state":"{{- state.state -}}","updated":{{- state.last_updated|as_timestamp(None) -}}}
          {%- else -%}{% if o.first %}None{% endif %}{%- endfor -%}
        {%- endfor -%}]
      {%- else -%}none{%- endif %}
    devices: >-
      {%- if ids|lower != 'unknown' and ids|lower != 'none' and not s is string and ids is iterable 
             and status|lower != 'none' and not status is string and status is iterable
             and areas|lower != 'none' and not areas is string and areas is iterable  -%}
        [{% for i in ids -%}
          {%- set j = status|selectattr('id','==',i)|map(attribute='entity_id')|first %}
          {%- set k = areas|selectattr('id','==',i)|map(attribute='area')|first %}
          {%- if not loop.first -%},{%- endif -%}
          {"id":"{{- i -}}","did":"{{- device_id(j) -}}","area":"{{- k -}}"}
        {%- endfor -%}]
      {%- else -%}none{%- endif %}
  action:
    - alias: 'update binary_sensor.motion_device_safety ids, etc..'
      service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_safety
        ids: >-
          {{ ids }}
        total: >-
          {{ total }}
        update: >-
          {{ update }}
        restart: >-
          {{ restart }}
        controls: >-
          {{ controls }}
        test: >-
          {{ test }}
        areas: >-
          {{ areas }}
        devices: >-
          {{ devices }}
        state: >-
          {{ states('binary_sensor.motion_device_safety') }}
    - variables:
        entities: >-
          {%- if status|lower != 'null' and not status is string and status is iterable and status|count > 0 -%}
            {{- status|map(attribute='entity_id')|list -}}
          {%- else -%}none{%- endif %}
    - condition: template
      value_template: >
        {{ entities|lower != 'none' }}
    - alias: 'set group.motion_device_class_safety sensors'
      service: group.set
      data_template:
        object_id: motion_device_class_safety
        entities: >-
          {{ entities }}

## update status

- id: motion_device_safety_status
  alias: motion_device_safety_status
  mode: single
  max_exceeded: silent
  initial_state: off
  trigger:
    - platform: state
      entity_id: binary_sensor.motion_device_safety_exist
    - platform: state
      entity_id: group.motion_device_class_safety
    - platform: state
      entity_id: binary_sensor.motion_device_safety
      attribute: 'ids'
    - platform: time_pattern
      seconds: '/30'
  variables:
    ids: >-
      {% set ids = state_attr('binary_sensor.motion_device_safety','ids') %}
      {{ ids }}
    status: >-
      {% set s = ids %}
      {%- if s|lower != 'none' and s|lower != 'unknown' and s is iterable -%}
        [{%- for i in s -%}
          {% set o = loop %}
          {%- for state in (states.sensor|list + states.binary_sensor|list) if device_attr(state.entity_id, 'name') == i and state.attributes.device_class|default('null')|string == 'safety' -%}
            {%- if not o.first or not loop.first -%},{%- endif -%}
            {"id":"{{- i -}}","entity_id":"{{- state.entity_id -}}","state":"{{- state.state -}}","updated":{{- state.last_updated|as_timestamp(None) -}}}
          {%- else -%}{% if o.first %}None{% endif %}{%- endfor -%}
        {%- endfor -%}]
      {%- else -%}none{%- endif %}
    high: >-
      {% if state_attr('binary_sensor.motion_device_safety_exist','high')|float('none') != 'none' %}
        {% set s = status %}
        {%- if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s is iterable -%}
          {% set n = state_attr('binary_sensor.motion_device_safety_exist','high')|float(0) %}
          [{% for i in s if 'entity_id' in i and states(i.entity_id)|float(n) > n -%}
            {%- if not loop.first -%},{%- endif -%}
            {"id":"{{- i.id -}}","entity_id":"{{- i.entity_id -}}","state":"{{- i.state -}}","updated":{{- states[i.entity_id].last_updated|as_timestamp(None) -}}}
          {%- endfor -%}]
        {%- else -%}none{%- endif %}
      {%- else -%}none{%- endif %}
    low: >-
      {% if state_attr('binary_sensor.motion_device_safety_exist','low')|float('none') != 'none' %}
        {% set s = status %}
        {%- if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s is iterable -%}
          {% set n = state_attr('binary_sensor.motion_device_safety_exist','low')|float(0) %}
          [{% for i in s if 'entity_id' in i and states(i.entity_id)|float(n) < n -%}
            {%- if not loop.first -%},{%- endif -%}
            {"id":"{{- i.id -}}","entity_id":"{{- i.entity_id -}}","state":"{{- i.state -}}","updated":{{- states[i.entity_id].last_updated|as_timestamp(None) -}}}
          {%- endfor -%}]
        {%- else -%}none{%- endif %}
      {%- else -%}none{%- endif %}
    good: >-
      {% set good = state_attr('binary_sensor.motion_device_safety_exist','good') %}
      {%- if good|lower != 'none' -%}
        {% set s = status %}
        {%- if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s is iterable -%}
          [{% for i in s if 'entity_id' in i and not i.entity_id is none and is_state(i.entity_id,good) -%}
            {%- if not loop.first -%},{%- endif -%}
            {"id":"{{- i.id -}}","entity_id":"{{- i.entity_id -}}","state":"{{- i.state -}}","updated":{{- states[i.entity_id].last_updated|as_timestamp(None) -}}}
          {%- endfor -%}]
        {%- else -%}none{%- endif %}
      {%- else -%}none{%- endif %}
    bad: >-
      {% set bad = state_attr('binary_sensor.motion_device_safety_exist','bad') %}
      {%- if bad|lower != 'none' -%}
        {% set s = status %}
        {%- if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s is iterable -%}
          [{% for i in s if 'entity_id' in i and not i.entity_id is none and is_state(i.entity_id,bad) -%}
            {%- if not loop.first -%},{%- endif -%}
            {"id":"{{- i.id -}}","entity_id":"{{- i.entity_id -}}","state":"{{- i.state -}}","updated":{{- states[i.entity_id].last_updated|as_timestamp(None) -}}}
          {%- endfor -%}]
        {%- else -%}none{%- endif %}
      {%- else -%}none{%- endif %}
    activity: >-
      {% set activity = state_attr('binary_sensor.motion_device_safety_exist','activity') %}
      {%- if activity|lower != 'none' -%}
        {% set s = status %}
        {%- if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s is iterable -%}
          [{% for i in s if 'entity_id' in i and not i.entity_id is none and is_state(i.entity_id,activity) -%}
            {%- if not loop.first -%},{%- endif -%}
            {"id":"{{- i.id -}}","entity_id":"{{- i.entity_id -}}","state":"{{- i.state -}}","updated":{{- states[i.entity_id].last_updated|as_timestamp(None) -}}}
          {%- endfor -%}]
        {%- else -%}none{%- endif %}
      {%- else -%}none{%- endif %}
    unavailable: >-
      {% set s = status %}
      {%- if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s is iterable -%}
        [{% for i in s if 'entity_id' in i and not i.entity_id is none and is_state(i.entity_id,'unavailable') -%}
          {%- if not loop.first -%},{%- endif -%}
            {"id":"{{- i.id -}}","entity_id":"{{- i.entity_id -}}","state":"{{- i.state -}}","updated":{{- states[i.entity_id].last_updated|as_timestamp(None) -}}}
        {%- endfor -%}]
      {%- else -%}none{%- endif %}
    values: >-
      {% set g = state_attr('binary_sensor.motion_device_safety_exist','good') %}
      {% set b = state_attr('binary_sensor.motion_device_safety_exist','bad') %}
      {%- if g|lower != 'none' and b|lower != 'none' -%}
        [{% for i in status if 'entity_id' in i and not i.entity_id is none -%}
          {%- if not loop.first -%},{%- endif -%}
          {% if is_state(i.entity_id,g) %}1{% elif is_state(i.entity_id,b) %}0{% else %}none{% endif %}
        {%- endfor -%}]
      {%- else -%}
        [{% for i in status if 'entity_id' in i and not i.entity_id is none -%}
          {%- if not loop.first -%},{%- endif -%}
          {{- states(i.entity_id)|float(none) -}}
        {%- endfor -%}]
      {%- endif %}
    value: >-
      {% if bad|lower != 'none' and not bad is string and bad is iterable -%}
        {%- if bad|count > 0 -%}
          {%- set result = 'on' -%}
        {%- else -%}
          {%- set result = 'off' -%}
        {%- endif -%}
      {%- endif -%}
      {%- if low|lower != 'none' and not low is string and low is iterable -%}
        {%- if low|count > 0 -%}
          {%- set result = 'on' -%}
        {%- else -%}
          {%- set result = 'off' -%}
        {%- endif -%}
      {%- endif -%}
      {%- if high|lower != 'none' and not high is string and high is iterable -%}
        {%- if high|count > 0 -%}
          {%- set result = 'on' -%}
        {%- else -%}
          {%- set result = 'off' -%}
        {%- endif -%}
      {%- endif -%}
      {%- if activity|lower != 'none' and not activity is string and activity is iterable -%}
        {%- if activity|count > 0 -%}
          {%- set result = 'on' -%}
        {%- else -%}
          {%- set result = 'off' -%}
        {%- endif -%}
      {%- endif -%}
      {%- if result is defined -%}
        {{- result -}}
      {%- else -%}unknown{%- endif %}
  action:
    - alias: 'update binary_sensor.motion_device_safety state'
      service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_safety
        status: >-
          {{ status }}
        high: >-
          {{ high }}
        low: >-
          {{ low }}
        good: >-
          {{ good }}
        bad: >-
          {{ bad }}
        activity: >-
          {{ activity }}
        unavailable: >-
          {{ unavailable }}
        values: >-
          {{ values }}
        state: >-
          {{ value }}

## update, restart, and other contols

- id: motion_device_safety_self_test
  alias: motion_device_safety_self_test
  mode: single
  initial_state: off
  trigger:
  condition:
    condition: and
    conditions:
      - alias: 'test if self_test is on'
        condition: template
        value_template: >-
          {{ is_state('binary_sensor.motion_device_safety_self_test','on') }}
  variables:
    entity_ids: >-
      {{ state_attr('binary_sensor.motion_device_safety_self_test','entity_ids') }}
  action:
    - alias: 'test safety sensors'
      repeat:
        while:
          - alias: 'test if not done'
            condition: template
            value_template: >-
              {% set s = entity_ids[repeat.index - 1]|default('unknown') %}
              {{ s|lower != 'unavailable' and s|lower != 'unknown' and s|lower != 'null' and s|string|length > 0 }}
        sequence:
          - alias: 'pressing button'
            service: button.press
            data_template:
              entity_id: >-
                {{ entity_ids[repeat.index - 1] }}

- id: motion_device_safety_mute
  alias: motion_device_safety_mute
  mode: single
  initial_state: off
  trigger:
  condition:
    condition: and
    conditions:
      - condition: template
        value_template: >-
          {{ is_state('binary_sensor.motion_device_safety_mute','on') }}
  variables:
    entity_ids: >-
      {{ state_attr('binary_sensor.motion_device_safety_mute','entity_ids') }}
  action:
    - alias: 'mute safety sensors'
      repeat:
        while:
          - condition: template
            value_template: >-
              {% set s = entity_ids[repeat.index - 1]|default('unknown') %}
              {{ s|lower != 'unavailable' and s|lower != 'unknown' and s|lower != 'null' and s|string|length > 0 }}
        sequence:
          - service: button.press
            data_template:
              entity_id: >-
                {{ entity_ids[repeat.index - 1] }}

- id: motion_device_safety_unmute
  alias: motion_device_safety_unmute
  mode: single
  initial_state: off
  trigger:
  condition:
    condition: and
    conditions:
      - condition: template
        value_template: >-
          {{ is_state('binary_sensor.motion_device_safety_unmute','on') }}
  variables:
    entity_ids: >-
      {{ state_attr('binary_sensor.motion_device_safety_unmute','entity_ids') }}
  action:
    - alias: 'unmute safety sensors'
      repeat:
        while:
          - condition: template
            value_template: >-
              {% set s = entity_ids[repeat.index - 1]|default('unknown') %}
              {{ s|lower != 'unavailable' and s|lower != 'unknown' and s|lower != 'null' and s|string|length > 0 }}
        sequence:
          - service: button.press
            data_template:
              entity_id: >-
                {{ entity_ids[repeat.index - 1] }}

- id: motion_device_safety_restart
  alias: motion_device_safety_restart
  mode: single
  initial_state: off
  trigger:
  condition:
    condition: and
    conditions:
      - condition: template
        value_template: >-
          {{ is_state('binary_sensor.motion_device_safety_restart','on') }}
  variables:
    entity_ids: >-
      {{ state_attr('binary_sensor.motion_device_safety_restart','entity_ids') }}
  action:
    - alias: 'restart safety sensors'
      repeat:
        while:
          - condition: template
            value_template: >-
              {% set s = entity_ids[repeat.index - 1]|default('unknown') %}
              {{ s|lower != 'unavailable' and s|lower != 'unknown' and s|lower != 'null' and s|string|length > 0 }}
        sequence:
          - service: button.press
            data_template:
              entity_id: >-
                {{ entity_ids[repeat.index - 1] }}

- id: motion_device_safety_update
  alias: motion_device_safety_update
  mode: single
  initial_state: off
  trigger:
  condition:
    condition: and
    conditions:
      - condition: template
        value_template: >-
          {{ is_state('binary_sensor.motion_device_safety_update','on') }}
  variables:
    entity_ids: >-
      {{ state_attr('binary_sensor.motion_device_safety_update','entity_ids') }}
  action:
    - alias: 'update safety sensors'
      repeat:
        while:
          - condition: template
            value_template: >-
              {% set s = entity_ids[repeat.index - 1]|default('unknown') %}
              {{ s|lower != 'unavailable' and s|lower != 'unknown' and s|lower != 'null' and s|string|length > 0 }}
        sequence:
          - service: button.press
            data_template:
              entity_id: >-
                {{ entity_ids[repeat.index - 1] }}

## notifications

- id: motion_device_safety_missing
  alias: motion_device_safety_missing
  mode: single
  initial_state: on
  trigger:
    - platform: state
      entity_id: binary_sensor.motion_device_safety_exist
    - platform: state
      entity_id: automation.motion_device_safety_rescan
  condition:
    condition: and
    conditions:
      - condition: template
        value_template: >
          {{ is_state('binary_sensor.motion_device_safety_exist','off') }}
      - condition: template
        value_template: >
          {{ is_state_attr('automation.motion_device_safety_rescan','current',0) }}
      - condition: template
        value_template: >
          {{ is_state_attr('binary_sensor.motion_device_safety_exist','required','on') }}
  variables:
    ip: >-
      {{ none }}
    latitude: >-
      {{ none }}
    longitude: >-
      {{ none }}
    notification_id: >-
      {{ 'device-safety-notify' }}
    icon: >-
      {% set icon = 'notification-safety-missing' %}
      {{ icon }}
    header: >-
      {{- '[![](/local/images/icons/' + icon + '.png?v1)]()' -}}
      {{- '<hr>' -}}
    footer: >-
      {{- '<hr>' -}}
    title: >-
      {% set title = states('input_text.motion_app') + ': safety sensor not found' %}
      {{ title }}
    body: >-
      The safety sensor could not be found; please setup the integration by clicking [here](/config/integrations).
    message: >-
      {{ header }}
      {{ body }}
      {{ footer }}
  action:
    - alias: 'notify missing safety sensor'
      service: persistent_notification.create
      data_template:
        title: >-
          {{- title -}}
        notification_id: >-
          {{- notification_id -}}
        message: >-
          {{- message -}}

# found

- id: motion_device_safety_found
  alias: motion_device_safety_found
  mode: single
  initial_state: on
  trigger:
    - platform: state
      entity_id: binary_sensor.motion_device_safety_exist
      to: 'on'
  variables:
    ids: >-
      {% set ids = state_attr('binary_sensor.motion_device_safety','ids') %}
      {{ ids }}
    devices: >-
      {% set devices = state_attr('binary_sensor.motion_device_safety','devices') %}
      {{ devices }}
    title: >-
      {% set title = states('input_text.motion_app') + ': safety sensor found' %}
      {{ title }}
    notification_id: >-
      {{ 'device-safety-notify' }}
    icon: >-
      {% set icon = 'notification-safety-found' %}
      {{ icon }}
    header: >-
      {{- '[![](/local/images/icons/' + icon + '.png?v1)]()' -}}
      {{- '<hr>' -}}
    footer: >-
      {{- '<hr>' -}}
    body: >-
      Sensor(s) for safety found:
      {{- '<p>' -}}
      {% if devices|lower != 'none' %}
        {%- for i in devices -%}
          {%- if not loop.first -%}<br>{%- endif -%}
          [{{ i.id }}](/config/devices/device/{{- i.did -}}) in [{{ i.area }}](/config/areas/area/{{- i.area -}})
        {%- endfor %}
      {%- endif %}
    message: >-
      {{ header }}
      {{ body }}
      {{ footer }}
  action:
    - alias: 'notify found safety sensor'
      service: persistent_notification.create
      data_template:
        title: >-
          {{- title -}}
        notification_id: >-
          {{- notification_id -}}
        message: >-
          {{- message -}}

- id: motion_device_safety_found_dismiss
  alias: motion_device_safety_found_dismiss
  mode: single
  initial_state: on
  trigger:
    - platform: state
      entity_id: binary_sensor.motion_device_safety_exist
      to: 'on'
      for:
        minutes: >-
          {{ states('input_number.motion_device_class_dismiss')|int(5) }}
  variables:
    notification_id: >-
      {{ 'device-safety-notify' }}
  action:
    - alias: 'dismiss safety found notification'
      service: persistent_notification.dismiss
      data_template:
        notification_id: >-
          {{ notification_id }}

# detected

- id: motion_device_safety_alarm_bad
  alias: motion_device_safety_alarm_bad
  mode: single
  initial_state: off
  trigger:
    - platform: template
      value_template: >-
        {{ not is_state_attr('binary_sensor.motion_device_safety_exist','bad','none')
           and is_state_attr('binary_sensor.motion_device_safety_exist','bad',states('binary_sensor.motion_device_safety')) }}
  variables:
    devices: >-
      {% set devices = none %}
      {% set s = state_attr('binary_sensor.motion_device_safety','devices') %}
      {% if s|lower != 'none' and s|lower != 'null' and not s is string and s is iterable and s|count > 0 %}
        {% set devices = s %}
      {%- endif %}
      {{ devices }}
    last: >-
      {% set last = none %}
      {% set s = state_attr('binary_sensor.motion_device_safety','status')|selectattr('state','==',state_attr('binary_sensor.motion_device_safety_exist','bad'))|sort(attribute='updated',reverse=true)|list %}
      {% if s|lower != 'none' and s|lower != 'null' and not s is string and s is iterable and s|count > 0 %}
        {% set last = s %}
      {%- endif %}
      {{ last  }}
    which: >-
      {% set which = none %}
      {% if last|lower != 'none' %}
        {% set which = last|map(attribute='id')|first %}
      {%- endif %}
      {{ which }}
    did: >-
      {% set did = none %}
      {% if devices|lower != 'none'  and which|lower != 'none' %}
        {% set did = devices|selectattr('id','==',which)|map(attribute='did')|first %}
      {%- endif %}
      {{ did }}
    where: >-
      {% set where = none %}
      {% if devices|lower != 'none'  and which|lower != 'none' %}
        {% set where = devices|selectattr('id','==',which)|map(attribute='area')|first %}
      {%- endif %}
      {{ where }}
  condition:
    condition: and
    conditions:
      - condition: template
        value_template: >
          {{ which|lower != 'none' and which|lower != 'null' and which|length > 0 }}
  action:
    - alias: 'update binary_sensor.motion_device_safety which, where, did'
      service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_safety
        which: >-
          {{ which }}
        where: >-
          {{ where }}
        did: >-
          {{ did }}
    - condition: template
      value_template: >
        {{ is_state_attr('binary_sensor.motion_device_safety_exist','alarm','on') }}
    - alias: 'trigger motion notification'
      service: automation.trigger
      data_template:
        entity_id: automation.motion_device_safety_bad_notify

# detected notify

- id: motion_device_safety_bad_notify
  alias: motion_device_safety_bad_notify
  mode: single
  initial_state: off
  trigger:
  variables:
    ids: >-
      {% set ids = state_attr('binary_sensor.motion_device_safety','ids') %}
      {{ ids }}
    which: >-
      {% set which = state_attr('binary_sensor.motion_device_safety','which') %}
      {{ which }}
    where: >-
      {% set where = state_attr('binary_sensor.motion_device_safety','where') %}
      {{ where }}
    did: >-
      {% set did = state_attr('binary_sensor.motion_device_safety','did') %}
      {{ did }}
    action: >-
      {% set action = 'safety-okay' %}
      {{ action }}
    url: >-
      {% set url = '/lovelace/safety-okay' %}
      {{ url }}
    title: >-
      {% set title = states('input_text.motion_app') + ': safety alarm' %}
      {{ title }}
    notification_id: >-
      {{ 'device-safety-status-' + which|string }}
    icon: >-
      {% set icon = 'notification-safety-detected' %}
      {{ icon }}
    header: >-
      {{- '[![](/local/images/icons/' + icon + '.png?v1)]()' -}}
      {{- '<hr>' -}}
    footer: >-
      {{ '<hr>' }}
      {%- if did|lower != 'none' and did|lower != 'null' -%}
        {{- '[![](https://img.shields.io/badge/device-info-blue.svg)](' + '/config/devices/device/' + did + ')' -}}
      {%- endif -%}
    body: >-
      Your home safety alarm was triggered; sensor: {{ which -}}; where: {{ where -}}.
      We have notified **{{- state_attr('sensor.motion_role_primary','friendly_name') -}}** about your safety alarm.
      If you are fine handling this situation on your own, click the **{{- action -}}** button below.
      {{- '<p>' -}}
      {{-  '[![](https://img.shields.io/badge/' + action + '-green.svg)](' + url + ')' -}}
    message: >-
      {{- header -}}
      {{- body -}}
      {{- footer -}}
  action:
    - alias: 'notify safety sensor activated'
      service: persistent_notification.create
      data_template:
        title: >-
          {{- title -}}
        notification_id: >-
          {{- notification_id -}}
        message: >-
          {{- message -}}

# cleared notify

- id: motion_device_safety_good_notify
  alias: motion_device_safety_good_notify
  mode: single
  initial_state: off
  trigger:
    - platform: template
      value_template: >-
        {{ is_state_attr('binary_sensor.motion_device_safety_exist','good',states('binary_sensor.motion_device_safety')) }}
  variables:
    which: >-
      {% set which = none %}
      {% set s = state_attr('binary_sensor.motion_device_safety','status')|selectattr('state','==','off')|sort(attribute='updated',reverse=true)|list %}
      {% if s|lower != 'none' and s|lower != 'null' and not s is string and s is iterable and s|count > 0 %}
        {% set which = s|map(attribute='id')|first  %}
      {%- endif %}
      {{ which }}
    notification_id: >-
      {{ 'device-safety-status-' + which|string }}
    icon: >-
      {% set icon = 'notification-safety-cleared' %}
      {{ icon }}
    title: >-
      {% set title = states('input_text.motion_app') + ': safety cleared' %}
      {{ title }}
    header: >-
      {{- '[![](/local/images/icons/' + icon + '.png?v1)]()' -}}
      {{- '<hr>' -}}
    footer: >-
      {{ '<hr>' }}
    body: >-
      Your home safety alarm has cleared; sensor: {{ which -}}.
    message: >-
      {{- header -}}
      {{- body -}}
      {{- footer -}}
  action:
    - condition: template
      value_template: >
        {{ which|lower != 'none' and is_state_attr('binary_sensor.motion_device_safety_exist','alarm','on') }}
    - alias: 'notify safety sensor activated'
      service: persistent_notification.create
      data_template:
        title: >-
          {{- title -}}
        notification_id: >-
          {{- notification_id -}}
        message: >-
          {{- message -}}

# cleared dismissed

- id: motion_device_safety_good_dismiss
  alias: motion_device_safety_good_dismiss
  mode: single
  initial_state: off
  trigger:
    - platform: template
      value_template: >-
        {{ is_state_attr('binary_sensor.motion_device_safety_exist','good',states('binary_sensor.motion_device_safety')) }}
      for:
        minutes: >-
          {{ states('input_number.motion_device_class_dismiss')|int(5) }}
  variables:
    ids: >-
      {%- set ids = state_attr('binary_sensor.motion_device_safety','ids') -%}
      {%- if ids|lower != 'none' and not ids is string and ids is iterable and ids|count > 0 -%}
        [{%- for i in ids -%}
          {%- if not loop.first -%},{%- endif -%}
          "{{- 'device-safety-status-' + i|string -}}"
        {%- endfor -%}]
      {%- else -%}none{%- endif %}
  condition:
    condition: and
    conditions:
      - condition: template
        value_template: >
          {{ ids|lower != 'none' and ids is iterable and ids|count > 0 }}
  action:
    - alias: 'dismiss safety status'
      repeat:
        while:
          - alias: 'test if not done'
            condition: template
            value_template: >-
              {% set s = ids[repeat.index - 1]|default('unknown') %}
              {{ s|lower != 'unavailable' and s|lower != 'unknown' and s|lower != 'null' and s|string|length > 0 }}
        sequence:
          - alias: 'dismissing persistent notification'
            service: persistent_notification.dismiss
            data_template:
              notification_id: >-
                {{ ids[repeat.index - 1] }}

## low

- id: motion_device_safety_alarm_low_on
  alias: motion_device_safety_alarm_low_on
  mode: single
  initial_state: off
  trigger:
    - platform: state
      entity_id: 'binary_sensor.motion_device_safety_low'
      to: 'on'
  variables:
    low: >-
      {{ state_attr('binary_sensor.motion_device_safety','low') }}
  condition:
    condition: and
    conditions:
      - condition: template
        value_template: >
          {{ low|lower != 'none' and low|lower != 'null' and low|length > 0 }}
  action:
    - condition: template
      value_template: >
        {{ is_state_attr('binary_sensor.motion_device_safety_exist','alarm','on') }}
    - alias: 'trigger motion notification'
      service: automation.trigger
      data_template:
        entity_id: automation.motion_device_safety_low_notify

- id: motion_device_safety_alarm_low_off
  alias: motion_device_safety_alarm_low_off
  mode: single
  initial_state: off
  trigger:
    - platform: state
      entity_id: 'binary_sensor.motion_device_safety_low'
      to: 'off'
  action:
    - condition: template
      value_template: >
        {{ is_state_attr('binary_sensor.motion_device_safety_exist','alarm','on') }}
    - alias: 'trigger safety low dismiss'
      service: automation.trigger
      data_template:
        entity_id: automation.motion_device_safety_low_dismiss

- id: motion_device_safety_low_notify
  alias: motion_device_safety_low_notify
  mode: single
  initial_state: off
  trigger:
  variables:
    low: >-
      {% set low = state_attr('binary_sensor.motion_device_safety','low') %}
      {{ low }}
    action: >-
      {% set action = 'safety-okay' %}
      {{ action }}
    url: >-
      {% set url = '/lovelace/safety-okay' %}
      {{ url }}
    title: >-
      {% set title = states('input_text.motion_app') + ': safety low alarm' %}
      {{ title }}
    notification_id: >-
      {{ 'device-safety-low' }}
    icon: >-
      {% set icon = 'notification-safety-low' %}
      {{ icon }}
    header: >-
      {{- '[![](/local/images/icons/' + icon + '.png?v1)]()' -}}
      {{- '<hr>' -}}
    footer: >-
      {{ '<hr>' }}
    body: >-
      Your home safety alarm was triggered:
      {{- '<p>' -}}
      {% if low|lower != 'none' and not low is string and low is iterable and low|count > 0 %}
        {%- for i in low -%}
          {%- if not loop.first -%}<br>{%- endif -%}
          [{{ i.id }}](/config/devices/device/{{- device_id(low[loop.index-1].entity_id) -}}) is {{ states(low[loop.index-1].entity_id) -}}{{- state_attr(low[loop.index-1].entity_id,'unit_of_measurement') }} in [{{ area_id(low[loop.index-1].entity_id) }}](/config/areas/area/{{- area_id(low[loop.index-1].entity_id) -}})
        {%- endfor %}
      {%- endif %}
      <p>
      We have notified **{{- state_attr('sensor.motion_role_primary','friendly_name') -}}** about your safety alarm.
      <br>
      If you are fine handling this situation on your own, click the **{{- action -}}** button below.
      {{- '<p>' -}}
      {{-  '[![](https://img.shields.io/badge/' + action + '-green.svg)](' + url + ')' -}}
    message: >-
      {{- header -}}
      {{- body -}}
      {{- footer -}}
  action:
    - alias: 'notify safety sensor activated'
      service: persistent_notification.create
      data_template:
        title: >-
          {{- title -}}
        notification_id: >-
          {{- notification_id -}}
        message: >-
          {{- message -}}

- id: motion_device_safety_low_dismiss
  alias: motion_device_safety_low_dismiss
  mode: single
  initial_state: off
  trigger:
  variables:
    notification_id: >-
      {{ 'device-safety-low' }}
  action:
    - alias: 'dismiss safety low notification'
      service: persistent_notification.dismiss
      data_template:
        notification_id: >-
          {{ notification_id }}

## high

- id: motion_device_safety_alarm_high_on
  alias: motion_device_safety_alarm_high_on
  mode: single
  initial_state: off
  trigger:
    - platform: state
      entity_id: 'binary_sensor.motion_device_safety_high'
      to: 'on'
  variables:
    high: >-
      {{ state_attr('binary_sensor.motion_device_safety','high') }}
  condition:
    condition: and
    conditions:
      - condition: template
        value_template: >
          {{ high|lower != 'none' and high|lower != 'null' and high|length > 0 }}
  action:
    - condition: template
      value_template: >
        {{ is_state_attr('binary_sensor.motion_device_safety_exist','alarm','on') }}
    - alias: 'trigger motion notification'
      service: automation.trigger
      data_template:
        entity_id: automation.motion_device_safety_high_notify

- id: motion_device_safety_alarm_high_off
  alias: motion_device_safety_alarm_high_off
  mode: single
  initial_state: off
  trigger:
    - platform: state
      entity_id: 'binary_sensor.motion_device_safety_high'
      to: 'off'
  action:
    - condition: template
      value_template: >
        {{ is_state_attr('binary_sensor.motion_device_safety_exist','alarm','on') }}
    - alias: 'trigger safety high dismiss'
      service: automation.trigger
      data_template:
        entity_id: automation.motion_device_safety_high_dismiss

- id: motion_device_safety_high_notify
  alias: motion_device_safety_high_notify
  mode: single
  initial_state: off
  trigger:
  variables:
    high: >-
      {% set high = state_attr('binary_sensor.motion_device_safety','high') %}
      {{ high }}
    action: >-
      {% set action = 'safety-okay' %}
      {{ action }}
    url: >-
      {% set url = '/lovelace/safety-okay' %}
      {{ url }}
    title: >-
      {% set title = states('input_text.motion_app') + ': safety high alarm' %}
      {{ title }}
    notification_id: >-
      {{ 'device-safety-high' }}
    icon: >-
      {% set icon = 'notification-safety-high' %}
      {{ icon }}
    header: >-
      {{- '[![](/local/images/icons/' + icon + '.png?v1)]()' -}}
      {{- '<hr>' -}}
    footer: >-
      {{ '<hr>' }}
    body: >-
      Your home safety alarm was triggered:
      {{- '<p>' -}}
      {% if high|lower != 'none' and not high is string and high is iterable and high|count > 0 %}
        {%- for i in high -%}
          {%- if not loop.first -%}<br>{%- endif -%}
          [{{ i.id }}](/config/devices/device/{{- device_id(high[loop.index-1].entity_id) -}}) is {{ states(high[loop.index-1].entity_id) -}}{{- state_attr(high[loop.index-1].entity_id,'unit_of_measurement') }} in [{{ area_id(high[loop.index-1].entity_id) }}](/config/areas/area/{{- area_id(high[loop.index-1].entity_id) -}})
        {%- endfor %}
      {%- endif %}
      <p>
      We have notified **{{- state_attr('sensor.motion_role_primary','friendly_name') -}}** about your safety alarm.
      <br>
      If you are fine handling this situation on your own, click the **{{- action -}}** button below.
      {{- '<p>' -}}
      {{-  '[![](https://img.shields.io/badge/' + action + '-green.svg)](' + url + ')' -}}
    message: >-
      {{- header -}}
      {{- body -}}
      {{- footer -}}
  action:
    - alias: 'notify safety sensor activated'
      service: persistent_notification.create
      data_template:
        title: >-
          {{- title -}}
        notification_id: >-
          {{- notification_id -}}
        message: >-
          {{- message -}}

- id: motion_device_safety_high_dismiss
  alias: motion_device_safety_high_dismiss
  mode: single
  initial_state: off
  trigger:
  variables:
    notification_id: >-
      {{ 'device-safety-high' }}
  action:
    - alias: 'dismiss safety high notification'
      service: persistent_notification.dismiss
      data_template:
        notification_id: >-
          {{ notification_id }}

## activity

- id: motion_device_safety_alarm_activity
  alias: motion_device_safety_alarm_activity
  mode: single
  initial_state: off
  trigger:
    - platform: state
      entity_id: 'binary_sensor.motion_device_safety_activity'
      to: 'on'
  variables:
    activity: >-
      {{ state_attr('binary_sensor.motion_device_safety','activity') }}
  condition:
    condition: and
    conditions:
      - condition: template
        value_template: >
          {{ activity|lower != 'none' and activity|lower != 'null' and activity|length > 0 }}
  action:
    - condition: template
      value_template: >
        {{ is_state_attr('binary_sensor.motion_device_safety_exist','alarm','on') }}
    - alias: 'trigger motion notification'
      service: automation.trigger
      data_template:
        entity_id: automation.motion_device_safety_activity_notify

- id: motion_device_safety_activity_notify
  alias: motion_device_safety_activity_notify
  mode: single
  initial_state: off
  trigger:
  variables:
    activity: >-
      {% set activity = state_attr('binary_sensor.motion_device_safety','activity') %}
      {{ activity }}
    action: >-
      {% set action = 'safety-okay' %}
      {{ action }}
    url: >-
      {% set url = '/lovelace/safety-okay' %}
      {{ url }}
    title: >-
      {% set title = states('input_text.motion_app') + ': safety activity alarm' %}
      {{ title }}
    notification_id: >-
      {{ 'device-safety-activity' }}
    icon: >-
      {% set icon = 'notification-safety-activity' %}
      {{ icon }}
    header: >-
      {{- '[![](/local/images/icons/' + icon + '.png?v1)]()' -}}
      {{- '<hr>' -}}
    footer: >-
      {{ '<hr>' }}
    body: >-
      Your home safety alarm was triggered:
      {{- '<p>' -}}
      {% if activity|lower != 'none' and not activity is string and activity is iterable and activity|count > 0 %}
        {%- for i in activity -%}
          {%- if not loop.first -%}<br>{%- endif -%}
          [{{ i.id }}](/config/devices/device/{{- device_id(activity[loop.index-1].entity_id) -}}) in [{{ area_id(activity[loop.index-1].entity_id) }}](/config/areas/area/{{- area_id(activity[loop.index-1].entity_id) -}})
        {%- endfor %}
      {%- endif %}
      <p>
      We have notified **{{- state_attr('sensor.motion_role_primary','friendly_name') -}}** about your safety alarm.
      <br>
      If you are fine handling this situation on your own, click the **{{- action -}}** button below.
      {{- '<p>' -}}
      {{-  '[![](https://img.shields.io/badge/' + action + '-green.svg)](' + url + ')' -}}
    message: >-
      {{- header -}}
      {{- body -}}
      {{- footer -}}
  action:
    - alias: 'notify safety sensor activated'
      service: persistent_notification.create
      data_template:
        title: >-
          {{- title -}}
        notification_id: >-
          {{- notification_id -}}
        message: >-
          {{- message -}}

