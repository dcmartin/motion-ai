###
# homeassistant/automation/motion/device/power.yaml
###

- id: motion_device_power_expired_watchdog
  alias: motion_device_power_expired_watchdog
  mode: 'single'
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
    - platform: time_pattern
      minutes: '/5'
  action:
    - alias: 'set expired'
      service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_power_expired
        last: >-
          {{ utcnow().timestamp() }}
        state: >-
          {%- if states is iterable and 'binary_sensor.motion_device_power' in states -%}
            {%- set s = states['binary_sensor.motion_device_power'] -%}
            {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s is iterable and 'last_updated' in s and s.last_updated|lower != 'none' -%}
              {%- set lu = s.last_updated %}
              {%- set s = state_attr('group.motion_device_power','entity_id') -%}
              {%- if s|lower != 'none' and s is iterable and s|count > 0 -%}
                {%- for i in s if 'last_updated' in states[i] and states[i].last_updated > lu -%}
                  {%- if loop.first -%}on{%- endif -%}
                {%- else -%}off{%- endfor -%}
              {%- else -%}off{%- endif -%}
            {%- else -%}off{%- endif -%}
          {%- else -%}off{%- endif -%}

- id: motion_device_power_automation_enable
  alias: motion_device_power_automation_enable
  mode: 'single'
  max_exceeded: 'warn'
  initial_state: 'on'
  trigger:
    platform: state
    entity_id: binary_sensor.motion_device_power_exist
    to: 'on'
  condition:
    - alias: 'test if power existence changed'
      condition: template
      value_template: >-
        {{ trigger.to_state.state != trigger.from_state.state }}
  variables:
    automations: >-
      {% set automations = ['motion_device_power_activity_dismiss','motion_device_power_activity','motion_device_power_activity_notify','motion_device_power_activity_off','motion_device_power_activity_on','motion_device_power_alarm_activity','motion_device_power_alarm_bad_on','motion_device_power_alarm_good_on','motion_device_power_alarm_high_off','motion_device_power_alarm_high_on','motion_device_power_alarm_high_spike_off','motion_device_power_alarm_high_spike_on','motion_device_power_alarm_low_off','motion_device_power_alarm_low_on','motion_device_power_alarm_low_spike_off','motion_device_power_alarm_low_spike_on','motion_device_power_bad','motion_device_power_bad_notify','motion_device_power_bad_off','motion_device_power_bad_on','motion_device_power_cleared_dismiss','motion_device_power_cleared_notify','motion_device_power_good','motion_device_power_good_notify','motion_device_power_good_off','motion_device_power_good_on','motion_device_power_high','motion_device_power_high_dismiss','motion_device_power_high_notify','motion_device_power_high_off','motion_device_power_high_on','motion_device_power_high_spike_on','motion_device_power_high_spike_off','motion_device_power_high_spike','motion_device_power_high_spike_dismiss','motion_device_power_high_spike_notify','motion_device_power_low','motion_device_power_low_dismiss','motion_device_power_low_notify','motion_device_power_low_off','motion_device_power_low_on','motion_device_power_low_spike','motion_device_power_low_spike_on','motion_device_power_low_spike_off','motion_device_power_low_spike_dismiss','motion_device_power_low_spike_notify','motion_device_power_mute','motion_device_power_restart','motion_device_power_self_test','motion_device_power_toggle','motion_device_power_turn_off','motion_device_power_turn_on','motion_device_power_unavailable','motion_device_power_unmute','motion_device_power_status','motion_device_power_status_watchdog','motion_device_power_update','motion_device_power_unavailable_watchdog','motion_device_power_expired','motion_device_power_automation_disable'] %}
      {{ automations }}
  action:
    - alias: 'enable automations for device_power'
      repeat:
        while:
          - condition: template
            value_template: >-
              {% set s = automations[repeat.index-1]|default('') %}
              {{ s|string != '' }}
        sequence:
          - variables:
              automation: >-
                {% set s = automations[repeat.index-1] %}
                {{ 'automation.' + s|string }}
          - condition: template
            value_template: >-
              {{ states(automation)|lower == 'off' }}
          - service: automation.turn_on
            data_template:
              entity_id: >-
                {{ automation }}
          - condition: template
            value_template: >
              {{ states('sensor.motion_log_level') in state_attr('sensor.motion_log_level','trace') }}
          - service: persistent_notification.create
            data_template:
              title: >-
                TRACE: power automation
              notification_id: >
                {{- 'log-automation-power' -}}
              message: >-
                Enable automation: {{ automation }}
    - alias: 'indicate complete'
      service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_power
        automations: >-
          {{ 'on' }}
    - choose:
        - conditions:
            - condition: template
              value_template: >
                {{ states('sensor.motion_log_level') in state_attr('sensor.motion_log_level','trace') }}
          sequence:
            - service: persistent_notification.create
              data_template:
                title: >-
                  TRACE: power enabled
                notification_id: >
                  {{- 'log-automation-power' -}}
                message: >-
                  All power automations enabled: {{ automations|count }}

- id: motion_device_power_automation_disable
  alias: motion_device_power_automation_disable
  initial_state: 'off'
  trigger:
    platform: state
    entity_id: binary_sensor.motion_device_power_exist
    to: 'off'
    for:
      minutes: 1
  condition:
    - alias: 'test if power existence changed'
      condition: template
      value_template: >-
        {{ trigger.from_state.state == 'on' }}
    - alias: 'test if enable'
      condition: template
      value_template: >-
        {{ is_state_attr('automation.motion_device_power_automation_enable','current',0) }}
  variables:
    automations: >-
      {% set automations = ['motion_device_power_activity_dismiss','motion_device_power_activity','motion_device_power_activity_notify','motion_device_power_activity_off','motion_device_power_activity_on','motion_device_power_alarm_activity','motion_device_power_alarm_bad_on','motion_device_power_alarm_good_on','motion_device_power_alarm_high_off','motion_device_power_alarm_high_on','motion_device_power_alarm_high_spike_off','motion_device_power_alarm_high_spike_on','motion_device_power_alarm_low_off','motion_device_power_alarm_low_on','motion_device_power_alarm_low_spike_off','motion_device_power_alarm_low_spike_on','motion_device_power_bad','motion_device_power_bad_notify','motion_device_power_bad_off','motion_device_power_bad_on','motion_device_power_cleared_dismiss','motion_device_power_cleared_notify','motion_device_power_good','motion_device_power_good_notify','motion_device_power_good_off','motion_device_power_good_on','motion_device_power_high','motion_device_power_high_dismiss','motion_device_power_high_notify','motion_device_power_high_off','motion_device_power_high_on','motion_device_power_high_spike_on','motion_device_power_high_spike_off','motion_device_power_high_spike','motion_device_power_high_spike_dismiss','motion_device_power_high_spike_notify','motion_device_power_low','motion_device_power_low_dismiss','motion_device_power_low_notify','motion_device_power_low_off','motion_device_power_low_on','motion_device_power_low_spike','motion_device_power_low_spike_on','motion_device_power_low_spike_off','motion_device_power_low_spike_dismiss','motion_device_power_low_spike_notify','motion_device_power_mute','motion_device_power_restart','motion_device_power_self_test','motion_device_power_toggle','motion_device_power_turn_off','motion_device_power_turn_on','motion_device_power_unavailable','motion_device_power_unmute','motion_device_power_status','motion_device_power_status_watchdog','motion_device_power_update','motion_device_power_unavailable_watchdog','motion_device_power_expired','motion_device_power_automation_disable'] %}
      {{ automations }}
  action:
    - alias: 'disable automations for device_power'
      repeat:
        while:
          - condition: template
            value_template: >-
              {% set s = automations[repeat.index-1]|default('') %}
              {{ s|string != '' }}
        sequence:
          - variables:
              automation: >-
                {% set s = automations[repeat.index-1] %}
                {{ 'automation.' + s|string }}
          - alias: 'test if automation is on'
            condition: template
            value_template: >-
              {{ states(automation)|lower == 'on' }}
          - service: automation.turn_off
            data_template:
              entity_id: >-
                {{ automation }}
          - condition: template
            value_template: >
              {{ states('sensor.motion_log_level') in state_attr('sensor.motion_log_level','trace') }}
          - service: persistent_notification.create
            data_template:
              title: >-
                {% set s = automations[repeat.index-1] %}
                TRACE: power automation
              notification_id: >
                {{- 'log-automation-power' -}}
              message: >-
                Disable automation: {{ automation }}
    - alias: 'indicate complete'
      service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_power
        automations: >-
          {{ 'off' }}
    - choose:
        - conditions:
            - condition: template
              value_template: >
                {{ states('sensor.motion_log_level') in state_attr('sensor.motion_log_level','trace') }}
          sequence:
            - service: persistent_notification.create
              data_template:
                title: >-
                  TRACE: power disabled
                notification_id: >
                  {{- 'log-automation-power' -}}
                message: >-
                  All power automations disabled: {{ automations|count }}

- id: motion_device_power_rescan_watchdog
  alias: motion_device_power_rescan_watchdog
  mode: 'restart'
  max_exceeded: 'warn'
  initial_state: 'on'
  trigger:
    - platform: time_pattern
      minutes: '/15'
  condition:
    - condition: template
      value_template: >-
        {{ is_state_attr('automation.motion_device_power_rescan', 'current', 0) }}
  action:
    - choose:
        - conditions:
            - condition: template
              value_template: >
                {{ states('sensor.motion_log_level') in state_attr('sensor.motion_log_level','debug') }}
            - condition: template
              value_template: >
                {{ trigger.platform == 'time_pattern' }}
          sequence:
            - service: persistent_notification.create
              data_template:
                title: >-
                  DEBUG: power rescan watchdog
                notification_id: >
                  {{- 'debug-power-rescan' -}}
                message: >-
                  Trigger platform: {{ trigger.platform }}
    - alias: 'trigger motion_device_power_rescan'
      service: automation.trigger
      data_template:
        entity_id: automation.motion_device_power_rescan

- id: motion_device_power_rescan
  alias: motion_device_power_rescan
  mode: 'restart'
  max_exceeded: 'warn'
  initial_state: 'on'
  trigger:
    - platform: 'state'
      entity_id: 'binary_sensor.motion_device_power_rescan'
      to: 'on'
      for:
        minutes: 1
  action:
    - wait_template: >-
        {{ is_state_attr('automation.motion_device_power_automation_enable','current',0) and is_state_attr('automation.motion_device_power_automation_disable','current',0) }} 
    - variables:
        class: >-
          {%- set class = 'power' -%}
          {{- class -}}
        all: >-
          {{- state_attr('binary_sensor.motion_device_power_rescan','all') -}}
        mobile: >-
          {{- state_attr('binary_sensor.motion_device_power_rescan','mobile') -}}
        ids: >-
          {% set ids = none %}
          {%- if all|lower != 'none' and all is iterable and all|count > 0 -%}
            {%- set ids = all|map(attribute='id')|sort|unique|list -%}
          {%- endif -%}
          {{ ids }}
        dids: >-
          {% set dids = none %}
          {%- if all|lower != 'none' and all is iterable and all|count > 0 -%}
            {%- set dids = all|map(attribute='did')|list -%}
          {%- endif -%}
          {{ dids }}
        mobiles: >-
          {%- if mobile|lower != 'none' and mobile is iterable and mobile|count > 0 -%}
            {{- mobile -}}
          {% else %}none{% endif %}
        total: >-
          {% set s = dids %}
          {% if not s is string and s is iterable %}
            {{ s|count }}
          {%- else -%}null{%- endif %}
        update: >-
          {%- set s = dids %}
          {%- if not s is string and s is iterable and s|count > 0 -%}
            [{%- for i in s -%}
              {%- set did = i -%}
              {%- set id = all|selectattr('did','==',did)|map(attribute='id')|first -%}
              {%- set o = loop -%}
              {%- for state in (states.button|list + states.switch|list) if device_id(state.entity_id) == did and state.attributes.device_class|default('null')|string == 'update' -%}
                {%- set v = state.state -%}
                {"did":"{{- did -}}","id":"{{- id -}}","entity_id":"{{- state.entity_id -}}"}
                {%- if not (loop.last and o.last) -%},{%- endif -%}
              {%- endfor -%}
            {%- endfor -%}]
          {%- else -%}none{%- endif %}
        restart: >-
          {%- set s = ids %}
          {%- if s|lower != 'unknown' and s|lower != 'none' and not s is string -%}
            [{%- for i in s -%}
              {%- set o = loop -%}
              {%- for state in (states.button|list + states.switch|list) if device_attr(state.entity_id, 'name') == i and state.attributes.device_class|default('null')|string == 'restart' -%}
                {%- set v = state.state -%}
                {"id":"{{- i -}}","entity_id":"{{- state.entity_id -}}"}
                {%- if not (loop.last and o.last) -%},{%- endif -%}
              {%- endfor -%}
            {%- endfor -%}]
          {%- else -%}none{%- endif %}
        controls: >-
          {%- set s = ids -%}
          {%- if s|lower != 'unknown' and s|lower != 'none' and not s is string -%}
            [{%- for i in s -%}
              {%- set o = loop -%}
              {%- for state in (states.button|list + states.switch|list) if device_attr(state.entity_id, 'name') == i and state.attributes.device_class|default('null')|string == 'null' -%}
                {%- set ctl = state_attr(state.entity_id,'friendly_name') -%}
                {"id":"{{- i -}}","entity_id":"{{- state.entity_id -}}","name":"{{- ctl -}}"}
                {%- if not (loop.last and o.last) -%},{%- endif -%}
              {%- endfor -%}
            {%- endfor -%}]
          {%- else -%}none{%- endif %}
        test: >-
          {%- set s = ids -%}
          {%- if s|lower != 'unknown' and s|lower != 'none' and not s is string -%}
            [{%- for i in s -%}
              {%- set o = loop -%}
              {%- for state in states.sensor if device_attr(state.entity_id, 'name') == i and state.attributes.self_test|default('none')|string != 'none' -%}
                {%- if not loop.first or not o.first -%},{%- endif -%}
                {"id":"{{- i -}}","entity_id":"{{- state.entity_id -}}","state":"{{- state.state -}}","updated":{{- state.last_updated|as_timestamp(None) -}}}
              {%- endfor -%}
            {%- endfor -%}]
          {%- else -%}null{%- endif %}
        areas: >-
          {%- set s = dids %}
          {%- if not s is string and s is iterable and s|count > 0 -%}
            [{%- for i in s -%}
              {%- set did = i -%}
              {%- set id = all|selectattr('did','==',did)|map(attribute='id')|first -%}
              {%- set o = loop -%}
              {%- for state in (states.sensor|list + states.binary_sensor|list) if device_id(state.entity_id) == did and (state.attributes.device_class|default('null')|string == class)  -%}
                {"did":"{{- did -}}","id":"{{- id -}}","area":"{{- area_id(state.entity_id) -}}","state":"{{- state.state -}}"}
                {%- if not (loop.last and o.last) -%},{%- endif -%}
              {%- endfor -%}
            {%- endfor -%}]
          {%- else -%}none{%- endif %}
        devices: >-
          {%- set s = dids %}
          {%- if not s is string and s is iterable and s|count > 0 -%}
            [{% for i in s -%}
              {%- set did = i -%}
              {%- set id = all|selectattr('did','==',did)|map(attribute='id')|first -%}
              {%- set k = areas|selectattr('did','==',did)|map(attribute='area')|first -%}
              {%- set n = device_attr(did,'name_by_user') -%}
              {%- if n|lower == 'none' -%}{%- set n = device_attr(did,'name') -%}{%- endif -%}
              {%- if not loop.first -%},{%- endif -%}
              {"id":"{{- id -}}", "did":"{{- did -}}", "friendly_name":"{{- n -}}", "manufacturer":"{{- device_attr(did,'manufacturer') -}}", "model":"{{- device_attr(did,'model') -}}", "sw_version":"{{- device_attr(did,'sw_version') -}}", "area":"{{- k -}}"}
            {%- endfor -%}]
          {%- else -%}none{%- endif %}
    - choose:
        - conditions:
            - condition: template
              value_template: >
                {{ states('sensor.motion_log_level') in state_attr('sensor.motion_log_level','trace') }}
          sequence:
            - service: persistent_notification.create
              data_template:
                title: >-
                  TRACE: power rescan
                notification_id: >
                  {{- 'log-power-rescan' -}}
                message: >-
                  Trigger: {{ trigger }}
    - alias: 'update binary_sensor.motion_device_power ids, etc..'
      service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_power
        scan: >-
          {{ utcnow().timestamp() }}
        ids: >-
          {{ ids }}
        dids: >-
          {{ dids }}
        mobiles: >-
          {{ mobiles }}
        total: >-
          {{ total }}
        update: >-
          {{ update }}
        restart: >-
          {{ restart }}
        controls: >-
          {%- if controls|lower != 'none' and not controls is string and controls is iterable and controls|count > 0  -%}
            {{ controls|reject('none')|list }}
          {%- else -%}
            {{ controls }}
          {%- endif -%}
        test: >-
          {{ test }}
        areas: >-
          {{ areas }}
        devices: >-
          {{ devices }}
        state: >-
          {{ states('binary_sensor.motion_device_' + class) }}

- id: motion_device_power_group_set
  alias: motion_device_power_group_set
  mode: 'restart'
  max_exceeded: 'warn'
  initial_state: 'on'
  trigger:
    - platform: 'state'
      entity_id: 'binary_sensor.motion_device_power_exist'
      to: 'on'
      for:
        minutes: 1
  variables:
    dids: >-
      {{ state_attr('binary_sensor.motion_device_power','dids') }}
    entities: >-
      {%- set s = dids %}
      {%- if not s is string and s is iterable and s|count > 0 -%}
        [{%- for i in s -%}
          {%- set did = i -%}
          {%- set o = loop -%}
          {%- for state in (states.sensor|list + states.binary_sensor|list) if device_id(state.entity_id) == did and (state.attributes.device_class|default('null')|string == 'power')  -%}
            "{{- state.entity_id -}}"
            {%- if not (loop.last and o.last) -%},{%- endif -%}
          {%- endfor -%}
        {%- endfor -%}]
      {%- else -%}[]{%- endif %}
  condition:
    condition: and
    conditions:
      - alias: 'test if entities is iterable'
        condition: template
        value_template: >-
          {{ entities is iterable }}
  action:
    - alias: 'set group.motion_device_power sensors'
      service: group.set
      data_template:
        object_id: motion_device_power
        entities: >-
          {{ entities }}

- id: motion_device_power_statistics_group_set
  alias: motion_device_power_statitics_group_set
  mode: 'restart'
  max_exceeded: 'warn'
  initial_state: 'on'
  trigger:
    - platform: 'state'
      entity_id: 'binary_sensor.motion_device_power_exist'
      to: 'on'
      for:
        minutes: 1
  variables:
    dids: >-
      {{ state_attr('binary_sensor.motion_device_power','dids') }}
    entities: >-
      {%- set s = dids -%}
      {%- if not s is string and s is iterable and s|count > 0 -%}
        [{%- for i in s -%}
          {%- set o = loop -%}
          {%- set did = i -%}
          {%- for state in (states.sensor|list + states.binary_sensor|list) if device_id(state.entity_id) == did and state.attributes.device_class|default('null')|lower == 'null' and state.attributes.state_class|default('null')|string == 'measurement' -%}
            "{{- state.entity_id -}}"
            {%- if not (o.last and loop.last) -%},{%- endif -%}
          {%- endfor -%}
        {%- endfor -%}]
      {%- else -%}[]{%- endif %}
  condition:
    condition: and
    conditions:
      - condition: template
        value_template: >-
          {{ entities is iterable }}
  action:
    - alias: 'set group.motion_device_power statistics'
      service: group.set
      data_template:
        object_id: motion_device_power_statistics
        entities: >-
          {{ entities }}

- id: motion_device_power_unavailable_on
  alias: motion_device_power_unavailable_on
  mode: 'restart'
  max_exceeded: 'warn'
  initial_state: 'on'
  trigger:
    - platform: state
      entity_id: binary_sensor.motion_device_power_exist
      to: 'on'
      for:
        minutes: 1
  variables:
    sensor: >-
      {% set sensor = 'binary_sensor.motion_device_power_unavailable' %}
      {{ sensor }}
    entitys: >-
      {% set entitys = state_attr('group.motion_device_class_unavailable','entity_id') %}
      {{ entitys }}
  condition:
    condition: and
    conditions:
      - alias: 'test if entitys is none or power not in entitys'
        condition: template
        value_template: >-
          {{ entitys|lower == 'none' or ( entitys is iterable and not sensor in entitys ) }}
  action:
    - alias: 'add power to UNAVAILABLE group'
      service: group.set
      data_template:
        object_id: motion_device_class_unavailable
        add_entities: >-
          {{ sensor }}

- id: motion_device_power_unavailable_off
  alias: motion_device_power_unavailable_off
  mode: 'restart'
  max_exceeded: 'warn'
  initial_state: 'on'
  trigger:
    - platform: state
      entity_id: binary_sensor.motion_device_power_exist
      to: 'off'
      for:
        minutes: 1
  variables:
    sensor: >-
      {% set sensor = 'binary_sensor.motion_device_power_unavailable' %}
      {{ sensor }}
    entitys: >-
      {% set entitys = [] %}
      {% set s = state_attr('group.motion_device_class_unavailable','entity_id') %}
      {% if s|lower != 'none' and s is iterable %}
        {% set entitys = s %}
      {% endif %}
      {{ entitys }}
  condition:
    condition: and
    conditions:
      - condition: template
        value_template: >-
          {{ sensor|lower != 'none' and entitys is iterable and sensor in entitys }}
  action:
    - variables:
        entities: >-
          {%- if entitys is iterable and entitys|count > 0 -%}
            {%- set entitys = entitys|list - [ sensor ] -%}
          {%- endif -%}
          {{ entitys }}
    - alias: 'remove power from UNAVAILABLE group'
      service: group.set
      data_template:
        object_id: motion_device_class_unavailable
        add_entities: >-
          {{ sensor }}

## update status

- id: motion_device_power_status_watchdog
  alias: motion_device_power_status_watchdog
  mode: 'restart'
  initial_state: 'off'
  trigger:
    - platform: time_pattern
      minutes: '/5'
  condition:
    - condition: template
      value_template: >-
        {{ is_state('automation.motion_device_power_status','on') }}
    - condition: template
      value_template: >-
        {{ is_state_attr('automation.motion_device_power_status', 'current', 0) }}
  action:
    - choose:
        - conditions:
            - condition: template
              value_template: >
                {{ states('sensor.motion_log_level') in state_attr('sensor.motion_log_level','debug') }}
            - condition: template
              value_template: >
                {{ trigger.platform == 'time_pattern' }}
          sequence:
            - service: persistent_notification.create
              data_template:
                title: >-
                  DEBUG: power status watchdog
                notification_id: >
                  {{- 'debug-power-status' -}}
                message: >-
                  Trigger platform: {{ trigger.platform }}
    - alias: 'trigger power status'
      service: automation.trigger
      data_template:
        entity_id: automation.motion_device_power_status

- id: motion_device_power_status
  alias: motion_device_power_status
  mode: 'restart'
  initial_state: 'off'
  trigger:
    - platform: 'state'
      entity_id: 'binary_sensor.motion_device_power_expired'
      to: 'on'
    - platform: 'state'
      entity_id: 'binary_sensor.motion_device_power_exist'
    - platform: state
      entity_id: 'binary_sensor.motion_device_power'
      attribute: 'automations'
      to: 'on'
    - platform: state
      entity_id: 'group.motion_device_power'
      to:
        - 'on'
        - 'off'
      for:
        minutes: 1
  variables:
    class: >-
      {% set class = 'power' %}
      {{ class }}
    ids: >-
      {% set ids = state_attr('binary_sensor.motion_device_power','ids') %}
      {{ ids }}
    dids: >-
      {% set dids = none %}
      {% set s = state_attr('binary_sensor.motion_device_power','devices') %}
      {%- if not s is string and s is iterable and s|count > 0 -%}
        {%- set dids = s|map(attribute='did')|list -%}
      {%- endif -%}
      {{ dids }}
    status: >-
      {%- set s = dids %}
      {%- if not s is string and s is iterable and s|count > 0 -%}
        [{%- for i in s -%}
          {%- set did = i -%}
          {%- set id = state_attr('binary_sensor.motion_device_power','devices')|selectattr('did','==',did)|map(attribute='id')|first -%}
          {%- set o = loop -%}
          {%- for state in (states.sensor|list + states.binary_sensor|list) if device_id(state.entity_id) == did and (state.attributes.device_class|default('null')|string == class)  -%}
            {"did":"{{- did -}}","id":"{{- id -}}","name":"{{- state_attr(state.entity_id,'friendly_name') -}}","entity_id":"{{- state.entity_id -}}","state":"{{- state.state -}}","updated":{{- state.last_updated|as_timestamp(None) -}}}
            {%- if not (loop.last and o.last) -%},{%- endif -%}
          {%- endfor -%}
        {%- endfor -%}]
      {%- else -%}none{%- endif %}
    statistics: >-
      {%- set s = ids -%}
      {%- if s|lower != 'unknown' and s|lower != 'none' and not s is string -%}
        [{%- for i in s -%}
          {%- set o = loop -%}
          {%- for state in (states.sensor|list + states.binary_sensor|list)
            if device_attr(state.entity_id, 'name') == i
              and state.attributes.device_class|default('null')|lower == 'null'
              and state.attributes.state_class|default('null')|string == 'measurement' -%}
            {"id":"{{- i -}}","name":"{{- state_attr(state.entity_id,'friendly_name') -}}","state":"{{- state.state }} {{ state.attributes.unit_of_measurement-}}","entity_id":"{{- state.entity_id -}}","updated":{{- state.last_updated|as_timestamp(None) -}}}
            {%- if not (loop.last and o.last) -%},{%- endif -%}
          {%- endfor -%}
        {%- endfor -%}]
      {%- else -%}none{%- endif %}
    high: >-
      {% if state_attr('binary_sensor.motion_device_power_exist','high')|float('none') != 'none' -%}
        {% set s = status -%}
        {%- if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s is iterable -%}
          {% set n = state_attr('binary_sensor.motion_device_power_exist','high')|float(0) -%}
          [{% for i in s if 'entity_id' in i and states(i.entity_id)|float(n) > n -%}
            {%- if not loop.first -%},{%- endif -%}
            {"id":"{{- i.id -}}","entity_id":"{{- i.entity_id -}}","state":"{{- i.state -}}","updated":{{- states[i.entity_id].last_updated|as_timestamp(None) -}}}
          {%- endfor -%}]
        {%- else -%}none{%- endif -%}
      {%- else -%}none{%- endif %}
    low: >-
      {% if state_attr('binary_sensor.motion_device_power_exist','low')|float('none') != 'none' -%}
        {%- set s = status -%}
        {%- if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s is iterable -%}
          {% set n = state_attr('binary_sensor.motion_device_power_exist','low')|float(0) -%}
          [{% for i in s if 'entity_id' in i and states(i.entity_id)|float(n) < n -%}
            {%- if not loop.first -%},{%- endif -%}
            {"id":"{{- i.id -}}","entity_id":"{{- i.entity_id -}}","state":"{{- i.state -}}","updated":{{- states[i.entity_id].last_updated|as_timestamp(None) -}}}
          {%- endfor -%}]
        {%- else -%}none{%- endif -%}
      {%- else -%}none{%- endif %}
    good: >-
      {% set good = state_attr('binary_sensor.motion_device_power_exist','good') -%}
      {%- if good|lower != 'none' -%}
        {% set s = status -%}
        {%- if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s is iterable -%}
          [{% for i in s if 'entity_id' in i and not i.entity_id is none and is_state(i.entity_id,good) -%}
            {%- if not loop.first -%},{%- endif -%}
            {"id":"{{- i.id -}}","entity_id":"{{- i.entity_id -}}","state":"{{- i.state -}}","updated":{{- states[i.entity_id].last_updated|as_timestamp(None) -}}}
          {%- endfor -%}]
        {%- else -%}none{%- endif -%}
      {%- else -%}none{%- endif %}
    bad: >-
      {% set bad = state_attr('binary_sensor.motion_device_power_exist','bad') -%}
      {%- if bad|lower != 'none' -%}
        {% set s = status -%}
        {%- if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s is iterable -%}
          [{% for i in s if 'entity_id' in i and not i.entity_id is none and is_state(i.entity_id,bad) -%}
            {%- if not loop.first -%},{%- endif -%}
            {"id":"{{- i.id -}}","entity_id":"{{- i.entity_id -}}","state":"{{- i.state -}}","updated":{{- states[i.entity_id].last_updated|as_timestamp(None) -}}}
          {%- endfor -%}]
        {%- else -%}none{%- endif -%}
      {%- else -%}none{%- endif %}
    activity: >-
      {% set activity = state_attr('binary_sensor.motion_device_power_exist','activity') -%}
      {%- if activity|lower != 'none' -%}
        {% set s = status -%}
        {%- if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s is iterable -%}
          [{% for i in s if 'entity_id' in i and not i.entity_id is none and is_state(i.entity_id,activity) -%}
            {%- if not loop.first -%},{%- endif -%}
            {"id":"{{- i.id -}}","entity_id":"{{- i.entity_id -}}","state":"{{- i.state -}}","updated":{{- states[i.entity_id].last_updated|as_timestamp(None) -}}}
          {%- endfor -%}]
        {%- else -%}none{%- endif -%}
      {%- else -%}none{%- endif %}
    found: >-
      {%- set s = status -%}
      {%- if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s is iterable -%}
        [{% for i in s if 'entity_id' in i and not i.entity_id is none and not is_state(i.entity_id,'unavailable') and not is_state(i.entity_id,'unknown') -%}
          {%- if not loop.first -%},{%- endif -%}
            {"id":"{{- i.id -}}","entity_id":"{{- i.entity_id -}}","state":"{{- i.state -}}","updated":{{- states[i.entity_id].last_updated|as_timestamp(None) -}}}
        {%- endfor -%}]
      {%- else -%}none{%- endif %}
    high_spike: >-
      {% if state_attr('binary_sensor.motion_device_power_exist','high_spike')|float('none') != 'none' -%}
        {% set s = status -%}
        {%- if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s is iterable -%}
          {% set n = state_attr('binary_sensor.motion_device_power_exist','high_spike')|float(0) -%}
          [{% for i in s if 'entity_id' in i and states(i.entity_id)|float(n) > n -%}
            {%- if not loop.first -%},{%- endif -%}
            {"id":"{{- i.id -}}","entity_id":"{{- i.entity_id -}}","state":"{{- i.state -}}","updated":{{- states[i.entity_id].last_updated|as_timestamp(None) -}}}
          {%- endfor -%}]
        {%- else -%}none{%- endif -%}
      {%- else -%}none{%- endif %}
    low_spike: >-
      {% if state_attr('binary_sensor.motion_device_power_exist','low_spike')|float('none') != 'none' -%}
        {% set s = status -%}
        {%- if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s is iterable -%}
          {% set n = state_attr('binary_sensor.motion_device_power_exist','low_spike')|float(0) -%}
          [{% for i in s if 'entity_id' in i and states(i.entity_id)|float(n) < n -%}
            {%- if not loop.first -%},{%- endif -%}
            {"id":"{{- i.id -}}","entity_id":"{{- i.entity_id -}}","state":"{{- i.state -}}","updated":{{- states[i.entity_id].last_updated|as_timestamp(None) -}}}
          {%- endfor -%}]
        {%- else -%}none{%- endif -%}
      {%- else -%}none{%- endif %}
    values: >-
      {% set g = state_attr('binary_sensor.motion_device_power_exist','good') -%}
      {% set b = state_attr('binary_sensor.motion_device_power_exist','bad') -%}
      {%- if g|lower != 'none' and b|lower != 'none' -%}
        [{% for i in status if 'entity_id' in i and not i.entity_id is none -%}
          {%- if not loop.first -%},{%- endif -%}
          {% if is_state(i.entity_id,g) %}1{% elif is_state(i.entity_id,b) %}0{% else %}none{% endif -%}
        {%- endfor -%}]
      {%- else -%}
        [{% for i in status if 'entity_id' in i and not i.entity_id is none -%}
          {%- if not loop.first -%},{%- endif -%}
          {{- states(i.entity_id)|float(none) -}}
        {%- endfor -%}]
      {%- endif %}
    value: >-
      {% if bad|lower != 'none' and not bad is string and bad is iterable -%}
        {%- if bad|count > 0 -%}
          {%- set result = 'on' -%}
        {%- else -%}
          {%- set result = 'off' -%}
        {%- endif -%}
      {%- endif -%}
      {%- if low|lower != 'none' and not low is string and low is iterable -%}
        {%- if low|count > 0 -%}
          {%- set result = 'on' -%}
        {%- else -%}
          {%- set result = 'off' -%}
        {%- endif -%}
      {%- endif -%}
      {%- if high|lower != 'none' and not high is string and high is iterable -%}
        {%- if high|count > 0 -%}
          {%- set result = 'on' -%}
        {%- else -%}
          {%- set result = 'off' -%}
        {%- endif -%}
      {%- endif -%}
      {%- if activity|lower != 'none' and not activity is string and activity is iterable -%}
        {%- if activity|count > 0 -%}
          {%- set result = 'on' -%}
        {%- else -%}
          {%- set result = 'off' -%}
        {%- endif -%}
      {%- endif -%}
      {%- if result is defined -%}
        {{- result -}}
      {%- else -%}unknown{%- endif %}
  action:
    - alias: 'update binary_sensor.motion_device_power state'
      service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_power
        status: >-
          {{ status }}
        statistics: >-
          {{ statistics|reject('none')|list }}
        high: >-
          {{ high }}
        low: >-
          {{ low }}
        good: >-
          {{ good }}
        bad: >-
          {{ bad }}
        high_spike: >-
          {{ high_spike }}
        low_spike: >-
          {{ low_spike }}
        found: >-
          {{ found }}
        activity: >-
          {{ activity }}
        values: >-
          {{ values }}
        state: >-
          {{ value }}

## change power class attributes (alarm,required,..)

# required

- id: motion_device_power_required_off
  alias: motion_device_power_required_off
  mode: single
  max_exceeded: 'warn'
  initial_state: 'on'
  trigger:
  condition:
    condition: and
    conditions:
      - alias: 'test setup complete'
        condition: template
        value_template: >-
          {{ is_state('binary_sensor.motion_device_class','on') }}
      - alias: 'power required not off'
        condition: template
        value_template: >-
          {{ 'power' in state_attr('binary_sensor.motion_device_class','sensors') or 'power' in state_attr('binary_sensor.motion_device_class','binary_sensors') }}
  variables:
    sensors: >-
      {% set s = state_attr('binary_sensor.motion_device_class','sensors') -%}
      {%- if s|lower != 'unknown' and s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
        {%- set s = s|reject('==','power')|list -%}
        {%- if not s is string and s is iterable and s|count > 0 -%}
          {% set sensors = s -%}
        {%- else -%}
          {% set sensors = [] -%}
        {%- endif %}
      {%- endif %}
      {%- if sensors is defined -%}
        {{ sensors }}
      {%- else -%}none{%- endif %}
    binary_sensors: >-
      {% set s = state_attr('binary_sensor.motion_device_class','binary_sensors') -%}
      {%- if s|lower != 'unknown' and s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
        {%- set s = s|reject('==','power')|list -%}
        {%- if not s is string and s is iterable and s|count > 0 -%}
          {% set binary_sensors = s -%}
        {%- else -%}
          {% set binary_sensors = [] -%}
        {%- endif %}
      {%- endif %}
      {%- if binary_sensors is defined -%}
        {{ binary_sensors }}
      {%- else -%}none{%- endif %}
  action:
    - alias: 'test if required list of device classes is valid'
      condition: template
      value_template: >-
        {{ sensors|lower != 'none' and binary_sensors|lower != 'none' }}
    - alias: 'update binary_sensor.motion_device_class required attribute; remove: power'
      service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_class
        sensors: >-
          {{ sensors }}
        binary_sensors: >-
          {{ binary_sensors }}
    - alias: 'update binary_sensor.motion_device_power_exist'
      service: homeassistant.update_entity
      entity_id: binary_sensor.motion_device_power_exist

- id: motion_device_power_required_on
  alias: motion_device_power_required_on
  mode: single
  max_exceeded: 'warn'
  initial_state: 'on'
  trigger:
  condition:
    condition: and
    conditions:
      - alias: 'test setup complete'
        condition: template
        value_template: >-
          {{ is_state('binary_sensor.motion_device_class','on') }}
      - alias: 'power required not on'
        condition: template
        value_template: >-
          {{ 'power' in state_attr('binary_sensor.motion_device_class','sensor_list') or 'power' in state_attr('binary_sensor.motion_device_class','binary_sensor_list')
            and (not 'power' in state_attr('binary_sensor.motion_device_class','sensors') or not 'power' in state_attr('binary_sensor.motion_device_class','binary_sensors')) }}
  variables:
    sensors: >-
      {% set s = state_attr('binary_sensor.motion_device_class','sensors') -%}
      {%- if s|lower != 'unknown' and s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
        {%- set sensors = (s|list + ['power'])|sort|unique|list -%}
      {%- else -%}
        {%- set sensors = ['power'] -%}
      {%- endif %}
      {%- if sensors is defined -%}
        {{ sensors }}
      {%- else -%}none{%- endif %}
    binary_sensors: >-
      {% set s = state_attr('binary_sensor.motion_device_class','binary_sensors') -%}
      {%- if s|lower != 'unknown' and s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
        {%- set binary_sensors = (s|list + ['power'])|sort|unique|list -%}
      {%- else -%}
        {%- set binary_sensors = ['power'] -%}
      {%- endif %}
      {%- if binary_sensors is defined -%}
        {{ binary_sensors }}
      {%- else -%}none{%- endif %}
  action:
    - alias: 'test if required list of device classes is valid'
      condition: template
      value_template: >-
        {{ sensors|lower != 'none' and binary_sensors|lower != 'none' }}
    - alias: 'update binary_sensor.motion_device_class required attribute; add: power'
      service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_class
        sensors: >-
          {{ sensors }}
        binary_sensors: >-
          {{ binary_sensors }}
    - alias: 'update binary_sensor.motion_device_power_exist'
      service: homeassistant.update_entity
      entity_id: binary_sensor.motion_device_power_exist

## bad

- id: motion_device_power_bad
  alias: motion_device_power_bad
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
    - platform: state
      entity_id: 'binary_sensor.motion_device_power'
      attribute: 'bad'
  variables:
    details: >-
      {% if state_attr('binary_sensor.motion_device_power_exist','bad')|lower != 'none' %}
        {% set s = state_attr('binary_sensor.motion_device_power','bad') %}
        {%- if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s is iterable -%}
          [{% for i in s -%}
            {%- if not loop.first -%},{%- endif -%}
            "{{- state_attr(i.entity_id,'friendly_name') }} is {{ states(i.entity_id) -}}"
          {%- endfor -%}]
        {%- else -%}none{%- endif %}
      {%- else -%}none{%- endif %}
    state: >-
      {% set s = state_attr('binary_sensor.motion_device_power','bad') %}
      {% if s|lower != 'null' and s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and not s is string and s is iterable %}
        {% if s|list|count > 0 %}on{% else %}off{% endif %}
      {% else %}unknown{% endif %}
    last: >-
      {% if state|lower == 'off' %}
        {{ state_attr('binary_sensor.motion_device_power_bad','last') }}
      {%- else -%}{{ utcnow().timestamp() }}{%- endif %}
  condition:
    - condition: template
      value_template: >
        {{ state|lower != 'unknown' }}
  action:
    - service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_power_bad
        last: >-
          {{ last }}
        details: >-
          {{ details }}
        state: >-
          {{ state }}
    - condition: template
      value_template: >
        {{ state|lower == 'on' }}
    - service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_power
        condition: >-
          {{ 'bad' }}

- id: motion_device_power_bad_off
  alias: motion_device_power_bad_off
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
  condition:
    condition: and
    conditions:
      - alias: 'test setup complete'
        condition: template
        value_template: >-
          {{ is_state('binary_sensor.motion_device_class','on') }}
      - alias: 'power bad not off'
        condition: template
        value_template: >-
          {{ 'power' in state_attr('binary_sensor.motion_device_class','bad') }}
  variables:
    bad: >-
      {% set s = state_attr('binary_sensor.motion_device_class','bad') -%}
      {%- if s|lower != 'unknown' and s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
        {%- set s = s|reject('==','power')|list -%}
        {%- if not s is string and s is iterable and s|count > 0 -%}
          {% set bad = s -%}
        {%- else -%}
          {% set bad = [] -%}
        {%- endif %}
      {%- endif %}
      {%- if bad is defined -%}
        {{ bad }}
      {%- else -%}none{%- endif %}
  action:
    - alias: 'test if bad list of device classes is valid'
      condition: template
      value_template: >-
        {{ bad|lower != 'none' }}
    - alias: 'update binary_sensor.motion_device_class bad attribute; remove: power'
      service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_class
        bad: >-
          {{ bad }}
    - alias: 'update binary_sensor.motion_device_power_exist'
      service: homeassistant.update_entity
      entity_id: binary_sensor.motion_device_power_exist

- id: motion_device_power_bad_on
  alias: motion_device_power_bad_on
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
  condition:
    condition: and
    conditions:
      - alias: 'test setup complete'
        condition: template
        value_template: >-
          {{ is_state('binary_sensor.motion_device_class','on') }}
      - alias: 'power bad not on'
        condition: template
        value_template: >-
          {{ not 'power' in state_attr('binary_sensor.motion_device_class','bad') }}
  variables:
    bad: >-
      {% set s = state_attr('binary_sensor.motion_device_class','bad') -%}
      {%- if s|lower != 'unknown' and s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
        {%- set bad = (s|list + ['power'])|sort|unique|list -%}
      {%- else -%}
        {%- set bad = ['power'] -%}
      {%- endif %}
      {%- if bad is defined -%}
        {{ bad }}
      {%- else -%}none{%- endif %}
  action:
    - alias: 'test if bad list of device classes is valid'
      condition: template
      value_template: >-
        {{ bad|lower != 'none' }}
    - alias: 'update binary_sensor.motion_device_class bad attribute; add: power'
      service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_class
        bad: >-
          {{ bad }}
    - alias: 'update binary_sensor.motion_device_power_exist'
      service: homeassistant.update_entity
      entity_id: binary_sensor.motion_device_power_exist

- id: motion_device_power_alarm_bad_on
  alias: motion_device_power_alarm_bad_on
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
    - platform: template
      value_template: >-
        {{ not is_state_attr('binary_sensor.motion_device_power_exist','bad','none')
           and is_state_attr('binary_sensor.motion_device_power_exist','bad',states('binary_sensor.motion_device_power')) }}
  variables:
    devices: >-
      {%- set devices = none -%}
      {% set s = state_attr('binary_sensor.motion_device_power','devices') %}
      {% if s|lower != 'none' and s|lower != 'null' and not s is string and s is iterable and s|count > 0 %}
        {% set devices = s %}
      {%- endif %}
      {{ devices }}
    last: >-
      {% set last = none %}
      {% set s = state_attr('binary_sensor.motion_device_power','status')|selectattr('state','==',state_attr('binary_sensor.motion_device_power_exist','bad'))|sort(attribute='updated',reverse=true)|list %}
      {% if s|lower != 'none' and s|lower != 'null' and not s is string and s is iterable and s|count > 0 %}
        {% set last = s %}
      {%- endif %}
      {{ last  }}
    which: >-
      {% set which = none %}
      {% if last|lower != 'none' %}
        {% set which = last|map(attribute='id')|first %}
      {%- endif %}
      {{ which }}
    did: >-
      {% set did = none %}
      {% if devices|lower != 'none'  and which|lower != 'none' %}
        {% set did = devices|selectattr('id','==',which)|map(attribute='did')|first %}
      {%- endif %}
      {{ did }}
    where: >-
      {% set where = none %}
      {% if devices|lower != 'none'  and which|lower != 'none' %}
        {% set where = devices|selectattr('id','==',which)|map(attribute='area')|first %}
      {%- endif %}
      {{ where }}
  condition:
    condition: and
    conditions:
      - condition: template
        value_template: >
          {{ which|lower != 'none' and which|lower != 'null' and which|length > 0 }}
  action:
    - variables:
        markdown: >-
          A <a href="/lovelace-primary/power">power</a> alarm was triggered ({{- state_attr('binary_sensor.motion_device_power_exist','bad')|string -}}); sensor: {{ which -}}; where: {{ where -}}; at {{ utcnow().timestamp()|timestamp_custom("%a %b %d %I:%M %p %Z",true,'unknown') -}}.
    - service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_power_bad
        markdown: >-
          {{ markdown }}
    - alias: 'update binary_sensor.motion_device_power which, where, did'
      service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_power
        which: >-
          {{ which }}
        where: >-
          {{ where }}
        did: >-
          {{ did }}
    - condition: template
      value_template: >
        {{ is_state_attr('binary_sensor.motion_device_power_exist','alarm','on') }}
    - alias: 'trigger motion notification'
      service: automation.trigger
      data_template:
        entity_id: automation.motion_device_power_bad_notify

- id: motion_device_power_bad_notify
  alias: motion_device_power_bad_notify
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
  variables:
    class: >-
      {{ 'power' }}
    ids: >-
      {% set ids = state_attr('binary_sensor.motion_device_power','ids') %}
      {{ ids }}
    dids: >-
      {% set dids = none %}
      {% set s = state_attr('binary_sensor.motion_device_power','devices') %}
      {%- if not s is string and s is iterable and s|count > 0 -%}
        {%- set dids = s|map(attribute='did')|list -%}
      {%- endif -%}
      {{ dids }}
    which: >-
      {% set which = state_attr('binary_sensor.motion_device_power','which') %}
      {{ which }}
    where: >-
      {% set where = state_attr('binary_sensor.motion_device_power','where') %}
      {{ where }}
    did: >-
      {% set did = state_attr('binary_sensor.motion_device_power','did') %}
      {{ did }}
    url: >-
      {% set url = '/lovelace-primary/' + class %}
      {{ url }}
    action: >-
      {% set action = class + '-alarm' %}
      {{ action }}
    title: >-
      {% set title = states('sensor.motion_app') + ': power bad' %}
      {{ title }}
    notification_id: >-
      {{ 'device-power-status-' + which|string }}
    icon: >-
      {% set icon = 'notification-power-bad' %}
      {{ icon }}
    header: >-
      {{- '<img src="/local/images/icons/' + icon + '.png?v1">' -}}
    footer: >-
      <a href="/notify-devices/{{- class -}}"><img src="https://img.shields.io/badge/{{- class -}}-device-cyan.svg"></a>
      {%- if did is defined and did|lower != 'none' and did|lower != 'null' -%}
         <br><a href="/config/devices/device/{{- did -}}"><img src="https://img.shields.io/badge/{{- class -}}-config-blue.svg"></a>
      {%- endif -%}
    body: >-
      {{ state_attr('binary_sensor.motion_device_power_bad','markdown') }}
      <p>We have notified <b>{{- state_attr('sensor.motion_role_primary','friendly_name') -}}</b> about your power alarm.
      To inspect and change the alarm click the <b>{{- action -}}</b> button below.
      {{- '<p>' -}}
      <a href="{{- url -}}"><img src="https://img.shields.io/badge/{{- action -}}-yellow.svg"></a>
    message: >-
      {{- header -}}<hr>{{- body -}}<br>{{- footer -}}
  action:
    - alias: 'notify power sensor activated'
      service: persistent_notification.create
      data_template:
        title: >-
          {{- title -}}
        notification_id: >-
          {{- notification_id -}}
        message: >-
          {{- message -}}

## low

- id: motion_device_power_low
  alias: motion_device_power_low
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
    - platform: state
      entity_id: 'binary_sensor.motion_device_power'
      attribute: 'low'
  variables:
    details: >-
      {% if state_attr('binary_sensor.motion_device_power_exist','low')|lower != 'none' %}
        {% set s = state_attr('binary_sensor.motion_device_power','low') %}
        {%- if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s is iterable -%}
          [{% for i in s -%}
            {%- if not loop.first -%},{%- endif -%}
            "{{- state_attr(i.entity_id,'friendly_name') }} is {{ states(i.entity_id)|int('unknown') -}}{{ state_attr(i.entity_id,'unit_of_measurement') -}}"
          {%- endfor -%}]
        {%- else -%}none{%- endif %}
      {%- else -%}none{%- endif %}
    state: >-
      {% set s = state_attr('binary_sensor.motion_device_power','low') %}
      {% if s|lower != 'null' and s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and not s is string and s is iterable %}
        {% if s|list|count > 0 %}on{% else %}off{% endif %}
      {% else %}unknown{% endif %}
    last: >-
      {% if state|lower == 'off' %}
        {{ state_attr('binary_sensor.motion_device_power_low','last') }}
      {%- else -%}{{ utcnow().timestamp() }}{%- endif %}
  condition:
    - condition: template
      value_template: >
        {{ state|lower != 'unknown' }}
  action:
    - service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_power_low
        last: >-
          {{ last }}
        details: >-
          {{ details }}
        state: >-
          {{ state }}
    - condition: template
      value_template: >
        {{ state|lower == 'on' }}
    - alias: 'set power condition: low'
      service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_power
        condition: >-
          {{ 'low' }}

- id: motion_device_power_low_off
  alias: motion_device_power_low_off
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
  condition:
    condition: and
    conditions:
      - alias: 'test setup complete'
        condition: template
        value_template: >-
          {{ is_state('binary_sensor.motion_device_class','on') }}
      - alias: 'power low not off'
        condition: template
        value_template: >-
          {{ 'power' in state_attr('binary_sensor.motion_device_class','low') }}
  variables:
    low: >-
      {% set s = state_attr('binary_sensor.motion_device_class','low') -%}
      {%- if s|lower != 'unknown' and s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
        {%- set s = s|reject('==','power')|list -%}
        {%- if not s is string and s is iterable and s|count > 0 -%}
          {% set low = s -%}
        {%- else -%}
          {% set low = [] -%}
        {%- endif %}
      {%- endif %}
      {%- if low is defined -%}
        {{ low }}
      {%- else -%}none{%- endif %}
  action:
    - alias: 'test if low list of device classes is valid'
      condition: template
      value_template: >-
        {{ low|lower != 'none' }}
    - alias: 'update binary_sensor.motion_device_class low attribute; remove: power'
      service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_class
        low: >-
          {{ low }}
    - alias: 'update binary_sensor.motion_device_power_exist'
      service: homeassistant.update_entity
      entity_id: binary_sensor.motion_device_power_exist

- id: motion_device_power_low_on
  alias: motion_device_power_low_on
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
  condition:
    condition: and
    conditions:
      - alias: 'test setup complete'
        condition: template
        value_template: >-
          {{ is_state('binary_sensor.motion_device_class','on') }}
      - alias: 'power low not on'
        condition: template
        value_template: >-
          {{ not 'power' in state_attr('binary_sensor.motion_device_class','low') }}
  variables:
    low: >-
      {% set s = state_attr('binary_sensor.motion_device_class','low') -%}
      {%- if s|lower != 'unknown' and s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
        {%- set low = (s|list + ['power'])|sort|unique|list -%}
      {%- else -%}
        {%- set low = ['power'] -%}
      {%- endif %}
      {%- if low is defined -%}
        {{ low }}
      {%- else -%}none{%- endif %}
  action:
    - alias: 'test if low list of device classes is valid'
      condition: template
      value_template: >-
        {{ low|lower != 'none' }}
    - alias: 'update binary_sensor.motion_device_class low attribute; add: power'
      service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_class
        low: >-
          {{ low }}
    - alias: 'update binary_sensor.motion_device_power_exist'
      service: homeassistant.update_entity
      entity_id: binary_sensor.motion_device_power_exist

- id: motion_device_power_alarm_low_on
  alias: motion_device_power_alarm_low_on
  mode: restart
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
    - platform: state
      entity_id: 'binary_sensor.motion_device_power_low'
      to: 'on'
  variables:
    low: >-
      {{ state_attr('binary_sensor.motion_device_power','low') }}
  condition:
    condition: and
    conditions:
      - condition: template
        value_template: >
          {{- low|lower != 'none' and low|lower != 'unknown' and not low is string and low is iterable and low|count > 0 -}}
  action:
    - variables:
        markdown: >-
          A <a href="/lovelace-primary/power">power</a> alarm was triggered (below {{ state_attr('binary_sensor.motion_device_power_exist','low') -}}) at {{ utcnow().timestamp()|timestamp_custom("%a %b %d %I:%M %p %Z",true,'unknown') }}:<br>
          {%- for i in low if 'id' in i and i.id|lower != 'none' -%}
            {%- if loop.first -%}{{- '<ul>' -}}{%- endif -%}
            {%- set d = state_attr('binary_sensor.motion_device_class','devices') -%}
            {%- if d|lower != 'none' and not d is string and d is iterable and d|count > 0 -%}
              {%- set d = d|selectattr('id','==',i.id)|list -%}
              {%- if d|lower != 'none' and not d is string and d is iterable and d|count > 0 -%}
                {%- set d = d|first -%}
                <li><a href="/config/devices/device/{{- d.did -}}">{{- d.friendly_name|replace("'","") -}}</a> is {{ states(low[loop.index-1].entity_id) -}}{{- state_attr(low[loop.index-1].entity_id,'unit_of_measurement') }} in <a href="/config/areas/area/{{- area_id(low[loop.index-1].entity_id) -}}">{{- area_id(low[loop.index-1].entity_id) -}}</a></li>
              {%- else -%}
                <li>{{- i.id }} power is {{ states(i.entity_id) -}}{{- state_attr(i.entity_id,'unit_of_measurement') }}</li>
              {%- endif -%}
            {%- else -%}
              {{- '<li>' -}}
              {{- i.id }} power is {{ states(i.entity_id) -}}{{- state_attr(i.entity_id,'unit_of_measurement') }}
              {{- '</li>' -}}
            {%- endif -%}
            {%- if loop.last -%}{{- '</ul>' -}}{%- endif -%}
          {%- endfor -%}
    - service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_power_low
        markdown: >-
          {{ markdown }}
    - condition: template
      value_template: >
        {{ is_state_attr('binary_sensor.motion_device_power_exist','alarm','on') }}
    - alias: 'trigger motion notification'
      service: automation.trigger
      data_template:
        entity_id: automation.motion_device_power_low_notify

- id: motion_device_power_alarm_low_off
  alias: motion_device_power_alarm_low_off
  mode: restart
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
    - platform: state
      entity_id: 'binary_sensor.motion_device_power_low'
      to: 'off'
  action:
    - condition: template
      value_template: >
        {{ is_state_attr('binary_sensor.motion_device_power_exist','alarm','on') }}
    - alias: 'trigger power low dismiss'
      service: automation.trigger
      data_template:
        entity_id: automation.motion_device_power_low_dismiss

- id: motion_device_power_low_notify
  alias: motion_device_power_low_notify
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
  variables:
    class: >-
      {{ 'power' }}
    low: >-
      {% set low = state_attr('binary_sensor.motion_device_power','low') %}
      {{ low }}
    action: >-
      {% set action = class + '-alarm' %}
      {{ action }}
    url: >-
      {% set url = '/lovelace-primary/' + class %}
      {{ url }}
    title: >-
      {% set title = states('sensor.motion_app') + ': power low' %}
      {{ title }}
    notification_id: >-
      {{ 'device-power-low' }}
    icon: >-
      {% set icon = 'notification-power-low' %}
      {{ icon }}
    header: >-
      {{- '<img src="/local/images/icons/' + icon + '.png?v1">' -}}
    footer: >-
      <a href="/notify-devices/{{- class -}}"><img src="https://img.shields.io/badge/{{- class -}}-device-cyan.svg"></a>
      {%- if did is defined and did|lower != 'none' and did|lower != 'null' -%}
         <br><a href="/config/devices/device/{{- did -}}"><img src="https://img.shields.io/badge/{{- class -}}-config-blue.svg"></a>
      {%- endif -%}
    body: >-
      {{ state_attr('binary_sensor.motion_device_power_low','markdown') }}
      <p>We have notified <b>{{- state_attr('sensor.motion_role_primary','friendly_name') -}}</b> about your power alarm.
      To inspect and change the alarm click the <b>{{- action -}}</b> button below.
      {{- '<p>' -}}
      <a href="{{- url -}}"><img src="https://img.shields.io/badge/{{- action -}}-yellow.svg"></a>
    message: >-
      {{- header -}}<hr>{{- body -}}<br>{{- footer -}}
  action:
    - alias: 'notify power sensor activated'
      service: persistent_notification.create
      data_template:
        title: >-
          {{- title -}}
        notification_id: >-
          {{- notification_id -}}
        message: >-
          {{- message -}}

- id: motion_device_power_dismiss
  alias: motion_device_power_dismiss
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
  variables:
    alarms: >-
      {% set alarms = state_attr('binary_sensor.motion_device_class','alarms') %}
      {{ alarms }}
  condition:
    - condition: template
      value_template: >
        {{ alarms|lower != 'none' and not alarms is string and alarms is iterable and alarms|count > 0 }}
  action:
    - repeat:
        while:
          - condition: template
            value_template: >-
              {% set s = alarms[repeat.index - 1]|default('unknown') -%}
              {{ s|lower != 'unavailable' and s|lower != 'unknown' and s|lower != 'null' and s|string|length > 0 }}
        sequence:
          - variables:
              alarm: >-
                {% set alarm = alarms[repeat.index - 1]|default('unknown') -%}
                {{ alarm }}
              automation: >-
                {{ 'automation.motion_device_power_' + alarm + '_dismiss' }}
          - service: automation.trigger
            data_template:
              entity_id: >-
                {{ automation }}

- id: motion_device_power_low_dismiss
  alias: motion_device_power_low_dismiss
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
  variables:
    notification_id: >-
      {{ 'device-power-low' }}
  action:
    - alias: 'dismiss power low notification'
      service: persistent_notification.dismiss
      data_template:
        notification_id: >-
          {{ notification_id }}

## high

- id: motion_device_power_high
  alias: motion_device_power_high
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
    - platform: state
      entity_id: 'binary_sensor.motion_device_power'
      attribute: 'high'
  variables:
    details: >-
      {% if state_attr('binary_sensor.motion_device_power_exist','high')|lower != 'none' %}
        {% set s = state_attr('binary_sensor.motion_device_power','high') %}
        {%- if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s is iterable -%}
          [{% for i in s -%}
            {%- if not loop.first -%},{%- endif -%}
            "{{- state_attr(i.entity_id,'friendly_name') }} is {{ states(i.entity_id)|int('unknown') -}}{{ state_attr(i.entity_id,'unit_of_measurement') -}}"
          {%- endfor -%}]
        {%- else -%}none{%- endif %}
      {%- else -%}none{%- endif %}
    state: >-
      {% set s = state_attr('binary_sensor.motion_device_power','high') %}
      {% if s|lower != 'null' and s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and not s is string and s is iterable %}
        {% if s|list|count > 0 %}on{% else %}off{% endif %}
      {% else %}unknown{% endif %}
    last: >-
      {% if state|lower == 'off' %}
        {{ state_attr('binary_sensor.motion_device_power_high','last') }}
      {%- else -%}{{ utcnow().timestamp() }}{%- endif %}
  condition:
    - condition: template
      value_template: >
        {{ state|lower != 'unknown' }}
  action:
    - service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_power_high
        last: >-
          {{ last }}
        details: >-
          {{ details }}
        state: >-
          {{ state }}
    - condition: template
      value_template: >
        {{ state|lower == 'on' }}
    - alias: 'set power condition: high'
      service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_power
        condition: >-
          {{ 'high' }}

- id: motion_device_power_high_off
  alias: motion_device_power_high_off
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
  condition:
    condition: and
    conditions:
      - alias: 'test setup complete'
        condition: template
        value_template: >-
          {{ is_state('binary_sensor.motion_device_class','on') }}
      - alias: 'power high not off'
        condition: template
        value_template: >-
          {{ 'power' in state_attr('binary_sensor.motion_device_class','high') }}
  variables:
    high: >-
      {% set s = state_attr('binary_sensor.motion_device_class','high') -%}
      {%- if s|lower != 'unknown' and s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
        {%- set s = s|reject('==','power')|list -%}
        {%- if not s is string and s is iterable and s|count > 0 -%}
          {% set high = s -%}
        {%- else -%}
          {% set high = [] -%}
        {%- endif %}
      {%- endif %}
      {%- if high is defined -%}
        {{ high }}
      {%- else -%}none{%- endif %}
  action:
    - alias: 'test if high list of device classes is valid'
      condition: template
      value_template: >-
        {{ high|lower != 'none' }}
    - alias: 'update binary_sensor.motion_device_class high attribute; remove: power'
      service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_class
        high: >-
          {{ high }}
    - alias: 'update binary_sensor.motion_device_power_exist'
      service: homeassistant.update_entity
      entity_id: binary_sensor.motion_device_power_exist

- id: motion_device_power_high_on
  alias: motion_device_power_high_on
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
  condition:
    condition: and
    conditions:
      - alias: 'test setup complete'
        condition: template
        value_template: >-
          {{ is_state('binary_sensor.motion_device_class','on') }}
      - alias: 'power high not on'
        condition: template
        value_template: >-
          {{ not 'power' in state_attr('binary_sensor.motion_device_class','high') }}
  variables:
    high: >-
      {% set s = state_attr('binary_sensor.motion_device_class','high') -%}
      {%- if s|lower != 'unknown' and s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
        {%- set high = (s|list + ['power'])|sort|unique|list -%}
      {%- else -%}
        {%- set high = ['power'] -%}
      {%- endif %}
      {%- if high is defined -%}
        {{ high }}
      {%- else -%}none{%- endif %}
  action:
    - alias: 'test if high list of device classes is valid'
      condition: template
      value_template: >-
        {{ high|lower != 'none' }}
    - alias: 'update binary_sensor.motion_device_class high attribute; add: power'
      service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_class
        high: >-
          {{ high }}
    - alias: 'update binary_sensor.motion_device_power_exist'
      service: homeassistant.update_entity
      entity_id: binary_sensor.motion_device_power_exist

- id: motion_device_power_alarm_high_on
  alias: motion_device_power_alarm_high_on
  mode: restart
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
    - platform: state
      entity_id: 'binary_sensor.motion_device_power_high'
      to: 'on'
  variables:
    high: >-
      {{ state_attr('binary_sensor.motion_device_power','high') }}
  condition:
    condition: and
    conditions:
      - condition: template
        value_template: >
          {{- high|lower != 'none' and high|lower != 'unknown' and not high is string and high is iterable and high|count > 0 -}}
  action:
    - variables:
        markdown: >-
          A <a href="/lovelace-primary/power">power</a> alarm was triggered (above {{ state_attr('binary_sensor.motion_device_power_exist','high') -}}) at {{ utcnow().timestamp()|timestamp_custom("%a %b %d %I:%M %p %Z",true,'unknown') }}:<br>
          {%- for i in high if 'id' in i and i.id|lower != 'none' -%}
            {%- if loop.first -%}{{- '<ul>' -}}{%- endif -%}
            {%- set d = state_attr('binary_sensor.motion_device_class','devices') -%}
            {%- if d|lower != 'none' and not d is string and d is iterable and d|count > 0 -%}
              {%- set d = d|selectattr('id','==',i.id)|list -%}
              {%- if d|lower != 'none' and not d is string and d is iterable and d|count > 0 -%}
                {%- set d = d|first -%}
                <li><a href="/config/devices/device/{{- d.did -}}">{{- d.friendly_name|replace("'","") -}}</a> is {{ states(high[loop.index-1].entity_id) -}}{{- state_attr(high[loop.index-1].entity_id,'unit_of_measurement') }} in <a href="/config/areas/area/{{- area_id(high[loop.index-1].entity_id) -}}">{{- area_id(high[loop.index-1].entity_id) -}}</a></li>
              {%- else -%}
                <li>{{- i.id }} power is {{ states(i.entity_id) -}}{{- state_attr(i.entity_id,'unit_of_measurement') }}</li>
              {%- endif -%}
            {%- else -%}
              {{- '<li>' -}}
              {{- i.id }} power is {{ states(i.entity_id) -}}{{- state_attr(i.entity_id,'unit_of_measurement') }}
              {{- '</li>' -}}
            {%- endif -%}
            {%- if loop.last -%}{{- '</ul>' -}}{%- endif -%}
          {%- endfor -%}
    - service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_power_high
        markdown: >-
          {{ markdown }}
    - condition: template
      value_template: >
        {{ is_state_attr('binary_sensor.motion_device_power_exist','alarm','on') }}
    - alias: 'trigger motion notification'
      service: automation.trigger
      data_template:
        entity_id: automation.motion_device_power_high_notify

- id: motion_device_power_alarm_high_off
  alias: motion_device_power_alarm_high_off
  mode: restart
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
    - platform: state
      entity_id: 'binary_sensor.motion_device_power_high'
      to: 'off'
  action:
    - condition: template
      value_template: >
        {{ is_state_attr('binary_sensor.motion_device_power_exist','alarm','on') }}
    - alias: 'trigger power high dismiss'
      service: automation.trigger
      data_template:
        entity_id: automation.motion_device_power_high_dismiss

- id: motion_device_power_high_notify
  alias: motion_device_power_high_notify
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
  variables:
    class: >-
      {{ 'power' }}
    high: >-
      {% set high = state_attr('binary_sensor.motion_device_power','high') %}
      {{ high }}
    action: >-
      {% set action = class + '-alarm' %}
      {{ action }}
    url: >-
      {% set url = '/lovelace-primary/' + class %}
      {{ url }}
    title: >-
      {% set title = states('sensor.motion_app') + ': ' + class + ' high' %}
      {{ title }}
    notification_id: >-
      {{ 'device-' + class + '-high' }}
    icon: >-
      {% set icon = 'notification-' + class + '-high' %}
      {{ icon }}
    header: >-
      {{- '<img src="/local/images/icons/' + icon + '.png?v1">' -}}
    footer: >-
      <a href="/notify-devices/{{- class -}}"><img src="https://img.shields.io/badge/{{- class -}}-device-cyan.svg"></a>
      {%- if did is defined and did|lower != 'none' and did|lower != 'null' -%}
         <br><a href="/config/devices/device/{{- did -}}"><img src="https://img.shields.io/badge/{{- class -}}-config-blue.svg"></a>
      {%- endif -%}
    body: >-
      {{ state_attr('binary_sensor.motion_device_power_high','markdown') }}
      <p>We have notified <b>{{- state_attr('sensor.motion_role_primary','friendly_name') -}}</b> about your power alarm.
      To inspect and change the alarm click the <b>{{- action -}}</b> button below.
      {{- '<p>' -}}
      <a href="{{- url -}}"><img src="https://img.shields.io/badge/{{- action -}}-yellow.svg"></a>
    message: >-
      {{- header -}}<hr>{{- body -}}<br>{{- footer -}}
  action:
    - alias: 'notify power sensor activated'
      service: persistent_notification.create
      data_template:
        title: >-
          {{- title -}}
        notification_id: >-
          {{- notification_id -}}
        message: >-
          {{- message -}}

- id: motion_device_power_high_dismiss
  alias: motion_device_power_high_dismiss
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
  variables:
    class: >-
      {{ 'power' }}
    notification_id: >-
      {{ 'device-' + class + '-high' }}
  action:
    - alias: 'dismiss power high notification'
      service: persistent_notification.dismiss
      data_template:
        notification_id: >-
          {{ notification_id }}

## good

- id: motion_device_power_good
  alias: motion_device_power_good
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
    - platform: state
      entity_id: 'binary_sensor.motion_device_power'
      attribute: 'good'
  variables:
    details: >-
      {% if state_attr('binary_sensor.motion_device_power_exist','good')|lower != 'none' %}
        {% set s = state_attr('binary_sensor.motion_device_power','good') %}
        {%- if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s is iterable -%}
          [{% for i in s -%}
            {%- if not loop.first -%},{%- endif -%}
            "{{- state_attr(i.entity_id,'friendly_name') }} is {{ states(i.entity_id) -}}"
          {%- endfor -%}]
        {%- else -%}none{%- endif %}
      {%- else -%}none{%- endif %}
    state: >-
      {% set s = state_attr('binary_sensor.motion_device_power','good') %}
      {% if s|lower != 'null' and s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and not s is string and s is iterable %}
        {% if s|list|count > 0 %}on{% else %}off{% endif %}
      {% else %}unknown{% endif %}
    last: >-
      {% if state|lower == 'off' %}
        {{ state_attr('binary_sensor.motion_device_power_good','last') }}
      {%- else -%}{{ utcnow().timestamp() }}{%- endif %}
  condition:
    - condition: template
      value_template: >
        {{ state|lower != 'unknown' }}
  action:
    - service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_power_good
        last: >-
          {{ last }}
        details: >-
          {{ details }}
        state: >-
          {{ state }}
    - condition: template
      value_template: >
        {{ state|lower == 'on' }}
    - service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_power
        condition: >-
          {{ 'good' }}

- id: motion_device_power_good_off
  alias: motion_device_power_good_off
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
  condition:
    condition: and
    conditions:
      - alias: 'test setup complete'
        condition: template
        value_template: >-
          {{ is_state('binary_sensor.motion_device_class','on') }}
      - alias: 'power good not off'
        condition: template
        value_template: >-
          {{ 'power' in state_attr('binary_sensor.motion_device_class','good') }}
  variables:
    good: >-
      {% set s = state_attr('binary_sensor.motion_device_class','good') -%}
      {%- if s|lower != 'unknown' and s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
        {%- set s = s|reject('==','power')|list -%}
        {%- if not s is string and s is iterable and s|count > 0 -%}
          {% set good = s -%}
        {%- else -%}
          {% set good = [] -%}
        {%- endif %}
      {%- endif %}
      {%- if good is defined -%}
        {{ good }}
      {%- else -%}none{%- endif %}
  action:
    - alias: 'test if good list of device classes is valid'
      condition: template
      value_template: >-
        {{ good|lower != 'none' }}
    - alias: 'update binary_sensor.motion_device_class good attribute; remove: power'
      service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_class
        good: >-
          {{ good }}
    - alias: 'update binary_sensor.motion_device_power_exist'
      service: homeassistant.update_entity
      entity_id: binary_sensor.motion_device_power_exist

- id: motion_device_power_good_on
  alias: motion_device_power_good_on
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
  condition:
    condition: and
    conditions:
      - alias: 'test setup complete'
        condition: template
        value_template: >-
          {{ is_state('binary_sensor.motion_device_class','on') }}
      - alias: 'power good not on'
        condition: template
        value_template: >-
          {{ not 'power' in state_attr('binary_sensor.motion_device_class','good') }}
  variables:
    good: >-
      {% set s = state_attr('binary_sensor.motion_device_class','good') -%}
      {%- if s|lower != 'unknown' and s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
        {%- set good = (s|list + ['power'])|sort|unique|list -%}
      {%- else -%}
        {%- set good = ['power'] -%}
      {%- endif %}
      {%- if good is defined -%}
        {{ good }}
      {%- else -%}none{%- endif %}
  action:
    - alias: 'test if good list of device classes is valid'
      condition: template
      value_template: >-
        {{ good|lower != 'none' }}
    - alias: 'update binary_sensor.motion_device_class good attribute; add: power'
      service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_class
        good: >-
          {{ good }}
    - alias: 'update binary_sensor.motion_device_power_exist'
      service: homeassistant.update_entity
      entity_id: binary_sensor.motion_device_power_exist

- id: motion_device_power_alarm_good_on
  alias: motion_device_power_alarm_good_on
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
    - platform: template
      value_template: >-
        {{ not is_state_attr('binary_sensor.motion_device_power_exist','good','none')
           and is_state_attr('binary_sensor.motion_device_power_exist','good',states('binary_sensor.motion_device_power')) }}
  variables:
    devices: >-
      {% set devices = none %}
      {% set s = state_attr('binary_sensor.motion_device_power','devices') %}
      {% if s|lower != 'none' and s|lower != 'null' and not s is string and s is iterable and s|count > 0 %}
        {% set devices = s %}
      {%- endif %}
      {{ devices }}
    last: >-
      {% set last = none %}
      {% set s = state_attr('binary_sensor.motion_device_power','status')|selectattr('state','==',state_attr('binary_sensor.motion_device_power_exist','good'))|sort(attribute='updated',reverse=true)|list %}
      {% if s|lower != 'none' and s|lower != 'null' and not s is string and s is iterable and s|count > 0 %}
        {% set last = s %}
      {%- endif %}
      {{ last  }}
    which: >-
      {% set which = none %}
      {% if last|lower != 'none' %}
        {% set which = last|map(attribute='id')|first %}
      {%- endif %}
      {{ which }}
    did: >-
      {% set did = none %}
      {% if devices|lower != 'none'  and which|lower != 'none' %}
        {% set did = devices|selectattr('id','==',which)|map(attribute='did')|first %}
      {%- endif %}
      {{ did }}
    where: >-
      {% set where = none %}
      {% if devices|lower != 'none'  and which|lower != 'none' %}
        {% set where = devices|selectattr('id','==',which)|map(attribute='area')|first %}
      {%- endif %}
      {{ where }}
  condition:
    condition: and
    conditions:
      - condition: template
        value_template: >
          {{ which|lower != 'none' and which|lower != 'null' and which|length > 0 }}
  action:
    - variables:
        markdown: >-
          A <a href="/lovelace-primary/power">power</a> alarm was triggered ({{- state_attr('binary_sensor.motion_device_power_exist','good')|string -}}); sensor: {{ which -}}; where: {{ where -}}; at {{ utcnow().timestamp()|timestamp_custom("%a %b %d %I:%M %p %Z",true,'unknown') -}}.
    - service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_power_good
        markdown: >-
          {{ markdown }}
    - alias: 'update binary_sensor.motion_device_power which, where, did'
      service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_power
        which: >-
          {{ which }}
        where: >-
          {{ where }}
        did: >-
          {{ did }}
    - condition: template
      value_template: >
        {{ is_state_attr('binary_sensor.motion_device_power_exist','alarm','on') }}
    - alias: 'trigger motion notification'
      service: automation.trigger
      data_template:
        entity_id: automation.motion_device_power_good_notify

- id: motion_device_power_good_notify
  alias: motion_device_power_good_notify
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
  variables:
    class: >-
      {{ 'power' }}
    ids: >-
      {% set ids = state_attr('binary_sensor.motion_device_power','ids') %}
      {{ ids }}
    dids: >-
      {% set dids = none %}
      {% set s = state_attr('binary_sensor.motion_device_power','devices') %}
      {%- if not s is string and s is iterable and s|count > 0 -%}
        {%- set dids = s|map(attribute='did')|list -%}
      {%- endif -%}
      {{ dids }}
    which: >-
      {% set which = state_attr('binary_sensor.motion_device_power','which') %}
      {{ which }}
    where: >-
      {% set where = state_attr('binary_sensor.motion_device_power','where') %}
      {{ where }}
    did: >-
      {% set did = state_attr('binary_sensor.motion_device_power','did') %}
      {{ did }}
    action: >-
      {% set action = class + '-alarm' %}
      {{ action }}
    url: >-
      {% set url = '/lovelace-primary/' + class %}
      {{ url }}
    title: >-
      {% set title = states('sensor.motion_app') + ': power good' %}
      {{ title }}
    notification_id: >-
      {{ 'device-power-status-' + which|string }}
    icon: >-
      {% set icon = 'notification-power-good' %}
      {{ icon }}
    header: >-
      {{- '<img src="/local/images/icons/' + icon + '.png?v1">' -}}
    footer: >-
      <a href="/notify-devices/{{- class -}}"><img src="https://img.shields.io/badge/{{- class -}}-device-cyan.svg"></a>
      {%- if did is defined and did|lower != 'none' and did|lower != 'null' -%}
         <br><a href="/config/devices/device/{{- did -}}"><img src="https://img.shields.io/badge/{{- class -}}-config-blue.svg"></a>
      {%- endif -%}
    body: >-
      {{ state_attr('binary_sensor.motion_device_power_good','markdown') }}
      <p>We have notified <b>{{- state_attr('sensor.motion_role_primary','friendly_name') -}}</b> about your power alarm.
      To inspect and change the alarm click the <b>{{- action -}}</b> button below.
      {{- '<p>' -}}
      <a href="{{- url -}}"><img src="https://img.shields.io/badge/{{- action -}}-yellow.svg"></a>
    message: >-
      {{- header -}}<hr>{{- body -}}<br>{{- footer -}}
  action:
    - alias: 'notify power sensor activated'
      service: persistent_notification.create
      data_template:
        title: >-
          {{- title -}}
        notification_id: >-
          {{- notification_id -}}
        message: >-
          {{- message -}}

## activity

- id: motion_device_power_activity
  alias: motion_device_power_activity
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
    - platform: state
      entity_id: 'binary_sensor.motion_device_power'
      attribute: 'activity'
  variables:
    ids: >-
      {% set s = state_attr('binary_sensor.motion_device_power','activity') %}
      {%- if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s is iterable -%}
        {{ s|map(attribute='id')|sort|unique|list }}
      {%- else -%}none{%- endif %}
    dids: >-
      {% set s = state_attr('binary_sensor.motion_device_power','activity') %}
      {%- if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s is iterable -%}
        {{ s|map(attribute='entity_id')|map('device_id')|sort|unique|list }}
      {%- else -%}none{%- endif %}
    details: >-
      {% set s = state_attr('binary_sensor.motion_device_power','activity') %}
      {%- if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' -%}
        {{ s }}
      {%- else -%}none{%- endif %}
    state: >-
      {% set s = state_attr('binary_sensor.motion_device_power','activity') %}
      {% if s|lower != 'null' and s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and not s is string and s is iterable %}
        {% if s|count > 0 %}on{% else %}off{% endif %}
      {% else %}unknown{% endif %}
    last: >-
      {% if state|lower == 'off' %}
        {{ state_attr('binary_sensor.motion_device_power_activity','last') }}
      {%- else -%}{{ utcnow().timestamp() }}{%- endif %}
    actions: >-
      {%- set actions = none -%}
      {%- set s = state_attr('binary_sensor.motion_device_power_activity','actions') -%}
      {%- if s|lower != 'none' and 'actions' in s -%}
        {%- set actions = s.actions -%}
      {%- endif -%}
      {{- actions -}}
    responses: >-
      {%- set responses = none -%}
      {%- set s = state_attr('binary_sensor.motion_device_power_activity','actions') -%}
      {%- if s|lower != 'none' and 'responses' in s -%}
        {%- set responses = s.responses -%}
      {%- endif -%}
      {{- responses -}}
    approve: >-
      {%- if actions|lower != 'none' and 'approve' in actions -%}
      {{ actions.approve }}
      {%- else -%}none{%- endif -%}
    deny: >-
      {%- if actions|lower != 'none' and 'deny' in actions -%}
      {{ actions.deny }}
      {%- else -%}none{%- endif -%}
    wait: >-
      {%- if actions|lower != 'none' and 'wait' in actions -%}
      {{ actions.wait }}
      {%- else -%}none{%- endif -%}
    help: >-
      {%- if actions|lower != 'none' and 'help' in actions -%}
      {{ actions.help }}
      {%- else -%}none{%- endif -%}
  condition:
    - condition: template
      value_template: >
        {{ state|lower != 'unknown' }}
  action:
    - service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_power_activity
        approve: >-
          {{ approve }}
        deny: >-
          {{ deny }}
        wait: >-
          {{ wait }}
        help: >-
          {{ help }}
        responses: >-
          {{ responses }}
        last: >-
          {{ last }}
        ids: >-
          {{ ids }}
        dids: >-
          {{ dids }}
        details: >-
          {{ details }}
        state: >-
          {{ state }}
    - condition: template
      value_template: >
        {{ state|lower == 'on' }}
    - alias: 'set power condition: activity'
      service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_power
        condition: >-
          {{ 'activity' }}

- id: motion_device_power_activity_off
  alias: motion_device_power_activity_off
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
  condition:
    condition: and
    conditions:
      - alias: 'test setup complete'
        condition: template
        value_template: >-
          {{ is_state('binary_sensor.motion_device_class','on') }}
      - alias: 'power activity not off'
        condition: template
        value_template: >-
          {{ 'power' in state_attr('binary_sensor.motion_device_class','activity') }}
  variables:
    activity: >-
      {% set s = state_attr('binary_sensor.motion_device_class','activity') -%}
      {%- if s|lower != 'unknown' and s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
        {%- set s = s|reject('==','power')|list -%}
        {%- if not s is string and s is iterable and s|count > 0 -%}
          {% set activity = s -%}
        {%- else -%}
          {% set activity = [] -%}
        {%- endif %}
      {%- endif %}
      {%- if activity is defined -%}
        {{ activity }}
      {%- else -%}none{%- endif %}
  action:
    - alias: 'test if activity list of device classes is valid'
      condition: template
      value_template: >-
        {{ activity|lower != 'none' }}
    - alias: 'update binary_sensor.motion_device_class activity attribute; remove: power'
      service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_class
        activity: >-
          {{ activity }}
    - alias: 'update binary_sensor.motion_device_power_exist'
      service: homeassistant.update_entity
      entity_id: binary_sensor.motion_device_power_exist

- id: motion_device_power_activity_on
  alias: motion_device_power_activity_on
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
  condition:
    condition: and
    conditions:
      - alias: 'test setup complete'
        condition: template
        value_template: >-
          {{ is_state('binary_sensor.motion_device_class','on') }}
      - alias: 'power activity not on'
        condition: template
        value_template: >-
          {{ not 'power' in state_attr('binary_sensor.motion_device_class','activity') }}
  variables:
    activity: >-
      {% set s = state_attr('binary_sensor.motion_device_class','activity') -%}
      {%- if s|lower != 'unknown' and s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
        {%- set activity = (s|list + ['power'])|sort|unique|list -%}
      {%- else -%}
        {%- set activity = ['power'] -%}
      {%- endif %}
      {%- if activity is defined -%}
        {{ activity }}
      {%- else -%}none{%- endif %}
  action:
    - alias: 'test if activity list of device classes is valid'
      condition: template
      value_template: >-
        {{ activity|lower != 'none' }}
    - alias: 'update binary_sensor.motion_device_class activity attribute; add: power'
      service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_class
        activity: >-
          {{ activity }}
    - alias: 'update binary_sensor.motion_device_power_exist'
      service: homeassistant.update_entity
      entity_id: binary_sensor.motion_device_power_exist

- id: motion_device_power_alarm_activity
  alias: motion_device_power_alarm_activity
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
    - platform: state
      entity_id: 'binary_sensor.motion_device_power_activity'
      to: 'on'
  variables:
    activity: >-
      {{ state_attr('binary_sensor.motion_device_power','activity') }}
  condition:
    condition: and
    conditions:
      - condition: template
        value_template: >
          {{ activity|lower != 'none' and activity|lower != 'null' and activity|length > 0 }}
  action:
    - variables:
        markdown: >-
          {%- if activity|lower != 'none' and not activity is string and activity is iterable and activity|count > 0 -%}
            A <a href="/lovelace-primary/power">power</a> <a href="/notify-activity">activity</a> alarm was triggered at {{ utcnow().timestamp()|timestamp_custom("%a %b %d %I:%M %p %Z",true,'unknown') }}:
            {%- for i in activity -%}
              {%- if loop.first -%}{{- '<ul>' -}}{%- endif -%}
              {%- set d = state_attr('binary_sensor.motion_device_class','devices') -%}
              {%- if d|lower != 'none' and d|lower != 'unknown' and not d is string and d is iterable and d|count > 0 -%}
                {%- set d = d|selectattr('did','==',device_id(i.entity_id))|list -%}
                {%- if d|lower != 'none' and d|lower != 'unknown' and not d is string and d is iterable and d|count > 0 -%}
                  {%- set d = d|first -%}
                  <li><a href="/config/devices/device/{{- d.did -}}">{{- d.friendly_name|replace("'","") -}}</a> in <a href="/config/areas/area/{{- area_id(activity[loop.index-1].entity_id) -}})">{{- area_id(activity[loop.index-1].entity_id) -}}</a></li>
                {%- else -%}
                  <li>{{- i.id }} is {{ states(i.entity_id) -}}</li>
                {%- endif -%}
              {%- else -%}
                <li>{{- i.id }} is {{ states(i.entity_id) -}}</li>
              {%- endif -%}
              {%- if loop.last -%}{{- '</ul>' -}}{%- endif -%}
            {%- endfor -%}
          {%- else -%}
            {{- '<b>No power activity</b>' -}}
          {%- endif -%}
    - service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_power_activity
        markdown: >-
          {{ markdown }}
    - condition: template
      value_template: >
        {{ is_state_attr('binary_sensor.motion_device_power_exist','alarm','on') }}
    - alias: 'trigger motion notification'
      service: automation.trigger
      data_template:
        entity_id: automation.motion_device_power_activity_notify

- id: motion_device_power_activity_notify
  alias: motion_device_power_activity_notify
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
  variables:
    class: >-
      {{ 'power' }}
    activity: >-
      {% set activity = state_attr('binary_sensor.motion_device_power','activity') %}
      {{ activity }}
    action: >-
      {% set action = class + '-alarm' %}
      {{ action }}
    url: >-
      {% set url = '/notify-activity/' %}
      {{ url }}
    title: >-
      {% set title = states('sensor.motion_app') + ': power activity alarm' %}
      {{ title }}
    notification_id: >-
      {{ 'device-power-activity' }}
    icon: >-
      {% set icon = 'notification-power-activity' %}
      {{ icon }}
    header: >-
      {{- '<img src="/local/images/icons/' + icon + '.png?v1">' -}}
    footer: >-
      <a href="/notify-devices/{{- class -}}"><img src="https://img.shields.io/badge/{{- class -}}-device-cyan.svg"></a>
      {%- if did is defined and did|lower != 'none' and did|lower != 'null' -%}
         <br><a href="/config/devices/device/{{- did -}}"><img src="https://img.shields.io/badge/{{- class -}}-config-blue.svg"></a>
      {%- endif -%}
    body: >-
      {{ state_attr('binary_sensor.motion_device_power_activity','markdown') }}
      <p>We have notified <b>{{- state_attr('sensor.motion_role_primary','friendly_name') -}}</b> about your power alarm.
      To inspect and change the alarm click the <b>{{- action -}}</b> button below.
      {{- '<p>' -}}
      <a href="{{- url -}}"><img src="https://img.shields.io/badge/{{- action -}}-yellow.svg"></a>
    message: >-
      {{- header -}}<hr>{{- body -}}<br>{{- footer -}}
  action:
    - alias: 'notify power sensor activated'
      service: persistent_notification.create
      data_template:
        title: >-
          {{- title -}}
        notification_id: >-
          {{- notification_id -}}
        message: >-
          {{- message -}}

## alarm (enabled w/o devices)

- id: motion_device_power_alarm_off
  alias: motion_device_power_alarm_off
  mode: single
  max_exceeded: 'warn'
  initial_state: 'on'
  trigger:
  condition:
    condition: and
    conditions:
      - alias: 'test setup complete'
        condition: template
        value_template: >-
          {{ is_state('binary_sensor.motion_device_class','on') }}
      - alias: 'power alarm not off'
        condition: template
        value_template: >-
          {{ 'power' in state_attr('binary_sensor.motion_device_class','alarm') }}
  variables:
    alarm: >-
      {% set s = state_attr('binary_sensor.motion_device_class','alarm') -%}
      {%- if s|lower != 'unknown' and s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
        {%- set s = s|reject('==','power')|list -%}
        {%- if not s is string and s is iterable and s|count > 0 -%}
          {% set alarm = s -%}
        {%- else -%}
          {% set alarm = [] -%}
        {%- endif %}
      {%- endif %}
      {%- if alarm is defined -%}
        {{ alarm }}
      {%- else -%}none{%- endif %}
  action:
    - alias: 'test if alarm list of device classes is valid'
      condition: template
      value_template: >-
        {{ alarm|lower != 'none' }}
    - alias: 'update binary_sensor.motion_device_class alarm attribute; remove: power'
      service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_class
        alarm: >-
          {{ alarm }}
    - alias: 'update binary_sensor.motion_device_power_exist'
      service: homeassistant.update_entity
      entity_id: binary_sensor.motion_device_power_exist

- id: motion_device_power_alarm_on
  alias: motion_device_power_alarm_on
  mode: single
  max_exceeded: 'warn'
  initial_state: 'on'
  trigger:
  condition:
    condition: and
    conditions:
      - alias: 'test setup complete'
        condition: template
        value_template: >-
          {{ is_state('binary_sensor.motion_device_class','on') }}
      - alias: 'power alarm not on'
        condition: template
        value_template: >-
          {{ not 'power' in state_attr('binary_sensor.motion_device_class','alarm') }}
  variables:
    alarm: >-
      {% set s = state_attr('binary_sensor.motion_device_class','alarm') -%}
      {%- if s|lower != 'unknown' and s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
        {%- set alarm = (s|list + ['power'])|sort|unique|list -%}
      {%- else -%}
        {%- set alarm = ['power'] -%}
      {%- endif %}
      {%- if alarm is defined -%}
        {{ alarm }}
      {%- else -%}none{%- endif %}
  action:
    - alias: 'test if alarm list of device classes is valid'
      condition: template
      value_template: >-
        {{ alarm|lower != 'none' }}
    - alias: 'update binary_sensor.motion_device_class alarm attribute; add: power'
      service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_class
        alarm: >-
          {{ alarm }}
    - alias: 'update binary_sensor.motion_device_power_exist'
      service: homeassistant.update_entity
      entity_id: binary_sensor.motion_device_power_exist

## toggle

- id: motion_device_power_turn_on
  alias: motion_device_power_turn_on
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
  condition:
    condition: and
    conditions:
      - condition: template
        value_template: >-
          {{ is_state('binary_sensor.motion_device_power_turn_on','on') }}
  variables:
    entity_ids: >-
      {{ state_attr('binary_sensor.motion_device_power_turn_on','entity_ids') }}
  action:
    - alias: 'turn_on power sensors'
      repeat:
        while:
          - condition: template
            value_template: >-
              {% set s = entity_ids[repeat.index - 1]|default('unknown') -%}
              {{ s|lower != 'unavailable' and s|lower != 'unknown' and s|lower != 'null' and s|string|length > 0 }}
        sequence:
          - service: switch.turn_on
            data_template:
              entity_id: >-
                {{ entity_ids[repeat.index - 1] }}

- id: motion_device_power_turn_off
  alias: motion_device_power_turn_off
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
  condition:
    condition: and
    conditions:
      - condition: template
        value_template: >-
          {{ is_state('binary_sensor.motion_device_power_turn_off','on') }}
  variables:
    entity_ids: >-
      {{ state_attr('binary_sensor.motion_device_power_turn_off','entity_ids') }}
  action:
    - alias: 'turn_off power sensors'
      repeat:
        while:
          - condition: template
            value_template: >-
              {% set s = entity_ids[repeat.index - 1]|default('unknown') -%}
              {{ s|lower != 'unavailable' and s|lower != 'unknown' and s|lower != 'null' and s|string|length > 0 }}
        sequence:
          - service: switch.turn_off
            data_template:
              entity_id: >-
                {{ entity_ids[repeat.index - 1] }}

- id: motion_device_power_toggle
  alias: motion_device_power_toggle
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
  condition:
    condition: and
    conditions:
      - condition: template
        value_template: >-
          {{ is_state('binary_sensor.motion_device_power_toggle','on') }}
  variables:
    entity_ids: >-
      {{ state_attr('binary_sensor.motion_device_power_toggle','entity_ids') }}
  action:
    - alias: 'toggle power sensors'
      repeat:
        while:
          - condition: template
            value_template: >-
              {% set s = entity_ids[repeat.index - 1]|default('unknown') -%}
              {{ s|lower != 'unavailable' and s|lower != 'unknown' and s|lower != 'null' and s|string|length > 0 }}
        sequence:
          - service: switch.toggle
            data_template:
              entity_id: >-
                {{ entity_ids[repeat.index - 1] }}

## update, restart, and other contols

- id: motion_device_power_self_test
  alias: motion_device_power_self_test
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
  condition:
    condition: and
    conditions:
      - alias: 'test if self_test is on'
        condition: template
        value_template: >-
          {{ is_state('binary_sensor.motion_device_power_self_test','on') }}
  variables:
    entity_ids: >-
      {{ state_attr('binary_sensor.motion_device_power_self_test','entity_ids') }}
  action:
    - alias: 'test power sensors'
      repeat:
        while:
          - alias: 'test if not done'
            condition: template
            value_template: >-
              {% set s = entity_ids[repeat.index - 1]|default('unknown') -%}
              {{ s|lower != 'unavailable' and s|lower != 'unknown' and s|lower != 'null' and s|string|length > 0 }}
        sequence:
          - alias: 'pressing button'
            service: button.press
            data_template:
              entity_id: >-
                {{ entity_ids[repeat.index - 1] }}

- id: motion_device_power_mute
  alias: motion_device_power_mute
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
  condition:
    condition: and
    conditions:
      - condition: template
        value_template: >-
          {{ is_state('binary_sensor.motion_device_power_mute','on') }}
  variables:
    entity_ids: >-
      {{ state_attr('binary_sensor.motion_device_power_mute','entity_ids') }}
  action:
    - alias: 'mute power sensors'
      repeat:
        while:
          - condition: template
            value_template: >-
              {% set s = entity_ids[repeat.index - 1]|default('unknown') -%}
              {{ s|lower != 'unavailable' and s|lower != 'unknown' and s|lower != 'null' and s|string|length > 0 }}
        sequence:
          - service: button.press
            data_template:
              entity_id: >-
                {{ entity_ids[repeat.index - 1] }}

- id: motion_device_power_unmute
  alias: motion_device_power_unmute
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
  condition:
    condition: and
    conditions:
      - condition: template
        value_template: >-
          {{ is_state('binary_sensor.motion_device_power_unmute','on') }}
  variables:
    entity_ids: >-
      {{ state_attr('binary_sensor.motion_device_power_unmute','entity_ids') }}
  action:
    - alias: 'unmute power sensors'
      repeat:
        while:
          - condition: template
            value_template: >-
              {% set s = entity_ids[repeat.index - 1]|default('unknown') -%}
              {{ s|lower != 'unavailable' and s|lower != 'unknown' and s|lower != 'null' and s|string|length > 0 }}
        sequence:
          - service: button.press
            data_template:
              entity_id: >-
                {{ entity_ids[repeat.index - 1] }}

- id: motion_device_power_restart
  alias: motion_device_power_restart
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
  condition:
    condition: and
    conditions:
      - condition: template
        value_template: >-
          {{ is_state('binary_sensor.motion_device_power_restart','on') }}
  variables:
    entity_ids: >-
      {{ state_attr('binary_sensor.motion_device_power_restart','entity_ids') }}
  action:
    - alias: 'restart power sensors'
      repeat:
        while:
          - condition: template
            value_template: >-
              {% set s = entity_ids[repeat.index - 1]|default('unknown') -%}
              {{ s|lower != 'unavailable' and s|lower != 'unknown' and s|lower != 'null' and s|string|length > 0 }}
        sequence:
          - service: button.press
            data_template:
              entity_id: >-
                {{ entity_ids[repeat.index - 1] }}

- id: motion_device_power_update
  alias: motion_device_power_update
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
  condition:
    condition: and
    conditions:
      - condition: template
        value_template: >-
          {{ is_state('binary_sensor.motion_device_power_update','on') }}
  variables:
    entity_ids: >-
      {{ state_attr('binary_sensor.motion_device_power_update','entity_ids') }}
  action:
    - alias: 'update power sensors'
      repeat:
        while:
          - condition: template
            value_template: >-
              {% set s = entity_ids[repeat.index - 1]|default('unknown') -%}
              {{ s|lower != 'unavailable' and s|lower != 'unknown' and s|lower != 'null' and s|string|length > 0 }}
        sequence:
          - service: button.press
            data_template:
              entity_id: >-
                {{ entity_ids[repeat.index - 1] }}

# found

- id: motion_device_power_found
  alias: motion_device_power_found
  mode: single
  max_exceeded: 'warn'
  initial_state: 'on'
  trigger:
    - platform: state
      entity_id: 'binary_sensor.motion_device_power_exist'
      to: 'on'
  variables:
    dids: >-
      {% set s = state_attr('binary_sensor.motion_device_power','found') %}
      {%- if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s is iterable -%}
        {{ s|map(attribute='did')|sort|unique|list }}
      {%- else -%}none{%- endif %}
    ids: >-
      {% set s = state_attr('binary_sensor.motion_device_power','found') %}
      {%- if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s is iterable -%}
        {{ s|map(attribute='id')|sort|unique|list }}
      {%- else -%}none{%- endif %}
    details: >-
      {% set s = state_attr('binary_sensor.motion_device_power','found') %}
      {%- if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s is iterable -%}
        "{{ s }}"
      {%- else -%}none{%- endif %}
    state: >-
      {% set s = state_attr('binary_sensor.motion_device_power','found') %}
      {% if s|lower != 'null' and s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and not s is string and s is iterable %}
        {% if s|count > 0 %}on{% else %}off{% endif %}
      {% else %}unknown{% endif %}
    last: >-
      {% if state|lower == 'off' %}
        {{ state_attr('binary_sensor.motion_device_power_found','last') }}
      {%- else -%}{{ utcnow().timestamp() }}{%- endif %}
    actions: >-
      {%- set actions = none -%}
      {%- set s = state_attr('binary_sensor.motion_device_power_actions','found') -%}
      {%- if s|lower != 'none' and 'actions' in s -%}
        {%- set actions = s.actions -%}
      {%- endif -%}
      {{- actions -}}
    responses: >-
      {%- set responses = none -%}
      {%- set s = state_attr('binary_sensor.motion_device_power_actions','found') -%}
      {%- if s|lower != 'none' and 'responses' in s -%}
        {%- set responses = s.responses -%}
      {%- endif -%}
      {{- responses -}}
    approve: >-
      {%- if actions|lower != 'none' and 'approve' in actions -%}
      {{ actions.approve }}
      {%- else -%}none{%- endif -%}
    deny: >-
      {%- if actions|lower != 'none' and 'deny' in actions -%}
      {{ actions.deny }}
      {%- else -%}none{%- endif -%}
    wait: >-
      {%- if actions|lower != 'none' and 'wait' in actions -%}
      {{ actions.wait }}
      {%- else -%}none{%- endif -%}
    help: >-
      {%- if actions|lower != 'none' and 'help' in actions -%}
      {{ actions.help }}
      {%- else -%}none{%- endif -%}
  condition:
    - condition: template
      value_template: >
        {{ state|lower != 'unknown' }}
  action:
    - service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_power_found
        approve: >-
          {{ approve }}
        deny: >-
          {{ deny }}
        wait: >-
          {{ wait }}
        help: >-
          {{ help }}
        responses: >-
          {{ responses }}
        last: >-
          {{ last }}
        dids: >-
          {{ dids }}
        ids: >-
          {{ ids }}
        details: >-
          {{ details }}
        state: >-
          {{ state }}
    - condition: template
      value_template: >
        {{ state|lower == 'on' }}
    - alias: 'set power condition: found'
      service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_power
        condition: >-
          {{ 'found' }}

- id: motion_device_power_devices
  alias: motion_device_power_devices
  mode: queued
  initial_state: 'on'
  trigger:
    - platform: state
      entity_id: binary_sensor.motion_device_power
      attribute: 'devices'
  variables:
    class: >-
      {%- set class = 'power' -%}
      {{ class }}
    devices: >-
      {% set devices = [] -%}
      {%- set s = state_attr('binary_sensor.motion_device_' + class,'devices') -%}
      {%- if s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
        {%- set devices = s -%}
      {%- endif -%}
      {{ devices }}
    dids: >-
      {% set dids = [] -%}
      {%- if devices|lower != 'none' and devices|count > 0 -%}
        {%- set s = devices|map(attribute='did')|list -%}
        {%- if s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
          {%- set dids = s|sort|unique|list -%}
        {%- endif -%}
      {%- endif -%}
      {{ dids }}
    shellys: >-
      {% set shellys = [] -%}
      {%- if devices|lower != 'none' and devices|count > 0 -%}
        {%- set s = devices|selectattr('manufacturer','==','Shelly')|map(attribute='did')|list -%}
        {%- if s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
          {%- set shellys = s|sort|unique|list -%}
        {%- endif -%}
      {%- endif -%}
      {{ shellys }}
    unicast: >-
      {% set unicast = [] -%}
      {%- if devices|lower != 'none' and devices|count > 0 -%}
        {%- set s = devices|selectattr('manufacturer','==','Shelly')|selectattr('model','search','Motion')|map(attribute='did')|list -%}
        {%- if s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
          {%- set unicast = s|sort|unique|list -%}
        {%- endif -%}
      {%- endif -%}
      {{ unicast }}
    old: >-
      {% set old = [] %}
      {% set s = trigger.to_state.state %}
      {%- if s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
        {% set old = s|sort|unique|list %}
      {% endif %}
      {{ old }}
  condition:
    condition: and
    conditions:
      - alias: 'test if no change in device ids'
        condition: template
        value_template: >-
          {{ old != dids }}
      - alias: 'test for device ids'
        condition: template
        value_template: >-
          {{ dids|lower != 'none' and not dids is string and dids is iterable and dids|count > 0 }}
  action:
    - alias: 'wait until unit_system is defined'
      wait_template: >-
       {% set s = state_attr('sensor.motion_hub','unit_system') %}
       {{- s|lower != 'none' and s is iterable -}}
    - alias: 'process (power) devices by device id'
      repeat:
        while:
          - alias: 'test if not done'
            condition: template
            value_template: >-
              {% set s = dids[repeat.index - 1]|default('none') %}
              {{ s|lower != 'none' }}
        sequence:
          - variables:
              did: >-
                {{ dids[repeat.index - 1]|default('none') }}
              sensor: >-
                {% set sensor = 'binary_sensor.motion_devices_' + did|string %}
                {{ sensor }}
              manufacturer: >-
                {% set manufacturer = device_attr(did,'manufacturer') %}
                {{ manufacturer }}
              model: >-
                {% set model = device_attr(did,'model') %}
                {{ model }}
              name: >-
                {% set name = device_attr(did,'name') %}
                {{ name }}
              url: >-
                {% set url = device_attr(did,'configuration_url') %}
                {{ url }}
              config: >-
                {% set config = [] %}
                {% if did in shellys %}
                  {% set config = config + [url|string + '/settings/cloud?enabled=false'] %}
                  {% if class|upper in ['TEMPERATURE'] %}
                    {% set u = 'C' %}
                    {% set s = state_attr('sensor.motion_hub','unit_system') %}
                    {% if s|lower != 'none' and s is iterable and class in s %}
                      {% set u = s[class]|replace('°','') %}
                    {% endif %}
                    {% set config = config + [url|string + '/settings?power_units=' + u|string] %}
                    {% set config = config + [url|string + '/settings?power_offset=0'] %}
                  {% endif %}
                {% endif %}
                {% if did in unicast %}
                  {% set config = config + [url|string + '/settings?motion_blind_time_minutes=1&coiot_enable=true&coiot_peer=' + states('sensor.host_lan_ipv4')] %}
                {% else %}
                  {% set config = config + [url|string + '/settings?coiot_enable=true&coiot_peer='] %}
                {% endif %}
                {{ config }}
              state: >-
                {% if not did in shellys %}on{% else %}unknown{% endif %}
          - alias: 'test if device sensor already in ALL devices group'
            condition: template
            value_template: >-
              {% set s = state_attr('group.motion_devices','entity_id') %}
              {{ s|lower == 'none' or ( not s is string and s is iterable and not sensor in s ) }}
          - alias: 'create power device sensor'
            service: python_script.set_state
            data_template:
              allow_create: true
              entity_id: >-
                {{  sensor }}
              did: >-
                {{  did }}
              manufacturer: >-
                {{- manufacturer -}}
              model: >-
                {{- model -}}
              name: >-
                {{- name -}}
              url: >-
                {{- url -}}
              config: >-
                {{- config -}}
              state: >-
                {{- state -}}
          - alias: 'add to ALL devices group'
            service: group.set
            data_template:
              object_id: motion_devices
              name: 'All devices'
              add_entities: >-
                {{ sensor }}

## notifications

- id: motion_device_power_found_notify
  alias: motion_device_power_found_notify
  mode: single
  max_exceeded: 'warn'
  initial_state: 'on'
  trigger:
    - platform: state
      entity_id: binary_sensor.motion_device_power_found
      to: 'on'
  variables:
    class: >-
      {{- 'power' -}}
    ids: >-
      {% set ids = state_attr('binary_sensor.motion_device_power','ids') -%}
      {{ ids }}
    devices: >-
      {% set devices = state_attr('binary_sensor.motion_device_power','devices') -%}
      {{ devices }}
    title: >-
      {% set title = states('sensor.motion_app') + ': power sensor found' -%}
      {{ title }}
    notification_id: >-
      {{ 'device-' + class|string + '-notify' }}
    icon: >-
      {% set icon = 'notification-' + class|string + '-found' -%}
      {{ icon }}
    header: >-
      {{- '<img src="/local/images/icons/' + icon + '.png?v1">' -}}
    footer: >-
      <a href="/notify-devices/{{- class -}}"><img src="https://img.shields.io/badge/{{- class -}}-device-cyan.svg"></a>
      {%- if did is defined and did|lower != 'none' and did|lower != 'null' -%}
         <br><a href="/config/devices/device/{{- did -}}"><img src="https://img.shields.io/badge/{{- class -}}-config-blue.svg"></a>
      {%- endif -%}
    body: >-
      {%- if devices|lower != 'none' -%}
        Sensor(s) for power found at {{ utcnow().timestamp()|timestamp_custom("%a %b %d %I:%M %p %Z",true,'unknown') -}}
        {%- for i in devices -%}
         {%- if loop.first -%}<ul>{%- endif -%}
          <li><a href="/config/devices/device/{{- i.did -}}">{{- i.friendly_name|replace("'","") -}}</a> in <a href="/config/areas/area/{{- i.area -}}">{{- i.area -}}</a></li>
         {%- if loop.last -%}</ul>{%- endif -%}
        {%- endfor -%}
      {%- else -%}
        <b>INVALID</b>: no devices with power sensors at {{ utcnow().timestamp()|timestamp_custom("%a %b %d %I:%M %p %Z",true,'unknown') }}
      {%- endif -%}
    message: >-
      {{- header -}}<hr>{{- body -}}<br>{{- footer -}}
  action:
    - alias: 'notify found power sensor'
      service: persistent_notification.create
      data_template:
        title: >-
          {{- title -}}
        notification_id: >-
          {{- notification_id -}}
        message: >-
          {{- message -}}

- id: motion_device_power_found_dismiss
  alias: motion_device_power_found_dismiss
  mode: single
  max_exceeded: 'warn'
  initial_state: 'on'
  trigger:
    - platform: state
      entity_id: binary_sensor.motion_device_power_exist
      to: 'on'
      for:
        minutes: >-
          {{ states('input_number.motion_device_class_dismiss')|int(5) }}
  variables:
    notification_id: >-
      {{ 'device-power-notify' }}
  action:
    - alias: 'dismiss power found notification'
      service: persistent_notification.dismiss
      data_template:
        notification_id: >-
          {{ notification_id }}

# cleared notify

- id: motion_device_power_cleared_notify
  alias: motion_device_power_cleared_notify
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
    - platform: template
      value_template: >-
        {{ is_state_attr('binary_sensor.motion_device_power_exist','good',states('binary_sensor.motion_device_power')) }}
  variables:
    class: >-
      {{- 'power' -}}
    which: >-
      {% set which = none %}
      {% set s = state_attr('binary_sensor.motion_device_power','status')|selectattr('state','==','off')|sort(attribute='updated',reverse=true)|list %}
      {% if s|lower != 'none' and s|lower != 'null' and not s is string and s is iterable and s|count > 0 %}
        {% set which = s|map(attribute='id')|first  %}
      {%- endif %}
      {{ which }}
    notification_id: >-
      {{ 'device-power-status-' + which|string }}
    icon: >-
      {% set icon = 'notification-power-good' %}
      {{ icon }}
    title: >-
      {% set title = states('sensor.motion_app') + ': power good (' + state_attr('binary_sensor.motion_device_power_exist','good')|string + ')' %}
      {{ title }}
    header: >-
      {{- '<img src="/local/images/icons/' + icon + '.png?v1">' -}}
    footer: >-
      <a href="/notify-devices/{{- class -}}"><img src="https://img.shields.io/badge/{{- class -}}-device-cyan.svg"></a>
      {%- if did is defined and did|lower != 'none' and did|lower != 'null' -%}
         <br><a href="/config/devices/device/{{- did -}}"><img src="https://img.shields.io/badge/{{- class -}}-config-blue.svg"></a>
      {%- endif -%}
    body: >-
      The <a href="/notify-devices/power">power</a> alarm has cleared; sensor: {{ which -}}.
    message: >-
      {{- header -}}{<hr>{- body -}}{{- footer -}}
  action:
    - condition: template
      value_template: >
        {{ which|lower != 'none' and is_state_attr('binary_sensor.motion_device_power_exist','alarm','on') }}
    - alias: 'notify power sensor activated'
      service: persistent_notification.create
      data_template:
        title: >-
          {{- title -}}
        notification_id: >-
          {{- notification_id -}}
        message: >-
          {{- message -}}

# cleared dismissed

- id: motion_device_power_cleared_dismiss
  alias: motion_device_power_cleared_dismiss
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
    - platform: template
      value_template: >-
        {{ is_state_attr('binary_sensor.motion_device_power_exist','good',states('binary_sensor.motion_device_power')) }}
      for:
        minutes: >-
          {{ states('input_number.motion_device_class_dismiss')|int(5) }}
  variables:
    ids: >-
      {%- set ids = state_attr('binary_sensor.motion_device_power','ids') -%}
      {%- if ids|lower != 'none' and not ids is string and ids is iterable and ids|count > 0 -%}
        [{%- for i in ids -%}
          {%- if not loop.first -%},{%- endif -%}
          "{{- 'device-power-status-' + i|string -}}"
        {%- endfor -%}]
      {%- else -%}none{%- endif %}
  condition:
    condition: and
    conditions:
      - condition: template
        value_template: >
          {{ ids|lower != 'none' and ids is iterable and ids|count > 0 }}
  action:
    - alias: 'dismiss power status'
      repeat:
        while:
          - alias: 'test if not done'
            condition: template
            value_template: >-
              {%- set s = ids[repeat.index - 1]|default('unknown') %}
              {{ s|lower != 'unavailable' and s|lower != 'unknown' and s|lower != 'null' and s|string|length > 0 }}
        sequence:
          - alias: 'dismissing persistent notification'
            service: persistent_notification.dismiss
            data_template:
              notification_id: >-
                {{ ids[repeat.index - 1] }}

# activity,unavailable

- id: motion_device_power_unavailable_monitor
  alias: motion_device_power_unavailable_monitor
  mode: restart
  initial_state: 'on'
  trigger:
    - platform: 'state'
      entity_id: 'binary_sensor.motion_device_power'
      attribute: 'status'
  variables:
    status: >-
      {{ state_attr('binary_sensor.motion_device_power','status') }}
    unavailable: >-
      {%- set s = status -%}
      {%- if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s is iterable -%}
        [{% for i in s if 'entity_id' in i and not i.entity_id is none and is_state(i.entity_id,'unavailable') or is_state(i.entity_id,'unknown') -%}
          {%- if not loop.first -%},{%- endif -%}
            {"id":"{{- i.id -}}","did":"{{- device_id(i.entity_id) -}}","entity_id":"{{- i.entity_id -}}","state":"{{- i.state -}}","updated":{{- states[i.entity_id].last_updated|as_timestamp(None) -}}}
        {%- endfor -%}]
      {%- else -%}[]{%- endif %}
  action:
    - alias: 'update binary_sensor.motion_device_power: unavailable'
      service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_power
        unavailable: >-
          {{ unavailable }}

- id: motion_device_power_unavailable
  alias: motion_device_power_unavailable
  mode: restart
  initial_state: 'off'
  trigger:
    - platform: 'state'
      entity_id: 'binary_sensor.motion_device_power'
      attribute: 'unavailable'
  variables:
    dids: >-
      {% set s = state_attr('binary_sensor.motion_device_power','unavailable') %}
      {%- if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s is iterable -%}
        {{ s|map(attribute='did')|sort|unique|list }}
      {%- else -%}none{%- endif %}
    ids: >-
      {% set s = state_attr('binary_sensor.motion_device_power','unavailable') %}
      {%- if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s is iterable -%}
        {{ s|map(attribute='id')|sort|unique|list }}
      {%- else -%}none{%- endif %}
    details: >-
      {% set s = state_attr('binary_sensor.motion_device_power','unavailable') -%}
        {%- if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s is iterable -%}
          { {%- for i in s|map(attribute='id')|sort|unique -%}
            {%- if not loop.first -%},{%- endif -%}
            "{{ i }}":
            [{%- for j in s|selectattr('id','==',i)|map(attribute='entity_id') -%}
              {%- if not loop.first -%},{%- endif -%}
              "{{- state_attr(j,'friendly_name') -}}"
            {%- endfor -%}]
          {% endfor %} }
        {%- else -%}none{%- endif %}
    state: >-
      {% set s = state_attr('binary_sensor.motion_device_power','unavailable') %}
      {% if s|lower != 'null' and s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and not s is string and s is iterable %}
        {% if s|count > 0 %}on{% else %}off{% endif %}
      {% else %}unknown{% endif %}
    last: >-
      {% if state|lower == 'off' %}
        {{ state_attr('binary_sensor.motion_device_power_unavailable','last') }}
      {%- else -%}{{ utcnow().timestamp() }}{%- endif %}
    markdown: >-
      {%- if details|lower != 'none' -%}
        {%- for i in details -%}
          {%- if loop.first -%}<ul>{%- endif -%}
          {{- '<li>' -}}
          {%- set e = state_attr('binary_sensor.motion_device_power','status') -%}
          {%- if e|lower != 'none' and not e is string and e is iterable and e|count > 0 -%}
            {%- set e = e|selectattr('id','==',i)|list -%}
            {%- if e|lower != 'none' and not e is string and e is iterable and e|count > 0 -%}
              {%- set e = e|first -%}
              {%- set did = device_id(e.entity_id) -%}
              <a href="/config/devices/device/{{- did -}}"><b>{{- device_attr(device_id(e.entity_id),'name') -}}</b></a><br><i>Last updated</i>: {{- e.updated|as_datetime|relative_time }} ago
              {%- for j in details[i] -%}
                {%- if loop.first -%}<ul>{%- endif -%}
                <li>{{- j -}}</li>
                {%- if loop.last %}</ul>{% endif -%}
              {%- endfor -%}
              {{- '</li>' -}}
              {%- if loop.last %}</ul>{% endif -%}
            {%- else -%}
              <li>{{ i }} is {{ states(i.entity_id) }}</li>
            {%- endif -%}
          {%- else -%}
            <li>{{ i }} is {{ states(i.entity_id) }}</li>
          {%- endif -%}
        {%- endfor -%}
      {%- else -%}
        <b>No unavailable power sensors</b> at {{ utcnow().timestamp()|timestamp_custom("%a %b %d %I:%M %p %Z",true,'unknown') -}}
      {%- endif %}
    actions: >-
      {%- set actions = none -%}
      {%- set s = state_attr('binary_sensor.motion_device_power_actions','unavailable') -%}
      {%- if s|lower != 'none' and 'actions' in s -%}
        {%- set actions = s.actions -%}
      {%- endif -%}
      {{- actions -}}
    responses: >-
      {%- set responses = none -%}
      {%- set s = state_attr('binary_sensor.motion_device_power_actions','unavailable') -%}
      {%- if s|lower != 'none' and 'responses' in s -%}
        {%- set responses = s.responses -%}
      {%- endif -%}
      {{- responses -}}
    approve: >-
      {%- if actions|lower != 'none' and 'approve' in actions -%}
      {{ actions.approve }}
      {%- else -%}none{%- endif -%}
    deny: >-
      {%- if actions|lower != 'none' and 'deny' in actions -%}
      {{ actions.deny }}
      {%- else -%}none{%- endif -%}
    wait: >-
      {%- if actions|lower != 'none' and 'wait' in actions -%}
      {{ actions.wait }}
      {%- else -%}none{%- endif -%}
    help: >-
      {%- if actions|lower != 'none' and 'help' in actions -%}
      {{ actions.help }}
      {%- else -%}none{%- endif -%}
  condition:
    - condition: template
      value_template: >
        {{ state|lower != 'unknown' }}
  action:
    - service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_power_unavailable
        last: >-
          {{ last }}
        ids: >-
          {{ ids }}
        dids: >-
          {{ dids }}
        approve: >-
          {{ approve }}
        deny: >-
          {{ deny }}
        wait: >-
          {{ wait }}
        help: >-
          {{ help }}
        responses: >-
          {{ responses }}
        details: >-
          {{ details }}
        markdown: >-
          {{ markdown }}
        state: >-
          {{ state }}
    - condition: template
      value_template: >
        {{ state|lower == 'on' }}
    - alias: 'set power condition: unavailable'
      service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_power
        condition: >-
          {{ 'unavailable' }}

# missing

- id: motion_device_power_missing_on
  alias: motion_device_power_missing_on
  mode: 'restart'
  max_exceeded: 'warn'
  initial_state: 'on'
  trigger:
    - platform: 'state'
      entity_id: 'binary_sensor.motion_device_power_missing'
  condition:
    - condition: template
      value_template: >
        {{ is_state('binary_sensor.motion_device_power_missing','on') }}
  action:
    - alias: 'set power condition: missing'
      service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_power
        condition: >-
          {{ 'missing' }}

- id: motion_device_power_missing_notify_watchdog
  alias: motion_device_power_missing_notify_watchdog
  mode: 'restart'
  initial_state: 'off'
  trigger:
    - platform: time_pattern
      minutes: '/5'
  condition:
    - condition: template
      value_template: >-
        {{ is_state('automation.motion_device_power_missing_notify','on') }}
    - condition: template
      value_template: >-
        {{ is_state_attr('automation.motion_device_power_missing_notify', 'current', 0) }}
  action:
    - choose:
        - conditions:
            - condition: template
              value_template: >
                {{ states('sensor.motion_log_level') in state_attr('sensor.motion_log_level','debug') }}
            - condition: template
              value_template: >
                {{ trigger.platform == 'time_pattern' }}
          sequence:
            - service: persistent_notification.create
              data_template:
                title: >-
                  DEBUG: power missing notify watchdog
                notification_id: >
                  {{- 'debug-power-missing-notify' -}}
                message: >-
                  Trigger platform: {{ trigger.platform }}
    - alias: 'trigger power status'
      service: automation.trigger
      data_template:
        entity_id: automation.motion_device_power_missing_notify

- id: motion_device_power_missing_notify
  alias: motion_device_power_missing_notify
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
    - platform: state
      entity_id: binary_sensor.motion_device_power_missing
      to: 'on'
  condition:
    - alias: 'test if power devices are missing'
      condition: template
      value_template: >-
        {{ is_state('binary_sensor.motion_device_power_missing','on') }}
  variables:
    class: >-
      {{ 'power' }}
    ip: >-
      {{ none }}
    latitude: >-
      {{ none }}
    longitude: >-
      {{ none }}
    notification_id: >-
      {{ 'device-power-missing' }}
    icon: >-
      {% set icon = 'notification-power-missing' %}
      {{ icon }}
    header: >-
      {{- '<img src="/local/images/icons/' + icon + '.png?v1">' -}}
    footer: >-
      <br><a href="/lovelace-primary/{{- class -}}"><img src="https://img.shields.io/badge/{{- class -}}-missing-orange.svg"></a>
      <br><a href="/notify-devices/{{- class -}}"><img src="https://img.shields.io/badge/{{- class -}}-device-cyan.svg"></a>
      {%- if did is defined and did|lower != 'none' and did|lower != 'null' -%}
         <br><a href="/config/devices/device/{{- did -}}"><img src="https://img.shields.io/badge/{{- class -}}-config-blue.svg"></a>
      {%- endif -%}
    title: >-
      {% set title = states('sensor.motion_app') + ': power sensor missing' %}
      {{ title }}
    body: >-
      {{ state_attr('binary_sensor.motion_device_power_missing','markdown') }}
    message: >-
      {{- header -}}<hr>{{- body -}}<p>{{- footer -}}
  action:
    - alias: 'notify missing power sensor'
      service: persistent_notification.create
      data_template:
        title: >-
          {{- title -}}
        notification_id: >-
          {{- notification_id -}}
        message: >-
          {{- message -}}

- id: motion_device_power_missing_dismiss
  alias: motion_device_power_missing_dismiss
  mode: single
  max_exceeded: 'warn'
  initial_state: 'on'
  trigger:
    - platform: state
      entity_id: binary_sensor.motion_device_power_missing
      to: 'off'
  variables:
    class: >-
      {{ 'power' }}
    notification_id: >-
      {{ 'device-' + class + '-missing' }}
  action:
    - alias: 'dismiss power missing notification'
      service: persistent_notification.dismiss
      data_template:
        notification_id: >-
          {{ notification_id }}

- id: motion_device_power_activity_dismiss
  alias: motion_device_power_activity_dismiss
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
  variables:
    class: >-
      {{ 'power' }}
    notification_id: >-
      {{ 'device-' + class + '-activity' }}
  action:
    - alias: 'dismiss power activity notification'
      service: persistent_notification.dismiss
      data_template:
        notification_id: >-
          {{ notification_id }}

## high_spike

- id: motion_device_power_high_spike
  alias: motion_device_power_high_spike
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
    - platform: state
      entity_id: 'binary_sensor.motion_device_power'
      attribute: 'high_spike'
  variables:
    details: >-
      {% if state_attr('binary_sensor.motion_device_power_exist','high_spike')|lower != 'none' %}
        {% set s = state_attr('binary_sensor.motion_device_power','high_spike') %}
        {%- if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s is iterable -%}
          [{% for i in s -%}
            {%- if not loop.first -%},{%- endif -%}
            "{{- state_attr(i.entity_id,'friendly_name') }} is {{ states(i.entity_id)|int('unknown') -}}{{ state_attr(i.entity_id,'unit_of_measurement') -}}"
          {%- endfor -%}]
        {%- else -%}none{%- endif %}
      {%- else -%}none{%- endif %}
    state: >-
      {% set s = state_attr('binary_sensor.motion_device_power','high_spike') %}
      {% if s|lower != 'null' and s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and not s is string and s is iterable %}
        {% if s|list|count > 0 %}on{% else %}off{% endif %}
      {% else %}unknown{% endif %}
    last: >-
      {% if state|lower == 'off' %}
        {{ state_attr('binary_sensor.motion_device_power_high_spike','last') }}
      {%- else -%}{{ utcnow().timestamp() }}{%- endif %}
  condition:
    - condition: template
      value_template: >
        {{ state|lower != 'unknown' }}
  action:
    - service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_power_high_spike
        last: >-
          {{ last }}
        details: >-
          {{ details }}
        state: >-
          {{ state }}
    - condition: template
      value_template: >
        {{ state|lower == 'on' }}
    - alias: 'set power condition: high_spike'
      service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_power
        condition: >-
          {{ 'high_spike' }}

- id: motion_device_power_high_spike_off
  alias: motion_device_power_high_spike_off
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
  condition:
    condition: and
    conditions:
      - alias: 'test setup complete'
        condition: template
        value_template: >-
          {{ is_state('binary_sensor.motion_device_class','on') }}
      - alias: 'power high_spike not off'
        condition: template
        value_template: >-
          {{ 'power' in state_attr('binary_sensor.motion_device_class','high_spike') }}
  variables:
    high_spike: >-
      {% set s = state_attr('binary_sensor.motion_device_class','high_spike') -%}
      {%- if s|lower != 'unknown' and s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
        {%- set s = s|reject('==','power')|list -%}
        {%- if not s is string and s is iterable and s|count > 0 -%}
          {% set high_spike = s -%}
        {%- else -%}
          {% set high_spike = [] -%}
        {%- endif %}
      {%- endif %}
      {%- if high_spike is defined -%}
        {{ high_spike }}
      {%- else -%}none{%- endif %}
  action:
    - alias: 'test if high_spike list of device classes is valid'
      condition: template
      value_template: >-
        {{ high_spike|lower != 'none' }}
    - alias: 'update binary_sensor.motion_device_class high_spike attribute; remove: power'
      service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_class
        high_spike: >-
          {{ high_spike }}
    - alias: 'update binary_sensor.motion_device_power_exist'
      service: homeassistant.update_entity
      entity_id: binary_sensor.motion_device_power_exist

- id: motion_device_power_high_spike_on
  alias: motion_device_power_high_spike_on
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
  condition:
    condition: and
    conditions:
      - alias: 'test setup complete'
        condition: template
        value_template: >-
          {{ is_state('binary_sensor.motion_device_class','on') }}
      - alias: 'power high_spike not on'
        condition: template
        value_template: >-
          {{ not 'power' in state_attr('binary_sensor.motion_device_class','high_spike') }}
  variables:
    high_spike: >-
      {% set s = state_attr('binary_sensor.motion_device_class','high_spike') -%}
      {%- if s|lower != 'unknown' and s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
        {%- set high_spike = (s|list + ['power'])|sort|unique|list -%}
      {%- else -%}
        {%- set high_spike = ['power'] -%}
      {%- endif %}
      {%- if high_spike is defined -%}
        {{ high_spike }}
      {%- else -%}none{%- endif %}
  action:
    - alias: 'test if high_spike list of device classes is valid'
      condition: template
      value_template: >-
        {{ high_spike|lower != 'none' }}
    - alias: 'update binary_sensor.motion_device_class high_spike attribute; add: power'
      service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_class
        high_spike: >-
          {{ high_spike }}
    - alias: 'update binary_sensor.motion_device_power_exist'
      service: homeassistant.update_entity
      entity_id: binary_sensor.motion_device_power_exist

- id: motion_device_power_alarm_high_spike_on
  alias: motion_device_power_alarm_high_spike_on
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
    - platform: state
      entity_id: 'binary_sensor.motion_device_power_high_spike'
      to: 'on'
  variables:
    limit: >-
      {{ state_attr('sensor.motion_device_power_high_spike','limit') }})
    high_spike: >-
      {{ state_attr('binary_sensor.motion_device_power','high_spike') }}
  condition:
    condition: and
    conditions:
      - condition: template
        value_template: >
          {{- high_spike|lower != 'none' and high_spike|lower != 'unknown' and not high_spike is string and high_spike is iterable and high_spike|count > 0 -}}
      - condition: template
        value_template: >
          {{- limit|lower != 'none' and limit|lower != 'unknown' and limit is number -}}
  action:
    - variables:
        markdown: >-
          The <a href="/lovelace-primary/power">power</a> spike alarm was triggered (above {{ limit -}}) at {{ utcnow().timestamp()|timestamp_custom("%a %b %d %I:%M %p %Z",true,'unknown') }}:<br>
          {%- for i in high_spike if 'id' in i and i.id|lower != 'none' -%}
            {%- if loop.first -%}{{- '<ul>' -}}{%- endif -%}
            {%- set d = state_attr('binary_sensor.motion_device_class','devices') -%}
            {%- if d|lower != 'none' and not d is string and d is iterable and d|count > 0 -%}
              {%- set d = d|selectattr('id','==',i.id)|list -%}
              {%- if d|lower != 'none' and not d is string and d is iterable and d|count > 0 -%}
                {%- set d = d|first -%}
                <li><a href="/config/devices/device/{{- d.did -}}">{{- d.friendly_name|replace("'","") -}}</a> is {{ states(high_spike[loop.index-1].entity_id) -}}{{- state_attr(high_spike[loop.index-1].entity_id,'unit_of_measurement') }} in <a href="/config/areas/area/{{- area_id(high_spike[loop.index-1].entity_id) -}}">{{- area_id(high_spike[loop.index-1].entity_id) -}}</a></li>
              {%- else -%}
                <li>{{- i.id }} power is {{ states(i.entity_id) -}}{{- state_attr(i.entity_id,'unit_of_measurement') }}</li>
              {%- endif -%}
            {%- else -%}
              {{- '<li>' -}}
              {{- i.id }} power is {{ states(i.entity_id) -}}{{- state_attr(i.entity_id,'unit_of_measurement') }}
              {{- '</li>' -}}
            {%- endif -%}
            {%- if loop.last -%}{{- '</ul>' -}}{%- endif -%}
          {%- endfor -%}
    - service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_power_high_spike
        markdown: >-
          {{ markdown }}
    - condition: template
      value_template: >
        {{ is_state_attr('binary_sensor.motion_device_power_exist','alarm','on') }}
    - alias: 'trigger motion notification'
      service: automation.trigger
      data_template:
        entity_id: automation.motion_device_power_high_spike_notify

- id: motion_device_power_alarm_high_spike_off
  alias: motion_device_power_alarm_high_spike_off
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
    - platform: state
      entity_id: 'binary_sensor.motion_device_power_high_spike'
      to: 'off'
  action:
    - condition: template
      value_template: >
        {{ is_state_attr('binary_sensor.motion_device_power_exist','alarm','on') }}
    - alias: 'trigger power high_spike dismiss'
      service: automation.trigger
      data_template:
        entity_id: automation.motion_device_power_high_spike_dismiss

- id: motion_device_power_high_spike_notify
  alias: motion_device_power_high_spike_notify
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
  variables:
    class: >-
      {{ 'power' }}
    high_spike: >-
      {% set high_spike = state_attr('binary_sensor.motion_device_power','high_spike') %}
      {{ high_spike }}
    action: >-
      {% set action = class + '-alarm' %}
      {{ action }}
    url: >-
      {% set url = '/lovelace-primary/' + class %}
      {{ url }}
    title: >-
      {% set title = states('sensor.motion_app') + ': ' + class + ' high_spike' %}
      {{ title }}
    notification_id: >-
      {{ 'device-' + class + '-high_spike' }}
    icon: >-
      {% set icon = 'notification-' + class + '-high_spike' %}
      {{ icon }}
    header: >-
      {{- '<img src="/local/images/icons/' + icon + '.png?v1">' -}}
    footer: >-
      <a href="/notify-devices/{{- class -}}"><img src="https://img.shields.io/badge/{{- class -}}-device-cyan.svg"></a>
      {%- if did is defined and did|lower != 'none' and did|lower != 'null' -%}
         <br><a href="/config/devices/device/{{- did -}}"><img src="https://img.shields.io/badge/{{- class -}}-config-blue.svg"></a>
      {%- endif -%}
    body: >-
      {{ state_attr('binary_sensor.motion_device_power_high_spike','markdown') }}
      <p>We have notified <b>{{- state_attr('sensor.motion_role_primary','friendly_name') -}}</b> about your power alarm.
      To inspect and change the alarm click the <b>{{- action -}}</b> button below.
      {{- '<p>' -}}
      <a href="{{- url -}}"><img src="https://img.shields.io/badge/{{- action -}}-yellow.svg"></a>
    message: >-
      {{- header -}}<hr>{{- body -}}<br>{{- footer -}}
  action:
    - alias: 'notify power sensor activated'
      service: persistent_notification.create
      data_template:
        title: >-
          {{- title -}}
        notification_id: >-
          {{- notification_id -}}
        message: >-
          {{- message -}}

- id: motion_device_power_high_spike_dismiss
  alias: motion_device_power_high_spike_dismiss
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
  variables:
    class: >-
      {{ 'power' }}
    notification_id: >-
      {{ 'device-' + class + '-high_spike' }}
  action:
    - alias: 'dismiss power high_spike notification'
      service: persistent_notification.dismiss
      data_template:
        notification_id: >-
          {{ notification_id }}

## low_spike

- id: motion_device_power_low_spike
  alias: motion_device_power_low_spike
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
    - platform: state
      entity_id: 'binary_sensor.motion_device_power'
      attribute: 'low_spike'
  variables:
    details: >-
      {% if state_attr('binary_sensor.motion_device_power_exist','low_spike')|lower != 'none' %}
        {% set s = state_attr('binary_sensor.motion_device_power','low_spike') %}
        {%- if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s is iterable -%}
          [{% for i in s -%}
            {%- if not loop.first -%},{%- endif -%}
            "{{- state_attr(i.entity_id,'friendly_name') }} is {{ states(i.entity_id)|int('unknown') -}}{{ state_attr(i.entity_id,'unit_of_measurement') -}}"
          {%- endfor -%}]
        {%- else -%}none{%- endif %}
      {%- else -%}none{%- endif %}
    state: >-
      {% set s = state_attr('binary_sensor.motion_device_power','low_spike') %}
      {% if s|lower != 'null' and s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and not s is string and s is iterable %}
        {% if s|list|count > 0 %}on{% else %}off{% endif %}
      {% else %}unknown{% endif %}
    last: >-
      {% if state|lower == 'off' %}
        {{ state_attr('binary_sensor.motion_device_power_low_spike','last') }}
      {%- else -%}{{ utcnow().timestamp() }}{%- endif %}
  condition:
    - condition: template
      value_template: >
        {{ state|lower != 'unknown' }}
  action:
    - service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_power_low_spike
        last: >-
          {{ last }}
        details: >-
          {{ details }}
        state: >-
          {{ state }}
    - condition: template
      value_template: >
        {{ state|lower == 'on' }}
    - alias: 'set power condition: low_spike'
      service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_power
        condition: >-
          {{ 'low_spike' }}

- id: motion_device_power_low_spike_off
  alias: motion_device_power_low_spike_off
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
  condition:
    condition: and
    conditions:
      - alias: 'test setup complete'
        condition: template
        value_template: >-
          {{ is_state('binary_sensor.motion_device_class','on') }}
      - alias: 'power low_spike not off'
        condition: template
        value_template: >-
          {{ 'power' in state_attr('binary_sensor.motion_device_class','low_spike') }}
  variables:
    low_spike: >-
      {% set s = state_attr('binary_sensor.motion_device_class','low_spike') -%}
      {%- if s|lower != 'unknown' and s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
        {%- set s = s|reject('==','power')|list -%}
        {%- if not s is string and s is iterable and s|count > 0 -%}
          {% set low_spike = s -%}
        {%- else -%}
          {% set low_spike = [] -%}
        {%- endif %}
      {%- endif %}
      {%- if low_spike is defined -%}
        {{ low_spike }}
      {%- else -%}none{%- endif %}
  action:
    - alias: 'test if low_spike list of device classes is valid'
      condition: template
      value_template: >-
        {{ low_spike|lower != 'none' }}
    - alias: 'update binary_sensor.motion_device_class low_spike attribute; remove: power'
      service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_class
        low_spike: >-
          {{ low_spike }}
    - alias: 'update binary_sensor.motion_device_power_exist'
      service: homeassistant.update_entity
      entity_id: binary_sensor.motion_device_power_exist

- id: motion_device_power_low_spike_on
  alias: motion_device_power_low_spike_on
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
  condition:
    condition: and
    conditions:
      - alias: 'test setup complete'
        condition: template
        value_template: >-
          {{ is_state('binary_sensor.motion_device_class','on') }}
      - alias: 'power low_spike not on'
        condition: template
        value_template: >-
          {{ not 'power' in state_attr('binary_sensor.motion_device_class','low_spike') }}
  variables:
    low_spike: >-
      {% set s = state_attr('binary_sensor.motion_device_class','low_spike') -%}
      {%- if s|lower != 'unknown' and s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
        {%- set low_spike = (s|list + ['power'])|sort|unique|list -%}
      {%- else -%}
        {%- set low_spike = ['power'] -%}
      {%- endif %}
      {%- if low_spike is defined -%}
        {{ low_spike }}
      {%- else -%}none{%- endif %}
  action:
    - alias: 'test if low_spike list of device classes is valid'
      condition: template
      value_template: >-
        {{ low_spike|lower != 'none' }}
    - alias: 'update binary_sensor.motion_device_class low_spike attribute; add: power'
      service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_class
        low_spike: >-
          {{ low_spike }}
    - alias: 'update binary_sensor.motion_device_power_exist'
      service: homeassistant.update_entity
      entity_id: binary_sensor.motion_device_power_exist

- id: motion_device_power_alarm_low_spike_on
  alias: motion_device_power_alarm_low_spike_on
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
    - platform: state
      entity_id: 'binary_sensor.motion_device_power_low_spike'
      to: 'on'
  variables:
    low_spike: >-
      {{ state_attr('binary_sensor.motion_device_power','low_spike') }}
    limit: >-
      {{ state_attr('sensor.motion_device_power_low_spike','limit') }})
  condition:
    condition: and
    conditions:
      - condition: template
        value_template: >
          {{- limit|lower != 'none' and limit|lower != 'unknown' and limit is number -}}
      - condition: template
        value_template: >
          {{- low_spike|lower != 'none' and low_spike|lower != 'unknown' and not low_spike is string and low_spike is iterable and low_spike|count > 0 -}}
  action:
    - variables:
        markdown: >-
          The <a href="/lovelace-primary/power">power</a> spike alarm was triggered (below {{ limit -}}) at {{ utcnow().timestamp()|timestamp_custom("%a %b %d %I:%M %p %Z",true,'unknown') }}:<br>
          {%- for i in low_spike if 'id' in i and i.id|lower != 'none' -%}
            {%- if loop.first -%}{{- '<ul>' -}}{%- endif -%}
            {%- set d = state_attr('binary_sensor.motion_device_class','devices') -%}
            {%- if d|lower != 'none' and not d is string and d is iterable and d|count > 0 -%}
              {%- set d = d|selectattr('id','==',i.id)|list -%}
              {%- if d|lower != 'none' and not d is string and d is iterable and d|count > 0 -%}
                {%- set d = d|first -%}
                <li><a href="/config/devices/device/{{- d.did -}}">{{- d.friendly_name|replace("'","") -}}</a> is {{ states(low_spike[loop.index-1].entity_id) -}}{{- state_attr(low_spike[loop.index-1].entity_id,'unit_of_measurement') }} in <a href="/config/areas/area/{{- area_id(low_spike[loop.index-1].entity_id) -}}">{{- area_id(low_spike[loop.index-1].entity_id) -}}</a></li>
              {%- else -%}
                <li>{{- i.id }} power is {{ states(i.entity_id) -}}{{- state_attr(i.entity_id,'unit_of_measurement') }}</li>
              {%- endif -%}
            {%- else -%}
              {{- '<li>' -}}
              {{- i.id }} power is {{ states(i.entity_id) -}}{{- state_attr(i.entity_id,'unit_of_measurement') }}
              {{- '</li>' -}}
            {%- endif -%}
            {%- if loop.last -%}{{- '</ul>' -}}{%- endif -%}
          {%- endfor -%}
    - service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_power_low_spike
        markdown: >-
          {{ markdown }}
    - condition: template
      value_template: >
        {{ is_state_attr('binary_sensor.motion_device_power_exist','alarm','on') }}
    - alias: 'trigger motion notification'
      service: automation.trigger
      data_template:
        entity_id: automation.motion_device_power_low_spike_notify

- id: motion_device_power_alarm_low_spike_off
  alias: motion_device_power_alarm_low_spike_off
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
    - platform: state
      entity_id: 'binary_sensor.motion_device_power_low_spike'
      to: 'off'
  action:
    - condition: template
      value_template: >
        {{ is_state_attr('binary_sensor.motion_device_power_exist','alarm','on') }}
    - alias: 'trigger power low_spike dismiss'
      service: automation.trigger
      data_template:
        entity_id: automation.motion_device_power_low_spike_dismiss

- id: motion_device_power_low_spike_notify
  alias: motion_device_power_low_spike_notify
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
  variables:
    class: >-
      {{ 'power' }}
    low_spike: >-
      {% set low_spike = state_attr('binary_sensor.motion_device_power','low_spike') %}
      {{ low_spike }}
    action: >-
      {% set action = class + '-alarm' %}
      {{ action }}
    url: >-
      {% set url = '/lovelace-primary/' + class %}
      {{ url }}
    title: >-
      {% set title = states('sensor.motion_app') + ': ' + class + ' low_spike' %}
      {{ title }}
    notification_id: >-
      {{ 'device-' + class + '-low_spike' }}
    icon: >-
      {% set icon = 'notification-' + class + '-low_spike' %}
      {{ icon }}
    header: >-
      {{- '<img src="/local/images/icons/' + icon + '.png?v1">' -}}
    footer: >-
      <a href="/notify-devices/{{- class -}}"><img src="https://img.shields.io/badge/{{- class -}}-device-cyan.svg"></a>
      {%- if did is defined and did|lower != 'none' and did|lower != 'null' -%}
         <br><a href="/config/devices/device/{{- did -}}"><img src="https://img.shields.io/badge/{{- class -}}-config-blue.svg"></a>
      {%- endif -%}
    body: >-
      {{ state_attr('binary_sensor.motion_device_power_low_spike','markdown') }}
      <p>We have notified <b>{{- state_attr('sensor.motion_role_primary','friendly_name') -}}</b> about your power alarm.
      To inspect and change the alarm click the <b>{{- action -}}</b> button below.
      {{- '<p>' -}}
      <a href="{{- url -}}"><img src="https://img.shields.io/badge/{{- action -}}-yellow.svg"></a>
    message: >-
      {{- header -}}<hr>{{- body -}}<br>{{- footer -}}
  action:
    - alias: 'notify power sensor activated'
      service: persistent_notification.create
      data_template:
        title: >-
          {{- title -}}
        notification_id: >-
          {{- notification_id -}}
        message: >-
          {{- message -}}

- id: motion_device_power_low_spike_dismiss
  alias: motion_device_power_low_spike_dismiss
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
  variables:
    class: >-
      {{ 'power' }}
    notification_id: >-
      {{ 'device-' + class + '-low_spike' }}
  action:
    - alias: 'dismiss power low_spike notification'
      service: persistent_notification.dismiss
      data_template:
        notification_id: >-
          {{ notification_id }}

### responses

- id: motion_device_power_response
  alias: motion_device_power_response
  mode: single
  max_exceeded: 'warn'
  initial_state: 'on'
  trigger:
    - platform: event
      event_type: mobile_app_notification_action
      event_data:
        action:
          - "device-power-bad-wait"
          - "device-power-bad-help"
          - "device-power-bad-deny"
          - "device-power-bad-approve"
          - "device-power-good-wait"
          - "device-power-good-help"
          - "device-power-good-deny"
          - "device-power-good-approve"
          - "device-power-high-wait"
          - "device-power-high-help"
          - "device-power-high-deny"
          - "device-power-high-approve"
          - "device-power-low-wait"
          - "device-power-low-help"
          - "device-power-low-deny"
          - "device-power-low-approve"
  variables:
    event: >-
      {% set event = none -%}
      {%- if trigger|lower != 'none' -%}
        {%- set event = trigger.event|default('none') -%}
      {%- endif -%}
      {{- event }}
    action: >-
      {% set action = none -%}
      {% if event|lower != 'none' and 'data' in event %}
        {% set action = event.data.action|default('none') %}
      {%- endif -%}
      {{- action }}
    condition: >-
      {% set condition = none -%}
      {% if action|lower != 'none' %}
        {% set condition = action|replace('device-power-','')|regex_replace('-.*','') %}
      {%- endif -%}
      {{- condition }}
    response: >-
      {% set response = none -%}
      {% if condition|lower != 'none' %}
        {% set response = action|replace('device-power-' + condition|string + '-','') %}
      {%- endif -%}
      {{- response }}
    origin: >-
      {% if trigger|lower != 'none' %}
        {{ trigger.event.origin|default(none) }}
      {% else %}none{% endif %}
    time_fired: >-
      {% if trigger|lower != 'none' %}
        {{ trigger.event.time_fired|default(none) }}
      {% else %}none{% endif %}
    context: >-
      {% if trigger|lower != 'none' %}
        {{ trigger.event.context }}
      {% else %}none{% endif %}
    responses: >-
      {%- set responses = none -%}
      {%- if condition|lower != 'none' and condition|string|length > 0 -%}
        {%- set s = state_attr('binary_sensor.motion_device_power_' + condition|string,'responses') -%}
        {%- if s|lower != 'none' -%}
          {%- set responses = s -%}
        {%- endif -%}
      {%- endif -%}
      {{- responses -}}
    actions: >-
      {%- set actions = none -%}
      {%- if condition|lower != 'none' and condition|string|length > 0 -%}
        {%- set s = state_attr('binary_sensor.motion_device_power_' + condition|string,'actions') -%}
        {%- if not s is string and s is iterable and 'did' in s -%}
          {%- set actions = s -%}
        {%- endif -%}
      {%- endif -%}
      {{- actions -}}
  action:
    - choose:
        - conditions:
            - condition: template
              value_template: >
                {{ states('sensor.motion_log_level') in state_attr('sensor.motion_log_level','trace') }}
          sequence:
            - service: persistent_notification.create
              data_template:
                title: >-
                  TRACE: Device power response
                notification_id: >-
                  {{ 'device-power-response' }}
                message: >-
                  Response <b>received</b>: "{{ response -}}"; condition: {{ condition|string -}}; context: {{ context|string -}}; at: {{ time_fired }}
    - choose:
        - alias: 'test if response in responses'
          conditions:
            - alias: 'test if response is valid'
              condition: template
              value_template: >
                {{ response|lower != 'none' and response|string|length > 0 }}
            - alias: 'test if responses are valid'
              condition: template
              value_template: >
                {{ responses|lower != 'none' and not responses is string and responses is iterable and responses|count > 0 }}
            - alias: 'test if response in responses'
              condition: template
              value_template: >
                {{ response|lower in responses }}
          sequence:
            - alias: 'set device_power: timestamp, when'
              variables:
                timestamp: >-
                  {{ utcnow().timestamp() }}
                when: >-
                  {{ timestamp|timestamp_custom("%a %b %d %I:%M %p %Z",true,'unknown') }}
                response: >-
                  {{ response }}
            - choose:
                - conditions:
                    - condition: template
                      value_template: >
                        {{ states('sensor.motion_log_level') in state_attr('sensor.motion_log_level','info') }}
                  sequence:
                    - service: persistent_notification.create
                      data_template:
                        title: >-
                          INFO: Device power updated
                        notification_id: >-
                          {{ 'device-power-response' }}
                        message: >-
                          Device power <b>updated</b> to <code>{{- response -}}</code> at <i>{{- when -}}</i>
            - alias: 'set binary_sensor.motion_device_power to new state w/ condition, response, actions, timestamp, when'
              service: python_script.set_state
              data_template:
                entity_id: binary_sensor.motion_device_power
                timestamp: >-
                  {{ timestamp }}
                when: >-
                  {{ when }}
                actions: >-
                  {{ actions }}
                condition: >-
                  {{ condition }}
                response: >-
                  {{ response }}
            - alias: 'trigger automation for power approve'
              service: automation.trigger
              data_template:
                entity_id: >-
                  {{ 'automation.motion_device_power_' + response|string }}

##

- id: motion_device_power_approve
  alias: motion_device_power_approve
  mode: single
  max_exceeded: 'warn'
  initial_state: 'on'
  trigger:
  variables:
    condition: >-
      {% set condition = state_attr('binary_sensor.motion_device_power','condition') %}
      {{ condition }}
    response: >-
      {% set response = 'approve' %}
      {{ response }}
    actions: >-
      {% set actions = state_attr('binary_sensor.motion_device_power_alarm_exist','actions') %}
      {{ actions }}
    function: >-
      {% set function = state_attr('sensor.motion_device_power_approve','function') %}
      {{- function -}}
    timestamp: >-
      {{ utcnow().timestamp() }}
    when: >-
      {{ timestamp|timestamp_custom("%a %b %d %I:%M %p %Z",true,'unknown') }}
  action:
    - choose:
        - conditions:
            - condition: template
              value_template: >
                {{ states('sensor.motion_log_level') in state_attr('sensor.motion_log_level','debug') }}
          sequence:
            - service: persistent_notification.create
              data_template:
                title: >-
                  DEBUG: power response: {{ response }}
                notification_id: >-
                  {{ 'device-power-response' }}
                message: >-
                  Device power response: <b>{{- response -}}</b> at <i>{{- when -}}</i>; Condition: {{ condition -}}; Trigger: {{ function -}}
    - alias: 'function not none'
      condition: template
      value_template: >-
        {{ function|lower != 'none' }}
    - alias: 'trigger automation for power approve'
      service: automation.trigger
      data_template:
        entity_id: >-
          {{ function }}

- id: motion_device_power_deny
  alias: motion_device_power_deny
  mode: single
  max_exceeded: 'warn'
  initial_state: 'on'
  trigger:
  variables:
    condition: >-
      {% set condition = state_attr('binary_sensor.motion_device_power','condition') %}
      {{ condition }}
    response: >-
      {% set response = 'deny' %}
      {{ response }}
    actions: >-
      {% set actions = state_attr('binary_sensor.motion_device_power_alarm_exist','actions') %}
      {{ actions }}
    function: >-
      {% set function = state_attr('sensor.motion_device_power_deny','function') %}
      {{- function -}}
    timestamp: >-
      {{ utcnow().timestamp() }}
    when: >-
      {{ timestamp|timestamp_custom("%a %b %d %I:%M %p %Z",true,'unknown') }}
  action:
    - choose:
        - conditions:
            - condition: template
              value_template: >
                {{ states('sensor.motion_log_level') in state_attr('sensor.motion_log_level','debug') }}
          sequence:
            - service: persistent_notification.create
              data_template:
                title: >-
                  DEBUG: power response: {{ response }}
                notification_id: >-
                  {{ 'device-power-response' }}
                message: >-
                  Device power response: <b>{{- response -}}</b> at <i>{{- when -}}</i>; Condition: {{ condition -}}; Trigger: {{ function -}}
    - alias: 'function not none'
      condition: template
      value_template: >-
        {{ function|lower != 'none' }}
    - alias: 'trigger automation for power deny'
      service: automation.trigger
      data_template:
        entity_id: >-
          {{ function }}

- id: motion_device_power_wait
  alias: motion_device_power_wait
  mode: single
  max_exceeded: 'warn'
  initial_state: 'on'
  trigger:
  variables:
    condition: >-
      {% set condition = state_attr('binary_sensor.motion_device_power','condition') %}
      {{ condition }}
    response: >-
      {% set response = 'wait' %}
      {{ response }}
    actions: >-
      {% set actions = state_attr('binary_sensor.motion_device_power_alarm_exist','actions') %}
      {{ actions }}
    function: >-
      {% set function = state_attr('sensor.motion_device_power_wait','function') %}
      {{- function -}}
    timestamp: >-
      {{ utcnow().timestamp() }}
    when: >-
      {{ timestamp|timestamp_custom("%a %b %d %I:%M %p %Z",true,'unknown') }}
  action:
    - choose:
        - conditions:
            - condition: template
              value_template: >
                {{ states('sensor.motion_log_level') in state_attr('sensor.motion_log_level','debug') }}
          sequence:
            - service: persistent_notification.create
              data_template:
                title: >-
                  DEBUG: power response: {{ response }}
                notification_id: >-
                  {{ 'device-power-response' }}
                message: >-
                  Device power response: <b>{{- response -}}</b> at <i>{{- when -}}</i>; Condition: {{ condition -}}; Trigger: {{ function -}}
    - alias: 'function not none'
      condition: template
      value_template: >-
        {{ function|lower != 'none' }}
    - alias: 'trigger automation for power wait'
      service: automation.trigger
      data_template:
        entity_id: >-
          {{ function }}

- id: motion_device_power_help
  alias: motion_device_power_help
  mode: single
  max_exceeded: 'warn'
  initial_state: 'on'
  trigger:
  variables:
    condition: >-
      {% set condition = state_attr('binary_sensor.motion_device_power','condition') %}
      {{ condition }}
    response: >-
      {% set response = 'help' %}
      {{ response }}
    actions: >-
      {% set actions = state_attr('binary_sensor.motion_device_power_alarm_exist','actions') %}
      {{ actions }}
    function: >-
      {% set function = state_attr('sensor.motion_device_power_help','function') %}
      {{- function -}}
    timestamp: >-
      {{ utcnow().timestamp() }}
    when: >-
      {{ timestamp|timestamp_custom("%a %b %d %I:%M %p %Z",true,'unknown') }}
  action:
    - choose:
        - conditions:
            - condition: template
              value_template: >
                {{ states('sensor.motion_log_level') in state_attr('sensor.motion_log_level','debug') }}
          sequence:
            - service: persistent_notification.create
              data_template:
                title: >-
                  DEBUG: power response: {{ response }}
                notification_id: >-
                  {{ 'device-power-response' }}
                message: >-
                  Device power response: <b>{{- response -}}</b> at <i>{{- when -}}</i>; Condition: {{ condition -}}; Trigger: {{ function -}}
    - alias: 'function not none'
      condition: template
      value_template: >-
        {{ function|lower != 'none' }}
    - alias: 'function automation for power help'
      service: automation.trigger
      data_template:
        entity_id: >-
          {{ function }}
