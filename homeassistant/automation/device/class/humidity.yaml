###
# homeassistant/automation/motion/device/humidity.yaml
###

- id: motion_device_humidity_automation_enable
  alias: motion_device_humidity_automation_enable
  initial_state: on
  trigger:
    platform: state
    entity_id: group.motion_device_humidity
    attribute: 'entity_id'
  condition:
    - condition: template
      value_template: >-
        {% set s = state_attr('group.motion_device_humidity','entity_id') %}
        {{ s|lower != 'none' and not s is string and s is iterable and s|count > 0 }}
  variables:
    automations: >-
      {% set automations = ['motion_device_humidity_activity','motion_device_humidity_activity_notify','motion_device_humidity_alarm_activity','motion_device_humidity_alarm_bad_on','motion_device_humidity_alarm_good_on','motion_device_humidity_alarm_high_off','motion_device_humidity_alarm_high_on','motion_device_humidity_alarm_low_off','motion_device_humidity_alarm_low_on','motion_device_humidity_bad','motion_device_humidity_bad_notify','motion_device_humidity_cleared_dismiss','motion_device_humidity_cleared_notify','motion_device_humidity_good','motion_device_humidity_good_notify','motion_device_humidity_high','motion_device_humidity_high_dismiss','motion_device_humidity_high_notify','motion_device_humidity_low','motion_device_humidity_low_dismiss','motion_device_humidity_low_notify','motion_device_humidity_mute','motion_device_humidity_restart','motion_device_humidity_self_test','motion_device_humidity_status','motion_device_humidity_unavailable','motion_device_humidity_unmute','motion_device_humidity_update'] %}
      {{ automations }}
  action:
    - alias: 'enable automations for device_humidity'
      repeat:
        while:
          - condition: template
            value_template: >-
              {% set s = automations[repeat.index-1]|default('') %}
              {{ s|string != '' }}
        sequence:
          - variables:
              automation: >-
                {% set s = automations[repeat.index-1] %}
                {{ 'automation.' + s|string }}
          - condition: template
            value_template: >-
              {{ states(automation)|lower == 'off' }}
          - service: automation.turn_on
            data_template:
              entity_id: >-
                {{ automation }}
          - condition: template
            value_template: >
              {{ states('input_select.motion_log_level') in ['debug'] }}
          - service: persistent_notification.create
            data_template:
              title: >-
                {% set s = automations[repeat.index-1] %}
                DEBUG: enabling {{ s }}
              notification_id: >
                {{- 'debug-automation-humidity' -}}
              message: >-
                Automation enabled: {{ automation }}

- id: motion_device_humidity_automation_disable
  alias: motion_device_humidity_automation_disable
  initial_state: on
  trigger:
    platform: state
    entity_id: group.motion_device_humidity
    attribute: 'entity_id'
  condition:
    - condition: template
      value_template: >-
        {% set s = state_attr('group.motion_device_humidity','entity_id') %}
        {{ s|lower == 'none' or s is string or not s is iterable or not s|count > 0 }}
  variables:
    automations: >-
      {% set automations = ['motion_device_humidity_activity','motion_device_humidity_activity_notify','motion_device_humidity_alarm_activity','motion_device_humidity_alarm_bad_on','motion_device_humidity_alarm_good_on','motion_device_humidity_alarm_high_off','motion_device_humidity_alarm_high_on','motion_device_humidity_alarm_low_off','motion_device_humidity_alarm_low_on','motion_device_humidity_bad','motion_device_humidity_bad_notify','motion_device_humidity_cleared_dismiss','motion_device_humidity_cleared_notify','motion_device_humidity_good','motion_device_humidity_good_notify','motion_device_humidity_high','motion_device_humidity_high_dismiss','motion_device_humidity_high_notify','motion_device_humidity_low','motion_device_humidity_low_dismiss','motion_device_humidity_low_notify','motion_device_humidity_mute','motion_device_humidity_restart','motion_device_humidity_self_test','motion_device_humidity_status','motion_device_humidity_unavailable','motion_device_humidity_unmute','motion_device_humidity_update'] %}
      {{ automations }}
  action:
    - alias: 'disable automations for device_humidity'
      repeat:
        while:
          - condition: template
            value_template: >-
              {% set s = automations[repeat.index-1]|default('') %}
              {{ s|string != '' }}
        sequence:
          - variables:
              automation: >-
                {% set s = automations[repeat.index-1] %}
                {{ 'automation.' + s|string }}
          - alias: 'test if automation is on'
            condition: template
            value_template: >-
              {{ states(automation)|lower == 'on' }}
          - service: automation.turn_off
            data_template:
              entity_id: >-
                {{ automation }}
          - condition: template
            value_template: >
              {{ states('input_select.motion_log_level') in ['debug'] }}
          - service: persistent_notification.create
            data_template:
              title: >-
                {% set s = automations[repeat.index-1] %}
                DEBUG: disabling {{ s }}
              notification_id: >
                {{- 'debug-automation-humidity' -}}
              message: >-
                Automation disables: {{ automation }}

- id: motion_device_humidity_rescan
  alias: motion_device_humidity_rescan
  mode: single
  max_exceeded: silent
  initial_state: on
  trigger:
    - platform: time_pattern
      minutes: '/5'
    - platform: homeassistant
      event: start
  variables:
    all: >-
      [{%- for state in (states.sensor|list + states.binary_sensor|list)
       if 'device_class' in state.attributes and device_id(state.entity_id)|lower != 'none'
        and state.attributes.device_class=='humidity' and not '.motion_' in state.entity_id
        and state.attributes.device_class=='humidity' and not '.motion_' in state.entity_id -%}
        {%- if not loop.first -%},{% endif -%}
        "{{- device_attr(device_id(state.entity_id),'name') -}}"
      {%- endfor -%}]
    local: >-
      {% set device_trackers = states.device_tracker|map(attribute='entity_id')|map('device_id')|list %}
      [{%- for state in (states.sensor|list + states.binary_sensor|list)
       if 'device_class' in state.attributes and device_id(state.entity_id)|lower != 'none'
        and state.attributes.device_class=='humidity' and not '.motion_' in state.entity_id
        and state.attributes.device_class=='humidity' and not '.motion_' in state.entity_id
        and device_id(state.entity_id) not in device_trackers -%}
        {%- if not loop.first -%},{% endif -%}
        "{{- device_attr(device_id(state.entity_id),'name') -}}"
      {%- endfor -%}]
    mobile: >-
      {% set device_trackers = states.device_tracker|map(attribute='entity_id')|map('device_id')|list %}
      [{%- for state in (states.sensor|list + states.binary_sensor|list)
       if 'device_class' in state.attributes and device_id(state.entity_id)|lower != 'none'
        and state.attributes.device_class=='humidity' and not '.motion_' in state.entity_id
        and state.attributes.device_class=='humidity' and not '.motion_' in state.entity_id
        and device_id(state.entity_id) in device_trackers -%}
        {%- if not loop.first -%},{% endif -%}
        "{{- device_attr(device_id(state.entity_id),'name') -}}"
      {%- endfor -%}]
    ids: >-
      {%- if all != [] and all is iterable and all|count > 0 -%}
        {{- all|sort|unique|list -}}
      {% else %}none{% endif %}
    mobiles: >-
      {%- if mobile != [] and mobile is iterable and mobile|count > 0 -%}
        {{- mobile|sort|unique|list -}}
      {% else %}none{% endif %}
    total: >-
      {% set s = local %}
      {% if s|lower != 'unknown' and s|lower != 'none' and not s is string %}
        {{ s|count }}
      {%- else -%}0{%- endif %}
    update: >-
      {% set s = ids %}
      {%- if s|lower != 'unknown' and s|lower != 'none' and not s is string -%}
        [{%- for i in s -%}
          {% set o = loop %}
          {%- for state in (states.button|list + states.switch|list) if device_attr(state.entity_id, 'name') == i and state.attributes.device_class|default('null')|string == 'update' -%}
            {%- set v = state.state -%}
            {"id":"{{- i -}}","entity_id":"{{- state.entity_id -}}"}
            {% if not loop.last and not o.last %},{% endif %}
          {%- endfor -%}
        {%- endfor -%}]
      {%- else -%}null{%- endif %}
    restart: >-
      {% set s = ids %}
      {%- if s|lower != 'unknown' and s|lower != 'none' and not s is string -%}
        [{%- for i in s -%}
          {% set o = loop %}
          {%- for state in (states.button|list + states.switch|list) if device_attr(state.entity_id, 'name') == i and state.attributes.device_class|default('null')|string == 'restart' -%}
            {%- set v = state.state -%}
            {"id":"{{- i -}}","entity_id":"{{- state.entity_id -}}"}
            {% if not loop.last and not o.last %},{% endif %}
          {%- endfor -%}
        {%- endfor -%}]
      {%- else -%}null{%- endif %}
    controls: >-
      {% set s = ids %}
      {%- if s|lower != 'unknown' and s|lower != 'none' and not s is string -%}
        [{%- for i in s -%}
          {% set o = loop %}
          {%- for state in (states.button|list + states.switch|list) if device_attr(state.entity_id, 'name') == i and state.attributes.device_class|default('null')|string == 'null' -%}
            {%- set ctl = state_attr(state.entity_id,'friendly_name') -%}
            {"id":"{{- i -}}","entity_id":"{{- state.entity_id -}}","control":"{{- ctl -}}"}
            {% if not loop.last and not o.last %},{% endif %}
          {%- endfor -%}
        {%- endfor -%}]
      {%- else -%}null{%- endif %}
    test: >-
      {% set s = ids %}
      {%- if s|lower != 'unknown' and s|lower != 'none' and not s is string -%}
        [{%- for i in s -%}
          {% set o = loop %}
          {%- for state in states.sensor if device_attr(state.entity_id, 'name') == i and state.attributes.self_test|default('none')|string != 'none' -%}
            {"id":"{{- i -}}","entity_id":"{{- state.entity_id -}}","state":"{{- state.state -}}","updated":{{- state.last_updated|as_timestamp(None) -}}}
            {% if not loop.last and not o.last %},{% endif %}
          {%- endfor -%}
        {%- endfor -%}]
      {%- else -%}null{%- endif %}
    areas: >-
      {% set s = ids -%}
      {%- if s|lower != 'unknown' and s|lower != 'none' and not s is string -%}
        [{%- for i in s -%}
          {% set o = loop %}
          {%- for state in (states.sensor|list + states.binary_sensor|list) if device_attr(state.entity_id, 'name') == i and state.attributes.device_class|default('null')|string == 'humidity' -%}
            {%- if not o.first or not loop.first -%},{%- endif -%}
            {%- set v = state.state -%}
            {"id":"{{- i -}}","area":"{{- area_id(state.entity_id) -}}","state":"{{- state.state -}}"}
          {%- endfor -%}
        {%- endfor -%}]
      {%- else -%}none{%- endif %}
    status: >-
      {% set s = ids %}
      {%- if s|lower != 'unknown' and s|lower != 'none' and not s is string -%}
        [{%- for i in s -%}
          {% set o = loop %}
          {%- for state in (states.sensor|list + states.binary_sensor|list) if device_attr(state.entity_id, 'name') == i and state.attributes.device_class|default('null')|string == 'humidity' -%}
            {%- if not o.first or not loop.first -%},{%- endif -%}
            {"id":"{{- i -}}","entity_id":"{{- state.entity_id -}}","state":"{{- state.state -}}","updated":{{- state.last_updated|as_timestamp(None) -}}}
          {%- else -%}{% if o.first %}None{% endif %}{%- endfor -%}
        {%- endfor -%}]
      {%- else -%}none{%- endif %}
    devices: >-
      {%- if ids|lower != 'unknown' and ids|lower != 'none' and not s is string and ids is iterable
             and status|lower != 'none' and not status is string and status is iterable
             and areas|lower != 'none' and not areas is string and areas is iterable -%}
        [{% for i in ids -%}
          {%- set j = status|selectattr('id','==',i)|map(attribute='entity_id')|first -%}
          {%- set k = areas|selectattr('id','==',i)|map(attribute='area')|first -%}
          {%- if not loop.first -%},{%- endif -%}
          {%- set n = device_attr(device_id(j),'name_by_user') -%}
          {%- if n|lower == 'none' -%}{%- set n = device_attr(device_id(j),'name') -%}{%- endif -%}
          { "id":"{{- i -}}", "did":"{{- device_id(j) -}}", "friendly_name":"{{- n -}}", "manufacturer":"{{- device_attr(device_id(j),'manufacturer') -}}", "model":"{{- device_attr(device_id(j),'model') -}}", "sw_version":"{{- device_attr(device_id(j),'sw_version') -}}", "area":"{{- k -}}" }
        {%- endfor -%}]
      {%- else -%}none{%- endif %}
  action:
    - alias: 'update binary_sensor.motion_device_humidity ids, etc..'
      service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_humidity
        ids: >-
          {{ ids }}
        mobiles: >-
          {{ mobiles }}
        total: >-
          {{ total }}
        update: >-
          {{ update }}
        restart: >-
          {{ restart }}
        controls: >-
          {{ controls }}
        test: >-
          {{ test }}
        areas: >-
          {{ areas }}
        devices: >-
          {{ devices }}
        state: >-
          {{ states('binary_sensor.motion_device_humidity') }}
    - variables:
        entities: >-
          {%- if status|lower != 'null' and not status is string and status is iterable and status|count > 0 -%}
            {{- status|map(attribute='entity_id')|list -}}
          {%- else -%}none{%- endif %}
    - condition: template
      value_template: >
        {{ entities|lower != 'none' }}
    - alias: 'set group.motion_device_humidity sensors'
      service: group.set
      data_template:
        object_id: motion_device_humidity
        entities: >-
          {{ entities }}

## update status

- id: motion_device_humidity_status
  alias: motion_device_humidity_status
  mode: single
  max_exceeded: silent
  initial_state: off
  trigger:
    - platform: state
      entity_id: binary_sensor.motion_device_humidity_exist
    - platform: state
      entity_id: group.motion_device_humidity
    - platform: state
      entity_id: binary_sensor.motion_device_humidity
      attribute: 'ids'
    - platform: time_pattern
      seconds: '/30'
  variables:
    ids: >-
      {% set ids = state_attr('binary_sensor.motion_device_humidity','ids') %}
      {{ ids }}
    status: >-
      {% set s = ids %}
      {%- if s|lower != 'none' and s|lower != 'unknown' and s is iterable -%}
        [{%- for i in s -%}
          {% set o = loop %}
          {%- for state in (states.sensor|list + states.binary_sensor|list) if device_attr(state.entity_id, 'name') == i and state.attributes.device_class|default('null')|string == 'humidity' -%}
            {%- if not o.first or not loop.first -%},{%- endif -%}
            {"id":"{{- i -}}","entity_id":"{{- state.entity_id -}}","state":"{{- state.state -}}","updated":{{- state.last_updated|as_timestamp(None) -}}}
          {%- else -%}{% if o.first %}None{% endif %}{%- endfor -%}
        {%- endfor -%}]
      {%- else -%}none{%- endif %}
    high: >-
      {% if state_attr('binary_sensor.motion_device_humidity_exist','high')|float('none') != 'none' %}
        {% set s = status %}
        {%- if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s is iterable -%}
          {% set n = state_attr('binary_sensor.motion_device_humidity_exist','high')|float(0) %}
          [{% for i in s if 'entity_id' in i and states(i.entity_id)|float(n) > n -%}
            {%- if not loop.first -%},{%- endif -%}
            {"id":"{{- i.id -}}","entity_id":"{{- i.entity_id -}}","state":"{{- i.state -}}","updated":{{- states[i.entity_id].last_updated|as_timestamp(None) -}}}
          {%- endfor -%}]
        {%- else -%}none{%- endif %}
      {%- else -%}none{%- endif %}
    low: >-
      {% if state_attr('binary_sensor.motion_device_humidity_exist','low')|float('none') != 'none' %}
        {% set s = status %}
        {%- if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s is iterable -%}
          {% set n = state_attr('binary_sensor.motion_device_humidity_exist','low')|float(0) %}
          [{% for i in s if 'entity_id' in i and states(i.entity_id)|float(n) < n -%}
            {%- if not loop.first -%},{%- endif -%}
            {"id":"{{- i.id -}}","entity_id":"{{- i.entity_id -}}","state":"{{- i.state -}}","updated":{{- states[i.entity_id].last_updated|as_timestamp(None) -}}}
          {%- endfor -%}]
        {%- else -%}none{%- endif %}
      {%- else -%}none{%- endif %}
    good: >-
      {% set good = state_attr('binary_sensor.motion_device_humidity_exist','good') %}
      {%- if good|lower != 'none' -%}
        {% set s = status %}
        {%- if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s is iterable -%}
          [{% for i in s if 'entity_id' in i and not i.entity_id is none and is_state(i.entity_id,good) -%}
            {%- if not loop.first -%},{%- endif -%}
            {"id":"{{- i.id -}}","entity_id":"{{- i.entity_id -}}","state":"{{- i.state -}}","updated":{{- states[i.entity_id].last_updated|as_timestamp(None) -}}}
          {%- endfor -%}]
        {%- else -%}none{%- endif %}
      {%- else -%}none{%- endif %}
    bad: >-
      {% set bad = state_attr('binary_sensor.motion_device_humidity_exist','bad') %}
      {%- if bad|lower != 'none' -%}
        {% set s = status %}
        {%- if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s is iterable -%}
          [{% for i in s if 'entity_id' in i and not i.entity_id is none and is_state(i.entity_id,bad) -%}
            {%- if not loop.first -%},{%- endif -%}
            {"id":"{{- i.id -}}","entity_id":"{{- i.entity_id -}}","state":"{{- i.state -}}","updated":{{- states[i.entity_id].last_updated|as_timestamp(None) -}}}
          {%- endfor -%}]
        {%- else -%}none{%- endif %}
      {%- else -%}none{%- endif %}
    activity: >-
      {% set activity = state_attr('binary_sensor.motion_device_humidity_exist','activity') %}
      {%- if activity|lower != 'none' -%}
        {% set s = status %}
        {%- if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s is iterable -%}
          [{% for i in s if 'entity_id' in i and not i.entity_id is none and is_state(i.entity_id,activity) -%}
            {%- if not loop.first -%},{%- endif -%}
            {"id":"{{- i.id -}}","entity_id":"{{- i.entity_id -}}","state":"{{- i.state -}}","updated":{{- states[i.entity_id].last_updated|as_timestamp(None) -}}}
          {%- endfor -%}]
        {%- else -%}none{%- endif %}
      {%- else -%}none{%- endif %}
    unavailable: >-
      {% set s = status %}
      {%- if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s is iterable -%}
        [{% for i in s if 'entity_id' in i and not i.entity_id is none and is_state(i.entity_id,'unavailable') -%}
          {%- if not loop.first -%},{%- endif -%}
            {"id":"{{- i.id -}}","entity_id":"{{- i.entity_id -}}","state":"{{- i.state -}}","updated":{{- states[i.entity_id].last_updated|as_timestamp(None) -}}}
        {%- endfor -%}]
      {%- else -%}none{%- endif %}
    values: >-
      {% set g = state_attr('binary_sensor.motion_device_humidity_exist','good') %}
      {% set b = state_attr('binary_sensor.motion_device_humidity_exist','bad') %}
      {%- if g|lower != 'none' and b|lower != 'none' -%}
        [{% for i in status if 'entity_id' in i and not i.entity_id is none -%}
          {%- if not loop.first -%},{%- endif -%}
          {% if is_state(i.entity_id,g) %}1{% elif is_state(i.entity_id,b) %}0{% else %}none{% endif %}
        {%- endfor -%}]
      {%- else -%}
        [{% for i in status if 'entity_id' in i and not i.entity_id is none -%}
          {%- if not loop.first -%},{%- endif -%}
          {{- states(i.entity_id)|float(none) -}}
        {%- endfor -%}]
      {%- endif %}
    value: >-
      {% if bad|lower != 'none' and not bad is string and bad is iterable -%}
        {%- if bad|count > 0 -%}
          {%- set result = 'on' -%}
        {%- else -%}
          {%- set result = 'off' -%}
        {%- endif -%}
      {%- endif -%}
      {%- if low|lower != 'none' and not low is string and low is iterable -%}
        {%- if low|count > 0 -%}
          {%- set result = 'on' -%}
        {%- else -%}
          {%- set result = 'off' -%}
        {%- endif -%}
      {%- endif -%}
      {%- if high|lower != 'none' and not high is string and high is iterable -%}
        {%- if high|count > 0 -%}
          {%- set result = 'on' -%}
        {%- else -%}
          {%- set result = 'off' -%}
        {%- endif -%}
      {%- endif -%}
      {%- if activity|lower != 'none' and not activity is string and activity is iterable -%}
        {%- if activity|count > 0 -%}
          {%- set result = 'on' -%}
        {%- else -%}
          {%- set result = 'off' -%}
        {%- endif -%}
      {%- endif -%}
      {%- if result is defined -%}
        {{- result -}}
      {%- else -%}unknown{%- endif %}
  action:
    - alias: 'update binary_sensor.motion_device_humidity state'
      service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_humidity
        status: >-
          {{ status }}
        high: >-
          {{ high }}
        low: >-
          {{ low }}
        good: >-
          {{ good }}
        bad: >-
          {{ bad }}
        activity: >-
          {{ activity }}
        unavailable: >-
          {{ unavailable }}
        values: >-
          {{ values }}
        state: >-
          {{ value }}

## update, restart, and other contols

- id: motion_device_humidity_self_test
  alias: motion_device_humidity_self_test
  mode: single
  initial_state: off
  trigger:
  condition:
    condition: and
    conditions:
      - alias: 'test if self_test is on'
        condition: template
        value_template: >-
          {{ is_state('binary_sensor.motion_device_humidity_self_test','on') }}
  variables:
    entity_ids: >-
      {{ state_attr('binary_sensor.motion_device_humidity_self_test','entity_ids') }}
  action:
    - alias: 'test humidity sensors'
      repeat:
        while:
          - alias: 'test if not done'
            condition: template
            value_template: >-
              {% set s = entity_ids[repeat.index - 1]|default('unknown') %}
              {{ s|lower != 'unavailable' and s|lower != 'unknown' and s|lower != 'null' and s|string|length > 0 }}
        sequence:
          - alias: 'pressing button'
            service: button.press
            data_template:
              entity_id: >-
                {{ entity_ids[repeat.index - 1] }}

- id: motion_device_humidity_mute
  alias: motion_device_humidity_mute
  mode: single
  initial_state: off
  trigger:
  condition:
    condition: and
    conditions:
      - condition: template
        value_template: >-
          {{ is_state('binary_sensor.motion_device_humidity_mute','on') }}
  variables:
    entity_ids: >-
      {{ state_attr('binary_sensor.motion_device_humidity_mute','entity_ids') }}
  action:
    - alias: 'mute humidity sensors'
      repeat:
        while:
          - condition: template
            value_template: >-
              {% set s = entity_ids[repeat.index - 1]|default('unknown') %}
              {{ s|lower != 'unavailable' and s|lower != 'unknown' and s|lower != 'null' and s|string|length > 0 }}
        sequence:
          - service: button.press
            data_template:
              entity_id: >-
                {{ entity_ids[repeat.index - 1] }}

- id: motion_device_humidity_unmute
  alias: motion_device_humidity_unmute
  mode: single
  initial_state: off
  trigger:
  condition:
    condition: and
    conditions:
      - condition: template
        value_template: >-
          {{ is_state('binary_sensor.motion_device_humidity_unmute','on') }}
  variables:
    entity_ids: >-
      {{ state_attr('binary_sensor.motion_device_humidity_unmute','entity_ids') }}
  action:
    - alias: 'unmute humidity sensors'
      repeat:
        while:
          - condition: template
            value_template: >-
              {% set s = entity_ids[repeat.index - 1]|default('unknown') %}
              {{ s|lower != 'unavailable' and s|lower != 'unknown' and s|lower != 'null' and s|string|length > 0 }}
        sequence:
          - service: button.press
            data_template:
              entity_id: >-
                {{ entity_ids[repeat.index - 1] }}

- id: motion_device_humidity_restart
  alias: motion_device_humidity_restart
  mode: single
  initial_state: off
  trigger:
  condition:
    condition: and
    conditions:
      - condition: template
        value_template: >-
          {{ is_state('binary_sensor.motion_device_humidity_restart','on') }}
  variables:
    entity_ids: >-
      {{ state_attr('binary_sensor.motion_device_humidity_restart','entity_ids') }}
  action:
    - alias: 'restart humidity sensors'
      repeat:
        while:
          - condition: template
            value_template: >-
              {% set s = entity_ids[repeat.index - 1]|default('unknown') %}
              {{ s|lower != 'unavailable' and s|lower != 'unknown' and s|lower != 'null' and s|string|length > 0 }}
        sequence:
          - service: button.press
            data_template:
              entity_id: >-
                {{ entity_ids[repeat.index - 1] }}

- id: motion_device_humidity_update
  alias: motion_device_humidity_update
  mode: single
  initial_state: off
  trigger:
  condition:
    condition: and
    conditions:
      - condition: template
        value_template: >-
          {{ is_state('binary_sensor.motion_device_humidity_update','on') }}
  variables:
    entity_ids: >-
      {{ state_attr('binary_sensor.motion_device_humidity_update','entity_ids') }}
  action:
    - alias: 'update humidity sensors'
      repeat:
        while:
          - condition: template
            value_template: >-
              {% set s = entity_ids[repeat.index - 1]|default('unknown') %}
              {{ s|lower != 'unavailable' and s|lower != 'unknown' and s|lower != 'null' and s|string|length > 0 }}
        sequence:
          - service: button.press
            data_template:
              entity_id: >-
                {{ entity_ids[repeat.index - 1] }}

## notifications

# found

- id: motion_device_humidity_found
  alias: motion_device_humidity_found
  mode: single
  initial_state: on
  trigger:
    - platform: state
      entity_id: binary_sensor.motion_device_humidity_exist
      to: 'on'
  variables:
    ids: >-
      {% set ids = state_attr('binary_sensor.motion_device_humidity','ids') %}
      {{ ids }}
    devices: >-
      {% set devices = state_attr('binary_sensor.motion_device_humidity','devices') %}
      {{ devices }}
    title: >-
      {% set title = states('input_text.motion_app') + ': humidity sensor found' %}
      {{ title }}
    notification_id: >-
      {{ 'device-humidity-notify' }}
    icon: >-
      {% set icon = 'notification-humidity-found' %}
      {{ icon }}
    header: >-
      {{- '[![](/local/images/icons/' + icon + '.png?v1)]()' -}}
      {{- '<hr>' -}}
    footer: >-
      {{- '<hr>' -}}
    body: >-
      Sensor(s) for humidity found at {{ utcnow().timestamp()|timestamp_custom("%a %b %d %I:%M %p %Z",true,'unknown') -}}:
      {{- '<ul>' -}}
      {%- if devices|lower != 'none' -%}
        {%- for i in devices -%}
          {{- '<li>' -}}
          [{{- i.friendly_name -}}](/config/devices/device/{{- i.did -}}) in [{{- i.area -}}](/config/areas/area/{{- i.area -}})
          {{- '</li>' -}}
        {%- endfor -%}
      {%- endif -%}
      {{- '</ul>' -}}
    message: >-
      {{ header }}
      {{ body }}
      {{ footer }}
  action:
    - alias: 'notify found humidity sensor'
      service: persistent_notification.create
      data_template:
        title: >-
          {{- title -}}
        notification_id: >-
          {{- notification_id -}}
        message: >-
          {{- message -}}

- id: motion_device_humidity_found_dismiss
  alias: motion_device_humidity_found_dismiss
  mode: single
  initial_state: on
  trigger:
    - platform: state
      entity_id: binary_sensor.motion_device_humidity_exist
      to: 'on'
      for:
        minutes: >-
          {{ states('input_number.motion_device_class_dismiss')|int(5) }}
  variables:
    notification_id: >-
      {{ 'device-humidity-notify' }}
  action:
    - alias: 'dismiss humidity found notification'
      service: persistent_notification.dismiss
      data_template:
        notification_id: >-
          {{ notification_id }}

# bad

- id: motion_device_humidity_alarm_bad_on
  alias: motion_device_humidity_alarm_bad_on
  mode: single
  initial_state: off
  trigger:
    - platform: template
      value_template: >-
        {{ not is_state_attr('binary_sensor.motion_device_humidity_exist','bad','none')
           and is_state_attr('binary_sensor.motion_device_humidity_exist','bad',states('binary_sensor.motion_device_humidity')) }}
  variables:
    devices: >-
      {% set devices = none %}
      {% set s = state_attr('binary_sensor.motion_device_humidity','devices') %}
      {% if s|lower != 'none' and s|lower != 'null' and not s is string and s is iterable and s|count > 0 %}
        {% set devices = s %}
      {%- endif %}
      {{ devices }}
    last: >-
      {% set last = none %}
      {% set s = state_attr('binary_sensor.motion_device_humidity','status')|selectattr('state','==',state_attr('binary_sensor.motion_device_humidity_exist','bad'))|sort(attribute='updated',reverse=true)|list %}
      {% if s|lower != 'none' and s|lower != 'null' and not s is string and s is iterable and s|count > 0 %}
        {% set last = s %}
      {%- endif %}
      {{ last  }}
    which: >-
      {% set which = none %}
      {% if last|lower != 'none' %}
        {% set which = last|map(attribute='id')|first %}
      {%- endif %}
      {{ which }}
    did: >-
      {% set did = none %}
      {% if devices|lower != 'none'  and which|lower != 'none' %}
        {% set did = devices|selectattr('id','==',which)|map(attribute='did')|first %}
      {%- endif %}
      {{ did }}
    where: >-
      {% set where = none %}
      {% if devices|lower != 'none'  and which|lower != 'none' %}
        {% set where = devices|selectattr('id','==',which)|map(attribute='area')|first %}
      {%- endif %}
      {{ where }}
  condition:
    condition: and
    conditions:
      - condition: template
        value_template: >
          {{ which|lower != 'none' and which|lower != 'null' and which|length > 0 }}
  action:
    - alias: 'update binary_sensor.motion_device_humidity which, where, did'
      service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_humidity
        which: >-
          {{ which }}
        where: >-
          {{ where }}
        did: >-
          {{ did }}
    - condition: template
      value_template: >
        {{ is_state_attr('binary_sensor.motion_device_humidity_exist','alarm','on') }}
    - alias: 'trigger motion notification'
      service: automation.trigger
      data_template:
        entity_id: automation.motion_device_humidity_bad_notify

# bad notify

- id: motion_device_humidity_bad_notify
  alias: motion_device_humidity_bad_notify
  mode: single
  initial_state: off
  trigger:
  variables:
    ids: >-
      {% set ids = state_attr('binary_sensor.motion_device_humidity','ids') %}
      {{ ids }}
    which: >-
      {% set which = state_attr('binary_sensor.motion_device_humidity','which') %}
      {{ which }}
    where: >-
      {% set where = state_attr('binary_sensor.motion_device_humidity','where') %}
      {{ where }}
    did: >-
      {% set did = state_attr('binary_sensor.motion_device_humidity','did') %}
      {{ did }}
    url: >-
      {% set url = '/lovelace/humidity-okay' %}
      {{ url }}
    title: >-
      {% set title = states('input_text.motion_app') + ': humidity bad' %}
      {{ title }}
    notification_id: >-
      {{ 'device-humidity-status-' + which|string }}
    icon: >-
      {% set icon = 'notification-humidity-bad' %}
      {{ icon }}
    header: >-
      {{- '[![](/local/images/icons/' + icon + '.png?v1)]()' -}}
      {{- '<hr>' -}}
    action: >-
      {% set action = 'humidity-okay' %}
      {{ action }}
    footer: >-
      {{ '<hr>' }}
      {%- if did|lower != 'none' and did|lower != 'null' -%}
        {{- '[![](https://img.shields.io/badge/device-info-blue.svg)](' + '/config/devices/device/' + did + ')' -}}
      {%- endif -%}
    body: >-
      Your home humidity alarm was triggered ({{- state_attr('binary_sensor.motion_device_humidity_exist','bad')|string -}}); sensor: {{ which -}}; where: {{ where -}}; at {{ utcnow().timestamp()|timestamp_custom("%a %b %d %I:%M %p %Z",true,'unknown') -}}.
      We have notified **{{- state_attr('sensor.motion_role_primary','friendly_name') -}}** about your humidity alarm.
      If you are fine handling this situation on your own, click the **{{- action -}}** button below.
      {{- '<p>' -}}
      {{-  '[![](https://img.shields.io/badge/' + action + '-green.svg)](' + url + ')' -}}
    message: >-
      {{- header -}}
      {{- body -}}
      {{- footer -}}
  action:
    - alias: 'notify humidity sensor activated'
      service: persistent_notification.create
      data_template:
        title: >-
          {{- title -}}
        notification_id: >-
          {{- notification_id -}}
        message: >-
          {{- message -}}

- id: motion_device_humidity_alarm_good_on
  alias: motion_device_humidity_alarm_good_on
  mode: single
  initial_state: off
  trigger:
    - platform: template
      value_template: >-
        {{ not is_state_attr('binary_sensor.motion_device_humidity_exist','good','none')
           and is_state_attr('binary_sensor.motion_device_humidity_exist','good',states('binary_sensor.motion_device_humidity')) }}
  variables:
    devices: >-
      {% set devices = none %}
      {% set s = state_attr('binary_sensor.motion_device_humidity','devices') %}
      {% if s|lower != 'none' and s|lower != 'null' and not s is string and s is iterable and s|count > 0 %}
        {% set devices = s %}
      {%- endif %}
      {{ devices }}
    last: >-
      {% set last = none %}
      {% set s = state_attr('binary_sensor.motion_device_humidity','status')|selectattr('state','==',state_attr('binary_sensor.motion_device_humidity_exist','good'))|sort(attribute='updated',reverse=true)|list %}
      {% if s|lower != 'none' and s|lower != 'null' and not s is string and s is iterable and s|count > 0 %}
        {% set last = s %}
      {%- endif %}
      {{ last  }}
    which: >-
      {% set which = none %}
      {% if last|lower != 'none' %}
        {% set which = last|map(attribute='id')|first %}
      {%- endif %}
      {{ which }}
    did: >-
      {% set did = none %}
      {% if devices|lower != 'none'  and which|lower != 'none' %}
        {% set did = devices|selectattr('id','==',which)|map(attribute='did')|first %}
      {%- endif %}
      {{ did }}
    where: >-
      {% set where = none %}
      {% if devices|lower != 'none'  and which|lower != 'none' %}
        {% set where = devices|selectattr('id','==',which)|map(attribute='area')|first %}
      {%- endif %}
      {{ where }}
  condition:
    condition: and
    conditions:
      - condition: template
        value_template: >
          {{ which|lower != 'none' and which|lower != 'null' and which|length > 0 }}
  action:
    - alias: 'update binary_sensor.motion_device_humidity which, where, did'
      service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_humidity
        which: >-
          {{ which }}
        where: >-
          {{ where }}
        did: >-
          {{ did }}
    - condition: template
      value_template: >
        {{ is_state_attr('binary_sensor.motion_device_humidity_exist','alarm','on') }}
    - alias: 'trigger motion notification'
      service: automation.trigger
      data_template:
        entity_id: automation.motion_device_humidity_good_notify

- id: motion_device_humidity_good_notify
  alias: motion_device_humidity_good_notify
  mode: single
  initial_state: off
  trigger:
  variables:
    ids: >-
      {% set ids = state_attr('binary_sensor.motion_device_humidity','ids') %}
      {{ ids }}
    which: >-
      {% set which = state_attr('binary_sensor.motion_device_humidity','which') %}
      {{ which }}
    where: >-
      {% set where = state_attr('binary_sensor.motion_device_humidity','where') %}
      {{ where }}
    did: >-
      {% set did = state_attr('binary_sensor.motion_device_humidity','did') %}
      {{ did }}
    action: >-
      {% set action = 'humidity-okay' %}
      {{ action }}
    url: >-
      {% set url = '/lovelace/humidity-okay' %}
      {{ url }}
    title: >-
      {% set title = states('input_text.motion_app') + ': humidity good' %}
      {{ title }}
    notification_id: >-
      {{ 'device-humidity-status-' + which|string }}
    icon: >-
      {% set icon = 'notification-humidity-good' %}
      {{ icon }}
    header: >-
      {{- '[![](/local/images/icons/' + icon + '.png?v1)]()' -}}
      {{- '<hr>' -}}
    footer: >-
      {{ '<hr>' }}
      {%- if did|lower != 'none' and did|lower != 'null' -%}
        {{- '[![](https://img.shields.io/badge/device-info-blue.svg)](' + '/config/devices/device/' + did + ')' -}}
      {%- endif -%}
    body: >-
      Your home humidity alarm was triggered ({{- state_attr('binary_sensor.motion_device_humidity_exist','good')|string -}}); sensor: {{ which -}}; where: {{ where -}}; at {{ utcnow().timestamp()|timestamp_custom("%a %b %d %I:%M %p %Z",true,'unknown') -}}.
      We have notified **{{- state_attr('sensor.motion_role_primary','friendly_name') -}}** about your humidity alarm.
      If you are fine handling this situation on your own, click the **{{- action -}}** button below.
      {{- '<p>' -}}
      {{-  '[![](https://img.shields.io/badge/' + action + '-green.svg)](' + url + ')' -}}
    message: >-
      {{- header -}}
      {{- body -}}
      {{- footer -}}
  action:
    - alias: 'notify humidity sensor activated'
      service: persistent_notification.create
      data_template:
        title: >-
          {{- title -}}
        notification_id: >-
          {{- notification_id -}}
        message: >-
          {{- message -}}

# cleared notify

- id: motion_device_humidity_cleared_notify
  alias: motion_device_humidity_cleared_notify
  mode: single
  initial_state: off
  trigger:
    - platform: template
      value_template: >-
        {{ is_state_attr('binary_sensor.motion_device_humidity_exist','good',states('binary_sensor.motion_device_humidity')) }}
  variables:
    which: >-
      {% set which = none %}
      {% set s = state_attr('binary_sensor.motion_device_humidity','status')|selectattr('state','==','off')|sort(attribute='updated',reverse=true)|list %}
      {% if s|lower != 'none' and s|lower != 'null' and not s is string and s is iterable and s|count > 0 %}
        {% set which = s|map(attribute='id')|first  %}
      {%- endif %}
      {{ which }}
    notification_id: >-
      {{ 'device-humidity-status-' + which|string }}
    icon: >-
      {% set icon = 'notification-humidity-good' %}
      {{ icon }}
    title: >-
      {% set title = states('input_text.motion_app') + ': humidity good (' + state_attr('binary_sensor.motion_device_humidity_exist','good')|string + ')' %}
      {{ title }}
    header: >-
      {{- '[![](/local/images/icons/' + icon + '.png?v1)]()' -}}
      {{- '<hr>' -}}
    footer: >-
      {{ '<hr>' }}
    body: >-
      Your home humidity alarm has cleared; sensor: {{ which -}}.
    message: >-
      {{- header -}}
      {{- body -}}
      {{- footer -}}
  action:
    - condition: template
      value_template: >
        {{ which|lower != 'none' and is_state_attr('binary_sensor.motion_device_humidity_exist','alarm','on') }}
    - alias: 'notify humidity sensor activated'
      service: persistent_notification.create
      data_template:
        title: >-
          {{- title -}}
        notification_id: >-
          {{- notification_id -}}
        message: >-
          {{- message -}}

# cleared dismissed

- id: motion_device_humidity_cleared_dismiss
  alias: motion_device_humidity_cleared_dismiss
  mode: single
  initial_state: off
  trigger:
    - platform: template
      value_template: >-
        {{ is_state_attr('binary_sensor.motion_device_humidity_exist','good',states('binary_sensor.motion_device_humidity')) }}
      for:
        minutes: >-
          {{ states('input_number.motion_device_class_dismiss')|int(5) }}
  variables:
    ids: >-
      {%- set ids = state_attr('binary_sensor.motion_device_humidity','ids') -%}
      {%- if ids|lower != 'none' and not ids is string and ids is iterable and ids|count > 0 -%}
        [{%- for i in ids -%}
          {%- if not loop.first -%},{%- endif -%}
          "{{- 'device-humidity-status-' + i|string -}}"
        {%- endfor -%}]
      {%- else -%}none{%- endif %}
  condition:
    condition: and
    conditions:
      - condition: template
        value_template: >
          {{ ids|lower != 'none' and ids is iterable and ids|count > 0 }}
  action:
    - alias: 'dismiss humidity status'
      repeat:
        while:
          - alias: 'test if not done'
            condition: template
            value_template: >-
              {% set s = ids[repeat.index - 1]|default('unknown') %}
              {{ s|lower != 'unavailable' and s|lower != 'unknown' and s|lower != 'null' and s|string|length > 0 }}
        sequence:
          - alias: 'dismissing persistent notification'
            service: persistent_notification.dismiss
            data_template:
              notification_id: >-
                {{ ids[repeat.index - 1] }}

## low

- id: motion_device_humidity_alarm_low_on
  alias: motion_device_humidity_alarm_low_on
  mode: single
  initial_state: off
  trigger:
    - platform: state
      entity_id: 'binary_sensor.motion_device_humidity_low'
      to: 'on'
  variables:
    low: >-
      {{ state_attr('binary_sensor.motion_device_humidity','low') }}
  condition:
    condition: and
    conditions:
      - condition: template
        value_template: >
          {{ low|lower != 'none' and low|lower != 'null' and low|length > 0 }}
  action:
    - condition: template
      value_template: >
        {{ is_state_attr('binary_sensor.motion_device_humidity_exist','alarm','on') }}
    - alias: 'trigger motion notification'
      service: automation.trigger
      data_template:
        entity_id: automation.motion_device_humidity_low_notify

- id: motion_device_humidity_alarm_low_off
  alias: motion_device_humidity_alarm_low_off
  mode: single
  initial_state: off
  trigger:
    - platform: state
      entity_id: 'binary_sensor.motion_device_humidity_low'
      to: 'off'
  action:
    - condition: template
      value_template: >
        {{ is_state_attr('binary_sensor.motion_device_humidity_exist','alarm','on') }}
    - alias: 'trigger humidity low dismiss'
      service: automation.trigger
      data_template:
        entity_id: automation.motion_device_humidity_low_dismiss

- id: motion_device_humidity_low_notify
  alias: motion_device_humidity_low_notify
  mode: single
  initial_state: off
  trigger:
  variables:
    low: >-
      {% set low = state_attr('binary_sensor.motion_device_humidity','low') %}
      {{ low }}
    action: >-
      {% set action = 'humidity-okay' %}
      {{ action }}
    url: >-
      {% set url = '/lovelace/humidity-okay' %}
      {{ url }}
    title: >-
      {% set title = states('input_text.motion_app') + ': humidity low' %}
      {{ title }}
    notification_id: >-
      {{ 'device-humidity-low' }}
    icon: >-
      {% set icon = 'notification-humidity-low' %}
      {{ icon }}
    header: >-
      {{- '[![](/local/images/icons/' + icon + '.png?v1)]()' -}}
      {{- '<hr>' -}}
    footer: >-
      {{ '<hr>' }}
    body: >-
      Your home humidity alarm was triggered (under {{ state_attr('binary_sensor.motion_device_humidity_exist','low') -}}) at {{ utcnow().timestamp()|timestamp_custom("%a %b %d %I:%M %p %Z",true,'unknown') }}:
      {{- '<ul>' -}}
      {%- if low|lower != 'none' and not low is string and low is iterable and low|count > 0 -%}
        {%- for i in low -%}
          {{- '<li>' -}}
          {%- set d = state_attr('binary_sensor.motion_device_class','devices')|selectattr('id','==',i.id)|first -%}
          [{{- d.friendly_name -}}](/config/devices/device/{{- d.did -}}) is {{ states(low[loop.index-1].entity_id) -}}{{- state_attr(low[loop.index-1].entity_id,'unit_of_measurement') }} in [{{- area_id(low[loop.index-1].entity_id) -}}](/config/areas/area/{{- area_id(low[loop.index-1].entity_id) -}})
          {{- '</li>' -}}
        {%- endfor -%}
      {%- endif -%}
      {{- '</ul>' -}}
      <p>
      We have notified **{{- state_attr('sensor.motion_role_primary','friendly_name') -}}** about your humidity alarm.
      <br>
      If you are fine handling this situation on your own, click the **{{- action -}}** button below.
      {{- '<p>' -}}
      {{-  '[![](https://img.shields.io/badge/' + action + '-green.svg)](' + url + ')' -}}
    message: >-
      {{- header -}}
      {{- body -}}
      {{- footer -}}
  action:
    - alias: 'notify humidity sensor activated'
      service: persistent_notification.create
      data_template:
        title: >-
          {{- title -}}
        notification_id: >-
          {{- notification_id -}}
        message: >-
          {{- message -}}

- id: motion_device_humidity_low_dismiss
  alias: motion_device_humidity_low_dismiss
  mode: single
  initial_state: off
  trigger:
  variables:
    notification_id: >-
      {{ 'device-humidity-low' }}
  action:
    - alias: 'dismiss humidity low notification'
      service: persistent_notification.dismiss
      data_template:
        notification_id: >-
          {{ notification_id }}

## high

- id: motion_device_humidity_alarm_high_on
  alias: motion_device_humidity_alarm_high_on
  mode: single
  initial_state: off
  trigger:
    - platform: state
      entity_id: 'binary_sensor.motion_device_humidity_high'
      to: 'on'
  variables:
    high: >-
      {{ state_attr('binary_sensor.motion_device_humidity','high') }}
  condition:
    condition: and
    conditions:
      - condition: template
        value_template: >
          {{ high|lower != 'none' and high|lower != 'null' and high|length > 0 }}
  action:
    - condition: template
      value_template: >
        {{ is_state_attr('binary_sensor.motion_device_humidity_exist','alarm','on') }}
    - alias: 'trigger motion notification'
      service: automation.trigger
      data_template:
        entity_id: automation.motion_device_humidity_high_notify

- id: motion_device_humidity_alarm_high_off
  alias: motion_device_humidity_alarm_high_off
  mode: single
  initial_state: off
  trigger:
    - platform: state
      entity_id: 'binary_sensor.motion_device_humidity_high'
      to: 'off'
  action:
    - condition: template
      value_template: >
        {{ is_state_attr('binary_sensor.motion_device_humidity_exist','alarm','on') }}
    - alias: 'trigger humidity high dismiss'
      service: automation.trigger
      data_template:
        entity_id: automation.motion_device_humidity_high_dismiss

- id: motion_device_humidity_high_notify
  alias: motion_device_humidity_high_notify
  mode: single
  initial_state: off
  trigger:
  variables:
    high: >-
      {% set high = state_attr('binary_sensor.motion_device_humidity','high') %}
      {{ high }}
    action: >-
      {% set action = 'humidity-okay' %}
      {{ action }}
    url: >-
      {% set url = '/lovelace/humidity-okay' %}
      {{ url }}
    title: >-
      {% set title = states('input_text.motion_app') + ': humidity high' %}
      {{ title }}
    notification_id: >-
      {{ 'device-humidity-high' }}
    icon: >-
      {% set icon = 'notification-humidity-high' %}
      {{ icon }}
    header: >-
      {{- '[![](/local/images/icons/' + icon + '.png?v1)]()' -}}
      {{- '<hr>' -}}
    footer: >-
      {{ '<hr>' }}
    body: >-
      Your home humidity alarm was triggered (above {{ state_attr('binary_sensor.motion_device_humidity_exist','high') -}}) at {{ utcnow().timestamp()|timestamp_custom("%a %b %d %I:%M %p %Z",true,'unknown') }}:
      {{- '<ul>' -}}
      {%- if high|lower != 'none' and not high is string and high is iterable and high|count > 0 -%}
        {%- for i in high -%}
          {{- '<li>' -}}
          {%- set d = state_attr('binary_sensor.motion_device_class','devices')|selectattr('id','==',i.id)|first -%}
          [{{- d.friendly_name -}}](/config/devices/device/{{- d.did -}}) is {{ states(high[loop.index-1].entity_id) -}}{{- state_attr(high[loop.index-1].entity_id,'unit_of_measurement') }} in [{{- area_id(high[loop.index-1].entity_id) -}}](/config/areas/area/{{- area_id(high[loop.index-1].entity_id) -}})
          {{- '</li>' -}}
        {%- endfor -%}
      {%- endif -%}
      {{- '</ul>' -}}
      <p>
      We have notified **{{- state_attr('sensor.motion_role_primary','friendly_name') -}}** about your humidity alarm.
      <br>
      If you are fine handling this situation on your own, click the **{{- action -}}** button below.
      {{- '<p>' -}}
      {{-  '[![](https://img.shields.io/badge/' + action + '-green.svg)](' + url + ')' -}}
    message: >-
      {{- header -}}
      {{- body -}}
      {{- footer -}}
  action:
    - alias: 'notify humidity sensor activated'
      service: persistent_notification.create
      data_template:
        title: >-
          {{- title -}}
        notification_id: >-
          {{- notification_id -}}
        message: >-
          {{- message -}}

- id: motion_device_humidity_high_dismiss
  alias: motion_device_humidity_high_dismiss
  mode: single
  initial_state: off
  trigger:
  variables:
    notification_id: >-
      {{ 'device-humidity-high' }}
  action:
    - alias: 'dismiss humidity high notification'
      service: persistent_notification.dismiss
      data_template:
        notification_id: >-
          {{ notification_id }}

## activity

- id: motion_device_humidity_alarm_activity
  alias: motion_device_humidity_alarm_activity
  mode: single
  initial_state: off
  trigger:
    - platform: state
      entity_id: 'binary_sensor.motion_device_humidity_activity'
      to: 'on'
  variables:
    activity: >-
      {{ state_attr('binary_sensor.motion_device_humidity','activity') }}
  condition:
    condition: and
    conditions:
      - condition: template
        value_template: >
          {{ activity|lower != 'none' and activity|lower != 'null' and activity|length > 0 }}
  action:
    - condition: template
      value_template: >
        {{ is_state_attr('binary_sensor.motion_device_humidity_exist','alarm','on') }}
    - alias: 'trigger motion notification'
      service: automation.trigger
      data_template:
        entity_id: automation.motion_device_humidity_activity_notify

- id: motion_device_humidity_activity_notify
  alias: motion_device_humidity_activity_notify
  mode: single
  initial_state: off
  trigger:
  variables:
    activity: >-
      {% set activity = state_attr('binary_sensor.motion_device_humidity','activity') %}
      {{ activity }}
    action: >-
      {% set action = 'humidity-okay' %}
      {{ action }}
    url: >-
      {% set url = '/lovelace/humidity-okay' %}
      {{ url }}
    title: >-
      {% set title = states('input_text.motion_app') + ': humidity activity alarm' %}
      {{ title }}
    notification_id: >-
      {{ 'device-humidity-activity' }}
    icon: >-
      {% set icon = 'notification-humidity-activity' %}
      {{ icon }}
    header: >-
      {{- '[![](/local/images/icons/' + icon + '.png?v1)]()' -}}
      {{- '<hr>' -}}
    footer: >-
      {{ '<hr>' }}
    body: >-
      Your home humidity alarm was triggered at {{ utcnow().timestamp()|timestamp_custom("%a %b %d %I:%M %p %Z",true,'unknown') }}:
      {{- '<ul>' -}}
      {%- if activity|lower != 'none' and not activity is string and activity is iterable and activity|count > 0 -%}
        {%- for i in activity -%}
          {{- '<li>' -}}
          {%- set d = state_attr('binary_sensor.motion_device_class','devices')|selectattr('id','==',i.id)|first -%}
          [{{- d.friendly_name -}}](/config/devices/device/{{- d.did -}}) in [{{- area_id(activity[loop.index-1].entity_id) -}}](/config/areas/area/{{- area_id(activity[loop.index-1].entity_id) -}})
          {{- '</li>' -}}
        {%- endfor -%}
      {%- endif -%}
      {{- '</ul>' -}}
      <p>
      We have notified **{{- state_attr('sensor.motion_role_primary','friendly_name') -}}** about your humidity alarm.
      <br>
      If you are fine handling this situation on your own, click the **{{- action -}}** button below.
      {{- '<p>' -}}
      {{-  '[![](https://img.shields.io/badge/' + action + '-green.svg)](' + url + ')' -}}
    message: >-
      {{- header -}}
      {{- body -}}
      {{- footer -}}
  action:
    - alias: 'notify humidity sensor activated'
      service: persistent_notification.create
      data_template:
        title: >-
          {{- title -}}
        notification_id: >-
          {{- notification_id -}}
        message: >-
          {{- message -}}

# good,bad,high,low

- id: motion_device_humidity_bad
  alias: motion_device_humidity_bad
  mode: single
  initial_state: off
  trigger:
    - platform: state
      entity_id: 'binary_sensor.motion_device_humidity'
      attribute: 'bad'
  variables:
    details: >-
      {% if state_attr('binary_sensor.motion_device_humidity_exist','bad')|lower != 'none' %}
        {% set s = state_attr('binary_sensor.motion_device_humidity','bad') %}
        {%- if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s is iterable -%}
          [{% for i in s -%}
            {%- if not loop.first -%},{%- endif -%}
            "{{- state_attr(i.entity_id,'friendly_name') -}} is {{ states(i.entity_id) -}}"
          {%- endfor -%}]
        {%- else -%}none{%- endif %}
      {%- else -%}none{%- endif %}
    state: >-
      {% set s = state_attr('binary_sensor.motion_device_humidity','bad') %}
      {% if s|lower != 'null' and s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and not s is string and s is iterable %}
        {% if s|list|count > 0 %}on{% else %}off{% endif %}
      {% else %}unknown{% endif %}
    last: >-
      {% if state|lower == 'off' %}
        {{ state_attr('binary_sensor.motion_device_humidity_bad','last') }}
      {%- else -%}{{ utcnow().timestamp() }}{%- endif %}
  condition:
    - condition: template
      value_template: >
        {{ state|lower != 'unknown' }}
  action:
    - service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_humidity_bad
        last: >-
          {{ last }}
        details: >-
          {{ details }}
        state: >-
          {{ state }}

- id: motion_device_humidity_good
  alias: motion_device_humidity_good
  mode: single
  initial_state: off
  trigger:
    - platform: state
      entity_id: 'binary_sensor.motion_device_humidity'
      attribute: 'good'
  variables:
    details: >-
      {% if state_attr('binary_sensor.motion_device_humidity_exist','good')|lower != 'none' %}
        {% set s = state_attr('binary_sensor.motion_device_humidity','good') %}
        {%- if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s is iterable -%}
          [{% for i in s -%}
            {%- if not loop.first -%},{%- endif -%}
            "{{- state_attr(i.entity_id,'friendly_name') -}} is {{ states(i.entity_id) -}}"
          {%- endfor -%}]
        {%- else -%}none{%- endif %}
      {%- else -%}none{%- endif %}
    state: >-
      {% set s = state_attr('binary_sensor.motion_device_humidity','good') %}
      {% if s|lower != 'null' and s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and not s is string and s is iterable %}
        {% if s|list|count > 0 %}on{% else %}off{% endif %}
      {% else %}unknown{% endif %}
    last: >-
      {% if state|lower == 'off' %}
        {{ state_attr('binary_sensor.motion_device_humidity_good','last') }}
      {%- else -%}{{ utcnow().timestamp() }}{%- endif %}
  condition:
    - condition: template
      value_template: >
        {{ state|lower != 'unknown' }}
  action:
    - service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_humidity_good
        last: >-
          {{ last }}
        details: >-
          {{ details }}
        state: >-
          {{ state }}

- id: motion_device_humidity_low
  alias: motion_device_humidity_low
  mode: single
  initial_state: off
  trigger:
    - platform: state
      entity_id: 'binary_sensor.motion_device_humidity'
      attribute: 'low'
  variables:
    details: >-
      {% if state_attr('binary_sensor.motion_device_humidity_exist','low')|lower != 'none' %}
        {% set s = state_attr('binary_sensor.motion_device_humidity','low') %}
        {%- if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s is iterable -%}
          [{% for i in s -%}
            {%- if not loop.first -%},{%- endif -%}
            "{{- state_attr(i.entity_id,'friendly_name') }} at {{ states(i.entity_id)|int('unknown') -}}{{ state_attr(i.entity_id,'unit_of_measurement') -}}"
          {%- endfor -%}]
        {%- else -%}none{%- endif %}
      {%- else -%}none{%- endif %}
    state: >-
      {% set s = state_attr('binary_sensor.motion_device_humidity','low') %}
      {% if s|lower != 'null' and s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and not s is string and s is iterable %}
        {% if s|list|count > 0 %}on{% else %}off{% endif %}
      {% else %}unknown{% endif %}
    last: >-
      {% if state|lower == 'off' %}
        {{ state_attr('binary_sensor.motion_device_humidity_low','last') }}
      {%- else -%}{{ utcnow().timestamp() }}{%- endif %}
  condition:
    - condition: template
      value_template: >
        {{ state|lower != 'unknown' }}
  action:
    - service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_humidity_low
        last: >-
          {{ last }}
        details: >-
          {{ details }}
        state: >-
          {{ state }}

- id: motion_device_humidity_high
  alias: motion_device_humidity_high
  mode: single
  initial_state: off
  trigger:
    - platform: state
      entity_id: 'binary_sensor.motion_device_humidity'
      attribute: 'high'
  variables:
    details: >-
      {% if state_attr('binary_sensor.motion_device_humidity_exist','high')|lower != 'none' %}
        {% set s = state_attr('binary_sensor.motion_device_humidity','high') %}
        {%- if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s is iterable -%}
          [{% for i in s -%}
            {%- if not loop.first -%},{%- endif -%}
            "{{- state_attr(i.entity_id,'friendly_name') }} at {{ states(i.entity_id)|int('unknown') -}}{{ state_attr(i.entity_id,'unit_of_measurement') -}}"
          {%- endfor -%}]
        {%- else -%}none{%- endif %}
      {%- else -%}none{%- endif %}
    state: >-
      {% set s = state_attr('binary_sensor.motion_device_humidity','high') %}
      {% if s|lower != 'null' and s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and not s is string and s is iterable %}
        {% if s|list|count > 0 %}on{% else %}off{% endif %}
      {% else %}unknown{% endif %}
    last: >-
      {% if state|lower == 'off' %}
        {{ state_attr('binary_sensor.motion_device_humidity_high','last') }}
      {%- else -%}{{ utcnow().timestamp() }}{%- endif %}
  condition:
    - condition: template
      value_template: >
        {{ state|lower != 'unknown' }}
  action:
    - service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_humidity_high
        last: >-
          {{ last }}
        details: >-
          {{ details }}
        state: >-
          {{ state }}

# activity,unavailable

- id: motion_device_humidity_activity
  alias: motion_device_humidity_activity
  mode: single
  initial_state: off
  trigger:
    - platform: state
      entity_id: 'binary_sensor.motion_device_humidity'
      attribute: 'activity'
  variables:
    ids: >-
      {% set s = state_attr('binary_sensor.motion_device_humidity','activity') %}
      {%- if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s is iterable -%}
        {{ s|map(attribute='id')|sort|unique|list }}
      {%- else -%}none{%- endif %}
    details: >-
      {% set s = state_attr('binary_sensor.motion_device_humidity','activity') %}
      {%- if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s is iterable -%}
        {{ s }}
      {%- else -%}none{%- endif %}
    state: >-
      {% set s = state_attr('binary_sensor.motion_device_humidity','activity') %}
      {% if s|lower != 'null' and s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and not s is string and s is iterable %}
        {% if s|count > 0 %}on{% else %}off{% endif %}
      {% else %}unknown{% endif %}
    last: >-
      {% if state|lower == 'off' %}
        {{ state_attr('binary_sensor.motion_device_humidity_activity','last') }}
      {%- else -%}{{ utcnow().timestamp() }}{%- endif %}
  condition:
    - condition: template
      value_template: >
        {{ state|lower != 'unknown' }}
  action:
    - service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_humidity_activity
        last: >-
          {{ last }}
        ids: >-
          {{ ids }}
        details: >-
          {{ details }}
        state: >-
          {{ state }}

- id: motion_device_humidity_unavailable
  alias: motion_device_humidity_unavailable
  mode: single
  initial_state: off
  trigger:
    - platform: state
      entity_id: 'binary_sensor.motion_device_humidity'
      attribute: 'unavailable'
  variables:
    ids: >-
      {% set s = state_attr('binary_sensor.motion_device_humidity','unavailable') %}
      {%- if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s is iterable -%}
        {{ s|map(attribute='id')|sort|unique|list }}
      {%- else -%}none{%- endif %}
    details: >-
      {% set s = state_attr('binary_sensor.motion_device_humidity','unavailable') -%}
        {%- if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s is iterable -%}
          { {%- for i in s|map(attribute='id')|sort|unique -%}
            {%- if not loop.first -%},{%- endif -%}
            "{{ i }}":
            [{%- for j in s|selectattr('id','==',i)|map(attribute='entity_id') -%}
              {%- if not loop.first -%},{%- endif -%}
              "{{- state_attr(j,'friendly_name') -}}"
            {%- endfor -%}]
          {% endfor %} }
        {%- else -%}none{%- endif %}
    state: >-
      {% set s = state_attr('binary_sensor.motion_device_humidity','unavailable') %}
      {% if s|lower != 'null' and s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and not s is string and s is iterable %}
        {% if s|count > 0 %}on{% else %}off{% endif %}
      {% else %}unknown{% endif %}
    last: >-
      {% if state|lower == 'off' %}
        {{ state_attr('binary_sensor.motion_device_humidity_unavailable','last') }}
      {%- else -%}{{ utcnow().timestamp() }}{%- endif %}
  condition:
    - condition: template
      value_template: >
        {{ state|lower != 'unknown' }}
  action:
    - service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_humidity_unavailable
        last: >-
          {{ last }}
        ids: >-
          {{ ids }}
        details: >-
          {{ details }}
        state: >-
          {{ state }}

- id: motion_device_humidity_missing
  alias: motion_device_humidity_missing
  mode: single
  initial_state: on
  trigger:
    - platform: template
      value_template: >-
        {{ is_state('binary_sensor.motion_device_humidity_exist','off')
           and is_state_attr('automation.motion_device_humidity_rescan','current',0)
           and is_state_attr('binary_sensor.motion_device_humidity_exist','required','on') }}
#      for:
#        seconds: >-
#          {{ states('input_number.motion_device_class_dismiss')|int(5) }}
  variables:
    state: >-
      {% set state = 'off' %}
      {% if is_state('binary_sensor.motion_device_humidity_exist','off')
       and is_state_attr('automation.motion_device_humidity_rescan','current',0)
       and is_state_attr('binary_sensor.motion_device_humidity_exist','required','on') %}
        {% set state = 'on' %}
      {% endif %}
      {{ state }}
    last: >-
      {% set last = state_attr('binary_sensor.motion_device_humidity_missing','last') %}
      {% if state|lower == 'off' %}
        {% set last = utcnow().timestamp() %}
      {% endif %}
      {{ last }}
  condition:
    - condition: template
      value_template: >
        {{ state|lower != 'unknown' }}
  action:
    - service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_humidity_missing
        last: >-
          {{ last }}
        state: >-
          {{ state }}

- id: motion_device_humidity_missing_notify
  alias: motion_device_humidity_missing_notify
  mode: single
  initial_state: on
  trigger:
    - platform: state
      entity_id: binary_sensor.motion_device_humidity_missing
      to: 'on'
  variables:
    ip: >-
      {{ none }}
    latitude: >-
      {{ none }}
    longitude: >-
      {{ none }}
    notification_id: >-
      {{ 'device-humidity-missing' }}
    icon: >-
      {% set icon = 'notification-humidity-missing' %}
      {{ icon }}
    header: >-
      {{- '[![](/local/images/icons/' + icon + '.png?v1)]()' -}}
      {{- '<hr>' -}}
    footer: >-
      {{- '<hr>' -}}
    title: >-
      {% set title = states('input_text.motion_app') + ': humidity sensor missing' %}
      {{ title }}
    body: >-
      The humidity sensor could not be found at {{ utcnow().timestamp()|timestamp_custom("%a %b %d %I:%M %p %Z",true,'unknown') -}}; please setup the integration by clicking [here](/config/integrations).
    message: >-
      {{ header }}
      {{ body }}
      {{ footer }}
  action:
    - alias: 'notify missing humidity sensor'
      service: persistent_notification.create
      data_template:
        title: >-
          {{- title -}}
        notification_id: >-
          {{- notification_id -}}
        message: >-
          {{- message -}}

