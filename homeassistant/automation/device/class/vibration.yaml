###
# homeassistant/automation/motion/device/vibration.yaml
###

- id: motion_device_vibration_automation_enable
  alias: motion_device_vibration_automation_enable
  initial_state: 'on'
  trigger:
    platform: state
    entity_id: group.motion_device_vibration
    attribute: 'entity_id'
  condition:
    - condition: template
      value_template: >-
        {% set s = state_attr('group.motion_device_vibration','entity_id') %}
        {{ s|lower != 'none' and not s is string and s is iterable and s|count > 0 }}
  variables:
    automations: >-
      {% set automations = ['motion_device_vibration_activity_dismiss','motion_device_vibration_activity','motion_device_vibration_activity_notify','motion_device_vibration_activity_off','motion_device_vibration_activity_on','motion_device_vibration_alarm_activity','motion_device_vibration_alarm_bad_on','motion_device_vibration_alarm_good_on','motion_device_vibration_alarm_high_off','motion_device_vibration_alarm_high_on','motion_device_vibration_alarm_high_spike_off','motion_device_vibration_alarm_high_spike_on','motion_device_vibration_alarm_low_off','motion_device_vibration_alarm_low_on','motion_device_vibration_alarm_low_spike_off','motion_device_vibration_alarm_low_spike_on','motion_device_vibration_bad','motion_device_vibration_bad_notify','motion_device_vibration_bad_off','motion_device_vibration_bad_on','motion_device_vibration_cleared_dismiss','motion_device_vibration_cleared_notify','motion_device_vibration_good','motion_device_vibration_good_notify','motion_device_vibration_good_off','motion_device_vibration_good_on','motion_device_vibration_high','motion_device_vibration_high_dismiss','motion_device_vibration_high_notify','motion_device_vibration_high_off','motion_device_vibration_high_on','motion_device_vibration_high_spike_on','motion_device_vibration_high_spike_off','motion_device_vibration_high_spike','motion_device_vibration_high_spike_dismiss','motion_device_vibration_high_spike_notify','motion_device_vibration_low','motion_device_vibration_low_dismiss','motion_device_vibration_low_notify','motion_device_vibration_low_off','motion_device_vibration_low_on','motion_device_vibration_low_spike','motion_device_vibration_low_spike_on','motion_device_vibration_low_spike_off','motion_device_vibration_low_spike_dismiss','motion_device_vibration_low_spike_notify','motion_device_vibration_mute','motion_device_vibration_restart','motion_device_vibration_self_test','motion_device_vibration_status','motion_device_vibration_toggle','motion_device_vibration_turn_off','motion_device_vibration_turn_on','motion_device_vibration_unavailable','motion_device_vibration_unmute','motion_device_vibration_update'] %}
      {{ automations }}
  action:
    - alias: 'enable automations for device_vibration'
      repeat:
        while:
          - condition: template
            value_template: >-
              {% set s = automations[repeat.index-1]|default('') %}
              {{ s|string != '' }}
        sequence:
          - variables:
              automation: >-
                {% set s = automations[repeat.index-1] %}
                {{ 'automation.' + s|string }}
          - condition: template
            value_template: >-
              {{ states(automation)|lower == 'off' }}
          - service: automation.turn_on
            data_template:
              entity_id: >-
                {{ automation }}
          - condition: template
            value_template: >
              {{ states('sensor.motion_log_level') in state_attr('sensor.motion_log_level','debug') }}
          - service: persistent_notification.create
            data_template:
              title: >-
                DEBUG: vibration automation
              notification_id: >
                {{- 'debug-automation-vibration' -}}
              message: >-
                Enable automation: {{ automation }}
    - alias: 'indicate complete'
      service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_vibration
        automations: >-
          {{ 'on' }}
    - choose:
        - conditions:
            - condition: template
              value_template: >
                {{ states('sensor.motion_log_level') in state_attr('sensor.motion_log_level','debug') }}
          sequence:
            - service: persistent_notification.create
              data_template:
                title: >-
                  DEBUG: vibration enabled
                notification_id: >
                  {{- 'debug-automation-vibration' -}}
                message: >-
                  All vibration automations enabled: {{ automations|count }}

- id: motion_device_vibration_automation_disable
  alias: motion_device_vibration_automation_disable
  initial_state: 'on'
  trigger:
    platform: state
    entity_id: group.motion_device_vibration
    attribute: 'entity_id'
  condition:
    - condition: template
      value_template: >-
        {% set s = state_attr('group.motion_device_vibration','entity_id') %}
        {{ s|lower == 'none' or s is string or not s is iterable or not s|count > 0 }}
  variables:
    automations: >-
      {% set automations = ['motion_device_vibration_activity_dismiss','motion_device_vibration_activity','motion_device_vibration_activity_notify','motion_device_vibration_activity_off','motion_device_vibration_activity_on','motion_device_vibration_alarm_activity','motion_device_vibration_alarm_bad_on','motion_device_vibration_alarm_good_on','motion_device_vibration_alarm_high_off','motion_device_vibration_alarm_high_on','motion_device_vibration_alarm_high_spike_off','motion_device_vibration_alarm_high_spike_on','motion_device_vibration_alarm_low_off','motion_device_vibration_alarm_low_on','motion_device_vibration_alarm_low_spike_off','motion_device_vibration_alarm_low_spike_on','motion_device_vibration_bad','motion_device_vibration_bad_notify','motion_device_vibration_bad_off','motion_device_vibration_bad_on','motion_device_vibration_cleared_dismiss','motion_device_vibration_cleared_notify','motion_device_vibration_good','motion_device_vibration_good_notify','motion_device_vibration_good_off','motion_device_vibration_good_on','motion_device_vibration_high','motion_device_vibration_high_dismiss','motion_device_vibration_high_notify','motion_device_vibration_high_off','motion_device_vibration_high_on','motion_device_vibration_high_spike_on','motion_device_vibration_high_spike_off','motion_device_vibration_high_spike','motion_device_vibration_high_spike_dismiss','motion_device_vibration_high_spike_notify','motion_device_vibration_low','motion_device_vibration_low_dismiss','motion_device_vibration_low_notify','motion_device_vibration_low_off','motion_device_vibration_low_on','motion_device_vibration_low_spike','motion_device_vibration_low_spike_on','motion_device_vibration_low_spike_off','motion_device_vibration_low_spike_dismiss','motion_device_vibration_low_spike_notify','motion_device_vibration_mute','motion_device_vibration_restart','motion_device_vibration_self_test','motion_device_vibration_status','motion_device_vibration_toggle','motion_device_vibration_turn_off','motion_device_vibration_turn_on','motion_device_vibration_unavailable','motion_device_vibration_unmute','motion_device_vibration_update'] %}
      {{ automations }}
  action:
    - alias: 'disable automations for device_vibration'
      repeat:
        while:
          - condition: template
            value_template: >-
              {% set s = automations[repeat.index-1]|default('') %}
              {{ s|string != '' }}
        sequence:
          - variables:
              automation: >-
                {% set s = automations[repeat.index-1] %}
                {{ 'automation.' + s|string }}
          - alias: 'test if automation is on'
            condition: template
            value_template: >-
              {{ states(automation)|lower == 'on' }}
          - service: automation.turn_off
            data_template:
              entity_id: >-
                {{ automation }}
          - condition: template
            value_template: >
              {{ states('sensor.motion_log_level') in state_attr('sensor.motion_log_level','debug') }}
          - service: persistent_notification.create
            data_template:
              title: >-
                {% set s = automations[repeat.index-1] %}
                DEBUG: vibration automation
              notification_id: >
                {{- 'debug-automation-vibration' -}}
              message: >-
                Disable automation: {{ automation }}
    - alias: 'indicate complete'
      service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_vibration
        automations: >-
          {{ 'off' }}
    - service: persistent_notification.create
      data_template:
        title: >-
          DEBUG: vibration disabled
        notification_id: >
          {{- 'debug-automation-vibration' -}}
        message: >-
          All vibration automations disabled: {{ automations|count }}

- id: motion_device_vibration_rescan
  alias: motion_device_vibration_rescan
  mode: 'single'
  max_exceeded: 'warn'
  initial_state: 'on'
  trigger:
    - platform: state
      entity_id: 'binary_sensor.motion_device_vibration_exist'
    - platform: time_pattern
      minutes: '/15'
  variables:
    class: >-
      {%- set class = 'vibration' -%}
      {{- class -}}
    all: >-
      [{%- for state in (states.sensor|list + states.binary_sensor|list)
       if 'device_class' in state.attributes and device_id(state.entity_id)|lower != 'none'
        and state.attributes.device_class==class and not '.motion_' in state.entity_id -%}
        {%- if not loop.first -%},{% endif -%}
        {%- set did = device_id(state.entity_id) -%}
        {%- set n = device_attr(did,'name_by_user') -%}
        {%- if n|lower == 'none' -%}{%- set n = device_attr(did,'name') -%}{%- endif -%}
        {% set n = n|replace('&','_')|replace(' ','_')|replace('\'','')|replace('-','_')|lower -%}
        {"id":"{{- n -}}","did":"{{- did  -}}"}
      {%- endfor -%}]
    local: >-
      {% set device_trackers = states.device_tracker|map(attribute='entity_id')|map('device_id')|list %}
      [{%- for state in (states.sensor|list + states.binary_sensor|list)
       if 'device_class' in state.attributes and device_id(state.entity_id)|lower != 'none'
        and state.attributes.device_class==class and not '.motion_' in state.entity_id
        and device_id(state.entity_id) not in device_trackers -%}
        {%- if not loop.first -%},{% endif -%}
        {%- set n = device_attr(device_id(state.entity_id),'name_by_user') -%}
        {%- if n|lower == 'none' -%}{%- set n = device_attr(device_id(state.entity_id),'name') -%}{%- endif -%}
        {% set n = n|replace('&','_')|replace(' ','_')|replace('\'','')|replace('-','_')|lower -%}
        "{{- n -}}"
      {%- endfor -%}]
    mobile: >-
      {% set device_trackers = states.device_tracker|map(attribute='entity_id')|map('device_id')|list %}
      [{%- for state in (states.sensor|list + states.binary_sensor|list)
       if 'device_class' in state.attributes and device_id(state.entity_id)|lower != 'none'
        and state.attributes.device_class==class and not '.motion_' in state.entity_id
        and device_id(state.entity_id) in device_trackers -%}
        {%- if not loop.first -%},{% endif -%}
        {%- set n = device_attr(device_id(state.entity_id),'name_by_user') -%}
        {%- if n|lower == 'none' -%}{%- set n = device_attr(device_id(state.entity_id),'name') -%}{%- endif -%}
        {% set n = n|replace('&','_')|replace(' ','_')|replace('\'','')|replace('-','_')|lower -%}
        "{{- n -}}"
      {%- endfor -%}]
    ids: >-
      {% set ids = none %}
      {%- if all != [] and all is iterable and all|count > 0 -%}
        {%- set ids = all|map(attribute='id')|list -%}
      {%- endif -%}
      {{ ids }}
    dids: >-
      {% set dids = none %}
      {%- if all != [] and all is iterable and all|count > 0 -%}
        {%- set dids = all|map(attribute='did')|list -%}
      {%- endif -%}
      {{ dids }}
    mobiles: >-
      {%- if mobile != [] and mobile is iterable and mobile|count > 0 -%}
        {{- mobile -}}
      {% else %}none{% endif %}
    total: >-
      {% set s = dids %}
      {% if not s is string and s is iterable %}
        {{ s|count }}
      {%- else -%}null{%- endif %}
    update: >-
      {%- set s = dids %}
      {%- if not s is string and s is iterable and s|count > 0 -%}
        [{%- for i in s -%}
          {%- set did = i -%}
          {%- set id = all|selectattr('did','==',did)|map(attribute='id')|first -%}
          {%- set o = loop -%}
          {%- for state in (states.button|list + states.switch|list) if device_id(state.entity_id) == did and state.attributes.device_class|default('null')|string == 'update' -%}
            {%- set v = state.state -%}
            {"did":"{{- did -}}","id":"{{- id -}}","entity_id":"{{- state.entity_id -}}"}
            {%- if not loop.last or not o.last -%},{%- endif -%}
          {%- endfor -%}
        {%- endfor -%}]
      {%- else -%}none{%- endif %}
    restart: >-
      {%- set s = ids %}
      {%- if s|lower != 'unknown' and s|lower != 'none' and not s is string -%}
        [{%- for i in s -%}
          {%- set o = loop -%}
          {%- for state in (states.button|list + states.switch|list) if device_attr(state.entity_id, 'name') == i and state.attributes.device_class|default('null')|string == 'restart' -%}
            {%- set v = state.state -%}
            {"id":"{{- i -}}","entity_id":"{{- state.entity_id -}}"}
            {%- if not loop.last or not o.last -%},{%- endif -%}
          {%- endfor -%}
        {%- endfor -%}]
      {%- else -%}none{%- endif %}
    controls: >-
      {%- set s = ids -%}
      {%- if s|lower != 'unknown' and s|lower != 'none' and not s is string -%}
        [{%- for i in s -%}
          {%- set o = loop -%}
          {%- for state in (states.button|list + states.switch|list) if device_attr(state.entity_id, 'name') == i and state.attributes.device_class|default('null')|string == 'null' -%}
            {%- set ctl = state_attr(state.entity_id,'friendly_name') -%}
            {"id":"{{- i -}}","entity_id":"{{- state.entity_id -}}","name":"{{- ctl -}}"}
            {%- if not loop.last or not o.last -%},{%- endif -%}
          {%- endfor -%}
        {%- endfor -%}]
      {%- else -%}none{%- endif %}
    test: >-
      {%- set s = ids -%}
      {%- if s|lower != 'unknown' and s|lower != 'none' and not s is string -%}
        [{%- for i in s -%}
          {%- set o = loop -%}
          {%- for state in states.sensor if device_attr(state.entity_id, 'name') == i and state.attributes.self_test|default('none')|string != 'none' -%}
            {%- if not loop.first or not o.first -%},{%- endif -%}
            {"id":"{{- i -}}","entity_id":"{{- state.entity_id -}}","state":"{{- state.state -}}","updated":{{- state.last_updated|as_timestamp(None) -}}}
          {%- endfor -%}
        {%- endfor -%}]
      {%- else -%}null{%- endif %}
    areas: >-
      {%- set s = dids %}
      {%- if not s is string and s is iterable and s|count > 0 -%}
        [{%- for i in s -%}
          {%- set did = i -%}
          {%- set id = all|selectattr('did','==',did)|map(attribute='id')|first -%}
          {%- set o = loop -%}
          {%- for state in (states.sensor|list + states.binary_sensor|list) if device_id(state.entity_id) == did and (state.attributes.device_class|default('null')|string == class)  -%}
            {"did":"{{- did -}}","id":"{{- id -}}","area":"{{- area_id(state.entity_id) -}}","state":"{{- state.state -}}"}
            {%- if not loop.last or not o.last -%},{%- endif -%}
          {%- endfor -%}
        {%- endfor -%}]
      {%- else -%}none{%- endif %}
    devices: >-
      {%- set s = dids %}
      {%- if not s is string and s is iterable and s|count > 0 -%}
        [{% for i in s -%}
          {%- set did = i -%}
          {%- set id = all|selectattr('did','==',did)|map(attribute='id')|first -%}
          {%- set k = areas|selectattr('did','==',did)|map(attribute='area')|first -%}
          {%- set n = device_attr(did,'name_by_user') -%}
          {%- if n|lower == 'none' -%}{%- set n = device_attr(did,'name') -%}{%- endif -%}
          {%- if not loop.first -%},{%- endif -%}
          {"id":"{{- id -}}", "did":"{{- did -}}", "friendly_name":"{{- n -}}", "manufacturer":"{{- device_attr(did,'manufacturer') -}}", "model":"{{- device_attr(did,'model') -}}", "sw_version":"{{- device_attr(did,'sw_version') -}}", "area":"{{- k -}}"}
        {%- endfor -%}]
      {%- else -%}none{%- endif %}
  action:
    - alias: 'update binary_sensor.motion_device_vibration ids, etc..'
      service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_vibration
        ids: >-
          {{ ids }}
        dids: >-
          {{ dids }}
        mobiles: >-
          {{ mobiles }}
        total: >-
          {{ total }}
        update: >-
          {{ update }}
        restart: >-
          {{ restart }}
        controls: >-
          {%- if controls|lower != 'none' and not controls is string and controls is iterable and controls|count > 0  -%}
            {{ controls|reject('none')|list }}
          {%- else -%}
            {{ controls }}
          {%- endif -%}
        test: >-
          {{ test }}
        areas: >-
          {{ areas }}
        devices: >-
          {{ devices }}
        state: >-
          {{ states('binary_sensor.motion_device_' + class) }}
    - variables:
        entities: >-
          {%- set s = dids %}
          {%- if not s is string and s is iterable and s|count > 0 -%}
            [{%- for i in s -%}
              {%- set did = i -%}
              {%- set o = loop -%}
              {%- for state in (states.sensor|list + states.binary_sensor|list) if device_id(state.entity_id) == did and (state.attributes.device_class|default('null')|string == class)  -%}
                "{{- state.entity_id -}}"
                {%- if not loop.last or not o.last -%},{%- endif -%}
              {%- endfor -%}
            {%- endfor -%}]
          {%- else -%}none{%- endif %}
    - alias: 'test if entities exist for vibration' 
      condition: template
      value_template: >
        {{ not entities is string and entities is iterable and entities|count > 0 }}
    - alias: 'set group.motion_device_vibration sensors'
      service: group.set
      data_template:
        object_id: motion_device_vibration
        entities: >-
          {{ entities }}
    - variables:
        statistics: >-
          {%- set s = dids -%}
          {%- if not s is string and s is iterable and s|count > 0 -%}
            [{%- for i in s -%}
              {%- set o = loop -%}
              {%- set did = i -%}
              {%- for state in (states.sensor|list + states.binary_sensor|list) if device_id(state.entity_id) == did and state.attributes.device_class|default('null')|lower == 'null' and state.attributes.state_class|default('null')|string == 'measurement' -%}
                {%- if not o.first or not loop.first -%},{%- endif -%}
                {"entity_id":"{{- state.entity_id -}}"}
              {%- else -%}{% if o.first %}None{% endif %}{%- endfor -%}
            {%- endfor -%}]
          {%- else -%}[]{%- endif %}
    - alias: 'set group.motion_device_vibration statistics'
      service: group.set
      data_template:
        object_id: motion_device_vibration_statistics
        entities: >-
          {{ statistics|reject('none')|map(attribute='entity_id')|list }}
    - alias: 'trigger temperatus status update'
      service: automation.trigger
      data_template:
        entity_id: automation.motion_device_vibration_status

## update status

- id: motion_device_vibration_status
  alias: motion_device_vibration_status
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
    - platform: time_pattern
      minutes: '/5'
  variables:
    class: >-
      {% set class = 'vibration' %}
      {{ class }}
    ids: >-
      {% set ids = state_attr('binary_sensor.motion_device_vibration','ids') %}
      {{ ids }}
    dids: >-
      {% set dids = none %}
      {% set s = state_attr('binary_sensor.motion_device_vibration','devices') %}
      {%- if not s is string and s is iterable and s|count > 0 -%}
        {%- set dids = s|map(attribute='did')|list -%}
      {%- endif -%}
      {{ dids }}
    status: >-
      {%- set s = dids %}
      {%- if not s is string and s is iterable and s|count > 0 -%}
        [{%- for i in s -%}
          {%- set did = i -%}
          {%- set id = state_attr('binary_sensor.motion_device_vibration','devices')|selectattr('did','==',did)|map(attribute='id')|first -%}
          {%- set o = loop -%}
          {%- for state in (states.sensor|list + states.binary_sensor|list) if device_id(state.entity_id) == did and (state.attributes.device_class|default('null')|string == class)  -%}
            {"did":"{{- did -}}","id":"{{- id -}}","name":"{{- state_attr(state.entity_id,'friendly_name') -}}","entity_id":"{{- state.entity_id -}}","state":"{{- state.state -}}","updated":{{- state.last_updated|as_timestamp(None) -}}}
            {%- if not loop.last or not o.last -%},{%- endif -%}
          {%- endfor -%}
        {%- endfor -%}]
      {%- else -%}none{%- endif %}
    statistics: >-
      {%- set s = ids -%}
      {%- if s|lower != 'unknown' and s|lower != 'none' and not s is string -%}
        [{%- for i in s -%}
          {%- set o = loop -%}
          {%- for state in (states.sensor|list + states.binary_sensor|list)
            if device_attr(state.entity_id, 'name') == i
              and state.attributes.device_class|default('null')|lower == 'null'
              and state.attributes.state_class|default('null')|string == 'measurement' -%}
            {%- if not o.first or not loop.first -%},{%- endif -%}
            {"id":"{{- i -}}","name":"{{- state_attr(state.entity_id,'friendly_name') -}}","state":"{{- state.state }} {{ state.attributes.unit_of_measurement-}}","entity_id":"{{- state.entity_id -}}","updated":{{- state.last_updated|as_timestamp(None) -}}}
          {%- else -%}{% if o.first %}None{% endif %}{%- endfor -%}
        {%- endfor -%}]
      {%- else -%}none{%- endif %}
    high: >-
      {% if state_attr('binary_sensor.motion_device_vibration_exist','high')|float('none') != 'none' -%}
        {% set s = status -%}
        {%- if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s is iterable -%}
          {% set n = state_attr('binary_sensor.motion_device_vibration_exist','high')|float(0) -%}
          [{% for i in s if 'entity_id' in i and states(i.entity_id)|float(n) > n -%}
            {%- if not loop.first -%},{%- endif -%}
            {"id":"{{- i.id -}}","entity_id":"{{- i.entity_id -}}","state":"{{- i.state -}}","updated":{{- states[i.entity_id].last_updated|as_timestamp(None) -}}}
          {%- endfor -%}]
        {%- else -%}none{%- endif -%}
      {%- else -%}none{%- endif %}
    low: >-
      {% if state_attr('binary_sensor.motion_device_vibration_exist','low')|float('none') != 'none' -%}
        {%- set s = status -%}
        {%- if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s is iterable -%}
          {% set n = state_attr('binary_sensor.motion_device_vibration_exist','low')|float(0) -%}
          [{% for i in s if 'entity_id' in i and states(i.entity_id)|float(n) < n -%}
            {%- if not loop.first -%},{%- endif -%}
            {"id":"{{- i.id -}}","entity_id":"{{- i.entity_id -}}","state":"{{- i.state -}}","updated":{{- states[i.entity_id].last_updated|as_timestamp(None) -}}}
          {%- endfor -%}]
        {%- else -%}none{%- endif -%}
      {%- else -%}none{%- endif %}
    good: >-
      {% set good = state_attr('binary_sensor.motion_device_vibration_exist','good') -%}
      {%- if good|lower != 'none' -%}
        {% set s = status -%}
        {%- if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s is iterable -%}
          [{% for i in s if 'entity_id' in i and not i.entity_id is none and is_state(i.entity_id,good) -%}
            {%- if not loop.first -%},{%- endif -%}
            {"id":"{{- i.id -}}","entity_id":"{{- i.entity_id -}}","state":"{{- i.state -}}","updated":{{- states[i.entity_id].last_updated|as_timestamp(None) -}}}
          {%- endfor -%}]
        {%- else -%}none{%- endif -%}
      {%- else -%}none{%- endif %}
    bad: >-
      {% set bad = state_attr('binary_sensor.motion_device_vibration_exist','bad') -%}
      {%- if bad|lower != 'none' -%}
        {% set s = status -%}
        {%- if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s is iterable -%}
          [{% for i in s if 'entity_id' in i and not i.entity_id is none and is_state(i.entity_id,bad) -%}
            {%- if not loop.first -%},{%- endif -%}
            {"id":"{{- i.id -}}","entity_id":"{{- i.entity_id -}}","state":"{{- i.state -}}","updated":{{- states[i.entity_id].last_updated|as_timestamp(None) -}}}
          {%- endfor -%}]
        {%- else -%}none{%- endif -%}
      {%- else -%}none{%- endif %}
    activity: >-
      {% set activity = state_attr('binary_sensor.motion_device_vibration_exist','activity') -%}
      {%- if activity|lower != 'none' -%}
        {% set s = status -%}
        {%- if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s is iterable -%}
          [{% for i in s if 'entity_id' in i and not i.entity_id is none and is_state(i.entity_id,activity) -%}
            {%- if not loop.first -%},{%- endif -%}
            {"id":"{{- i.id -}}","entity_id":"{{- i.entity_id -}}","state":"{{- i.state -}}","updated":{{- states[i.entity_id].last_updated|as_timestamp(None) -}}}
          {%- endfor -%}]
        {%- else -%}none{%- endif -%}
      {%- else -%}none{%- endif %}
    unavailable: >-
      {%- set s = status -%}
      {%- if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s is iterable -%}
        [{% for i in s if 'entity_id' in i and not i.entity_id is none and is_state(i.entity_id,'unavailable') or is_state(i.entity_id,'unknown') -%}
          {%- if not loop.first -%},{%- endif -%}
            {"id":"{{- i.id -}}","entity_id":"{{- i.entity_id -}}","state":"{{- i.state -}}","updated":{{- states[i.entity_id].last_updated|as_timestamp(None) -}}}
        {%- endfor -%}]
      {%- else -%}none{%- endif %}
    found: >-
      {%- set s = status -%}
      {%- if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s is iterable -%}
        [{% for i in s if 'entity_id' in i and not i.entity_id is none and not is_state(i.entity_id,'unavailable') and not is_state(i.entity_id,'unknown') -%}
          {%- if not loop.first -%},{%- endif -%}
            {"id":"{{- i.id -}}","entity_id":"{{- i.entity_id -}}","state":"{{- i.state -}}","updated":{{- states[i.entity_id].last_updated|as_timestamp(None) -}}}
        {%- endfor -%}]
      {%- else -%}none{%- endif %}
    missing: >-
      {%- set s = status -%}
      {%- if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s is iterable -%}
        [{% for i in s if 'entity_id' in i and not i.entity_id is none and is_state(i.entity_id,'none') -%}
          {%- if not loop.first -%},{%- endif -%}
            {"id":"{{- i.id -}}","entity_id":"{{- i.entity_id -}}","state":"{{- i.state -}}","updated":{{- states[i.entity_id].last_updated|as_timestamp(None) -}}}
        {%- endfor -%}]
      {%- else -%}none{%- endif %}
    high_spike: >-
      {% if state_attr('binary_sensor.motion_device_vibration_exist','high_spike')|float('none') != 'none' -%}
        {% set s = status -%}
        {%- if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s is iterable -%}
          {% set n = state_attr('binary_sensor.motion_device_vibration_exist','high_spike')|float(0) -%}
          [{% for i in s if 'entity_id' in i and states(i.entity_id)|float(n) > n -%}
            {%- if not loop.first -%},{%- endif -%}
            {"id":"{{- i.id -}}","entity_id":"{{- i.entity_id -}}","state":"{{- i.state -}}","updated":{{- states[i.entity_id].last_updated|as_timestamp(None) -}}}
          {%- endfor -%}]
        {%- else -%}none{%- endif -%}
      {%- else -%}none{%- endif %}
    low_spike: >-
      {% if state_attr('binary_sensor.motion_device_vibration_exist','low_spike')|float('none') != 'none' -%}
        {% set s = status -%}
        {%- if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s is iterable -%}
          {% set n = state_attr('binary_sensor.motion_device_vibration_exist','low_spike')|float(0) -%}
          [{% for i in s if 'entity_id' in i and states(i.entity_id)|float(n) < n -%}
            {%- if not loop.first -%},{%- endif -%}
            {"id":"{{- i.id -}}","entity_id":"{{- i.entity_id -}}","state":"{{- i.state -}}","updated":{{- states[i.entity_id].last_updated|as_timestamp(None) -}}}
          {%- endfor -%}]
        {%- else -%}none{%- endif -%}
      {%- else -%}none{%- endif %}
    values: >-
      {% set g = state_attr('binary_sensor.motion_device_vibration_exist','good') -%}
      {% set b = state_attr('binary_sensor.motion_device_vibration_exist','bad') -%}
      {%- if g|lower != 'none' and b|lower != 'none' -%}
        [{% for i in status if 'entity_id' in i and not i.entity_id is none -%}
          {%- if not loop.first -%},{%- endif -%}
          {% if is_state(i.entity_id,g) %}1{% elif is_state(i.entity_id,b) %}0{% else %}none{% endif -%}
        {%- endfor -%}]
      {%- else -%}
        [{% for i in status if 'entity_id' in i and not i.entity_id is none -%}
          {%- if not loop.first -%},{%- endif -%}
          {{- states(i.entity_id)|float(none) -}}
        {%- endfor -%}]
      {%- endif %}
    value: >-
      {% if bad|lower != 'none' and not bad is string and bad is iterable -%}
        {%- if bad|count > 0 -%}
          {%- set result = 'on' -%}
        {%- else -%}
          {%- set result = 'off' -%}
        {%- endif -%}
      {%- endif -%}
      {%- if low|lower != 'none' and not low is string and low is iterable -%}
        {%- if low|count > 0 -%}
          {%- set result = 'on' -%}
        {%- else -%}
          {%- set result = 'off' -%}
        {%- endif -%}
      {%- endif -%}
      {%- if high|lower != 'none' and not high is string and high is iterable -%}
        {%- if high|count > 0 -%}
          {%- set result = 'on' -%}
        {%- else -%}
          {%- set result = 'off' -%}
        {%- endif -%}
      {%- endif -%}
      {%- if activity|lower != 'none' and not activity is string and activity is iterable -%}
        {%- if activity|count > 0 -%}
          {%- set result = 'on' -%}
        {%- else -%}
          {%- set result = 'off' -%}
        {%- endif -%}
      {%- endif -%}
      {%- if result is defined -%}
        {{- result -}}
      {%- else -%}unknown{%- endif %}
  action:
    - alias: 'update binary_sensor.motion_device_vibration state'
      service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_vibration
        status: >-
          {{ status }}
        statistics: >-
          {{ statistics|reject('none')|list }}
        high: >-
          {{ high }}
        low: >-
          {{ low }}
        good: >-
          {{ good }}
        bad: >-
          {{ bad }}
        high_spike: >-
          {{ high_spike }}
        low_spike: >-
          {{ low_spike }}
        found: >-
          {{ found }}
        missing: >-
          {{ missing }}
        activity: >-
          {{ activity }}
        unavailable: >-
          {{ unavailable }}
        values: >-
          {{ values }}
        state: >-
          {{ value }}

## change vibration class attributes (alarm,required,..)

# required

- id: motion_device_vibration_required_off
  alias: motion_device_vibration_required_off
  mode: single
  max_exceeded: 'warn'
  initial_state: 'on'
  trigger:
  condition:
    condition: and
    conditions:
      - alias: 'test setup complete'
        condition: template
        value_template: >-
          {{ is_state('binary_sensor.motion_device_class','on') }}
      - alias: 'vibration required not off'
        condition: template
        value_template: >-
          {{ 'vibration' in state_attr('binary_sensor.motion_device_class','sensors') or 'vibration' in state_attr('binary_sensor.motion_device_class','binary_sensors') }}
  variables:
    sensors: >-
      {% set s = state_attr('binary_sensor.motion_device_class','sensors') -%}
      {%- if s|lower != 'unknown' and s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
        {%- set s = s|reject('==','vibration')|list -%}
        {%- if not s is string and s is iterable and s|count > 0 -%}
          {% set sensors = s -%}
        {%- else -%}
          {% set sensors = [] -%}
        {%- endif %}
      {%- endif %}
      {%- if sensors is defined -%}
        {{ sensors }}
      {%- else -%}none{%- endif %}
    binary_sensors: >-
      {% set s = state_attr('binary_sensor.motion_device_class','binary_sensors') -%}
      {%- if s|lower != 'unknown' and s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
        {%- set s = s|reject('==','vibration')|list -%}
        {%- if not s is string and s is iterable and s|count > 0 -%}
          {% set binary_sensors = s -%}
        {%- else -%}
          {% set binary_sensors = [] -%}
        {%- endif %}
      {%- endif %}
      {%- if binary_sensors is defined -%}
        {{ binary_sensors }}
      {%- else -%}none{%- endif %}
  action:
    - alias: 'test if required list of device classes is valid'
      condition: template
      value_template: >-
        {{ sensors|lower != 'none' and binary_sensors|lower != 'none' }}
    - alias: 'update binary_sensor.motion_device_class required attribute; remove: vibration'
      service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_class
        sensors: >-
          {{ sensors }}
        binary_sensors: >-
          {{ binary_sensors }}
    - alias: 'update binary_sensor.motion_device_vibration_exist'
      service: homeassistant.update_entity
      entity_id: binary_sensor.motion_device_vibration_exist

- id: motion_device_vibration_required_on
  alias: motion_device_vibration_required_on
  mode: single
  max_exceeded: 'warn'
  initial_state: 'on'
  trigger:
  condition:
    condition: and
    conditions:
      - alias: 'test setup complete'
        condition: template
        value_template: >-
          {{ is_state('binary_sensor.motion_device_class','on') }}
      - alias: 'vibration required not on'
        condition: template
        value_template: >-
          {{ 'vibration' in state_attr('binary_sensor.motion_device_class','sensor_list') or 'vibration' in state_attr('binary_sensor.motion_device_class','binary_sensor_list')
            and (not 'vibration' in state_attr('binary_sensor.motion_device_class','sensors') or not 'vibration' in state_attr('binary_sensor.motion_device_class','binary_sensors')) }}
  variables:
    sensors: >-
      {% set s = state_attr('binary_sensor.motion_device_class','sensors') -%}
      {%- if s|lower != 'unknown' and s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
        {%- set sensors = (s|list + ['vibration'])|sort|unique|list -%}
      {%- else -%}
        {%- set sensors = ['vibration'] -%}
      {%- endif %}
      {%- if sensors is defined -%}
        {{ sensors }}
      {%- else -%}none{%- endif %}
    binary_sensors: >-
      {% set s = state_attr('binary_sensor.motion_device_class','binary_sensors') -%}
      {%- if s|lower != 'unknown' and s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
        {%- set binary_sensors = (s|list + ['vibration'])|sort|unique|list -%}
      {%- else -%}
        {%- set binary_sensors = ['vibration'] -%}
      {%- endif %}
      {%- if binary_sensors is defined -%}
        {{ binary_sensors }}
      {%- else -%}none{%- endif %}
  action:
    - alias: 'test if required list of device classes is valid'
      condition: template
      value_template: >-
        {{ sensors|lower != 'none' and binary_sensors|lower != 'none' }}
    - alias: 'update binary_sensor.motion_device_class required attribute; add: vibration'
      service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_class
        sensors: >-
          {{ sensors }}
        binary_sensors: >-
          {{ binary_sensors }}
    - alias: 'update binary_sensor.motion_device_vibration_exist'
      service: homeassistant.update_entity
      entity_id: binary_sensor.motion_device_vibration_exist

## bad

- id: motion_device_vibration_bad
  alias: motion_device_vibration_bad
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
    - platform: state
      entity_id: 'binary_sensor.motion_device_vibration'
      attribute: 'bad'
  variables:
    details: >-
      {% if state_attr('binary_sensor.motion_device_vibration_exist','bad')|lower != 'none' %}
        {% set s = state_attr('binary_sensor.motion_device_vibration','bad') %}
        {%- if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s is iterable -%}
          [{% for i in s -%}
            {%- if not loop.first -%},{%- endif -%}
            "{{- state_attr(i.entity_id,'friendly_name') }} is {{ states(i.entity_id) -}}"
          {%- endfor -%}]
        {%- else -%}none{%- endif %}
      {%- else -%}none{%- endif %}
    state: >-
      {% set s = state_attr('binary_sensor.motion_device_vibration','bad') %}
      {% if s|lower != 'null' and s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and not s is string and s is iterable %}
        {% if s|list|count > 0 %}on{% else %}off{% endif %}
      {% else %}unknown{% endif %}
    last: >-
      {% if state|lower == 'off' %}
        {{ state_attr('binary_sensor.motion_device_vibration_bad','last') }}
      {%- else -%}{{ utcnow().timestamp() }}{%- endif %}
  condition:
    - condition: template
      value_template: >
        {{ state|lower != 'unknown' }}
  action:
    - service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_vibration_bad
        last: >-
          {{ last }}
        details: >-
          {{ details }}
        state: >-
          {{ state }}
    - condition: template
      value_template: >
        {{ state|lower == 'on' }}
    - service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_vibration
        condition: >-
          {{ 'bad' }}

- id: motion_device_vibration_bad_off
  alias: motion_device_vibration_bad_off
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
  condition:
    condition: and
    conditions:
      - alias: 'test setup complete'
        condition: template
        value_template: >-
          {{ is_state('binary_sensor.motion_device_class','on') }}
      - alias: 'vibration bad not off'
        condition: template
        value_template: >-
          {{ 'vibration' in state_attr('binary_sensor.motion_device_class','bad') }}
  variables:
    bad: >-
      {% set s = state_attr('binary_sensor.motion_device_class','bad') -%}
      {%- if s|lower != 'unknown' and s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
        {%- set s = s|reject('==','vibration')|list -%}
        {%- if not s is string and s is iterable and s|count > 0 -%}
          {% set bad = s -%}
        {%- else -%}
          {% set bad = [] -%}
        {%- endif %}
      {%- endif %}
      {%- if bad is defined -%}
        {{ bad }}
      {%- else -%}none{%- endif %}
  action:
    - alias: 'test if bad list of device classes is valid'
      condition: template
      value_template: >-
        {{ bad|lower != 'none' }}
    - alias: 'update binary_sensor.motion_device_class bad attribute; remove: vibration'
      service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_class
        bad: >-
          {{ bad }}
    - alias: 'update binary_sensor.motion_device_vibration_exist'
      service: homeassistant.update_entity
      entity_id: binary_sensor.motion_device_vibration_exist

- id: motion_device_vibration_bad_on
  alias: motion_device_vibration_bad_on
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
  condition:
    condition: and
    conditions:
      - alias: 'test setup complete'
        condition: template
        value_template: >-
          {{ is_state('binary_sensor.motion_device_class','on') }}
      - alias: 'vibration bad not on'
        condition: template
        value_template: >-
          {{ not 'vibration' in state_attr('binary_sensor.motion_device_class','bad') }}
  variables:
    bad: >-
      {% set s = state_attr('binary_sensor.motion_device_class','bad') -%}
      {%- if s|lower != 'unknown' and s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
        {%- set bad = (s|list + ['vibration'])|sort|unique|list -%}
      {%- else -%}
        {%- set bad = ['vibration'] -%}
      {%- endif %}
      {%- if bad is defined -%}
        {{ bad }}
      {%- else -%}none{%- endif %}
  action:
    - alias: 'test if bad list of device classes is valid'
      condition: template
      value_template: >-
        {{ bad|lower != 'none' }}
    - alias: 'update binary_sensor.motion_device_class bad attribute; add: vibration'
      service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_class
        bad: >-
          {{ bad }}
    - alias: 'update binary_sensor.motion_device_vibration_exist'
      service: homeassistant.update_entity
      entity_id: binary_sensor.motion_device_vibration_exist

- id: motion_device_vibration_alarm_bad_on
  alias: motion_device_vibration_alarm_bad_on
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
    - platform: template
      value_template: >-
        {{ not is_state_attr('binary_sensor.motion_device_vibration_exist','bad','none')
           and is_state_attr('binary_sensor.motion_device_vibration_exist','bad',states('binary_sensor.motion_device_vibration')) }}
  variables:
    devices: >-
      {%- set devices = none -%}
      {% set s = state_attr('binary_sensor.motion_device_vibration','devices') %}
      {% if s|lower != 'none' and s|lower != 'null' and not s is string and s is iterable and s|count > 0 %}
        {% set devices = s %}
      {%- endif %}
      {{ devices }}
    last: >-
      {% set last = none %}
      {% set s = state_attr('binary_sensor.motion_device_vibration','status')|selectattr('state','==',state_attr('binary_sensor.motion_device_vibration_exist','bad'))|sort(attribute='updated',reverse=true)|list %}
      {% if s|lower != 'none' and s|lower != 'null' and not s is string and s is iterable and s|count > 0 %}
        {% set last = s %}
      {%- endif %}
      {{ last  }}
    which: >-
      {% set which = none %}
      {% if last|lower != 'none' %}
        {% set which = last|map(attribute='id')|first %}
      {%- endif %}
      {{ which }}
    did: >-
      {% set did = none %}
      {% if devices|lower != 'none'  and which|lower != 'none' %}
        {% set did = devices|selectattr('id','==',which)|map(attribute='did')|first %}
      {%- endif %}
      {{ did }}
    where: >-
      {% set where = none %}
      {% if devices|lower != 'none'  and which|lower != 'none' %}
        {% set where = devices|selectattr('id','==',which)|map(attribute='area')|first %}
      {%- endif %}
      {{ where }}
  condition:
    condition: and
    conditions:
      - condition: template
        value_template: >
          {{ which|lower != 'none' and which|lower != 'null' and which|length > 0 }}
  action:
    - variables:
        markdown: >-
          A <a href="/lovelace-primary/vibration">vibration</a> alarm was triggered ({{- state_attr('binary_sensor.motion_device_vibration_exist','bad')|string -}}); sensor: {{ which -}}; where: {{ where -}}; at {{ utcnow().timestamp()|timestamp_custom("%a %b %d %I:%M %p %Z",true,'unknown') -}}.
    - service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_vibration_bad
        markdown: >-
          {{ markdown }}
    - alias: 'update binary_sensor.motion_device_vibration which, where, did'
      service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_vibration
        which: >-
          {{ which }}
        where: >-
          {{ where }}
        did: >-
          {{ did }}
    - condition: template
      value_template: >
        {{ is_state_attr('binary_sensor.motion_device_vibration_exist','alarm','on') }}
    - alias: 'trigger motion notification'
      service: automation.trigger
      data_template:
        entity_id: automation.motion_device_vibration_bad_notify

- id: motion_device_vibration_bad_notify
  alias: motion_device_vibration_bad_notify
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
  variables:
    class: >-
      {{ 'vibration' }}
    ids: >-
      {% set ids = state_attr('binary_sensor.motion_device_vibration','ids') %}
      {{ ids }}
    dids: >-
      {% set dids = none %}
      {% set s = state_attr('binary_sensor.motion_device_vibration','devices') %}
      {%- if not s is string and s is iterable and s|count > 0 -%}
        {%- set dids = s|map(attribute='did')|list -%}
      {%- endif -%}
      {{ dids }}
    which: >-
      {% set which = state_attr('binary_sensor.motion_device_vibration','which') %}
      {{ which }}
    where: >-
      {% set where = state_attr('binary_sensor.motion_device_vibration','where') %}
      {{ where }}
    did: >-
      {% set did = state_attr('binary_sensor.motion_device_vibration','did') %}
      {{ did }}
    url: >-
      {% set url = '/lovelace-primary/' + class %}
      {{ url }}
    action: >-
      {% set action = class + '-alarm' %}
      {{ action }}
    title: >-
      {% set title = states('sensor.motion_app') + ': vibration bad' %}
      {{ title }}
    notification_id: >-
      {{ 'device-vibration-status-' + which|string }}
    icon: >-
      {% set icon = 'notification-vibration-bad' %}
      {{ icon }}
    header: >-
      {{- '<img src="/local/images/icons/' + icon + '.png?v1">' -}}
    footer: >-
      <a href="/notify-devices/{{- class -}}"><img src="https://img.shields.io/badge/{{- class -}}-device-cyan.svg"></a>
      {%- if did is defined and did|lower != 'none' and did|lower != 'null' -%}
         <br><a href="/config/devices/device/{{- did -}}"><img src="https://img.shields.io/badge/{{- class -}}-config-blue.svg"></a>
      {%- endif -%}
    body: >-
      {{ state_attr('binary_sensor.motion_device_vibration_bad','markdown') }}
      <p>We have notified <b>{{- state_attr('sensor.motion_role_primary','friendly_name')|replace("'","") -}}</b> about your vibration alarm.
      To inspect and change the alarm click the <b>{{- action -}}</b> button below.
      {{- '<p>' -}}
      <a href="{{- url -}}"><img src="https://img.shields.io/badge/{{- action -}}-yellow.svg"></a>
    message: >-
      {{- header -}}<hr>{{- body -}}<br>{{- footer -}}
  action:
    - alias: 'notify vibration sensor activated'
      service: persistent_notification.create
      data_template:
        title: >-
          {{- title -}}
        notification_id: >-
          {{- notification_id -}}
        message: >-
          {{- message -}}

## low

- id: motion_device_vibration_low
  alias: motion_device_vibration_low
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
    - platform: state
      entity_id: 'binary_sensor.motion_device_vibration'
      attribute: 'low'
  variables:
    details: >-
      {% if state_attr('binary_sensor.motion_device_vibration_exist','low')|lower != 'none' %}
        {% set s = state_attr('binary_sensor.motion_device_vibration','low') %}
        {%- if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s is iterable -%}
          [{% for i in s -%}
            {%- if not loop.first -%},{%- endif -%}
            "{{- state_attr(i.entity_id,'friendly_name') }} is {{ states(i.entity_id)|int('unknown') -}}{{ state_attr(i.entity_id,'unit_of_measurement') -}}"
          {%- endfor -%}]
        {%- else -%}none{%- endif %}
      {%- else -%}none{%- endif %}
    state: >-
      {% set s = state_attr('binary_sensor.motion_device_vibration','low') %}
      {% if s|lower != 'null' and s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and not s is string and s is iterable %}
        {% if s|list|count > 0 %}on{% else %}off{% endif %}
      {% else %}unknown{% endif %}
    last: >-
      {% if state|lower == 'off' %}
        {{ state_attr('binary_sensor.motion_device_vibration_low','last') }}
      {%- else -%}{{ utcnow().timestamp() }}{%- endif %}
  condition:
    - condition: template
      value_template: >
        {{ state|lower != 'unknown' }}
  action:
    - service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_vibration_low
        last: >-
          {{ last }}
        details: >-
          {{ details }}
        state: >-
          {{ state }}
    - condition: template
      value_template: >
        {{ state|lower == 'on' }}
    - alias: 'set vibration condition: low'
      service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_vibration
        condition: >-
          {{ 'low' }}

- id: motion_device_vibration_low_off
  alias: motion_device_vibration_low_off
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
  condition:
    condition: and
    conditions:
      - alias: 'test setup complete'
        condition: template
        value_template: >-
          {{ is_state('binary_sensor.motion_device_class','on') }}
      - alias: 'vibration low not off'
        condition: template
        value_template: >-
          {{ 'vibration' in state_attr('binary_sensor.motion_device_class','low') }}
  variables:
    low: >-
      {% set s = state_attr('binary_sensor.motion_device_class','low') -%}
      {%- if s|lower != 'unknown' and s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
        {%- set s = s|reject('==','vibration')|list -%}
        {%- if not s is string and s is iterable and s|count > 0 -%}
          {% set low = s -%}
        {%- else -%}
          {% set low = [] -%}
        {%- endif %}
      {%- endif %}
      {%- if low is defined -%}
        {{ low }}
      {%- else -%}none{%- endif %}
  action:
    - alias: 'test if low list of device classes is valid'
      condition: template
      value_template: >-
        {{ low|lower != 'none' }}
    - alias: 'update binary_sensor.motion_device_class low attribute; remove: vibration'
      service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_class
        low: >-
          {{ low }}
    - alias: 'update binary_sensor.motion_device_vibration_exist'
      service: homeassistant.update_entity
      entity_id: binary_sensor.motion_device_vibration_exist

- id: motion_device_vibration_low_on
  alias: motion_device_vibration_low_on
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
  condition:
    condition: and
    conditions:
      - alias: 'test setup complete'
        condition: template
        value_template: >-
          {{ is_state('binary_sensor.motion_device_class','on') }}
      - alias: 'vibration low not on'
        condition: template
        value_template: >-
          {{ not 'vibration' in state_attr('binary_sensor.motion_device_class','low') }}
  variables:
    low: >-
      {% set s = state_attr('binary_sensor.motion_device_class','low') -%}
      {%- if s|lower != 'unknown' and s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
        {%- set low = (s|list + ['vibration'])|sort|unique|list -%}
      {%- else -%}
        {%- set low = ['vibration'] -%}
      {%- endif %}
      {%- if low is defined -%}
        {{ low }}
      {%- else -%}none{%- endif %}
  action:
    - alias: 'test if low list of device classes is valid'
      condition: template
      value_template: >-
        {{ low|lower != 'none' }}
    - alias: 'update binary_sensor.motion_device_class low attribute; add: vibration'
      service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_class
        low: >-
          {{ low }}
    - alias: 'update binary_sensor.motion_device_vibration_exist'
      service: homeassistant.update_entity
      entity_id: binary_sensor.motion_device_vibration_exist

- id: motion_device_vibration_alarm_low_on
  alias: motion_device_vibration_alarm_low_on
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
    - platform: state
      entity_id: 'binary_sensor.motion_device_vibration_low'
      to: 'on'
  variables:
    low: >-
      {{ state_attr('binary_sensor.motion_device_vibration','low') }}
  condition:
    condition: and
    conditions:
      - condition: template
        value_template: >
          {{- low|lower != 'none' and low|lower != 'unknown' and not low is string and low is iterable and low|count > 0 -}}
  action:
    - variables:
        markdown: >-
          A <a href="/lovelace-primary/vibration">vibration</a> alarm was triggered (below {{ state_attr('binary_sensor.motion_device_vibration_exist','low') -}}) at {{ utcnow().timestamp()|timestamp_custom("%a %b %d %I:%M %p %Z",true,'unknown') }}:<br>
          {%- for i in low if 'id' in i and i.id|lower != 'none' -%}
            {%- if loop.first -%}{{- '<ul>' -}}{%- endif -%}
            {%- set d = state_attr('binary_sensor.motion_device_class','devices') -%}
            {%- if d|lower != 'none' and not d is string and d is iterable and d|count > 0 -%}
              {%- set d = d|selectattr('id','==',i.id)|list -%}
              {%- if d|lower != 'none' and not d is string and d is iterable and d|count > 0 -%}
                {%- set d = d|first -%}
                <li><a href="/config/devices/device/{{- d.did -}}">{{- d.friendly_name|replace("'","") -}}</a> is {{ states(low[loop.index-1].entity_id) -}}{{- state_attr(low[loop.index-1].entity_id,'unit_of_measurement') }} in <a href="/config/areas/area/{{- area_id(low[loop.index-1].entity_id) -}}">{{- area_id(low[loop.index-1].entity_id) -}}</a></li>
              {%- else -%}
                <li>{{- i.id }} vibration is {{ states(i.entity_id) -}}{{- state_attr(i.entity_id,'unit_of_measurement') }}</li>
              {%- endif -%}
            {%- else -%}
              {{- '<li>' -}}
              {{- i.id }} vibration is {{ states(i.entity_id) -}}{{- state_attr(i.entity_id,'unit_of_measurement') }}
              {{- '</li>' -}}
            {%- endif -%}
            {%- if loop.last -%}{{- '</ul>' -}}{%- endif -%}
          {%- endfor -%}
    - service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_vibration_low
        markdown: >-
          {{ markdown }}
    - condition: template
      value_template: >
        {{ is_state_attr('binary_sensor.motion_device_vibration_exist','alarm','on') }}
    - alias: 'trigger motion notification'
      service: automation.trigger
      data_template:
        entity_id: automation.motion_device_vibration_low_notify

- id: motion_device_vibration_alarm_low_off
  alias: motion_device_vibration_alarm_low_off
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
    - platform: state
      entity_id: 'binary_sensor.motion_device_vibration_low'
      to: 'off'
  action:
    - condition: template
      value_template: >
        {{ is_state_attr('binary_sensor.motion_device_vibration_exist','alarm','on') }}
    - alias: 'trigger vibration low dismiss'
      service: automation.trigger
      data_template:
        entity_id: automation.motion_device_vibration_low_dismiss

- id: motion_device_vibration_low_notify
  alias: motion_device_vibration_low_notify
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
  variables:
    class: >-
      {{ 'vibration' }}
    low: >-
      {% set low = state_attr('binary_sensor.motion_device_vibration','low') %}
      {{ low }}
    action: >-
      {% set action = class + '-alarm' %}
      {{ action }}
    url: >-
      {% set url = '/lovelace-primary/' + class %}
      {{ url }}
    title: >-
      {% set title = states('sensor.motion_app') + ': vibration low' %}
      {{ title }}
    notification_id: >-
      {{ 'device-vibration-low' }}
    icon: >-
      {% set icon = 'notification-vibration-low' %}
      {{ icon }}
    header: >-
      {{- '<img src="/local/images/icons/' + icon + '.png?v1">' -}}
    footer: >-
      <a href="/notify-devices/{{- class -}}"><img src="https://img.shields.io/badge/{{- class -}}-device-cyan.svg"></a>
      {%- if did is defined and did|lower != 'none' and did|lower != 'null' -%}
         <br><a href="/config/devices/device/{{- did -}}"><img src="https://img.shields.io/badge/{{- class -}}-config-blue.svg"></a>
      {%- endif -%}
    body: >-
      {{ state_attr('binary_sensor.motion_device_vibration_low','markdown') }}
      <p>We have notified <b>{{- state_attr('sensor.motion_role_primary','friendly_name')|replace("'","") -}}</b> about your vibration alarm.
      To inspect and change the alarm click the <b>{{- action -}}</b> button below.
      {{- '<p>' -}}
      <a href="{{- url -}}"><img src="https://img.shields.io/badge/{{- action -}}-yellow.svg"></a>
    message: >-
      {{- header -}}<hr>{{- body -}}<br>{{- footer -}}
  action:
    - alias: 'notify vibration sensor activated'
      service: persistent_notification.create
      data_template:
        title: >-
          {{- title -}}
        notification_id: >-
          {{- notification_id -}}
        message: >-
          {{- message -}}

- id: motion_device_vibration_dismiss
  alias: motion_device_vibration_dismiss
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
  variables:
    alarms: >-
      {% set alarms = state_attr('binary_sensor.motion_device_class','alarms') %}
      {{ alarms }}
  condition:
    - condition: template
      value_template: >
        {{ alarms|lower != 'none' and not alarms is string and alarms is iterable and alarms|count > 0 }}
  action:
    - repeat:
        while:
          - condition: template
            value_template: >-
              {% set s = alarms[repeat.index - 1]|default('unknown') -%}
              {{ s|lower != 'unavailable' and s|lower != 'unknown' and s|lower != 'null' and s|string|length > 0 }}
        sequence:
          - variables:
              alarm: >-
                {% set alarm = alarms[repeat.index - 1]|default('unknown') -%}
                {{ alarm }}
              automation: >-
                {{ 'automation.motion_device_vibration_' + alarm + '_dismiss' }}
          - service: automation.trigger
            data_template:
              entity_id: >-
                {{ automation }}

- id: motion_device_vibration_low_dismiss
  alias: motion_device_vibration_low_dismiss
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
  variables:
    notification_id: >-
      {{ 'device-vibration-low' }}
  action:
    - alias: 'dismiss vibration low notification'
      service: persistent_notification.dismiss
      data_template:
        notification_id: >-
          {{ notification_id }}

## high

- id: motion_device_vibration_high
  alias: motion_device_vibration_high
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
    - platform: state
      entity_id: 'binary_sensor.motion_device_vibration'
      attribute: 'high'
  variables:
    details: >-
      {% if state_attr('binary_sensor.motion_device_vibration_exist','high')|lower != 'none' %}
        {% set s = state_attr('binary_sensor.motion_device_vibration','high') %}
        {%- if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s is iterable -%}
          [{% for i in s -%}
            {%- if not loop.first -%},{%- endif -%}
            "{{- state_attr(i.entity_id,'friendly_name') }} is {{ states(i.entity_id)|int('unknown') -}}{{ state_attr(i.entity_id,'unit_of_measurement') -}}"
          {%- endfor -%}]
        {%- else -%}none{%- endif %}
      {%- else -%}none{%- endif %}
    state: >-
      {% set s = state_attr('binary_sensor.motion_device_vibration','high') %}
      {% if s|lower != 'null' and s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and not s is string and s is iterable %}
        {% if s|list|count > 0 %}on{% else %}off{% endif %}
      {% else %}unknown{% endif %}
    last: >-
      {% if state|lower == 'off' %}
        {{ state_attr('binary_sensor.motion_device_vibration_high','last') }}
      {%- else -%}{{ utcnow().timestamp() }}{%- endif %}
  condition:
    - condition: template
      value_template: >
        {{ state|lower != 'unknown' }}
  action:
    - service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_vibration_high
        last: >-
          {{ last }}
        details: >-
          {{ details }}
        state: >-
          {{ state }}
    - condition: template
      value_template: >
        {{ state|lower == 'on' }}
    - alias: 'set vibration condition: high'
      service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_vibration
        condition: >-
          {{ 'high' }}

- id: motion_device_vibration_high_off
  alias: motion_device_vibration_high_off
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
  condition:
    condition: and
    conditions:
      - alias: 'test setup complete'
        condition: template
        value_template: >-
          {{ is_state('binary_sensor.motion_device_class','on') }}
      - alias: 'vibration high not off'
        condition: template
        value_template: >-
          {{ 'vibration' in state_attr('binary_sensor.motion_device_class','high') }}
  variables:
    high: >-
      {% set s = state_attr('binary_sensor.motion_device_class','high') -%}
      {%- if s|lower != 'unknown' and s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
        {%- set s = s|reject('==','vibration')|list -%}
        {%- if not s is string and s is iterable and s|count > 0 -%}
          {% set high = s -%}
        {%- else -%}
          {% set high = [] -%}
        {%- endif %}
      {%- endif %}
      {%- if high is defined -%}
        {{ high }}
      {%- else -%}none{%- endif %}
  action:
    - alias: 'test if high list of device classes is valid'
      condition: template
      value_template: >-
        {{ high|lower != 'none' }}
    - alias: 'update binary_sensor.motion_device_class high attribute; remove: vibration'
      service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_class
        high: >-
          {{ high }}
    - alias: 'update binary_sensor.motion_device_vibration_exist'
      service: homeassistant.update_entity
      entity_id: binary_sensor.motion_device_vibration_exist

- id: motion_device_vibration_high_on
  alias: motion_device_vibration_high_on
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
  condition:
    condition: and
    conditions:
      - alias: 'test setup complete'
        condition: template
        value_template: >-
          {{ is_state('binary_sensor.motion_device_class','on') }}
      - alias: 'vibration high not on'
        condition: template
        value_template: >-
          {{ not 'vibration' in state_attr('binary_sensor.motion_device_class','high') }}
  variables:
    high: >-
      {% set s = state_attr('binary_sensor.motion_device_class','high') -%}
      {%- if s|lower != 'unknown' and s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
        {%- set high = (s|list + ['vibration'])|sort|unique|list -%}
      {%- else -%}
        {%- set high = ['vibration'] -%}
      {%- endif %}
      {%- if high is defined -%}
        {{ high }}
      {%- else -%}none{%- endif %}
  action:
    - alias: 'test if high list of device classes is valid'
      condition: template
      value_template: >-
        {{ high|lower != 'none' }}
    - alias: 'update binary_sensor.motion_device_class high attribute; add: vibration'
      service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_class
        high: >-
          {{ high }}
    - alias: 'update binary_sensor.motion_device_vibration_exist'
      service: homeassistant.update_entity
      entity_id: binary_sensor.motion_device_vibration_exist

- id: motion_device_vibration_alarm_high_on
  alias: motion_device_vibration_alarm_high_on
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
    - platform: state
      entity_id: 'binary_sensor.motion_device_vibration_high'
      to: 'on'
  variables:
    high: >-
      {{ state_attr('binary_sensor.motion_device_vibration','high') }}
  condition:
    condition: and
    conditions:
      - condition: template
        value_template: >
          {{- high|lower != 'none' and high|lower != 'unknown' and not high is string and high is iterable and high|count > 0 -}}
  action:
    - variables:
        markdown: >-
          A <a href="/lovelace-primary/vibration">vibration</a> alarm was triggered (above {{ state_attr('binary_sensor.motion_device_vibration_exist','high') -}}) at {{ utcnow().timestamp()|timestamp_custom("%a %b %d %I:%M %p %Z",true,'unknown') }}:<br>
          {%- for i in high if 'id' in i and i.id|lower != 'none' -%}
            {%- if loop.first -%}{{- '<ul>' -}}{%- endif -%}
            {%- set d = state_attr('binary_sensor.motion_device_class','devices') -%}
            {%- if d|lower != 'none' and not d is string and d is iterable and d|count > 0 -%}
              {%- set d = d|selectattr('id','==',i.id)|list -%}
              {%- if d|lower != 'none' and not d is string and d is iterable and d|count > 0 -%}
                {%- set d = d|first -%}
                <li><a href="/config/devices/device/{{- d.did -}}">{{- d.friendly_name|replace("'","") -}}</a> is {{ states(high[loop.index-1].entity_id) -}}{{- state_attr(high[loop.index-1].entity_id,'unit_of_measurement') }} in <a href="/config/areas/area/{{- area_id(high[loop.index-1].entity_id) -}}">{{- area_id(high[loop.index-1].entity_id) -}}</a></li>
              {%- else -%}
                <li>{{- i.id }} vibration is {{ states(i.entity_id) -}}{{- state_attr(i.entity_id,'unit_of_measurement') }}</li>
              {%- endif -%}
            {%- else -%}
              {{- '<li>' -}}
              {{- i.id }} vibration is {{ states(i.entity_id) -}}{{- state_attr(i.entity_id,'unit_of_measurement') }}
              {{- '</li>' -}}
            {%- endif -%}
            {%- if loop.last -%}{{- '</ul>' -}}{%- endif -%}
          {%- endfor -%}
    - service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_vibration_high
        markdown: >-
          {{ markdown }}
    - condition: template
      value_template: >
        {{ is_state_attr('binary_sensor.motion_device_vibration_exist','alarm','on') }}
    - alias: 'trigger motion notification'
      service: automation.trigger
      data_template:
        entity_id: automation.motion_device_vibration_high_notify

- id: motion_device_vibration_alarm_high_off
  alias: motion_device_vibration_alarm_high_off
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
    - platform: state
      entity_id: 'binary_sensor.motion_device_vibration_high'
      to: 'off'
  action:
    - condition: template
      value_template: >
        {{ is_state_attr('binary_sensor.motion_device_vibration_exist','alarm','on') }}
    - alias: 'trigger vibration high dismiss'
      service: automation.trigger
      data_template:
        entity_id: automation.motion_device_vibration_high_dismiss

- id: motion_device_vibration_high_notify
  alias: motion_device_vibration_high_notify
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
  variables:
    class: >-
      {{ 'vibration' }}
    high: >-
      {% set high = state_attr('binary_sensor.motion_device_vibration','high') %}
      {{ high }}
    action: >-
      {% set action = class + '-alarm' %}
      {{ action }}
    url: >-
      {% set url = '/lovelace-primary/' + class %}
      {{ url }}
    title: >-
      {% set title = states('sensor.motion_app') + ': ' + class + ' high' %}
      {{ title }}
    notification_id: >-
      {{ 'device-' + class + '-high' }}
    icon: >-
      {% set icon = 'notification-' + class + '-high' %}
      {{ icon }}
    header: >-
      {{- '<img src="/local/images/icons/' + icon + '.png?v1">' -}}
    footer: >-
      <a href="/notify-devices/{{- class -}}"><img src="https://img.shields.io/badge/{{- class -}}-device-cyan.svg"></a>
      {%- if did is defined and did|lower != 'none' and did|lower != 'null' -%}
         <br><a href="/config/devices/device/{{- did -}}"><img src="https://img.shields.io/badge/{{- class -}}-config-blue.svg"></a>
      {%- endif -%}
    body: >-
      {{ state_attr('binary_sensor.motion_device_vibration_high','markdown') }}
      <p>We have notified <b>{{- state_attr('sensor.motion_role_primary','friendly_name')|replace("'","") -}}</b> about your vibration alarm.
      To inspect and change the alarm click the <b>{{- action -}}</b> button below.
      {{- '<p>' -}}
      <a href="{{- url -}}"><img src="https://img.shields.io/badge/{{- action -}}-yellow.svg"></a>
    message: >-
      {{- header -}}<hr>{{- body -}}<br>{{- footer -}}
  action:
    - alias: 'notify vibration sensor activated'
      service: persistent_notification.create
      data_template:
        title: >-
          {{- title -}}
        notification_id: >-
          {{- notification_id -}}
        message: >-
          {{- message -}}

- id: motion_device_vibration_high_dismiss
  alias: motion_device_vibration_high_dismiss
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
  variables:
    class: >-
      {{ 'vibration' }}
    notification_id: >-
      {{ 'device-' + class + '-high' }}
  action:
    - alias: 'dismiss vibration high notification'
      service: persistent_notification.dismiss
      data_template:
        notification_id: >-
          {{ notification_id }}

## good

- id: motion_device_vibration_good
  alias: motion_device_vibration_good
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
    - platform: state
      entity_id: 'binary_sensor.motion_device_vibration'
      attribute: 'good'
  variables:
    details: >-
      {% if state_attr('binary_sensor.motion_device_vibration_exist','good')|lower != 'none' %}
        {% set s = state_attr('binary_sensor.motion_device_vibration','good') %}
        {%- if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s is iterable -%}
          [{% for i in s -%}
            {%- if not loop.first -%},{%- endif -%}
            "{{- state_attr(i.entity_id,'friendly_name') }} is {{ states(i.entity_id) -}}"
          {%- endfor -%}]
        {%- else -%}none{%- endif %}
      {%- else -%}none{%- endif %}
    state: >-
      {% set s = state_attr('binary_sensor.motion_device_vibration','good') %}
      {% if s|lower != 'null' and s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and not s is string and s is iterable %}
        {% if s|list|count > 0 %}on{% else %}off{% endif %}
      {% else %}unknown{% endif %}
    last: >-
      {% if state|lower == 'off' %}
        {{ state_attr('binary_sensor.motion_device_vibration_good','last') }}
      {%- else -%}{{ utcnow().timestamp() }}{%- endif %}
  condition:
    - condition: template
      value_template: >
        {{ state|lower != 'unknown' }}
  action:
    - service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_vibration_good
        last: >-
          {{ last }}
        details: >-
          {{ details }}
        state: >-
          {{ state }}
    - condition: template
      value_template: >
        {{ state|lower == 'on' }}
    - service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_vibration
        condition: >-
          {{ 'good' }}

- id: motion_device_vibration_good_off
  alias: motion_device_vibration_good_off
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
  condition:
    condition: and
    conditions:
      - alias: 'test setup complete'
        condition: template
        value_template: >-
          {{ is_state('binary_sensor.motion_device_class','on') }}
      - alias: 'vibration good not off'
        condition: template
        value_template: >-
          {{ 'vibration' in state_attr('binary_sensor.motion_device_class','good') }}
  variables:
    good: >-
      {% set s = state_attr('binary_sensor.motion_device_class','good') -%}
      {%- if s|lower != 'unknown' and s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
        {%- set s = s|reject('==','vibration')|list -%}
        {%- if not s is string and s is iterable and s|count > 0 -%}
          {% set good = s -%}
        {%- else -%}
          {% set good = [] -%}
        {%- endif %}
      {%- endif %}
      {%- if good is defined -%}
        {{ good }}
      {%- else -%}none{%- endif %}
  action:
    - alias: 'test if good list of device classes is valid'
      condition: template
      value_template: >-
        {{ good|lower != 'none' }}
    - alias: 'update binary_sensor.motion_device_class good attribute; remove: vibration'
      service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_class
        good: >-
          {{ good }}
    - alias: 'update binary_sensor.motion_device_vibration_exist'
      service: homeassistant.update_entity
      entity_id: binary_sensor.motion_device_vibration_exist

- id: motion_device_vibration_good_on
  alias: motion_device_vibration_good_on
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
  condition:
    condition: and
    conditions:
      - alias: 'test setup complete'
        condition: template
        value_template: >-
          {{ is_state('binary_sensor.motion_device_class','on') }}
      - alias: 'vibration good not on'
        condition: template
        value_template: >-
          {{ not 'vibration' in state_attr('binary_sensor.motion_device_class','good') }}
  variables:
    good: >-
      {% set s = state_attr('binary_sensor.motion_device_class','good') -%}
      {%- if s|lower != 'unknown' and s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
        {%- set good = (s|list + ['vibration'])|sort|unique|list -%}
      {%- else -%}
        {%- set good = ['vibration'] -%}
      {%- endif %}
      {%- if good is defined -%}
        {{ good }}
      {%- else -%}none{%- endif %}
  action:
    - alias: 'test if good list of device classes is valid'
      condition: template
      value_template: >-
        {{ good|lower != 'none' }}
    - alias: 'update binary_sensor.motion_device_class good attribute; add: vibration'
      service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_class
        good: >-
          {{ good }}
    - alias: 'update binary_sensor.motion_device_vibration_exist'
      service: homeassistant.update_entity
      entity_id: binary_sensor.motion_device_vibration_exist

- id: motion_device_vibration_alarm_good_on
  alias: motion_device_vibration_alarm_good_on
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
    - platform: template
      value_template: >-
        {{ not is_state_attr('binary_sensor.motion_device_vibration_exist','good','none')
           and is_state_attr('binary_sensor.motion_device_vibration_exist','good',states('binary_sensor.motion_device_vibration')) }}
  variables:
    devices: >-
      {% set devices = none %}
      {% set s = state_attr('binary_sensor.motion_device_vibration','devices') %}
      {% if s|lower != 'none' and s|lower != 'null' and not s is string and s is iterable and s|count > 0 %}
        {% set devices = s %}
      {%- endif %}
      {{ devices }}
    last: >-
      {% set last = none %}
      {% set s = state_attr('binary_sensor.motion_device_vibration','status')|selectattr('state','==',state_attr('binary_sensor.motion_device_vibration_exist','good'))|sort(attribute='updated',reverse=true)|list %}
      {% if s|lower != 'none' and s|lower != 'null' and not s is string and s is iterable and s|count > 0 %}
        {% set last = s %}
      {%- endif %}
      {{ last  }}
    which: >-
      {% set which = none %}
      {% if last|lower != 'none' %}
        {% set which = last|map(attribute='id')|first %}
      {%- endif %}
      {{ which }}
    did: >-
      {% set did = none %}
      {% if devices|lower != 'none'  and which|lower != 'none' %}
        {% set did = devices|selectattr('id','==',which)|map(attribute='did')|first %}
      {%- endif %}
      {{ did }}
    where: >-
      {% set where = none %}
      {% if devices|lower != 'none'  and which|lower != 'none' %}
        {% set where = devices|selectattr('id','==',which)|map(attribute='area')|first %}
      {%- endif %}
      {{ where }}
  condition:
    condition: and
    conditions:
      - condition: template
        value_template: >
          {{ which|lower != 'none' and which|lower != 'null' and which|length > 0 }}
  action:
    - variables:
        markdown: >-
          A <a href="/lovelace-primary/vibration">vibration</a> alarm was triggered ({{- state_attr('binary_sensor.motion_device_vibration_exist','good')|string -}}); sensor: {{ which -}}; where: {{ where -}}; at {{ utcnow().timestamp()|timestamp_custom("%a %b %d %I:%M %p %Z",true,'unknown') -}}.
    - service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_vibration_good
        markdown: >-
          {{ markdown }}
    - alias: 'update binary_sensor.motion_device_vibration which, where, did'
      service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_vibration
        which: >-
          {{ which }}
        where: >-
          {{ where }}
        did: >-
          {{ did }}
    - condition: template
      value_template: >
        {{ is_state_attr('binary_sensor.motion_device_vibration_exist','alarm','on') }}
    - alias: 'trigger motion notification'
      service: automation.trigger
      data_template:
        entity_id: automation.motion_device_vibration_good_notify

- id: motion_device_vibration_good_notify
  alias: motion_device_vibration_good_notify
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
  variables:
    class: >-
      {{ 'vibration' }}
    ids: >-
      {% set ids = state_attr('binary_sensor.motion_device_vibration','ids') %}
      {{ ids }}
    dids: >-
      {% set dids = none %}
      {% set s = state_attr('binary_sensor.motion_device_vibration','devices') %}
      {%- if not s is string and s is iterable and s|count > 0 -%}
        {%- set dids = s|map(attribute='did')|list -%}
      {%- endif -%}
      {{ dids }}
    which: >-
      {% set which = state_attr('binary_sensor.motion_device_vibration','which') %}
      {{ which }}
    where: >-
      {% set where = state_attr('binary_sensor.motion_device_vibration','where') %}
      {{ where }}
    did: >-
      {% set did = state_attr('binary_sensor.motion_device_vibration','did') %}
      {{ did }}
    action: >-
      {% set action = class + '-alarm' %}
      {{ action }}
    url: >-
      {% set url = '/lovelace-primary/' + class %}
      {{ url }}
    title: >-
      {% set title = states('sensor.motion_app') + ': vibration good' %}
      {{ title }}
    notification_id: >-
      {{ 'device-vibration-status-' + which|string }}
    icon: >-
      {% set icon = 'notification-vibration-good' %}
      {{ icon }}
    header: >-
      {{- '<img src="/local/images/icons/' + icon + '.png?v1">' -}}
    footer: >-
      <a href="/notify-devices/{{- class -}}"><img src="https://img.shields.io/badge/{{- class -}}-device-cyan.svg"></a>
      {%- if did is defined and did|lower != 'none' and did|lower != 'null' -%}
         <br><a href="/config/devices/device/{{- did -}}"><img src="https://img.shields.io/badge/{{- class -}}-config-blue.svg"></a>
      {%- endif -%}
    body: >-
      {{ state_attr('binary_sensor.motion_device_vibration_good','markdown') }}
      <p>We have notified <b>{{- state_attr('sensor.motion_role_primary','friendly_name')|replace("'","") -}}</b> about your vibration alarm.
      To inspect and change the alarm click the <b>{{- action -}}</b> button below.
      {{- '<p>' -}}
      <a href="{{- url -}}"><img src="https://img.shields.io/badge/{{- action -}}-yellow.svg"></a>
    message: >-
      {{- header -}}<hr>{{- body -}}<br>{{- footer -}}
  action:
    - alias: 'notify vibration sensor activated'
      service: persistent_notification.create
      data_template:
        title: >-
          {{- title -}}
        notification_id: >-
          {{- notification_id -}}
        message: >-
          {{- message -}}

## activity

- id: motion_device_vibration_activity
  alias: motion_device_vibration_activity
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
    - platform: state
      entity_id: 'binary_sensor.motion_device_vibration'
      attribute: 'activity'
  variables:
    ids: >-
      {% set s = state_attr('binary_sensor.motion_device_vibration','activity') %}
      {%- if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s is iterable -%}
        {{ s|map(attribute='id')|sort|unique|list }}
      {%- else -%}none{%- endif %}
    dids: >-
      {% set s = state_attr('binary_sensor.motion_device_vibration','activity') %}
      {%- if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s is iterable -%}
        {{ s|map(attribute='did')|sort|unique|list }}
      {%- else -%}none{%- endif %}
    details: >-
      {% set s = state_attr('binary_sensor.motion_device_vibration','activity') %}
      {%- if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' -%}
        {{ s }}
      {%- else -%}none{%- endif %}
    state: >-
      {% set s = state_attr('binary_sensor.motion_device_vibration','activity') %}
      {% if s|lower != 'null' and s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and not s is string and s is iterable %}
        {% if s|count > 0 %}on{% else %}off{% endif %}
      {% else %}unknown{% endif %}
    last: >-
      {% if state|lower == 'off' %}
        {{ state_attr('binary_sensor.motion_device_vibration_activity','last') }}
      {%- else -%}{{ utcnow().timestamp() }}{%- endif %}
    actions: >-
      {%- set actions = none -%}
      {%- set s = state_attr('binary_sensor.motion_device_vibration_activity','actions') -%}
      {%- if s|lower != 'none' and 'actions' in s -%}
        {%- set actions = s.actions -%}
      {%- endif -%}
      {{- actions -}}
    responses: >-
      {%- set responses = none -%}
      {%- set s = state_attr('binary_sensor.motion_device_vibration_activity','actions') -%}
      {%- if s|lower != 'none' and 'responses' in s -%}
        {%- set responses = s.responses -%}
      {%- endif -%}
      {{- responses -}}
    approve: >-
      {%- if actions|lower != 'none' and 'approve' in actions -%}
      {{ actions.approve }}
      {%- else -%}none{%- endif -%}
    deny: >-
      {%- if actions|lower != 'none' and 'deny' in actions -%}
      {{ actions.deny }}
      {%- else -%}none{%- endif -%}
    wait: >-
      {%- if actions|lower != 'none' and 'wait' in actions -%}
      {{ actions.wait }}
      {%- else -%}none{%- endif -%}
    help: >-
      {%- if actions|lower != 'none' and 'help' in actions -%}
      {{ actions.help }}
      {%- else -%}none{%- endif -%}
  condition:
    - condition: template
      value_template: >
        {{ state|lower != 'unknown' }}
  action:
    - service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_vibration_activity
        approve: >-
          {{ approve }}
        deny: >-
          {{ deny }}
        wait: >-
          {{ wait }}
        help: >-
          {{ help }}
        responses: >-
          {{ responses }}
        last: >-
          {{ last }}
        ids: >-
          {{ ids }}
        dids: >-
          {{ dids }}
        details: >-
          {{ details }}
        state: >-
          {{ state }}
    - condition: template
      value_template: >
        {{ state|lower == 'on' }}
    - alias: 'set vibration condition: activity'
      service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_vibration
        condition: >-
          {{ 'activity' }}

- id: motion_device_vibration_activity_off
  alias: motion_device_vibration_activity_off
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
  condition:
    condition: and
    conditions:
      - alias: 'test setup complete'
        condition: template
        value_template: >-
          {{ is_state('binary_sensor.motion_device_class','on') }}
      - alias: 'vibration activity not off'
        condition: template
        value_template: >-
          {{ 'vibration' in state_attr('binary_sensor.motion_device_class','activity') }}
  variables:
    activity: >-
      {% set s = state_attr('binary_sensor.motion_device_class','activity') -%}
      {%- if s|lower != 'unknown' and s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
        {%- set s = s|reject('==','vibration')|list -%}
        {%- if not s is string and s is iterable and s|count > 0 -%}
          {% set activity = s -%}
        {%- else -%}
          {% set activity = [] -%}
        {%- endif %}
      {%- endif %}
      {%- if activity is defined -%}
        {{ activity }}
      {%- else -%}none{%- endif %}
  action:
    - alias: 'test if activity list of device classes is valid'
      condition: template
      value_template: >-
        {{ activity|lower != 'none' }}
    - alias: 'update binary_sensor.motion_device_class activity attribute; remove: vibration'
      service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_class
        activity: >-
          {{ activity }}
    - alias: 'update binary_sensor.motion_device_vibration_exist'
      service: homeassistant.update_entity
      entity_id: binary_sensor.motion_device_vibration_exist

- id: motion_device_vibration_activity_on
  alias: motion_device_vibration_activity_on
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
  condition:
    condition: and
    conditions:
      - alias: 'test setup complete'
        condition: template
        value_template: >-
          {{ is_state('binary_sensor.motion_device_class','on') }}
      - alias: 'vibration activity not on'
        condition: template
        value_template: >-
          {{ not 'vibration' in state_attr('binary_sensor.motion_device_class','activity') }}
  variables:
    activity: >-
      {% set s = state_attr('binary_sensor.motion_device_class','activity') -%}
      {%- if s|lower != 'unknown' and s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
        {%- set activity = (s|list + ['vibration'])|sort|unique|list -%}
      {%- else -%}
        {%- set activity = ['vibration'] -%}
      {%- endif %}
      {%- if activity is defined -%}
        {{ activity }}
      {%- else -%}none{%- endif %}
  action:
    - alias: 'test if activity list of device classes is valid'
      condition: template
      value_template: >-
        {{ activity|lower != 'none' }}
    - alias: 'update binary_sensor.motion_device_class activity attribute; add: vibration'
      service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_class
        activity: >-
          {{ activity }}
    - alias: 'update binary_sensor.motion_device_vibration_exist'
      service: homeassistant.update_entity
      entity_id: binary_sensor.motion_device_vibration_exist

- id: motion_device_vibration_alarm_activity
  alias: motion_device_vibration_alarm_activity
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
    - platform: state
      entity_id: 'binary_sensor.motion_device_vibration_activity'
      to: 'on'
  variables:
    activity: >-
      {{ state_attr('binary_sensor.motion_device_vibration','activity') }}
  condition:
    condition: and
    conditions:
      - condition: template
        value_template: >
          {{ activity|lower != 'none' and activity|lower != 'null' and activity|length > 0 }}
  action:
    - variables:
        markdown: >-
          {%- if activity|lower != 'none' and not activity is string and activity is iterable and activity|count > 0 -%}
            A <a href="/lovelace-primary/vibration">vibration</a> <a href="/notify-activity">activity</a> alarm was triggered at {{ utcnow().timestamp()|timestamp_custom("%a %b %d %I:%M %p %Z",true,'unknown') }}:
            {%- for i in activity -%}
              {%- if loop.first -%}{{- '<ul>' -}}{%- endif -%}
              {%- set d = state_attr('binary_sensor.motion_device_class','devices') -%}
              {%- if d|lower != 'none' and d|lower != 'unknown' and not d is string and d is iterable and d|count > 0 -%}
                {%- set d = d|selectattr('name','==',i.name)|list -%}
                {%- if d|lower != 'none' and d|lower != 'unknown' and not d is string and d is iterable and d|count > 0 -%}
                  {%- set d = d|first -%}
                  <li><a href="/config/devices/device/{{- d.did -}}">{{- d.friendly_name|replace("'","") -}}</a> in <a href="/config/areas/area/{{- area_id(activity[loop.index-1].entity_id) -}})">{{- area_id(activity[loop.index-1].entity_id) -}}</a></li>
                {%- else -%}
                  <li>{{- i.id -}} is {{ states(i.entity_id) -}}</li>
                {%- endif -%}
              {%- else -%}
                <li>{{- i.id -}} is {{ states(i.entity_id) -}}</li>
              {%- endif -%}
              {%- if loop.last -%}{{- '</ul>' -}}{%- endif -%}
            {%- endfor -%}
          {%- else -%}
            {{- '<b>No vibration activity</b>' -}}
          {%- endif -%}
    - service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_vibration_activity
        markdown: >-
          {{ markdown }}
    - condition: template
      value_template: >
        {{ is_state_attr('binary_sensor.motion_device_vibration_exist','alarm','on') }}
    - alias: 'trigger motion notification'
      service: automation.trigger
      data_template:
        entity_id: automation.motion_device_vibration_activity_notify

- id: motion_device_vibration_activity_notify
  alias: motion_device_vibration_activity_notify
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
  variables:
    class: >-
      {{ 'vibration' }}
    activity: >-
      {% set activity = state_attr('binary_sensor.motion_device_vibration','activity') %}
      {{ activity }}
    action: >-
      {% set action = class + '-alarm' %}
      {{ action }}
    url: >-
      {% set url = '/notify-activity/' %}
      {{ url }}
    title: >-
      {% set title = states('sensor.motion_app') + ': vibration activity alarm' %}
      {{ title }}
    notification_id: >-
      {{ 'device-vibration-activity' }}
    icon: >-
      {% set icon = 'notification-vibration-activity' %}
      {{ icon }}
    header: >-
      {{- '<img src="/local/images/icons/' + icon + '.png?v1">' -}}
    footer: >-
      <a href="/notify-devices/{{- class -}}"><img src="https://img.shields.io/badge/{{- class -}}-device-cyan.svg"></a>
      {%- if did is defined and did|lower != 'none' and did|lower != 'null' -%}
         <br><a href="/config/devices/device/{{- did -}}"><img src="https://img.shields.io/badge/{{- class -}}-config-blue.svg"></a>
      {%- endif -%}
    body: >-
      {{ state_attr('binary_sensor.motion_device_class_vibration_activity','markdown') }}
      <p>We have notified <b>{{- state_attr('sensor.motion_role_primary','friendly_name')|replace("'","") -}}</b> about your vibration alarm.
      To inspect and change the alarm click the <b>{{- action -}}</b> button below.
      {{- '<p>' -}}
      <a href="{{- url -}}"><img src="https://img.shields.io/badge/{{- action -}}-yellow.svg"></a>
    message: >-
      {{- header -}}<hr>{{- body -}}<br>{{- footer -}}
  action:
    - alias: 'notify vibration sensor activated'
      service: persistent_notification.create
      data_template:
        title: >-
          {{- title -}}
        notification_id: >-
          {{- notification_id -}}
        message: >-
          {{- message -}}

## alarm (enabled w/o devices)

- id: motion_device_vibration_alarm_off
  alias: motion_device_vibration_alarm_off
  mode: single
  max_exceeded: 'warn'
  initial_state: 'on'
  trigger:
  condition:
    condition: and
    conditions:
      - alias: 'test setup complete'
        condition: template
        value_template: >-
          {{ is_state('binary_sensor.motion_device_class','on') }}
      - alias: 'vibration alarm not off'
        condition: template
        value_template: >-
          {{ 'vibration' in state_attr('binary_sensor.motion_device_class','alarm') }}
  variables:
    alarm: >-
      {% set s = state_attr('binary_sensor.motion_device_class','alarm') -%}
      {%- if s|lower != 'unknown' and s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
        {%- set s = s|reject('==','vibration')|list -%}
        {%- if not s is string and s is iterable and s|count > 0 -%}
          {% set alarm = s -%}
        {%- else -%}
          {% set alarm = [] -%}
        {%- endif %}
      {%- endif %}
      {%- if alarm is defined -%}
        {{ alarm }}
      {%- else -%}none{%- endif %}
  action:
    - alias: 'test if alarm list of device classes is valid'
      condition: template
      value_template: >-
        {{ alarm|lower != 'none' }}
    - alias: 'update binary_sensor.motion_device_class alarm attribute; remove: vibration'
      service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_class
        alarm: >-
          {{ alarm }}
    - alias: 'update binary_sensor.motion_device_vibration_exist'
      service: homeassistant.update_entity
      entity_id: binary_sensor.motion_device_vibration_exist

- id: motion_device_vibration_alarm_on
  alias: motion_device_vibration_alarm_on
  mode: single
  max_exceeded: 'warn'
  initial_state: 'on'
  trigger:
  condition:
    condition: and
    conditions:
      - alias: 'test setup complete'
        condition: template
        value_template: >-
          {{ is_state('binary_sensor.motion_device_class','on') }}
      - alias: 'vibration alarm not on'
        condition: template
        value_template: >-
          {{ not 'vibration' in state_attr('binary_sensor.motion_device_class','alarm') }}
  variables:
    alarm: >-
      {% set s = state_attr('binary_sensor.motion_device_class','alarm') -%}
      {%- if s|lower != 'unknown' and s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
        {%- set alarm = (s|list + ['vibration'])|sort|unique|list -%}
      {%- else -%}
        {%- set alarm = ['vibration'] -%}
      {%- endif %}
      {%- if alarm is defined -%}
        {{ alarm }}
      {%- else -%}none{%- endif %}
  action:
    - alias: 'test if alarm list of device classes is valid'
      condition: template
      value_template: >-
        {{ alarm|lower != 'none' }}
    - alias: 'update binary_sensor.motion_device_class alarm attribute; add: vibration'
      service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_class
        alarm: >-
          {{ alarm }}
    - alias: 'update binary_sensor.motion_device_vibration_exist'
      service: homeassistant.update_entity
      entity_id: binary_sensor.motion_device_vibration_exist

## toggle

- id: motion_device_vibration_turn_on
  alias: motion_device_vibration_turn_on
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
  condition:
    condition: and
    conditions:
      - condition: template
        value_template: >-
          {{ is_state('binary_sensor.motion_device_vibration_turn_on','on') }}
  variables:
    entity_ids: >-
      {{ state_attr('binary_sensor.motion_device_vibration_turn_on','entity_ids') }}
  action:
    - alias: 'turn_on vibration sensors'
      repeat:
        while:
          - condition: template
            value_template: >-
              {% set s = entity_ids[repeat.index - 1]|default('unknown') -%}
              {{ s|lower != 'unavailable' and s|lower != 'unknown' and s|lower != 'null' and s|string|length > 0 }}
        sequence:
          - service: switch.turn_on
            data_template:
              entity_id: >-
                {{ entity_ids[repeat.index - 1] }}

- id: motion_device_vibration_turn_off
  alias: motion_device_vibration_turn_off
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
  condition:
    condition: and
    conditions:
      - condition: template
        value_template: >-
          {{ is_state('binary_sensor.motion_device_vibration_turn_off','on') }}
  variables:
    entity_ids: >-
      {{ state_attr('binary_sensor.motion_device_vibration_turn_off','entity_ids') }}
  action:
    - alias: 'turn_off vibration sensors'
      repeat:
        while:
          - condition: template
            value_template: >-
              {% set s = entity_ids[repeat.index - 1]|default('unknown') -%}
              {{ s|lower != 'unavailable' and s|lower != 'unknown' and s|lower != 'null' and s|string|length > 0 }}
        sequence:
          - service: switch.turn_off
            data_template:
              entity_id: >-
                {{ entity_ids[repeat.index - 1] }}

- id: motion_device_vibration_toggle
  alias: motion_device_vibration_toggle
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
  condition:
    condition: and
    conditions:
      - condition: template
        value_template: >-
          {{ is_state('binary_sensor.motion_device_vibration_toggle','on') }}
  variables:
    entity_ids: >-
      {{ state_attr('binary_sensor.motion_device_vibration_toggle','entity_ids') }}
  action:
    - alias: 'toggle vibration sensors'
      repeat:
        while:
          - condition: template
            value_template: >-
              {% set s = entity_ids[repeat.index - 1]|default('unknown') -%}
              {{ s|lower != 'unavailable' and s|lower != 'unknown' and s|lower != 'null' and s|string|length > 0 }}
        sequence:
          - service: switch.toggle
            data_template:
              entity_id: >-
                {{ entity_ids[repeat.index - 1] }}

## update, restart, and other contols

- id: motion_device_vibration_self_test
  alias: motion_device_vibration_self_test
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
  condition:
    condition: and
    conditions:
      - alias: 'test if self_test is on'
        condition: template
        value_template: >-
          {{ is_state('binary_sensor.motion_device_vibration_self_test','on') }}
  variables:
    entity_ids: >-
      {{ state_attr('binary_sensor.motion_device_vibration_self_test','entity_ids') }}
  action:
    - alias: 'test vibration sensors'
      repeat:
        while:
          - alias: 'test if not done'
            condition: template
            value_template: >-
              {% set s = entity_ids[repeat.index - 1]|default('unknown') -%}
              {{ s|lower != 'unavailable' and s|lower != 'unknown' and s|lower != 'null' and s|string|length > 0 }}
        sequence:
          - alias: 'pressing button'
            service: button.press
            data_template:
              entity_id: >-
                {{ entity_ids[repeat.index - 1] }}

- id: motion_device_vibration_mute
  alias: motion_device_vibration_mute
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
  condition:
    condition: and
    conditions:
      - condition: template
        value_template: >-
          {{ is_state('binary_sensor.motion_device_vibration_mute','on') }}
  variables:
    entity_ids: >-
      {{ state_attr('binary_sensor.motion_device_vibration_mute','entity_ids') }}
  action:
    - alias: 'mute vibration sensors'
      repeat:
        while:
          - condition: template
            value_template: >-
              {% set s = entity_ids[repeat.index - 1]|default('unknown') -%}
              {{ s|lower != 'unavailable' and s|lower != 'unknown' and s|lower != 'null' and s|string|length > 0 }}
        sequence:
          - service: button.press
            data_template:
              entity_id: >-
                {{ entity_ids[repeat.index - 1] }}

- id: motion_device_vibration_unmute
  alias: motion_device_vibration_unmute
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
  condition:
    condition: and
    conditions:
      - condition: template
        value_template: >-
          {{ is_state('binary_sensor.motion_device_vibration_unmute','on') }}
  variables:
    entity_ids: >-
      {{ state_attr('binary_sensor.motion_device_vibration_unmute','entity_ids') }}
  action:
    - alias: 'unmute vibration sensors'
      repeat:
        while:
          - condition: template
            value_template: >-
              {% set s = entity_ids[repeat.index - 1]|default('unknown') -%}
              {{ s|lower != 'unavailable' and s|lower != 'unknown' and s|lower != 'null' and s|string|length > 0 }}
        sequence:
          - service: button.press
            data_template:
              entity_id: >-
                {{ entity_ids[repeat.index - 1] }}

- id: motion_device_vibration_restart
  alias: motion_device_vibration_restart
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
  condition:
    condition: and
    conditions:
      - condition: template
        value_template: >-
          {{ is_state('binary_sensor.motion_device_vibration_restart','on') }}
  variables:
    entity_ids: >-
      {{ state_attr('binary_sensor.motion_device_vibration_restart','entity_ids') }}
  action:
    - alias: 'restart vibration sensors'
      repeat:
        while:
          - condition: template
            value_template: >-
              {% set s = entity_ids[repeat.index - 1]|default('unknown') -%}
              {{ s|lower != 'unavailable' and s|lower != 'unknown' and s|lower != 'null' and s|string|length > 0 }}
        sequence:
          - service: button.press
            data_template:
              entity_id: >-
                {{ entity_ids[repeat.index - 1] }}

- id: motion_device_vibration_update
  alias: motion_device_vibration_update
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
  condition:
    condition: and
    conditions:
      - condition: template
        value_template: >-
          {{ is_state('binary_sensor.motion_device_vibration_update','on') }}
  variables:
    entity_ids: >-
      {{ state_attr('binary_sensor.motion_device_vibration_update','entity_ids') }}
  action:
    - alias: 'update vibration sensors'
      repeat:
        while:
          - condition: template
            value_template: >-
              {% set s = entity_ids[repeat.index - 1]|default('unknown') -%}
              {{ s|lower != 'unavailable' and s|lower != 'unknown' and s|lower != 'null' and s|string|length > 0 }}
        sequence:
          - service: button.press
            data_template:
              entity_id: >-
                {{ entity_ids[repeat.index - 1] }}

# found

- id: motion_device_vibration_found
  alias: motion_device_vibration_found
  mode: single
  max_exceeded: 'warn'
  initial_state: 'on'
  trigger:
    - platform: state
      entity_id: 'binary_sensor.motion_device_vibration_exist'
      to: 'on'
  variables:
    dids: >-
      {% set s = state_attr('binary_sensor.motion_device_vibration','found') %}
      {%- if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s is iterable -%}
        {{ s|map(attribute='did')|sort|unique|list }}
      {%- else -%}none{%- endif %}
    ids: >-
      {% set s = state_attr('binary_sensor.motion_device_vibration','found') %}
      {%- if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s is iterable -%}
        {{ s|map(attribute='id')|sort|unique|list }}
      {%- else -%}none{%- endif %}
    details: >-
      {% set s = state_attr('binary_sensor.motion_device_vibration','found') %}
      {%- if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s is iterable -%}
        "{{ s }}"
      {%- else -%}none{%- endif %}
    state: >-
      {% set s = state_attr('binary_sensor.motion_device_vibration','found') %}
      {% if s|lower != 'null' and s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and not s is string and s is iterable %}
        {% if s|count > 0 %}on{% else %}off{% endif %}
      {% else %}unknown{% endif %}
    last: >-
      {% if state|lower == 'off' %}
        {{ state_attr('binary_sensor.motion_device_vibration_found','last') }}
      {%- else -%}{{ utcnow().timestamp() }}{%- endif %}
    actions: >-
      {%- set actions = none -%}
      {%- set s = state_attr('binary_sensor.motion_device_vibration_actions','found') -%}
      {%- if s|lower != 'none' and 'actions' in s -%}
        {%- set actions = s.actions -%}
      {%- endif -%}
      {{- actions -}}
    responses: >-
      {%- set responses = none -%}
      {%- set s = state_attr('binary_sensor.motion_device_vibration_actions','found') -%}
      {%- if s|lower != 'none' and 'responses' in s -%}
        {%- set responses = s.responses -%}
      {%- endif -%}
      {{- responses -}}
    approve: >-
      {%- if actions|lower != 'none' and 'approve' in actions -%}
      {{ actions.approve }}
      {%- else -%}none{%- endif -%}
    deny: >-
      {%- if actions|lower != 'none' and 'deny' in actions -%}
      {{ actions.deny }}
      {%- else -%}none{%- endif -%}
    wait: >-
      {%- if actions|lower != 'none' and 'wait' in actions -%}
      {{ actions.wait }}
      {%- else -%}none{%- endif -%}
    help: >-
      {%- if actions|lower != 'none' and 'help' in actions -%}
      {{ actions.help }}
      {%- else -%}none{%- endif -%}
  condition:
    - condition: template
      value_template: >
        {{ state|lower != 'unknown' }}
  action:
    - service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_vibration_found
        approve: >-
          {{ approve }}
        deny: >-
          {{ deny }}
        wait: >-
          {{ wait }}
        help: >-
          {{ help }}
        responses: >-
          {{ responses }}
        last: >-
          {{ last }}
        dids: >-
          {{ dids }}
        ids: >-
          {{ ids }}
        details: >-
          {{ details }}
        state: >-
          {{ state }}
    - condition: template
      value_template: >
        {{ state|lower == 'on' }}
    - alias: 'set vibration condition: found'
      service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_vibration
        condition: >-
          {{ 'found' }}

- id: motion_device_vibration_devices
  alias: motion_device_vibration_devices
  mode: queued
  initial_state: 'on'
  trigger:
    - platform: state
      entity_id: binary_sensor.motion_device_vibration
      attribute: 'devices'
  variables:
    devices: >-
      {% set devices = [] -%}
      {%- set s = state_attr('binary_sensor.motion_device_vibration','devices') -%}
      {%- if s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
        {%- set devices = s -%}
      {%- endif -%}
      {{ devices }}
    dids: >-
      {% set dids = [] -%}
      {%- if devices|lower != 'none' and devices|count > 0 -%}
        {%- set s = devices|map(attribute='did')|list -%}
        {%- if s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
          {%- set dids = s|sort|unique|list -%}
        {%- endif -%}
      {%- endif -%}
      {{ dids }}
    shellys: >-
      {% set shellys = [] -%}
      {%- if devices|lower != 'none' and devices|count > 0 -%}
        {%- set s = devices|selectattr('manufacturer','==','Shelly')|map(attribute='did')|list -%}
        {%- if s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
          {%- set shellys = s|sort|unique|list -%}
        {%- endif -%}
      {%- endif -%}
      {{ shellys }}
    old: >-
      {% set old = [] %}
      {% set s = trigger.to_state.state %}
      {%- if s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
        {% set old = s|sort|unique|list %}
      {% endif %}
      {{ old }}
  condition:
    condition: and
    conditions:
      - alias: 'test if no change in device ids'
        condition: template
        value_template: >-
          {{ old != dids }}
      - alias: 'test for device ids'
        condition: template
        value_template: >-
          {{ dids|lower != 'none' and not dids is string and dids is iterable and dids|count > 0 }}
  action:
    - alias: 'process all vibration devices by device id'
      repeat:
        while:
          - alias: 'test if not done'
            condition: template
            value_template: >-
              {% set s = dids[repeat.index - 1]|default('none') %}
              {{ s|lower != 'none' }}
        sequence:
          - variables:
              did: >-
                {{ dids[repeat.index - 1]|default('none') }}
              sensor: >-
                {% set sensor = 'binary_sensor.motion_devices_' + did|string %}
                {{ sensor }}
              manufacturer: >-
                {% set manufacturer = device_attr(did,'manufacturer') %}
                {{ manufacturer }}
              model: >-
                {% set model = device_attr(did,'model') %}
                {{ model }}
              name: >-
                {% set name = device_attr(did,'name') %}
                {{ name }}
              url: >-
                {% set url = device_attr(did,'configuration_url') %}
                {{ url }}
              config: >-
                {% set config = [] %}
                {% if did in shellys %}
                  {% set cloud= url|string + '/settings/cloud?enabled=false' %}
                  {% set coiot = url|string + '/settings?coiot_enable=true&coiot_peer=' + states('sensor.host_lan_ipv4') %}
                  {% set config = [cloud,coiot] %}
                {% endif %}
                {{ config }}
              state: >-
                {% if not did in shellys %}on{% else %}unknown{% endif %}
          - alias: 'test if device sensor already in vibration group'
            condition: template
            value_template: >-
              {% set s = state_attr('group.motion_devices','entity_id') %}
              {{ s|lower == 'none' or ( not s is string and s is iterable and not sensor in s ) }}
          - alias: 'create vibration device sensor'
            service: python_script.set_state
            data_template:
              allow_create: true
              entity_id: >-
                {{  sensor }}
              manufacturer: >-
                {{- manufacturer -}}
              model: >-
                {{- model -}}
              name: >-
                {{- name -}}
              url: >-
                {{- url -}}
              config: >-
                {{- config -}}
              state: >-
                {{- state -}}
          - alias: 'add to group'
            service: group.set
            data_template:
              object_id: motion_devices
              name: 'All devices'
              add_entities: >-
                {{ sensor }}

## notifications

- id: motion_device_vibration_found_notify
  alias: motion_device_vibration_found_notify
  mode: single
  max_exceeded: 'warn'
  initial_state: 'on'
  trigger:
    - platform: state
      entity_id: binary_sensor.motion_device_vibration_found
      to: 'on'
  variables:
    class: >-
      {{- 'vibration' -}}
    ids: >-
      {% set ids = state_attr('binary_sensor.motion_device_vibration','ids') -%}
      {{ ids }}
    devices: >-
      {% set devices = state_attr('binary_sensor.motion_device_vibration','devices') -%}
      {{ devices }}
    title: >-
      {% set title = states('sensor.motion_app') + ': vibration sensor found' -%}
      {{ title }}
    notification_id: >-
      {{ 'device-' + class|string + '-notify' }}
    icon: >-
      {% set icon = 'notification-' + class|string + '-found' -%}
      {{ icon }}
    header: >-
      {{- '<img src="/local/images/icons/' + icon + '.png?v1">' -}}
    footer: >-
      <a href="/notify-devices/{{- class -}}"><img src="https://img.shields.io/badge/{{- class -}}-device-cyan.svg"></a>
      {%- if did is defined and did|lower != 'none' and did|lower != 'null' -%}
         <br><a href="/config/devices/device/{{- did -}}"><img src="https://img.shields.io/badge/{{- class -}}-config-blue.svg"></a>
      {%- endif -%}
    body: >-
      {%- if devices|lower != 'none' -%}
        Sensor(s) for vibration found at {{ utcnow().timestamp()|timestamp_custom("%a %b %d %I:%M %p %Z",true,'unknown') -}}
        {%- for i in devices -%}
         {%- if loop.first -%}<ul>{%- endif -%}
          <li><a href="/config/devices/device/{{- i.did -}}">{{- i.friendly_name|replace("'","") -}}</a> in <a href="/config/areas/area/{{- i.area -}}">{{- i.area -}}</a></li>
         {%- if loop.last -%}</ul>{%- endif -%}
        {%- endfor -%}
      {%- else -%}
        <b>INVALID</b>: no devices with vibration sensors at {{ utcnow().timestamp()|timestamp_custom("%a %b %d %I:%M %p %Z",true,'unknown') }}
      {%- endif -%}
    message: >-
      {{- header -}}<hr>{{- body -}}<br>{{- footer -}}
  action:
    - alias: 'notify found vibration sensor'
      service: persistent_notification.create
      data_template:
        title: >-
          {{- title -}}
        notification_id: >-
          {{- notification_id -}}
        message: >-
          {{- message -}}

- id: motion_device_vibration_found_dismiss
  alias: motion_device_vibration_found_dismiss
  mode: single
  max_exceeded: 'warn'
  initial_state: 'on'
  trigger:
    - platform: state
      entity_id: binary_sensor.motion_device_vibration_exist
      to: 'on'
      for:
        minutes: >-
          {{ states('input_number.motion_device_class_dismiss')|int(5) }}
  variables:
    notification_id: >-
      {{ 'device-vibration-notify' }}
  action:
    - alias: 'dismiss vibration found notification'
      service: persistent_notification.dismiss
      data_template:
        notification_id: >-
          {{ notification_id }}

# cleared notify

- id: motion_device_vibration_cleared_notify
  alias: motion_device_vibration_cleared_notify
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
    - platform: template
      value_template: >-
        {{ is_state_attr('binary_sensor.motion_device_vibration_exist','good',states('binary_sensor.motion_device_vibration')) }}
  variables:
    class: >-
      {{- 'vibration' -}}
    which: >-
      {% set which = none %}
      {% set s = state_attr('binary_sensor.motion_device_vibration','status')|selectattr('state','==','off')|sort(attribute='updated',reverse=true)|list %}
      {% if s|lower != 'none' and s|lower != 'null' and not s is string and s is iterable and s|count > 0 %}
        {% set which = s|map(attribute='id')|first  %}
      {%- endif %}
      {{ which }}
    notification_id: >-
      {{ 'device-vibration-status-' + which|string }}
    icon: >-
      {% set icon = 'notification-vibration-good' %}
      {{ icon }}
    title: >-
      {% set title = states('sensor.motion_app') + ': vibration good (' + state_attr('binary_sensor.motion_device_vibration_exist','good')|string + ')' %}
      {{ title }}
    header: >-
      {{- '<img src="/local/images/icons/' + icon + '.png?v1">' -}}
    footer: >-
      <a href="/notify-devices/{{- class -}}"><img src="https://img.shields.io/badge/{{- class -}}-device-cyan.svg"></a>
      {%- if did is defined and did|lower != 'none' and did|lower != 'null' -%}
         <br><a href="/config/devices/device/{{- did -}}"><img src="https://img.shields.io/badge/{{- class -}}-config-blue.svg"></a>
      {%- endif -%}
    body: >-
      The <a href="/notify-devices/vibration">vibration</a> alarm has cleared; sensor: {{ which -}}.
    message: >-
      {{- header -}}{<hr>{- body -}}{{- footer -}}
  action:
    - condition: template
      value_template: >
        {{ which|lower != 'none' and is_state_attr('binary_sensor.motion_device_vibration_exist','alarm','on') }}
    - alias: 'notify vibration sensor activated'
      service: persistent_notification.create
      data_template:
        title: >-
          {{- title -}}
        notification_id: >-
          {{- notification_id -}}
        message: >-
          {{- message -}}

# cleared dismissed

- id: motion_device_vibration_cleared_dismiss
  alias: motion_device_vibration_cleared_dismiss
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
    - platform: template
      value_template: >-
        {{ is_state_attr('binary_sensor.motion_device_vibration_exist','good',states('binary_sensor.motion_device_vibration')) }}
      for:
        minutes: >-
          {{ states('input_number.motion_device_class_dismiss')|int(5) }}
  variables:
    ids: >-
      {%- set ids = state_attr('binary_sensor.motion_device_vibration','ids') -%}
      {%- if ids|lower != 'none' and not ids is string and ids is iterable and ids|count > 0 -%}
        [{%- for i in ids -%}
          {%- if not loop.first -%},{%- endif -%}
          "{{- 'device-vibration-status-' + i|string -}}"
        {%- endfor -%}]
      {%- else -%}none{%- endif %}
  condition:
    condition: and
    conditions:
      - condition: template
        value_template: >
          {{ ids|lower != 'none' and ids is iterable and ids|count > 0 }}
  action:
    - alias: 'dismiss vibration status'
      repeat:
        while:
          - alias: 'test if not done'
            condition: template
            value_template: >-
              {%- set s = ids[repeat.index - 1]|default('unknown') %}
              {{ s|lower != 'unavailable' and s|lower != 'unknown' and s|lower != 'null' and s|string|length > 0 }}
        sequence:
          - alias: 'dismissing persistent notification'
            service: persistent_notification.dismiss
            data_template:
              notification_id: >-
                {{ ids[repeat.index - 1] }}

# activity,unavailable

- id: motion_device_vibration_unavailable
  alias: motion_device_vibration_unavailable
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
    - platform: state
      entity_id: 'binary_sensor.motion_device_vibration_exist'
      to: 'on'
    - platform: state
      entity_id: 'binary_sensor.motion_device_vibration'
      attribute: 'unavailable'
    - platform: time_pattern
      minutes: '/5'
  variables:
    dids: >-
      {% set s = state_attr('binary_sensor.motion_device_vibration','unavailable') %}
      {%- if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s is iterable -%}
        {{ s|map(attribute='did')|sort|unique|list }}
      {%- else -%}none{%- endif %}
    ids: >-
      {% set s = state_attr('binary_sensor.motion_device_vibration','unavailable') %}
      {%- if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s is iterable -%}
        {{ s|map(attribute='id')|sort|unique|list }}
      {%- else -%}none{%- endif %}
    details: >-
      {% set s = state_attr('binary_sensor.motion_device_vibration','unavailable') -%}
        {%- if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s is iterable -%}
          { {%- for i in s|map(attribute='id')|sort|unique -%}
            {%- if not loop.first -%},{%- endif -%}
            "{{ i }}":
            [{%- for j in s|selectattr('id','==',i)|map(attribute='entity_id') -%}
              {%- if not loop.first -%},{%- endif -%}
              "{{- state_attr(j,'friendly_name') -}}"
            {%- endfor -%}]
          {% endfor %} }
        {%- else -%}none{%- endif %}
    state: >-
      {% set s = state_attr('binary_sensor.motion_device_vibration','unavailable') %}
      {% if s|lower != 'null' and s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and not s is string and s is iterable %}
        {% if s|count > 0 %}on{% else %}off{% endif %}
      {% else %}unknown{% endif %}
    last: >-
      {% if state|lower == 'off' %}
        {{ state_attr('binary_sensor.motion_device_vibration_unavailable','last') }}
      {%- else -%}{{ utcnow().timestamp() }}{%- endif %}
    markdown: >-
      {%- if details|lower != 'none' -%}
        {%- for i in details -%}
          {%- if loop.first -%}<ul>{%- endif -%}
          {{- '<li>' -}}
          {%- set e = state_attr('binary_sensor.motion_device_vibration','status') -%}
          {%- if e|lower != 'none' and not e is string and e is iterable and e|count > 0 -%}
            {%- set e = e|selectattr('id','==',i)|list -%}
            {%- if e|lower != 'none' and not e is string and e is iterable and e|count > 0 -%}
              {%- set e = e|first -%}
              {%- set did = device_id(e.entity_id) -%}
              <a href="/config/devices/device/{{- did -}}"><b>{{- device_attr(device_id(e.entity_id),'name') -}}</b></a><br><i>Last updated</i>: {{- e.updated|as_datetime|relative_time }} ago
              {%- for j in details[i] -%}
                {%- if loop.first -%}<ul>{%- endif -%}
                <li>{{- j -}}</li>
                {%- if loop.last %}</ul>{% endif -%}
              {%- endfor -%}
              {{- '</li>' -}}
              {%- if loop.last %}</ul>{% endif -%}
            {%- else -%}
              <li>{{ i }} is {{ states(i.entity_id) }}</li>
            {%- endif -%}
          {%- else -%}
            <li>{{ i }} is {{ states(i.entity_id) }}</li>
          {%- endif -%}
        {%- endfor -%}
      {%- else -%}
        <b>No unavailable vibration sensors</b> at {{ utcnow().timestamp()|timestamp_custom("%a %b %d %I:%M %p %Z",true,'unknown') -}}
      {%- endif %}
    actions: >-
      {%- set actions = none -%}
      {%- set s = state_attr('binary_sensor.motion_device_vibration_actions','unavailable') -%}
      {%- if s|lower != 'none' and 'actions' in s -%}
        {%- set actions = s.actions -%}
      {%- endif -%}
      {{- actions -}}
    responses: >-
      {%- set responses = none -%}
      {%- set s = state_attr('binary_sensor.motion_device_vibration_actions','unavailable') -%}
      {%- if s|lower != 'none' and 'responses' in s -%}
        {%- set responses = s.responses -%}
      {%- endif -%}
      {{- responses -}}
    approve: >-
      {%- if actions|lower != 'none' and 'approve' in actions -%}
      {{ actions.approve }}
      {%- else -%}none{%- endif -%}
    deny: >-
      {%- if actions|lower != 'none' and 'deny' in actions -%}
      {{ actions.deny }}
      {%- else -%}none{%- endif -%}
    wait: >-
      {%- if actions|lower != 'none' and 'wait' in actions -%}
      {{ actions.wait }}
      {%- else -%}none{%- endif -%}
    help: >-
      {%- if actions|lower != 'none' and 'help' in actions -%}
      {{ actions.help }}
      {%- else -%}none{%- endif -%}
  condition:
    - condition: template
      value_template: >
        {{ state|lower != 'unknown' }}
  action:
    - service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_vibration_unavailable
        last: >-
          {{ last }}
        ids: >-
          {{ ids }}
        dids: >-
          {{ dids }}
        approve: >-
          {{ approve }}
        deny: >-
          {{ deny }}
        wait: >-
          {{ wait }}
        help: >-
          {{ help }}
        responses: >-
          {{ responses }}
        details: >-
          {{ details }}
        markdown: >-
          {{ markdown }}
        state: >-
          {{ state }}
    - condition: template
      value_template: >
        {{ state|lower == 'on' }}
    - alias: 'set vibration condition: unavailable'
      service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_vibration
        condition: >-
          {{ 'unavailable' }}

# missing

- id: motion_device_vibration_missing
  alias: motion_device_vibration_missing
  mode: single
  max_exceeded: 'warn'
  initial_state: 'on'
  trigger:
    - platform: state
      entity_id: binary_sensor.motion_device_vibration_exist
      to: 'off'
    - platform: template
      value_template: >-
        {{ is_state_attr('automation.motion_device_vibration_rescan','current',0)
           and is_state_attr('binary_sensor.motion_device_vibration_exist','required','on') }}
  variables:
    state: >-
      {% set state = 'off' %}
      {% if is_state('binary_sensor.motion_device_vibration_exist','off')
       and is_state_attr('automation.motion_device_vibration_rescan','current',0)
       and is_state_attr('binary_sensor.motion_device_vibration_exist','required','on') %}
        {% set state = 'on' %}
      {% endif %}
      {{ state }}
    last: >-
      {% set last = state_attr('binary_sensor.motion_device_vibration_missing','last') %}
      {% if state|lower == 'off' or last|lower == 'none' %}
        {% set last = utcnow().timestamp() %}
      {% endif %}
      {{ last }}
    actions: >-
      {%- set actions = none -%}
      {%- set s = state_attr('binary_sensor.motion_device_vibration_missing','actions') -%}
      {%- if s|lower != 'none' and 'actions' in s -%}
        {%- set actions = s.actions -%}
      {%- endif -%}
      {{- actions -}}
    responses: >-
      {%- set responses = none -%}
      {%- set s = state_attr('binary_sensor.motion_device_vibration_missing','actions') -%}
      {%- if s|lower != 'none' and 'responses' in s -%}
        {%- set responses = s.responses -%}
      {%- endif -%}
      {{- responses -}}
    approve: >-
      {%- if actions|lower != 'none' and 'approve' in actions -%}
      {{ actions.approve }}
      {%- else -%}none{%- endif -%}
    deny: >-
      {%- if actions|lower != 'none' and 'deny' in actions -%}
      {{ actions.deny }}
      {%- else -%}none{%- endif -%}
    wait: >-
      {%- if actions|lower != 'none' and 'wait' in actions -%}
      {{ actions.wait }}
      {%- else -%}none{%- endif -%}
    help: >-
      {%- if actions|lower != 'none' and 'help' in actions -%}
      {{ actions.help }}
      {%- else -%}none{%- endif -%}
  condition:
    - condition: template
      value_template: >
        {{ state|lower != 'unknown' }}
    - condition: template
      value_template: >
        {{ is_state('binary_sensor.motion_device_vibration_exist','off') }}
  action:
    - service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_vibration_missing
        approve: >-
          {{ approve }}
        deny: >-
          {{ deny }}
        wait: >-
          {{ wait }}
        help: >-
          {{ help }}
        responses: >-
          {{ responses }}
        last: >-
          {{ last }}
        markdown: >-
          No vibration sensors have been found at {{ utcnow().timestamp()|timestamp_custom("%a %b %d %I:%M %p %Z",true,'unknown') -}};
          {%- if last|as_datetime|relative_time|lower != 'none' -%}
            for {{ last|as_datetime|relative_time -}};
          {%- endif -%}
          please setup the integration by clicking <a href="/config/integrations">here</a>.
        state: >-
          {{ state }}
    - condition: template
      value_template: >
        {{ state|lower == 'on' }}
    - alias: 'set vibration condition: missing'
      service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_vibration
        condition: >-
          {{ 'missing' }}

- id: motion_device_vibration_missing_notify
  alias: motion_device_vibration_missing_notify
  mode: single
  max_exceeded: 'warn'
  initial_state: 'on'
  trigger:
    - platform: state
      entity_id: binary_sensor.motion_device_vibration_missing
      to: 'on'
  variables:
    class: >-
      {{ 'vibration' }}
    ip: >-
      {{ none }}
    latitude: >-
      {{ none }}
    longitude: >-
      {{ none }}
    notification_id: >-
      {{ 'device-vibration-missing' }}
    icon: >-
      {% set icon = 'notification-vibration-missing' %}
      {{ icon }}
    header: >-
      {{- '<img src="/local/images/icons/' + icon + '.png?v1">' -}}
    footer: >-
      <br><a href="/lovelace-primary/{{- class -}}"><img src="https://img.shields.io/badge/{{- class -}}-missing-orange.svg"></a>
      <br><a href="/notify-devices/{{- class -}}"><img src="https://img.shields.io/badge/{{- class -}}-device-cyan.svg"></a>
      {%- if did is defined and did|lower != 'none' and did|lower != 'null' -%}
         <br><a href="/config/devices/device/{{- did -}}"><img src="https://img.shields.io/badge/{{- class -}}-config-blue.svg"></a>
      {%- endif -%}
    title: >-
      {% set title = states('sensor.motion_app') + ': vibration sensor missing' %}
      {{ title }}
    body: >-
      {{ state_attr('binary_sensor.motion_device_vibration_missing','markdown') }}
    message: >-
      {{- header -}}<hr>{{- body -}}<p>{{- footer -}}
  action:
    - alias: 'notify missing vibration sensor'
      service: persistent_notification.create
      data_template:
        title: >-
          {{- title -}}
        notification_id: >-
          {{- notification_id -}}
        message: >-
          {{- message -}}

- id: motion_device_vibration_missing_dismiss
  alias: motion_device_vibration_missing_dismiss
  mode: single
  max_exceeded: 'warn'
  initial_state: 'on'
  trigger:
  variables:
    class: >-
      {{ 'vibration' }}
    notification_id: >-
      {{ 'device-' + class + '-missing' }}
  action:
    - alias: 'dismiss vibration missing notification'
      service: persistent_notification.dismiss
      data_template:
        notification_id: >-
          {{ notification_id }}

- id: motion_device_vibration_activity_dismiss
  alias: motion_device_vibration_activity_dismiss
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
  variables:
    class: >-
      {{ 'vibration' }}
    notification_id: >-
      {{ 'device-' + class + '-activity' }}
  action:
    - alias: 'dismiss vibration activity notification'
      service: persistent_notification.dismiss
      data_template:
        notification_id: >-
          {{ notification_id }}

## high_spike

- id: motion_device_vibration_high_spike
  alias: motion_device_vibration_high_spike
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
    - platform: state
      entity_id: 'binary_sensor.motion_device_vibration'
      attribute: 'high_spike'
  variables:
    details: >-
      {% if state_attr('binary_sensor.motion_device_vibration_exist','high_spike')|lower != 'none' %}
        {% set s = state_attr('binary_sensor.motion_device_vibration','high_spike') %}
        {%- if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s is iterable -%}
          [{% for i in s -%}
            {%- if not loop.first -%},{%- endif -%}
            "{{- state_attr(i.entity_id,'friendly_name') }} is {{ states(i.entity_id)|int('unknown') -}}{{ state_attr(i.entity_id,'unit_of_measurement') -}}"
          {%- endfor -%}]
        {%- else -%}none{%- endif %}
      {%- else -%}none{%- endif %}
    state: >-
      {% set s = state_attr('binary_sensor.motion_device_vibration','high_spike') %}
      {% if s|lower != 'null' and s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and not s is string and s is iterable %}
        {% if s|list|count > 0 %}on{% else %}off{% endif %}
      {% else %}unknown{% endif %}
    last: >-
      {% if state|lower == 'off' %}
        {{ state_attr('binary_sensor.motion_device_vibration_high_spike','last') }}
      {%- else -%}{{ utcnow().timestamp() }}{%- endif %}
  condition:
    - condition: template
      value_template: >
        {{ state|lower != 'unknown' }}
  action:
    - service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_vibration_high_spike
        last: >-
          {{ last }}
        details: >-
          {{ details }}
        state: >-
          {{ state }}
    - condition: template
      value_template: >
        {{ state|lower == 'on' }}
    - alias: 'set vibration condition: high_spike'
      service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_vibration
        condition: >-
          {{ 'high_spike' }}

- id: motion_device_vibration_high_spike_off
  alias: motion_device_vibration_high_spike_off
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
  condition:
    condition: and
    conditions:
      - alias: 'test setup complete'
        condition: template
        value_template: >-
          {{ is_state('binary_sensor.motion_device_class','on') }}
      - alias: 'vibration high_spike not off'
        condition: template
        value_template: >-
          {{ 'vibration' in state_attr('binary_sensor.motion_device_class','high_spike') }}
  variables:
    high_spike: >-
      {% set s = state_attr('binary_sensor.motion_device_class','high_spike') -%}
      {%- if s|lower != 'unknown' and s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
        {%- set s = s|reject('==','vibration')|list -%}
        {%- if not s is string and s is iterable and s|count > 0 -%}
          {% set high_spike = s -%}
        {%- else -%}
          {% set high_spike = [] -%}
        {%- endif %}
      {%- endif %}
      {%- if high_spike is defined -%}
        {{ high_spike }}
      {%- else -%}none{%- endif %}
  action:
    - alias: 'test if high_spike list of device classes is valid'
      condition: template
      value_template: >-
        {{ high_spike|lower != 'none' }}
    - alias: 'update binary_sensor.motion_device_class high_spike attribute; remove: vibration'
      service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_class
        high_spike: >-
          {{ high_spike }}
    - alias: 'update binary_sensor.motion_device_vibration_exist'
      service: homeassistant.update_entity
      entity_id: binary_sensor.motion_device_vibration_exist

- id: motion_device_vibration_high_spike_on
  alias: motion_device_vibration_high_spike_on
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
  condition:
    condition: and
    conditions:
      - alias: 'test setup complete'
        condition: template
        value_template: >-
          {{ is_state('binary_sensor.motion_device_class','on') }}
      - alias: 'vibration high_spike not on'
        condition: template
        value_template: >-
          {{ not 'vibration' in state_attr('binary_sensor.motion_device_class','high_spike') }}
  variables:
    high_spike: >-
      {% set s = state_attr('binary_sensor.motion_device_class','high_spike') -%}
      {%- if s|lower != 'unknown' and s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
        {%- set high_spike = (s|list + ['vibration'])|sort|unique|list -%}
      {%- else -%}
        {%- set high_spike = ['vibration'] -%}
      {%- endif %}
      {%- if high_spike is defined -%}
        {{ high_spike }}
      {%- else -%}none{%- endif %}
  action:
    - alias: 'test if high_spike list of device classes is valid'
      condition: template
      value_template: >-
        {{ high_spike|lower != 'none' }}
    - alias: 'update binary_sensor.motion_device_class high_spike attribute; add: vibration'
      service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_class
        high_spike: >-
          {{ high_spike }}
    - alias: 'update binary_sensor.motion_device_vibration_exist'
      service: homeassistant.update_entity
      entity_id: binary_sensor.motion_device_vibration_exist

- id: motion_device_vibration_alarm_high_spike_on
  alias: motion_device_vibration_alarm_high_spike_on
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
    - platform: state
      entity_id: 'binary_sensor.motion_device_vibration_high_spike'
      to: 'on'
  variables:
    limit: >-
      {{ state_attr('sensor.motion_device_vibration_high_spike','limit') }})
    high_spike: >-
      {{ state_attr('binary_sensor.motion_device_vibration','high_spike') }}
  condition:
    condition: and
    conditions:
      - condition: template
        value_template: >
          {{- high_spike|lower != 'none' and high_spike|lower != 'unknown' and not high_spike is string and high_spike is iterable and high_spike|count > 0 -}}
      - condition: template
        value_template: >
          {{- limit|lower != 'none' and limit|lower != 'unknown' and limit is number -}}
  action:
    - variables:
        markdown: >-
          The <a href="/lovelace-primary/vibration">vibration</a> spike alarm was triggered (above {{ limit -}}) at {{ utcnow().timestamp()|timestamp_custom("%a %b %d %I:%M %p %Z",true,'unknown') }}:<br>
          {%- for i in high_spike if 'id' in i and i.id|lower != 'none' -%}
            {%- if loop.first -%}{{- '<ul>' -}}{%- endif -%}
            {%- set d = state_attr('binary_sensor.motion_device_class','devices') -%}
            {%- if d|lower != 'none' and not d is string and d is iterable and d|count > 0 -%}
              {%- set d = d|selectattr('id','==',i.id)|list -%}
              {%- if d|lower != 'none' and not d is string and d is iterable and d|count > 0 -%}
                {%- set d = d|first -%}
                <li><a href="/config/devices/device/{{- d.did -}}">{{- d.friendly_name|replace("'","") -}}</a> is {{ states(high_spike[loop.index-1].entity_id) -}}{{- state_attr(high_spike[loop.index-1].entity_id,'unit_of_measurement') }} in <a href="/config/areas/area/{{- area_id(high_spike[loop.index-1].entity_id) -}}">{{- area_id(high_spike[loop.index-1].entity_id) -}}</a></li>
              {%- else -%}
                <li>{{- i.id }} vibration is {{ states(i.entity_id) -}}{{- state_attr(i.entity_id,'unit_of_measurement') }}</li>
              {%- endif -%}
            {%- else -%}
              {{- '<li>' -}}
              {{- i.id }} vibration is {{ states(i.entity_id) -}}{{- state_attr(i.entity_id,'unit_of_measurement') }}
              {{- '</li>' -}}
            {%- endif -%}
            {%- if loop.last -%}{{- '</ul>' -}}{%- endif -%}
          {%- endfor -%}
    - service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_vibration_high_spike
        markdown: >-
          {{ markdown }}
    - condition: template
      value_template: >
        {{ is_state_attr('binary_sensor.motion_device_vibration_exist','alarm','on') }}
    - alias: 'trigger motion notification'
      service: automation.trigger
      data_template:
        entity_id: automation.motion_device_vibration_high_spike_notify

- id: motion_device_vibration_alarm_high_spike_off
  alias: motion_device_vibration_alarm_high_spike_off
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
    - platform: state
      entity_id: 'binary_sensor.motion_device_vibration_high_spike'
      to: 'off'
  action:
    - condition: template
      value_template: >
        {{ is_state_attr('binary_sensor.motion_device_vibration_exist','alarm','on') }}
    - alias: 'trigger vibration high_spike dismiss'
      service: automation.trigger
      data_template:
        entity_id: automation.motion_device_vibration_high_spike_dismiss

- id: motion_device_vibration_high_spike_notify
  alias: motion_device_vibration_high_spike_notify
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
  variables:
    class: >-
      {{ 'vibration' }}
    high_spike: >-
      {% set high_spike = state_attr('binary_sensor.motion_device_vibration','high_spike') %}
      {{ high_spike }}
    action: >-
      {% set action = class + '-alarm' %}
      {{ action }}
    url: >-
      {% set url = '/lovelace-primary/' + class %}
      {{ url }}
    title: >-
      {% set title = states('sensor.motion_app') + ': ' + class + ' high_spike' %}
      {{ title }}
    notification_id: >-
      {{ 'device-' + class + '-high_spike' }}
    icon: >-
      {% set icon = 'notification-' + class + '-high_spike' %}
      {{ icon }}
    header: >-
      {{- '<img src="/local/images/icons/' + icon + '.png?v1">' -}}
    footer: >-
      <a href="/notify-devices/{{- class -}}"><img src="https://img.shields.io/badge/{{- class -}}-device-cyan.svg"></a>
      {%- if did is defined and did|lower != 'none' and did|lower != 'null' -%}
         <br><a href="/config/devices/device/{{- did -}}"><img src="https://img.shields.io/badge/{{- class -}}-config-blue.svg"></a>
      {%- endif -%}
    body: >-
      {{ state_attr('binary_sensor.motion_device_vibration_high_spike','markdown') }}
      <p>We have notified <b>{{- state_attr('sensor.motion_role_primary','friendly_name')|replace("'","") -}}</b> about your vibration alarm.
      To inspect and change the alarm click the <b>{{- action -}}</b> button below.
      {{- '<p>' -}}
      <a href="{{- url -}}"><img src="https://img.shields.io/badge/{{- action -}}-yellow.svg"></a>
    message: >-
      {{- header -}}<hr>{{- body -}}<br>{{- footer -}}
  action:
    - alias: 'notify vibration sensor activated'
      service: persistent_notification.create
      data_template:
        title: >-
          {{- title -}}
        notification_id: >-
          {{- notification_id -}}
        message: >-
          {{- message -}}

- id: motion_device_vibration_high_spike_dismiss
  alias: motion_device_vibration_high_spike_dismiss
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
  variables:
    class: >-
      {{ 'vibration' }}
    notification_id: >-
      {{ 'device-' + class + '-high_spike' }}
  action:
    - alias: 'dismiss vibration high_spike notification'
      service: persistent_notification.dismiss
      data_template:
        notification_id: >-
          {{ notification_id }}

## low_spike

- id: motion_device_vibration_low_spike
  alias: motion_device_vibration_low_spike
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
    - platform: state
      entity_id: 'binary_sensor.motion_device_vibration'
      attribute: 'low_spike'
  variables:
    details: >-
      {% if state_attr('binary_sensor.motion_device_vibration_exist','low_spike')|lower != 'none' %}
        {% set s = state_attr('binary_sensor.motion_device_vibration','low_spike') %}
        {%- if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s is iterable -%}
          [{% for i in s -%}
            {%- if not loop.first -%},{%- endif -%}
            "{{- state_attr(i.entity_id,'friendly_name') }} is {{ states(i.entity_id)|int('unknown') -}}{{ state_attr(i.entity_id,'unit_of_measurement') -}}"
          {%- endfor -%}]
        {%- else -%}none{%- endif %}
      {%- else -%}none{%- endif %}
    state: >-
      {% set s = state_attr('binary_sensor.motion_device_vibration','low_spike') %}
      {% if s|lower != 'null' and s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and not s is string and s is iterable %}
        {% if s|list|count > 0 %}on{% else %}off{% endif %}
      {% else %}unknown{% endif %}
    last: >-
      {% if state|lower == 'off' %}
        {{ state_attr('binary_sensor.motion_device_vibration_low_spike','last') }}
      {%- else -%}{{ utcnow().timestamp() }}{%- endif %}
  condition:
    - condition: template
      value_template: >
        {{ state|lower != 'unknown' }}
  action:
    - service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_vibration_low_spike
        last: >-
          {{ last }}
        details: >-
          {{ details }}
        state: >-
          {{ state }}
    - condition: template
      value_template: >
        {{ state|lower == 'on' }}
    - alias: 'set vibration condition: low_spike'
      service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_vibration
        condition: >-
          {{ 'low_spike' }}

- id: motion_device_vibration_low_spike_off
  alias: motion_device_vibration_low_spike_off
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
  condition:
    condition: and
    conditions:
      - alias: 'test setup complete'
        condition: template
        value_template: >-
          {{ is_state('binary_sensor.motion_device_class','on') }}
      - alias: 'vibration low_spike not off'
        condition: template
        value_template: >-
          {{ 'vibration' in state_attr('binary_sensor.motion_device_class','low_spike') }}
  variables:
    low_spike: >-
      {% set s = state_attr('binary_sensor.motion_device_class','low_spike') -%}
      {%- if s|lower != 'unknown' and s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
        {%- set s = s|reject('==','vibration')|list -%}
        {%- if not s is string and s is iterable and s|count > 0 -%}
          {% set low_spike = s -%}
        {%- else -%}
          {% set low_spike = [] -%}
        {%- endif %}
      {%- endif %}
      {%- if low_spike is defined -%}
        {{ low_spike }}
      {%- else -%}none{%- endif %}
  action:
    - alias: 'test if low_spike list of device classes is valid'
      condition: template
      value_template: >-
        {{ low_spike|lower != 'none' }}
    - alias: 'update binary_sensor.motion_device_class low_spike attribute; remove: vibration'
      service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_class
        low_spike: >-
          {{ low_spike }}
    - alias: 'update binary_sensor.motion_device_vibration_exist'
      service: homeassistant.update_entity
      entity_id: binary_sensor.motion_device_vibration_exist

- id: motion_device_vibration_low_spike_on
  alias: motion_device_vibration_low_spike_on
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
  condition:
    condition: and
    conditions:
      - alias: 'test setup complete'
        condition: template
        value_template: >-
          {{ is_state('binary_sensor.motion_device_class','on') }}
      - alias: 'vibration low_spike not on'
        condition: template
        value_template: >-
          {{ not 'vibration' in state_attr('binary_sensor.motion_device_class','low_spike') }}
  variables:
    low_spike: >-
      {% set s = state_attr('binary_sensor.motion_device_class','low_spike') -%}
      {%- if s|lower != 'unknown' and s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
        {%- set low_spike = (s|list + ['vibration'])|sort|unique|list -%}
      {%- else -%}
        {%- set low_spike = ['vibration'] -%}
      {%- endif %}
      {%- if low_spike is defined -%}
        {{ low_spike }}
      {%- else -%}none{%- endif %}
  action:
    - alias: 'test if low_spike list of device classes is valid'
      condition: template
      value_template: >-
        {{ low_spike|lower != 'none' }}
    - alias: 'update binary_sensor.motion_device_class low_spike attribute; add: vibration'
      service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_class
        low_spike: >-
          {{ low_spike }}
    - alias: 'update binary_sensor.motion_device_vibration_exist'
      service: homeassistant.update_entity
      entity_id: binary_sensor.motion_device_vibration_exist

- id: motion_device_vibration_alarm_low_spike_on
  alias: motion_device_vibration_alarm_low_spike_on
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
    - platform: state
      entity_id: 'binary_sensor.motion_device_vibration_low_spike'
      to: 'on'
  variables:
    low_spike: >-
      {{ state_attr('binary_sensor.motion_device_vibration','low_spike') }}
    limit: >-
      {{ state_attr('sensor.motion_device_vibration_low_spike','limit') }})
  condition:
    condition: and
    conditions:
      - condition: template
        value_template: >
          {{- limit|lower != 'none' and limit|lower != 'unknown' and limit is number -}}
      - condition: template
        value_template: >
          {{- low_spike|lower != 'none' and low_spike|lower != 'unknown' and not low_spike is string and low_spike is iterable and low_spike|count > 0 -}}
  action:
    - variables:
        markdown: >-
          The <a href="/lovelace-primary/vibration">vibration</a> spike alarm was triggered (below {{ limit -}}) at {{ utcnow().timestamp()|timestamp_custom("%a %b %d %I:%M %p %Z",true,'unknown') }}:<br>
          {%- for i in low_spike if 'id' in i and i.id|lower != 'none' -%}
            {%- if loop.first -%}{{- '<ul>' -}}{%- endif -%}
            {%- set d = state_attr('binary_sensor.motion_device_class','devices') -%}
            {%- if d|lower != 'none' and not d is string and d is iterable and d|count > 0 -%}
              {%- set d = d|selectattr('id','==',i.id)|list -%}
              {%- if d|lower != 'none' and not d is string and d is iterable and d|count > 0 -%}
                {%- set d = d|first -%}
                <li><a href="/config/devices/device/{{- d.did -}}">{{- d.friendly_name|replace("'","") -}}</a> is {{ states(low_spike[loop.index-1].entity_id) -}}{{- state_attr(low_spike[loop.index-1].entity_id,'unit_of_measurement') }} in <a href="/config/areas/area/{{- area_id(low_spike[loop.index-1].entity_id) -}}">{{- area_id(low_spike[loop.index-1].entity_id) -}}</a></li>
              {%- else -%}
                <li>{{- i.id }} vibration is {{ states(i.entity_id) -}}{{- state_attr(i.entity_id,'unit_of_measurement') }}</li>
              {%- endif -%}
            {%- else -%}
              {{- '<li>' -}}
              {{- i.id }} vibration is {{ states(i.entity_id) -}}{{- state_attr(i.entity_id,'unit_of_measurement') }}
              {{- '</li>' -}}
            {%- endif -%}
            {%- if loop.last -%}{{- '</ul>' -}}{%- endif -%}
          {%- endfor -%}
    - service: python_script.set_state
      data_template:
        entity_id: binary_sensor.motion_device_vibration_low_spike
        markdown: >-
          {{ markdown }}
    - condition: template
      value_template: >
        {{ is_state_attr('binary_sensor.motion_device_vibration_exist','alarm','on') }}
    - alias: 'trigger motion notification'
      service: automation.trigger
      data_template:
        entity_id: automation.motion_device_vibration_low_spike_notify

- id: motion_device_vibration_alarm_low_spike_off
  alias: motion_device_vibration_alarm_low_spike_off
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
    - platform: state
      entity_id: 'binary_sensor.motion_device_vibration_low_spike'
      to: 'off'
  action:
    - condition: template
      value_template: >
        {{ is_state_attr('binary_sensor.motion_device_vibration_exist','alarm','on') }}
    - alias: 'trigger vibration low_spike dismiss'
      service: automation.trigger
      data_template:
        entity_id: automation.motion_device_vibration_low_spike_dismiss

- id: motion_device_vibration_low_spike_notify
  alias: motion_device_vibration_low_spike_notify
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
  variables:
    class: >-
      {{ 'vibration' }}
    low_spike: >-
      {% set low_spike = state_attr('binary_sensor.motion_device_vibration','low_spike') %}
      {{ low_spike }}
    action: >-
      {% set action = class + '-alarm' %}
      {{ action }}
    url: >-
      {% set url = '/lovelace-primary/' + class %}
      {{ url }}
    title: >-
      {% set title = states('sensor.motion_app') + ': ' + class + ' low_spike' %}
      {{ title }}
    notification_id: >-
      {{ 'device-' + class + '-low_spike' }}
    icon: >-
      {% set icon = 'notification-' + class + '-low_spike' %}
      {{ icon }}
    header: >-
      {{- '<img src="/local/images/icons/' + icon + '.png?v1">' -}}
    footer: >-
      <a href="/notify-devices/{{- class -}}"><img src="https://img.shields.io/badge/{{- class -}}-device-cyan.svg"></a>
      {%- if did is defined and did|lower != 'none' and did|lower != 'null' -%}
         <br><a href="/config/devices/device/{{- did -}}"><img src="https://img.shields.io/badge/{{- class -}}-config-blue.svg"></a>
      {%- endif -%}
    body: >-
      {{ state_attr('binary_sensor.motion_device_vibration_low_spike','markdown') }}
      <p>We have notified <b>{{- state_attr('sensor.motion_role_primary','friendly_name')|replace("'","") -}}</b> about your vibration alarm.
      To inspect and change the alarm click the <b>{{- action -}}</b> button below.
      {{- '<p>' -}}
      <a href="{{- url -}}"><img src="https://img.shields.io/badge/{{- action -}}-yellow.svg"></a>
    message: >-
      {{- header -}}<hr>{{- body -}}<br>{{- footer -}}
  action:
    - alias: 'notify vibration sensor activated'
      service: persistent_notification.create
      data_template:
        title: >-
          {{- title -}}
        notification_id: >-
          {{- notification_id -}}
        message: >-
          {{- message -}}

- id: motion_device_vibration_low_spike_dismiss
  alias: motion_device_vibration_low_spike_dismiss
  mode: single
  max_exceeded: 'warn'
  initial_state: 'off'
  trigger:
  variables:
    class: >-
      {{ 'vibration' }}
    notification_id: >-
      {{ 'device-' + class + '-low_spike' }}
  action:
    - alias: 'dismiss vibration low_spike notification'
      service: persistent_notification.dismiss
      data_template:
        notification_id: >-
          {{ notification_id }}

### responses

- id: motion_device_vibration_response
  alias: motion_device_vibration_response
  mode: single
  max_exceeded: 'warn'
  initial_state: 'on'
  trigger:
    - platform: event
      event_type: mobile_app_notification_action
      event_data:
        action:
          - "device-vibration-bad-wait"
          - "device-vibration-bad-help"
          - "device-vibration-bad-deny"
          - "device-vibration-bad-approve"
          - "device-vibration-good-wait"
          - "device-vibration-good-help"
          - "device-vibration-good-deny"
          - "device-vibration-good-approve"
          - "device-vibration-high-wait"
          - "device-vibration-high-help"
          - "device-vibration-high-deny"
          - "device-vibration-high-approve"
          - "device-vibration-low-wait"
          - "device-vibration-low-help"
          - "device-vibration-low-deny"
          - "device-vibration-low-approve"
  variables:
    event: >-
      {% set event = none -%}
      {%- if trigger|lower != 'none' -%}
        {%- set event = trigger.event|default('none') -%}
      {%- endif -%}
      {{- event }}
    action: >-
      {% set action = none -%}
      {% if event|lower != 'none' and 'data' in event %}
        {% set action = event.data.action|default('none') %}
      {%- endif -%}
      {{- action }}
    condition: >-
      {% set condition = none -%}
      {% if action|lower != 'none' %}
        {% set condition = action|replace('device-vibration-','')|regex_replace('-.*','') %}
      {%- endif -%}
      {{- condition }}
    response: >-
      {% set response = none -%}
      {% if condition|lower != 'none' %}
        {% set response = action|replace('device-vibration-' + condition|string + '-','') %}
      {%- endif -%}
      {{- response }}
    origin: >-
      {% if trigger|lower != 'none' %}
        {{ trigger.event.origin|default(none) }}
      {% else %}none{% endif %}
    time_fired: >-
      {% if trigger|lower != 'none' %}
        {{ trigger.event.time_fired|default(none) }}
      {% else %}none{% endif %}
    context: >-
      {% if trigger|lower != 'none' %}
        {{ trigger.event.context }}
      {% else %}none{% endif %}
    responses: >-
      {%- set responses = none -%}
      {%- if condition|lower != 'none' and condition|string|length > 0 -%}
        {%- set s = state_attr('binary_sensor.motion_device_vibration_' + condition|string,'responses') -%}
        {%- if s|lower != 'none' -%}
          {%- set responses = s -%}
        {%- endif -%}
      {%- endif -%}
      {{- responses -}}
    actions: >-
      {%- set actions = none -%}
      {%- if condition|lower != 'none' and condition|string|length > 0 -%}
        {%- set s = state_attr('binary_sensor.motion_device_vibration_' + condition|string,'actions') -%}
        {%- if not s is string and s is iterable and 'did' in s -%}
          {%- set actions = s -%}
        {%- endif -%}
      {%- endif -%}
      {{- actions -}}
  action:
    - choose:
        - conditions:
            - condition: template
              value_template: >
                {{ states('sensor.motion_log_level') in state_attr('sensor.motion_log_level','debug') }}
          sequence:
            - service: persistent_notification.create
              data_template:
                title: >-
                  DEBUG: Device vibration response
                notification_id: >-
                  {{ 'device-vibration-response' }}
                message: >-
                  Response <b>received</b>: "{{ response -}}"; condition: {{ condition|string -}}; context: {{ context|string -}}; at: {{ time_fired }}
    - choose:
        - alias: 'test if response in responses'
          conditions:
            - alias: 'test if response is valid'
              condition: template
              value_template: >
                {{ response|lower != 'none' and response|string|length > 0 }}
            - alias: 'test if responses are valid'
              condition: template
              value_template: >
                {{ responses|lower != 'none' and not responses is string and responses is iterable and responses|count > 0 }}
            - alias: 'test if response in responses'
              condition: template
              value_template: >
                {{ response|lower in responses }}
          sequence:
            - alias: 'set device_vibration: timestamp, when'
              variables:
                timestamp: >-
                  {{ utcnow().timestamp() }}
                when: >-
                  {{ timestamp|timestamp_custom("%a %b %d %I:%M %p %Z",true,'unknown') }}
                response: >-
                  {{ response }}
            - choose:
                - conditions:
                    - condition: template
                      value_template: >
                        {{ states('sensor.motion_log_level') in state_attr('sensor.motion_log_level','info') }}
                  sequence:
                    - service: persistent_notification.create
                      data_template:
                        title: >-
                          INFO: Device vibration updated
                        notification_id: >-
                          {{ 'device-vibration-response' }}
                        message: >-
                          Device vibration <b>updated</b> to <code>{{- response -}}</code> at <i>{{- when -}}</i>
            - alias: 'set binary_sensor.motion_device_vibration to new state w/ condition, response, actions, timestamp, when'
              service: python_script.set_state
              data_template:
                entity_id: binary_sensor.motion_device_vibration
                timestamp: >-
                  {{ timestamp }}
                when: >-
                  {{ when }}
                actions: >-
                  {{ actions }}
                condition: >-
                  {{ condition }}
                response: >-
                  {{ response }}
            - alias: 'trigger automation for vibration approve'
              service: automation.trigger
              data_template:
                entity_id: >-
                  {{ 'automation.motion_device_vibration_' + response|string }}

##

- id: motion_device_vibration_approve
  alias: motion_device_vibration_approve
  mode: single
  max_exceeded: 'warn'
  initial_state: 'on'
  trigger:
  variables:
    condition: >-
      {% set condition = state_attr('binary_sensor.motion_device_vibration','condition') %}
      {{ condition }}
    response: >-
      {% set response = 'approve' %}
      {{ response }}
    actions: >-
      {% set actions = state_attr('binary_sensor.motion_device_vibration_alarm_exist','actions') %}
      {{ actions }}
    function: >-
      {% set function = state_attr('sensor.motion_device_vibration_approve','function') %}
      {{- function -}}
    timestamp: >-
      {{ utcnow().timestamp() }}
    when: >-
      {{ timestamp|timestamp_custom("%a %b %d %I:%M %p %Z",true,'unknown') }}
  action:
    - choose:
        - conditions:
            - condition: template
              value_template: >
                {{ states('sensor.motion_log_level') in state_attr('sensor.motion_log_level','debug') }}
          sequence:
            - service: persistent_notification.create
              data_template:
                title: >-
                  DEBUG: vibration response: {{ response }}
                notification_id: >-
                  {{ 'device-vibration-response' }}
                message: >-
                  Device vibration response: <b>{{- response -}}</b> at <i>{{- when -}}</i>; Condition: {{ condition -}}; Trigger: {{ function -}}
    - alias: 'function not none'
      condition: template
      value_template: >-
        {{ function|lower != 'none' }}
    - alias: 'trigger automation for vibration approve'
      service: automation.trigger
      data_template:
        entity_id: >-
          {{ function }}

- id: motion_device_vibration_deny
  alias: motion_device_vibration_deny
  mode: single
  max_exceeded: 'warn'
  initial_state: 'on'
  trigger:
  variables:
    condition: >-
      {% set condition = state_attr('binary_sensor.motion_device_vibration','condition') %}
      {{ condition }}
    response: >-
      {% set response = 'deny' %}
      {{ response }}
    actions: >-
      {% set actions = state_attr('binary_sensor.motion_device_vibration_alarm_exist','actions') %}
      {{ actions }}
    function: >-
      {% set function = state_attr('sensor.motion_device_vibration_deny','function') %}
      {{- function -}}
    timestamp: >-
      {{ utcnow().timestamp() }}
    when: >-
      {{ timestamp|timestamp_custom("%a %b %d %I:%M %p %Z",true,'unknown') }}
  action:
    - choose:
        - conditions:
            - condition: template
              value_template: >
                {{ states('sensor.motion_log_level') in state_attr('sensor.motion_log_level','debug') }}
          sequence:
            - service: persistent_notification.create
              data_template:
                title: >-
                  DEBUG: vibration response: {{ response }}
                notification_id: >-
                  {{ 'device-vibration-response' }}
                message: >-
                  Device vibration response: <b>{{- response -}}</b> at <i>{{- when -}}</i>; Condition: {{ condition -}}; Trigger: {{ function -}}
    - alias: 'function not none'
      condition: template
      value_template: >-
        {{ function|lower != 'none' }}
    - alias: 'trigger automation for vibration deny'
      service: automation.trigger
      data_template:
        entity_id: >-
          {{ function }}

- id: motion_device_vibration_wait
  alias: motion_device_vibration_wait
  mode: single
  max_exceeded: 'warn'
  initial_state: 'on'
  trigger:
  variables:
    condition: >-
      {% set condition = state_attr('binary_sensor.motion_device_vibration','condition') %}
      {{ condition }}
    response: >-
      {% set response = 'wait' %}
      {{ response }}
    actions: >-
      {% set actions = state_attr('binary_sensor.motion_device_vibration_alarm_exist','actions') %}
      {{ actions }}
    function: >-
      {% set function = state_attr('sensor.motion_device_vibration_wait','function') %}
      {{- function -}}
    timestamp: >-
      {{ utcnow().timestamp() }}
    when: >-
      {{ timestamp|timestamp_custom("%a %b %d %I:%M %p %Z",true,'unknown') }}
  action:
    - choose:
        - conditions:
            - condition: template
              value_template: >
                {{ states('sensor.motion_log_level') in state_attr('sensor.motion_log_level','debug') }}
          sequence:
            - service: persistent_notification.create
              data_template:
                title: >-
                  DEBUG: vibration response: {{ response }}
                notification_id: >-
                  {{ 'device-vibration-response' }}
                message: >-
                  Device vibration response: <b>{{- response -}}</b> at <i>{{- when -}}</i>; Condition: {{ condition -}}; Trigger: {{ function -}}
    - alias: 'function not none'
      condition: template
      value_template: >-
        {{ function|lower != 'none' }}
    - alias: 'trigger automation for vibration wait'
      service: automation.trigger
      data_template:
        entity_id: >-
          {{ function }}

- id: motion_device_vibration_help
  alias: motion_device_vibration_help
  mode: single
  max_exceeded: 'warn'
  initial_state: 'on'
  trigger:
  variables:
    condition: >-
      {% set condition = state_attr('binary_sensor.motion_device_vibration','condition') %}
      {{ condition }}
    response: >-
      {% set response = 'help' %}
      {{ response }}
    actions: >-
      {% set actions = state_attr('binary_sensor.motion_device_vibration_alarm_exist','actions') %}
      {{ actions }}
    function: >-
      {% set function = state_attr('sensor.motion_device_vibration_help','function') %}
      {{- function -}}
    timestamp: >-
      {{ utcnow().timestamp() }}
    when: >-
      {{ timestamp|timestamp_custom("%a %b %d %I:%M %p %Z",true,'unknown') }}
  action:
    - choose:
        - conditions:
            - condition: template
              value_template: >
                {{ states('sensor.motion_log_level') in state_attr('sensor.motion_log_level','debug') }}
          sequence:
            - service: persistent_notification.create
              data_template:
                title: >-
                  DEBUG: vibration response: {{ response }}
                notification_id: >-
                  {{ 'device-vibration-response' }}
                message: >-
                  Device vibration response: <b>{{- response -}}</b> at <i>{{- when -}}</i>; Condition: {{ condition -}}; Trigger: {{ function -}}
    - alias: 'function not none'
      condition: template
      value_template: >-
        {{ function|lower != 'none' }}
    - alias: 'function automation for vibration help'
      service: automation.trigger
      data_template:
        entity_id: >-
          {{ function }}
