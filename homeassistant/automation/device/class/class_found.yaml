###
# homeassistant/automation/device/class/class_found.yaml
###

- id: motion_device_class_found_on
  alias: motion_device_class_found_on
  mode: single
  initial_state: 'on'
  trigger:
    - platform: state
      entity_id: 'binary_sensor.motion_device_class_found'
      to: 'on'
      from:
        - 'off'
        - 'unknown'
  action:
    - alias: 'trigger device_class_found_notify'
      service: automation.trigger
      entity_id: automation.motion_device_class_found_notify

- id: motion_device_class_found_response
  alias: motion_device_class_found_response
  mode: single
  initial_state: 'on'
  trigger:
    - platform: event
      event_type: mobile_app_notification_action
      event_data:
        action: "device-found-wait"
    - platform: event
      event_type: mobile_app_notification_action
      event_data:
        action: "device-found-help"
    - platform: event
      event_type: mobile_app_notification_action
      event_data:
        action: "device-found-deny"
    - platform: event
      event_type: mobile_app_notification_action
      event_data:
        action: "device-found-approve"
  variables:
    origin: >-
      {% if trigger|lower != 'none' %}
        {{ trigger.event.origin|default(none) }}
      {% else %}none{% endif %}
    time_fired: >-
      {% if trigger|lower != 'none' %}
        {{ trigger.event.time_fired|default(none) }}
      {% else %}none{% endif %}
    context: >-
      {% if trigger|lower != 'none' %}
        {{ trigger.event.context }}
      {% else %}none{% endif %}
    responses: >-
      {%- set response = none -%}
      {%- set s = state_attr('binary_sensor.motion_device_class_found','responses') -%}
      {%- if s|lower != 'none' -%}
        {%- set response = s -%}
      {%- endif -%}
      {{- responses -}}
    actions: >-
      {%- set actions = none -%}
      {%- set s = state_attr('binary_sensor.motion_device_class_found','actions') -%}
      {%- if s|lower != 'none' and 'id' in s -%}
        {%- set actions = s -%}
      {%- endif -%}
      {{- actions -}}
    response: >-
      {% if trigger|lower != 'none' and actions|lower != 'none' %}
        {{ trigger.event.data.action|default(none)|string|replace(actions.id|string + '-','') }}
      {% else %}
        {{- state_attr('binary_sensor.motion_device_class_found','response') -}}
      {% endif %}
  action:
    - choose:
        - conditions:
            - condition: template
              value_template: >
                {{ states('input_select.motion_log_level') in ['debug','info'] }}
          sequence:
            - service: persistent_notification.create
              data_template:
                title: >-
                  INFO: Device found response
                notification_id: >-
                  {{ 'device-found-response' }}
                message: >-
                  Response <b>received</b>: "{{ response -}}"; context: {{ context|string -}}; at: {{ time_fired }}
    - choose:
        - alias: 'test if response in responses'
          conditions:
            - condition: template
              value_template: >
                {{ response|lower in responses }}
          sequence:
            - alias: 'set device_class_found: timestamp, when'
              variables:
                timestamp: >-
                  {{ utcnow().timestamp() }}
                when: >-
                  {{ timestamp|timestamp_custom("%a %b %d %I:%M %p %Z",true,'unknown') }}
                response: >-
                  {{ response }}
            - choose:
                - conditions:
                    - condition: template
                      value_template: >
                        {{ states('input_select.motion_log_level') in ['debug','info','notice','alert','critical'] }}
                  sequence:
                    - service: persistent_notification.create
                      data_template:
                        title: >-
                          ALERT: Missing device updated
                        notification_id: >-
                          {{ 'device-found-response' }}
                        message: >-
                          Missing device <b>updated</b> to <code>{{- response -}}</code> at <i>{{- when -}}</i>
            - alias: 'set sensor.motion_restart_notify to new state w/ timestamp, when'
              service: python_script.set_state
              data_template:
                entity_id: binary_sensor.motion_device_class_found
                timestamp: >-
                  {{ timestamp }}
                when: >-
                  {{ when }}
                response: >-
                  {{ response }}

##

- id: motion_device_class_found_approve
  alias: motion_device_class_found_approve
  mode: single
  initial_state: 'on'
  trigger:
    - platform: state
      entity_id: 'binary_sensor.motion_device_class_found'
      attribute: 'response'
      to: 'approve'
  variables:
    timestamp: >-
      {{ utcnow().timestamp() }}
    when: >-
      {{ timestamp|timestamp_custom("%a %b %d %I:%M %p %Z",true,'unknown') }}
    response: >-
      {{ 'approve' }}
    actions: >-
      {{ state_attr('binary_sensor.motion_device_class_found','actions') }}
  action:
    - choose:
        - conditions:
            - condition: template
              value_template: >
                {{ states('input_select.motion_log_level') in ['debug','info'] }}
          sequence:
            - service: persistent_notification.create
              data_template:
                title: >-
                  INFO: Missing device: {{ response }}
                notification_id: >-
                  {{ 'device-found-response' }}
                message: >-
                  Missing device response: "{{- response -}}" at <i>{{- when -}}</i>

- id: motion_device_class_found_deny
  alias: motion_device_class_found_deny
  mode: single
  initial_state: 'on'
  trigger:
    - platform: state
      entity_id: 'binary_sensor.motion_device_class_found'
      attribute: 'response'
      to: 'deny'
  variables:
    timestamp: >-
      {{ utcnow().timestamp() }}
    when: >-
      {{ timestamp|timestamp_custom("%a %b %d %I:%M %p %Z",true,'unknown') }}
    response: >-
      {{ 'deny' }}
    actions: >-
      {{ state_attr('binary_sensor.motion_device_class_found','actions') }}
  action:
    - choose:
        - conditions:
            - condition: template
              value_template: >
                {{ states('input_select.motion_log_level') in ['debug','info'] }}
          sequence:
            - service: persistent_notification.create
              data_template:
                title: >-
                  INFO: Missing device: {{ response }}
                notification_id: >-
                  {{ 'device-found-response' }}
                message: >-
                  Missing device response: "{{- response -}}" at <i>{{- when -}}</i>

- id: motion_device_class_found_wait
  alias: motion_device_class_found_wait
  mode: single
  initial_state: 'on'
  trigger:
    - platform: state
      entity_id: 'binary_sensor.motion_device_class_found'
      attribute: 'response'
      to: 'wait'
  variables:
    timestamp: >-
      {{ utcnow().timestamp() }}
    when: >-
      {{ timestamp|timestamp_custom("%a %b %d %I:%M %p %Z",true,'unknown') }}
    response: >-
      {{ 'wait' }}
    actions: >-
      {{ state_attr('binary_sensor.motion_device_class_found','actions') }}
  action:
    - choose:
        - conditions:
            - condition: template
              value_template: >
                {{ states('input_select.motion_log_level') in ['debug','info'] }}
          sequence:
            - service: persistent_notification.create
              data_template:
                title: >-
                  INFO: Missing device: {{ response }}
                notification_id: >-
                  {{ 'device-found-response' }}
                message: >-
                  Missing device response: "{{- response -}}" at <i>{{- when -}}</i>

- id: motion_device_class_found_help
  alias: motion_device_class_found_help
  mode: single
  initial_state: 'on'
  trigger:
    - platform: state
      entity_id: 'binary_sensor.motion_device_class_found'
      attribute: 'response'
      to: 'help'
  variables:
    timestamp: >-
      {{ utcnow().timestamp() }}
    when: >-
      {{ timestamp|timestamp_custom("%a %b %d %I:%M %p %Z",true,'unknown') }}
    response: >-
      {{ 'help' }}
    actions: >-
      {{ state_attr('binary_sensor.motion_device_class_found','actions') }}
  action:
    - choose:
        - conditions:
            - condition: template
              value_template: >
                {{ states('input_select.motion_log_level') in ['debug','info'] }}
          sequence:
            - service: persistent_notification.create
              data_template:
                title: >-
                  INFO: Missing device: {{ response }}
                notification_id: >-
                  {{ 'device-found-response' }}
                message: >-
                  Missing device response: "{{- response -}}" at <i>{{- when -}}</i>
