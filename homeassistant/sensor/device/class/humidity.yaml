###
# homeassistant/sensor/device/class/humidity.yaml
###

- platform: template
  sensors:
    motion_device_humidity:
      friendly_name: Measurement humidity
      device_class: 'humidity'
      unit_of_measurement: '%'
      attribute_templates:
        ids: >-
          {{- state_attr('binary_sensor.motion_device_humidity','ids') -}}
        total: >-
          {{- state_attr('binary_sensor.motion_device_humidity','total') -}}
        measurement: >-
          {{- state_attr('binary_sensor.motion_device_humidity','status') -}}
        areas: >-
          {{- state_attr('binary_sensor.motion_device_humidity','areas') -}}
        values: >-
          {{- state_attr('binary_sensor.motion_device_humidity','values') -}}
        min: >-
          {% set s = state_attr('binary_sensor.motion_device_humidity','values') -%}
          {% if s|lower != 'none' and not s is string %}
            {% set s = s|select('number')|list|default(none) %}
            {% if s|lower != 'none' and s|count > 0 %}
              {{ s|min }}
            {% else %}none{% endif %}
          {% else %}none{% endif %}
        max: >-
          {% set s = state_attr('binary_sensor.motion_device_humidity','values') -%}
          {% if s|lower != 'none' and not s is string %}
            {% set s = s|select('number')|list|default(none) %}
            {% if s|lower != 'none' and s|count > 0 %}
              {{ s|max }}
            {% else %}none{% endif %}
          {% else %}none{% endif %}
        mean: >-
          {% set s = state_attr('binary_sensor.motion_device_humidity','values') -%}
          {% if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'null' %}
            {% set s = s|select('number')|list|default(none) %}
            {% if s|lower != 'none' and s|count > 0 %}
              {{ '%0.2f'|format(s|average) }}
            {% else %}none{% endif %}
          {% else %}none{% endif %}
        sum: >-
          {% set s = state_attr('binary_sensor.motion_device_humidity','values') -%}
          {% if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'null' %}
            {% set s = s|select('number')|list|default(none) %}
            {% if s|lower != 'none' and s|count > 0 %}
              {{ '%0.2f'|format(s|sum) }}
            {% else %}none{% endif %}
          {% else %}none{% endif %}
        count: >-
          {% set s = state_attr('binary_sensor.motion_device_humidity','values') -%}
          {% if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'null' %}
            {% set s = s|select('number')|list|default(none) %}
            {% if s|lower != 'none' and s|count > 0 %}
              {{ (s|count) }}
            {% else %}none{% endif %}
          {% else %}none{% endif %}
      value_template: >-
        {{- state_attr('sensor.motion_device_humidity','mean') -}}

# max

- platform: template
  sensors:
    motion_device_humidity_max:
      friendly_name: Maximum humidity
      device_class: 'humidity'
      unit_of_measurement: '%'
      value_template: >-
        {{ state_attr('sensor.motion_device_humidity','max')|float(none) }}
    motion_device_humidity_1d_max:
      friendly_name: 'Maximum humidity (1d)'
      unit_of_measurement: '%'
      attribute_templates:
        min: >-
         {{ states('sensor.motion_device_humidity_max_1d_min') }}
        max: >-
         {{ states('sensor.motion_device_humidity_max_1d_max') }}
        mean: >-
         {{ states('sensor.motion_device_humidity_max_1d_mean') }}
        stdev: >-
         {{ states('sensor.motion_device_humidity_max_1d_stdev') }}
        coverage: >-
         {{ state_attr('sensor.motion_device_humidity_max_1d_max','age_coverage_ratio')|float(0) * 100 }}
        usage: >-
         {{ state_attr('sensor.motion_device_humidity_max_1d_max','buffer_usage_ratio')|float(0) * 100 }}
      value_template: >-
        {{ state_attr('sensor.motion_device_humidity_1d_max','max')|float(none) }}

- platform: statistics
  name: motion_device_humidity_max_1d_mean
  entity_id: sensor.motion_device_humidity_max
  state_characteristic: mean
  sampling_size: 1000
  max_age:
    days: 1

- platform: statistics
  name: motion_device_humidity_max_1d_stdev
  entity_id: sensor.motion_device_humidity_max
  state_characteristic: standard_deviation
  sampling_size: 1000
  max_age:
    days: 1

- platform: statistics
  name: motion_device_humidity_max_1d_min
  entity_id: sensor.motion_device_humidity_max
  state_characteristic: value_min
  sampling_size: 1000
  max_age:
    days: 1

- platform: statistics
  name: motion_device_humidity_max_1d_max
  entity_id: sensor.motion_device_humidity_max
  state_characteristic: value_max
  sampling_size: 1000
  max_age:
    days: 1
