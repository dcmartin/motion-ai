###
# homeassistant/sensor/device/class/connectivity_response.yaml
###

- platform: template
  sensors:
    motion_device_connectivity_approve:
      value_template: >-
        {%- set response = 'approve' -%}
        {%- set state = none -%}
        {%- set s = state_attr('binary_sensor.motion_device_connectivity','condition') -%}
        {%- if s|lower != 'none' -%}
          {% set condition = s|string -%}
          {% set s = state_attr('binary_sensor.motion_device_connectivity_alarm_exist','actions') -%}
          {%- if s|lower != 'none' -%}
            {% set actions = s %}
            {% set s = actions|selectattr('id','search',condition)|list %}
            {% if s|lower != 'none' and not s is string and s is iterable and s|count > 0 %}
              {% set action = s|first %}
              {% set s = action.actions|selectattr('id','==',response)|list %}
              {% if s|lower != 'none' and not action is string and action is iterable and s|count > 0 %}
                {% set state = (s|first).title %}
              {%- endif -%}
            {%- endif -%}
          {%- endif -%}
        {%- endif -%}
        {{- state -}}
    motion_device_connectivity_deny:
      value_template: >-
        {%- set response = 'deny' -%}
        {%- set state = none -%}
        {%- set s = state_attr('binary_sensor.motion_device_connectivity','condition') -%}
        {%- if s|lower != 'none' -%}
          {% set condition = s|string -%}
          {% set s = state_attr('binary_sensor.motion_device_connectivity_alarm_exist','actions') -%}
          {%- if s|lower != 'none' -%}
            {% set actions = s %}
            {% set s = actions|selectattr('id','search',condition)|list %}
            {% if s|lower != 'none' and not s is string and s is iterable and s|count > 0 %}
              {% set action = s|first %}
              {% set s = action.actions|selectattr('id','==',response)|list %}
              {% if s|lower != 'none' and not action is string and action is iterable and s|count > 0 %}
                {% set state = (s|first).title %}
              {%- endif -%}
            {%- endif -%}
          {%- endif -%}
        {%- endif -%}
        {{- state -}}
    motion_device_connectivity_wait:
      value_template: >-
        {%- set response = 'wait' -%}
        {%- set state = none -%}
        {%- set s = state_attr('binary_sensor.motion_device_connectivity','condition') -%}
        {%- if s|lower != 'none' -%}
          {% set condition = s|string -%}
          {% set s = state_attr('binary_sensor.motion_device_connectivity_alarm_exist','actions') -%}
          {%- if s|lower != 'none' -%}
            {% set actions = s %}
            {% set s = actions|selectattr('id','search',condition)|list %}
            {% if s|lower != 'none' and not s is string and s is iterable and s|count > 0 %}
              {% set action = s|first %}
              {% set s = action.actions|selectattr('id','==',response)|list %}
              {% if s|lower != 'none' and not action is string and action is iterable and s|count > 0 %}
                {% set state = (s|first).title %}
              {%- endif -%}
            {%- endif -%}
          {%- endif -%}
        {%- endif -%}
        {{- state -}}
    motion_device_connectivity_help:
      value_template: >-
        {%- set response = 'help' -%}
        {%- set state = none -%}
        {%- set s = state_attr('binary_sensor.motion_device_connectivity','condition') -%}
        {%- if s|lower != 'none' -%}
          {% set condition = s|string -%}
          {% set s = state_attr('binary_sensor.motion_device_connectivity_alarm_exist','actions') -%}
          {%- if s|lower != 'none' -%}
            {% set actions = s %}
            {% set s = actions|selectattr('id','search',condition)|list %}
            {% if s|lower != 'none' and not s is string and s is iterable and s|count > 0 %}
              {% set action = s|first %}
              {% set s = action.actions|selectattr('id','==',response)|list %}
              {% if s|lower != 'none' and not action is string and action is iterable and s|count > 0 %}
                {% set state = (s|first).title %}
              {%- endif -%}
            {%- endif -%}
          {%- endif -%}
        {%- endif -%}
        {{- state -}}
