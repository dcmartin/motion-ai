###
# homeassistant/sensor/device/class/illuminance.yaml
###

- platform: template
  sensors:
    motion_device_illuminance:
      friendly_name: Measurement illuminance
      device_class: 'illuminance'
      unit_of_measurement: 'lx'
      attribute_templates:
        ids: >-
          {{- state_attr('binary_sensor.motion_device_illuminance','ids') -}}
        total: >-
          {{- state_attr('binary_sensor.motion_device_illuminance','total') -}}
        measurement: >-
          {{- state_attr('binary_sensor.motion_device_illuminance','status') -}}
        areas: >-
          {{- state_attr('binary_sensor.motion_device_illuminance','areas') -}}
        values: >-
          {{- state_attr('binary_sensor.motion_device_illuminance','values') -}}
        min: >-
          {% set s = state_attr('binary_sensor.motion_device_illuminance','values') -%}
          {% if s|lower != 'none' and not s is string %}
            {% set s = s|select('number')|list|default(none) %}
            {% if s|lower != 'none' and s|count > 0 %}
              {{ s|min }}
            {% else %}none{% endif %}
          {% else %}none{% endif %}
        max: >-
          {% set s = state_attr('binary_sensor.motion_device_illuminance','values') -%}
          {% if s|lower != 'none' and not s is string %}
            {% set s = s|select('number')|list|default(none) %}
            {% if s|lower != 'none' and s|count > 0 %}
              {{ s|max }}
            {% else %}none{% endif %}
          {% else %}none{% endif %}
        mean: >-
          {% set s = state_attr('binary_sensor.motion_device_illuminance','values') -%}
          {% if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'null' %}
            {% set s = s|select('number')|list|default(none) %}
            {% if s|lower != 'none' and s|count > 0 %}
              {{ '%0.2f'|format(s|average) }}
            {% else %}none{% endif %}
          {% else %}none{% endif %}
        sum: >-
          {% set s = state_attr('binary_sensor.motion_device_illuminance','values') -%}
          {% if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'null' %}
            {% set s = s|select('number')|list|default(none) %}
            {% if s|lower != 'none' and s|count > 0 %}
              {{ '%0.2f'|format(s|sum) }}
            {% else %}none{% endif %}
          {% else %}none{% endif %}
        count: >-
          {% set s = state_attr('binary_sensor.motion_device_illuminance','values') -%}
          {% if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'null' %}
            {% set s = s|select('number')|list|default(none) %}
            {% if s|lower != 'none' and s|count > 0 %}
              {{ (s|count) }}
            {% else %}none{% endif %}
          {% else %}none{% endif %}
      value_template: >-
        {{- state_attr('sensor.motion_device_illuminance','mean') -}}

# max

- platform: template
  sensors:
    motion_device_illuminance_max:
      friendly_name: Maximum illuminance
      device_class: 'illuminance'
      unit_of_measurement: 'lx'
      attribute_templates:
        spike: >-
          {{ states('binary_sensor.motion_device_illuminance_spike') }}
        min: >-
         {{ states('sensor.motion_device_illuminance_max_1d_min') }}
        max: >-
         {{ states('sensor.motion_device_illuminance_max_1d_max') }}
        mean: >-
         {{ states('sensor.motion_device_illuminance_max_1d_mean') }}
        stdev: >-
         {{ states('sensor.motion_device_illuminance_max_1d_stdev') }}
        coverage: >-
         {{ state_attr('sensor.motion_device_illuminance_max_1d_max','age_coverage_ratio')|float(0) * 100 }}
        usage: >-
         {{ state_attr('sensor.motion_device_illuminance_max_1d_max','buffer_usage_ratio')|float(0) * 100 }}
      value_template: >-
        {{ state_attr('sensor.motion_device_illuminance','max')|float(none) }}
    motion_device_illuminance_1d_max:
      friendly_name: 'Maximum illuminance (1d)'
      unit_of_measurement: 'lx'
      value_template: >-
        {{ state_attr('sensor.motion_device_illuminance_max','max')|float(none) }}

- platform: statistics
  name: motion_device_illuminance_max_1d_mean
  entity_id: sensor.motion_device_illuminance_max
  state_characteristic: mean
  sampling_size: 1000
  max_age:
    days: 1

- platform: statistics
  name: motion_device_illuminance_max_1d_stdev
  entity_id: sensor.motion_device_illuminance_max
  state_characteristic: standard_deviation
  sampling_size: 1000
  max_age:
    days: 1

- platform: statistics
  name: motion_device_illuminance_max_1d_min
  entity_id: sensor.motion_device_illuminance_max
  state_characteristic: value_min
  sampling_size: 1000
  max_age:
    days: 1

- platform: statistics
  name: motion_device_illuminance_max_1d_max
  entity_id: sensor.motion_device_illuminance_max
  state_characteristic: value_max
  sampling_size: 1000
  max_age:
    days: 1
