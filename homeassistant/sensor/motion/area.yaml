###
## homeassistant/sensor/motion/area.yaml
###

- platform: template
  sensors:
    motion_areas:
      unique_id: motion_areas
      friendly_name: 'Motion Area(s)'
      unit_of_measurement: count
      icon_template: >-
        {%- if states('sensor.motion_areas')|lower != 'unknown' -%}
          {{- 'mdi:home-map-marker' -}}
        {%- else -%}
          {{- 'mdi:home-search' -}}
        {%- endif -%}
      attribute_templates:
        ids: >-
          {# set through automation/motion/area.yaml #}
          {{- 'unknown' -}}
        areas: >-
          {# set through automation/motion/area.yaml #}
          {{- 'unknown' -}}
        locations: >-
          {# set through automation/motion/area.yaml #}
          {{- 'unknown' -}}
      value_template: >-
        {# set through automation/motion/area.yaml #}
        {{- 'unknown' -}}
