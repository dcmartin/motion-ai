###
# homeassistant/sensor/motion/detected_animal.yaml
###

- platform: template
  sensors:
    motion_detected_animal_api:
      friendly_name: '🐄 API'
      icon_template: 'mdi:cow'
      value_template: >-
        {% set d = 'http://127.0.0.1:7999' %}
        {% set c = states('sensor.motion_detected_animal_where') %}
        {% set s = state_attr('sensor.motion_addon_config','config') %}
        {% if s|lower != 'null' and s|lower != 'unavailable' and s|lower != 'unknown' and s|lower != 'none' and s is mapping %}
          {% set s = s.cameras|selectattr("name","==",c)|list %}
          {% if s|lower != 'unavailable' and s|lower != 'unknown' and s|lower != 'none' and s|length > 0 %}
            {% set s = s|map(attribute="addon_api")|first %}
            {% if s|lower != 'null' and s|lower != 'unavailable' and s|lower != 'unknown' and s|lower != 'none' and s|length > 0 %}
              {{ s }}
            {% else %}{{ d }}{% endif %}
          {% else %}{{ d }}{% endif %}
        {% else %}{{ d }}{% endif %}

## none

- platform: template
  sensors:
    motion_camera_detected_animal_none_count_array:
      value_template: >-
        {% set s = state_attr('sensor.motion_addon_config','config') %}
        {% if s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and s != null %}
          {% set s = s.cameras|map(attribute="name")|list  %}
          {% if s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and s|list is iterable %}
            [{%- for i in s if states('sensor.motion_' + i|string + '_camera_detected_animal_level') == 'none' -%}
              {%- if loop.first -%}{%- else -%},{%- endif -%}
              {{- '1' -}}
            {%- endfor -%}]
          {%- else -%}null{%- endif -%}
        {%- else -%}null{%- endif -%}

- platform: template
  sensors:
    motion_camera_detected_animal_none_count:
      friendly_name: '📷🐄 none (∀Σ)'
      unit_of_measurement: '📷'
      icon_template: 'mdi:numeric-0-box'
      value_template: >-
        {% set s = states('sensor.motion_camera_detected_animal_none_count_array') %}
        {% if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' %}
          {% set s = s|from_json %}
          {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' and s is iterable -%}
            {%- set total = s|length -%}
            {%- set list = s|select("number")|list -%}
            {%- set count = list|count -%}
            {%- if count > 0 -%}
              {{- list|sum|int -}}
            {%- endif -%}
          {%- endif -%}
        {%- endif -%}

## null

- platform: template
  sensors:
    motion_camera_detected_animal_null_count_array:
      value_template: >-
        {% set s = state_attr('sensor.motion_addon_config','config') %}
        {% if s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and s != null %}
          {% set s = s.cameras|map(attribute="name")|list  %}
          {% if s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and s|list is iterable %}
            [{%- for i in s if states('sensor.motion_' + i|string + '_camera_detected_animal_level')|lower == 'null' -%}
              {%- if loop.first -%}{%- else -%},{%- endif -%}
              {{- '1' -}}
            {%- endfor -%}]
          {%- else -%}null{%- endif -%}
        {%- else -%}null{%- endif -%}

- platform: template
  sensors:
    motion_camera_detected_animal_null_count:
      friendly_name: '📷🐄 null (∀Σ)'
      unit_of_measurement: '📷'
      icon_template: 'mdi:null'
      value_template: >-
        {% set s = states('sensor.motion_camera_detected_animal_null_count_array') %}
        {% if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' %}
          {% set s = s|from_json %}
          {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' and s is iterable -%}
            {%- set total = s|length -%}
            {%- set list = s|select("number")|list -%}
            {%- set count = list|count -%}
            {%- if count > 0 -%}
              {{- list|sum|int -}}
            {%- endif -%}
          {%- endif -%}
        {%- endif -%}

## unknown

- platform: template
  sensors:
    motion_camera_detected_animal_unknown_count_array:
      value_template: >-
        {% set s = state_attr('sensor.motion_addon_config','config') %}
        {% if s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and s != null %}
          {% set s = s.cameras|map(attribute="name")|list  %}
          {% if s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and s|list is iterable %}
            [{%- for i in s if states('sensor.motion_' + i|string + '_camera_detected_animal_level')|lower == 'unknown' and i != 'latest' -%}
              {%- if loop.first -%}{%- else -%},{%- endif -%}
              {{- '1' -}}
            {%- endfor -%}]
          {%- else -%}null{%- endif -%}
        {%- else -%}null{%- endif -%}

- platform: template
  sensors:
    motion_camera_detected_animal_unknown_count:
      friendly_name: '📷🐄 unknown (∀Σ)'
      unit_of_measurement: '📷'
      icon_template: 'mdi:help'
      value_template: >-
        {% set s = states('sensor.motion_camera_detected_animal_unknown_count_array') %}
        {% if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' %}
          {% set s = s|from_json %}
          {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' and s is iterable -%}
            {%- set total = s|length -%}
            {%- set list = s|select("number")|list -%}
            {%- set count = list|count -%}
            {%- if count > 0 -%}
              {{- list|sum|int -}}
            {%- endif -%}
          {%- endif -%}
        {%- endif -%}

## nominal

- platform: template
  sensors:
    motion_camera_detected_animal_nominal_count_array:
      value_template: >-
        {% set s = state_attr('sensor.motion_addon_config','config') %}
        {% if s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and s != null %}
          {% set s = s.cameras|map(attribute="name")|list  %}
          {% if s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and s|list is iterable %}
            [{%- for i in s if states('sensor.motion_' + i|string + '_camera_detected_animal_level') == 'nominal' -%}
              {%- if loop.first -%}{%- else -%},{%- endif -%}
              {{- '1' -}}
            {%- endfor -%}]
          {%- else -%}null{%- endif -%}
        {%- else -%}null{%- endif -%}

- platform: template
  sensors:
    motion_camera_detected_animal_nominal_count:
      friendly_name: '📷🐄 nominal (∀Σ)'
      unit_of_measurement: '📷'
      icon_template: 'mdi:hand-okay'
      value_template: >-
        {% set s = states('sensor.motion_camera_detected_animal_nominal_count_array') %}
        {% if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' %}
          {% set s = s|from_json %}
          {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' and s is iterable -%}
            {%- set total = s|length -%}
            {%- set list = s|select("number")|list -%}
            {%- set count = list|count -%}
            {%- if count > 0 -%}
              {{- list|sum|int -}}
            {%- endif -%}
          {%- endif -%}
        {%- endif -%}

## low

- platform: template
  sensors:
    motion_camera_detected_animal_low_count_array:
      value_template: >-
        {% set s = state_attr('sensor.motion_addon_config','config') %}
        {% if s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and s != null %}
          {% set s = s.cameras|map(attribute="name")|list  %}
          {% if s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and s|list is iterable %}
            [{%- for i in s if states('sensor.motion_' + i|string + '_camera_detected_animal_level') == 'low' -%}
              {%- if loop.first -%}{%- else -%},{%- endif -%}
              {{- '1' -}}
            {%- endfor -%}]
          {%- else -%}null{%- endif -%}
        {%- else -%}null{%- endif -%}

- platform: template
  sensors:
    motion_camera_detected_animal_low_count:
      friendly_name: '📷🐄 low (∀Σ)'
      unit_of_measurement: '📷'
      icon_template: 'mdi:arrow-down'
      value_template: >-
        {% set s = states('sensor.motion_camera_detected_animal_low_count_array') %}
        {% if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' %}
          {% set s = s|from_json %}
          {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' and s is iterable -%}
            {%- set total = s|length -%}
            {%- set list = s|select("number")|list -%}
            {%- set count = list|count -%}
            {%- if count > 0 -%}
              {{- list|sum|int -}}
            {%- endif -%}
          {%- endif -%}
        {%- endif -%}

## verylow

- platform: template
  sensors:
    motion_camera_detected_animal_verylow_count_array:
      value_template: >-
        {% set s = state_attr('sensor.motion_addon_config','config') %}
        {% if s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and s != null %}
          {% set s = s.cameras|map(attribute="name")|list  %}
          {% if s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and s|list is iterable %}
            [{%- for i in s if states('sensor.motion_' + i|string + '_camera_detected_animal_level') == 'verylow' -%}
              {%- if loop.first -%}{%- else -%},{%- endif -%}
              {{- '1' -}}
            {%- endfor -%}]
          {%- else -%}null{%- endif -%}
        {%- else -%}null{%- endif -%}

- platform: template
  sensors:
    motion_camera_detected_animal_verylow_count:
      friendly_name: '📷🐄 verylow (∀Σ)'
      unit_of_measurement: '📷'
      icon_template: 'mdi:arrow-collapse-down'
      value_template: >-
        {% set s = states('sensor.motion_camera_detected_animal_verylow_count_array') %}
        {% if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' %}
          {% set s = s|from_json %}
          {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' and s is iterable -%}
            {%- set total = s|length -%}
            {%- set list = s|select("number")|list -%}
            {%- set count = list|count -%}
            {%- if count > 0 -%}
              {{- list|sum|int -}}
            {%- endif -%}
          {%- endif -%}
        {%- endif -%}


## high

- platform: template
  sensors:
    motion_camera_detected_animal_high_count_array:
      value_template: >-
        {% set s = state_attr('sensor.motion_addon_config','config') %}
        {% if s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and s != null %}
          {% set s = s.cameras|map(attribute="name")|list  %}
          {% if s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and s|list is iterable %}
            [{%- for i in s if states('sensor.motion_' + i|string + '_camera_detected_animal_level') == 'high' -%}
              {%- if loop.first -%}{%- else -%},{%- endif -%}
              {{- '1' -}}
            {%- endfor -%}]
          {%- else -%}null{%- endif -%}
        {%- else -%}null{%- endif -%}

- platform: template
  sensors:
    motion_camera_detected_animal_high_count:
      friendly_name: '📷🐄 high (∀Σ)'
      unit_of_measurement: '📷'
      icon_template: 'mdi:arrow-up'
      value_template: >-
        {% set s = states('sensor.motion_camera_detected_animal_high_count_array') %}
        {% if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' %}
          {% set s = s|from_json %}
          {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' and s is iterable -%}
            {%- set total = s|length -%}
            {%- set list = s|select("number")|list -%}
            {%- set count = list|count -%}
            {%- if count > 0 -%}
              {{- list|sum|int -}}
            {%- endif -%}
          {%- endif -%}
        {%- endif -%}

## veryhigh

- platform: template
  sensors:
    motion_camera_detected_animal_veryhigh_count_array:
      value_template: >-
        {% set s = state_attr('sensor.motion_addon_config','config') %}
        {% if s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and s != null %}
          {% set s = s.cameras|map(attribute="name")|list  %}
          {% if s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and s|list is iterable %}
            [{%- for i in s if states('sensor.motion_' + i|string + '_camera_detected_animal_level') == 'veryhigh' -%}
              {%- if loop.first -%}{%- else -%},{%- endif -%}
              {{- '1' -}}
            {%- endfor -%}]
          {%- else -%}null{%- endif -%}
        {%- else -%}null{%- endif -%}

- platform: template
  sensors:
    motion_camera_detected_animal_veryhigh_count:
      friendly_name: '📷🐄 veryhigh (∀Σ)'
      unit_of_measurement: '📷'
      icon_template: 'mdi:arrow-collapse-up'
      value_template: >-
        {% set s = states('sensor.motion_camera_detected_animal_veryhigh_count_array') %}
        {% if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' %}
          {% set s = s|from_json %}
          {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' and s is iterable -%}
            {%- set total = s|length -%}
            {%- set list = s|select("number")|list -%}
            {%- set count = list|count -%}
            {%- if count > 0 -%}
              {{- list|sum|int -}}
            {%- endif -%}
          {%- endif -%}
        {%- endif -%}

## counts

- platform: template
  sensors:
    motion_detected_animal_count_array:
      value_template: >-
        {%- set s = state_attr('input_select.motion_camera_name','options') -%}
        {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|length > 0 -%}
          [{%- for i in s -%}
            {%- set j = states('sensor.motion_' + i + '_detected_animal_count') -%}
            {%- if loop.first -%}{%- else -%},{%- endif -%}
            {%- if j|lower != 'none' and j|lower != 'unknown' and j|lower != 'unavailable' and j|lower != 'null' -%}
              {%- if j|int > 0 -%}{{- j|int -}}{%- else -%}0{%- endif -%}
            {%- else -%}null{%- endif -%}
          {%- endfor -%}]
        {%- else -%}null{%- endif -%}

- platform: template
  sensors:
    motion_detected_animal_counts:
      friendly_name: '🐄 sum ∀Σ'
      icon_template: 'mdi:cow'
      unit_of_measurement: '🐄'
      value_template: >-
        {% set s = states('sensor.motion_detected_animal_count_array') %}
        {% if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' %}
          {% set s = s|from_json %}
          {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' and s is iterable -%}
            {%- set total = s|length -%}
            {%- set list = s|select("number")|list -%}
            {%- set count = list|count -%}
            {%- if count > 0 -%}
              {{- list|sum|int -}}
            {%- else -%}null{%- endif -%}
          {%- else -%}null{%- endif -%}
        {%- else -%}null{%- endif -%}

# 24h
- platform: statistics
  name: motion_detected_animal_counts_24h
  entity_id: sensor.motion_detected_animal_counts
  sampling_size: 1200
  max_age:
    hours: 24

- platform: template
  sensors:
    motion_detected_animal_counts_24h_mean:
      friendly_name: '🐄 ∀Σ μ 24h'
      icon_template: 'mdi:cow'
      unit_of_measurement: '🐄'
      value_template: >-
        {% if states('sensor.motion_detected_animal_counts_24h') != 'none' %}
          {{ states('sensor.motion_detected_animal_counts_24h')|float }}
        {%- else -%}null{%- endif -%}

# 1m
- platform: statistics
  name: motion_detected_animal_counts_1m
  entity_id: sensor.motion_detected_animal_counts
  sampling_size: 20
  max_age:
    minutes: 1

- platform: template
  sensors:
    motion_detected_animal_counts_1m_mean:
      friendly_name: '🐄 ∀Σ μ 1m'
      icon_template: 'mdi:cow'
      unit_of_measurement: '🐄'
      value_template: >-
        {% if states('sensor.motion_detected_animal_counts_1m') != 'none' %}
          {{ states('sensor.motion_detected_animal_counts_1m')|float }}
        {%- else -%}null{%- endif -%}

# 5m
- platform: statistics
  name: motion_detected_animal_counts_5m
  entity_id: sensor.motion_detected_animal_counts
  sampling_size: 100
  max_age:
    minutes: 5

- platform: template
  sensors:
    motion_detected_animal_counts_5m_mean:
      friendly_name: '🐄 ∀Σ μ 5m'
      icon_template: 'mdi:cow'
      unit_of_measurement: '🐄'
      value_template: >-
        {% if states('sensor.motion_detected_animal_counts_5m') != 'none' %}
          {{ states('sensor.motion_detected_animal_counts_5m')|float }}
        {%- else -%}null{%- endif -%}

# 15m
- platform: statistics
  name: motion_detected_animal_counts_15m
  entity_id: sensor.motion_detected_animal_counts
  sampling_size: 300
  max_age:
    minutes: 15

- platform: template
  sensors:
    motion_detected_animal_counts_15m_mean:
      friendly_name: '🐄 ∀Σ μ 15m'
      icon_template: 'mdi:cow'
      unit_of_measurement: '🐄'
      value_template: >-
        {% if states('sensor.motion_detected_animal_counts_15m') != 'none' %}
          {{ states('sensor.motion_detected_animal_counts_15m')|float }}
        {%- else -%}null{%- endif -%}

# 30m

- platform: statistics
  name: motion_detected_animal_counts_30m
  entity_id: sensor.motion_detected_animal_counts
  sampling_size: 60
  max_age:
    minutes: 30

- platform: template
  sensors:
    motion_detected_animal_counts_30m_mean:
      friendly_name: '🐄 ∀Σ μ 30m'
      icon_template: 'mdi:cow'
      unit_of_measurement: '🐄'
      value_template: >-
        {% if states('sensor.motion_detected_animal_counts_30m') != 'none' %}
          {{ states('sensor.motion_detected_animal_counts_30m')|float }}
        {%- else -%}null{%- endif -%}

## DELTA

- platform: template
  sensors:
    motion_detected_animal_counts_1m_delta:
      friendly_name: '🐄 ∀Σ μ 1m Δ'
      icon_template: 'mdi:cow'
      unit_of_measurement: '🐄'
      value_template: >-
        {% set f = states('sensor.motion_detected_animal_counts_24h') %}
        {% set o = states('sensor.motion_detected_animal_counts_1m') %}
        {% if f|lower != 'none' and o|lower != 'none' %}
          {{ (f|float - o|float)|abs }}
        {%- else -%}null{%- endif %}

- platform: statistics
  name: motion_detected_animal_counts_1m_delta_statistics
  entity_id: sensor.motion_detected_animal_counts_1m_delta
  sampling_size: 100
  max_age:
    minutes: 5

- platform: template
  sensors:
    motion_detected_animal_counts_1m_delta_mean:
      friendly_name: '🐄 ∀Σ 1m Δμ'
      icon_template: 'mdi:cow'
      unit_of_measurement: '🐄'
      value_template: >-
        {% if states('sensor.motion_detected_animal_counts_1m_delta_statistics')|lower != 'unknown' %}
          {{ states('sensor.motion_detected_animal_counts_1m_delta_statistics')|float }}
        {%- else -%}null{%- endif -%}

- platform: template
  sensors:
    motion_detected_animal_counts_5m_delta:
      friendly_name: '🐄 ∀Σ μ 5m Δ'
      icon_template: 'mdi:cow'
      unit_of_measurement: '🐄'
      value_template: >-
        {% set f = states('sensor.motion_detected_animal_counts_5m') %}
        {% set o = states('sensor.motion_detected_animal_counts_1m') %}
        {% if f|lower != 'none' and o|lower != 'none' %}
          {{ (f|float - o|float)|abs }}
        {%- else -%}null{%- endif %}

- platform: statistics
  name: motion_detected_animal_counts_5m_delta_statistics
  entity_id: sensor.motion_detected_animal_counts_5m_delta
  sampling_size: 100
  max_age:
    minutes: 30

- platform: template
  sensors:
    motion_detected_animal_counts_5m_delta_mean:
      friendly_name: '🐄 ∀Σ 5m Δμ'
      icon_template: 'mdi:cow'
      unit_of_measurement: '🐄'
      value_template: >-
        {% if states('sensor.motion_detected_animal_counts_5m_delta_statistics')|lower != 'unknown' %}
          {{ states('sensor.motion_detected_animal_counts_5m_delta_statistics')|float }}
        {%- else -%}null{%- endif -%}

- platform: template
  sensors:
    motion_detected_animal_counts_15m_delta:
      friendly_name: '🐄 ∀Σ μ 15m Δ'
      icon_template: 'mdi:cow'
      unit_of_measurement: '🐄'
      value_template: >-
        {% set f = states('sensor.motion_detected_animal_counts_15m') %}
        {% set o = states('sensor.motion_detected_animal_counts_1m') %}
        {% if f|lower != 'none' and o|lower != 'none' %}
          {{ (f|float - o|float)|abs }}
        {%- else -%}null{%- endif %}

- platform: statistics
  name: motion_detected_animal_counts_15m_delta_statistics
  entity_id: sensor.motion_detected_animal_counts_15m_delta
  sampling_size: 100
  max_age:
    minutes: 90

- platform: template
  sensors:
    motion_detected_animal_counts_15m_delta_mean:
      friendly_name: '🐄 ∀Σ 15m Δμ'
      icon_template: 'mdi:cow'
      unit_of_measurement: '🐄'
      value_template: >-
        {% if states('sensor.motion_detected_animal_counts_15m_delta_statistics')|lower != 'unknown' %}
          {{ states('sensor.motion_detected_animal_counts_15m_delta_statistics')|float }}
        {%- else -%}null{%- endif -%}

- platform: template
  sensors:
    motion_detected_animal_counts_30m_delta:
      friendly_name: '🐄 ∀Σ μ 30m Δ'
      icon_template: 'mdi:cow'
      unit_of_measurement: '🐄'
      value_template: >-
        {% set f = states('sensor.motion_detected_animal_counts_30m') %}
        {% set o = states('sensor.motion_detected_animal_counts_1m') %}
        {% if f|lower != 'none' and o|lower != 'none' %}
          {{ (f|float - o|float)|abs }}
        {%- else -%}null{%- endif %}

- platform: statistics
  name: motion_detected_animal_counts_30m_delta_statistics
  entity_id: sensor.motion_detected_animal_counts_30m_delta
  sampling_size: 100
  max_age:
    minutes: 180

- platform: template
  sensors:
    motion_detected_animal_counts_30m_delta_mean:
      friendly_name: '🐄 ∀Σ 30m Δμ'
      icon_template: 'mdi:cow'
      unit_of_measurement: '🐄'
      value_template: >-
        {% if states('sensor.motion_detected_animal_counts_30m_delta_statistics')|lower != 'unknown' %}
          {{ states('sensor.motion_detected_animal_counts_30m_delta_statistics')|float }}
        {%- else -%}null{%- endif -%}

# logical

- platform: template
  sensors:
    motion_detected_animal_most:
      friendly_name: '🐄 mean max'
      icon_template: 'mdi:cow'
      unit_of_measurement: '🐄'
      value_template: >-
        {% set s = states('sensor.motion_detected_animal_count_array') %}
        {% if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' %}
          {% set s = s|from_json %}
          {% if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' and s is iterable and s|length > 0 %}
            {% set s = s|select("number")|list %}
            {% if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s is iterable and s|length > 0 %}
              {{ s|max }}
            {%- else -%}null{%- endif -%}
          {%- else -%}null{%- endif -%}
        {%- else -%}null{%- endif -%}

- platform: template
  sensors:
    motion_detected_animal_occupancy:
      friendly_name: '🐄 occupancy %'
      icon_template: 'mdi:cow'
      unit_of_measurement: '%'
      value_template: >-
        {% set n = states('sensor.motion_detected_animal_counts') %}
        {% set s = states('sensor.motion_detected_animal_count_array') %}
        {% if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' %}
          {% set s = s|from_json %}
          {% if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s is iterable %}
            [{% for i in s -%}
              {%- if loop.first %}{% else %},{% endif %}
               {%- if i|int > 0 and n|int > 0 -%}{{ ((i|float / n|float)*100)|int }}{% else %}0{%- endif -%}
            {%- endfor -%}]
          {%- else -%}null{%- endif -%}
        {%- else -%}null{%- endif -%}

- platform: template
  sensors:
    motion_detected_animal_occupancy_max:
      friendly_name: '🐄 occupancy max'
      icon_template: 'mdi:cow'
      unit_of_measurement: '%'
      value_template: >-
        {% set s = states('sensor.motion_detected_animal_occupancy') %}
        {% if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' %}
            {% set s = s|from_json %}
            {% if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' and s is iterable %}
            {{ s|max }}
          {%- else -%}null{%- endif -%}
        {%- else -%}null{%- endif -%}

- platform: template
  sensors:
    motion_detected_animal_camera_mosts:
      friendly_name: '🐄 most cameras'
      icon_template: 'mdi:cow'
      value_template: >-
        {%- set s = states('sensor.motion_detected_animal_count_array') -%}
        {%- set m = states('sensor.motion_detected_animal_most') -%}
        {%- set o = state_attr('input_select.motion_camera_name','options') -%}
        {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null'
           and o|lower != 'none' and o|lower != 'unknown' and o|lower != 'unavailable' and o|length > 0
           and m|lower != 'none' and m|lower != 'unknown' and m|lower != 'unavailable' and m|int > 0 -%}
          {%- set s = s|from_json -%}
          {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s is iterable -%}
            {%- for p in s -%}
              {%- if p|lower != 'null' and p|int >= m|int -%}
                {{ o[loop.index0] }}
              {% endif -%}
            {%- endfor -%}
          {%- else -%}null{%- endif -%}
        {%- else -%}null{%- endif -%}

- platform: template
  sensors:
    motion_detected_animal_count_status:
      friendly_name: '🐄 count'
      icon_template: 'mdi:cow'
      value_template: >-
        {%- set p = states('sensor.motion_detected_animal_counts') -%}
        {%- if p|lower != 'none' and p|lower != 'unknown' and p|lower != 'unavailable' and p|lower != 'null' -%}
          ∀Σ: {{ p -}};
          {% set a = states('sensor.motion_detected_animal_counts_24h') %}
          {%- if a|lower != 'none' and a|lower != 'unknown' and a|lower != 'unavailable' and a|lower != 'null' %}
            μ: {{ a -}};
            n: {{ state_attr('sensor.motion_detected_animal_counts_24h','count') }}
          {%- endif %}
        {%- else -%}
          {%- set s = states.sensor.motion_detected_animal_count_status.state -%}
          {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' %}
            {{ s }}
          {%- else -%}Pending{%- endif -%}
        {%- endif -%}

## agos

- platform: template
  sensors:
    motion_detected_animal_ago_array:
      value_template: >-
        {%- set s = state_attr('input_select.motion_camera_name','options') -%}
        {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|length > 0 -%}
          [{%- for i in s -%}
            {%- set j = states('sensor.motion_' + i + '_detected_animal_ago') -%}
            {%- if loop.first -%}{%- else -%},{%- endif -%}
            {%- if j|lower != 'none' and j|lower != 'unknown' and j|lower != 'unavailable' -%}
              {%- if j|lower != 'null' and j|int >= 0 and j|int < 86400 -%}{{- j|int -}}{%- else -%}null{%- endif -%}
            {%- else -%}null{%- endif -%}
          {%- endfor -%}]
        {%- else -%}null{%- endif -%}

- platform: template
  sensors:
    motion_detected_animal_agos:
      friendly_name: '🐄 mean ∀⧖'
      icon_template: 'mdi:cow'
      unit_of_measurement: 's'
      value_template: >-
        {%- set s = states('sensor.motion_detected_animal_ago_array') -%}
        {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' -%}
          {%- set s = s|from_json -%}
          {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' and s is iterable -%}
            {%- set total = s|length -%}
            {%- set list = s|select("number")|list -%}
            {%- set count = list|count -%}
            {%- if count > 0 -%}
              {{- list|min|int -}}
            {%- else -%}null{%- endif -%}
          {%- else -%}null{%- endif -%}
        {%- else -%}null{%- endif -%}

- platform: template
  sensors:
    motion_detected_animal_agos_min:
      friendly_name: '🐄 min ∀⧖'
      icon_template: 'mdi:cow'
      unit_of_measurement: 's'
      value_template: >-
        {%- set s = states('sensor.motion_detected_animal_ago_array') -%}
        {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' -%}
          {%- set s = s|from_json -%}
          {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' and s is iterable -%}
            {%- set list = s|select("number")|list -%}
            {%- if list|length > 0 -%}
              {{- list|min|int -}}
            {%- else -%}null{%- endif -%}
          {%- else -%}null{%- endif -%}
        {%- else -%}null{%- endif -%}

# 24h
- platform: statistics
  name: motion_detected_animal_agos_24h
  entity_id: sensor.motion_detected_animal_agos
  sampling_size: 1200
  max_age:
    hours: 24

- platform: template
  sensors:
    motion_detected_animal_agos_24h_mean:
      friendly_name: '🐄 ∀⧖ μ 24h'
      icon_template: 'mdi:cow'
      unit_of_measurement: 's'
      value_template: >-
        {% if states('sensor.motion_detected_animal_agos_24h') != 'none' %}
          {{ states('sensor.motion_detected_animal_agos_24h')|float }}
        {%- else -%}null{%- endif -%}

# 1m
- platform: statistics
  name: motion_detected_animal_agos_1m
  entity_id: sensor.motion_detected_animal_agos
  sampling_size: 20
  max_age:
    minutes: 1

- platform: template
  sensors:
    motion_detected_animal_agos_1m_mean:
      friendly_name: '🐄 ∀⧖ μ 1m'
      icon_template: 'mdi:cow'
      unit_of_measurement: 's'
      value_template: >-
        {% if states('sensor.motion_detected_animal_agos_1m') != 'none' %}
          {{ states('sensor.motion_detected_animal_agos_1m')|float }}
        {%- else -%}null{%- endif -%}

# 5m
- platform: statistics
  name: motion_detected_animal_agos_5m
  entity_id: sensor.motion_detected_animal_agos
  sampling_size: 100
  max_age:
    minutes: 5

- platform: template
  sensors:
    motion_detected_animal_agos_5m_mean:
      friendly_name: '🐄 ∀⧖ μ 5m'
      icon_template: 'mdi:cow'
      unit_of_measurement: 's'
      value_template: >-
        {% if states('sensor.motion_detected_animal_agos_5m') != 'none' %}
          {{ states('sensor.motion_detected_animal_agos_5m')|float }}
        {%- else -%}null{%- endif -%}

# 15m
- platform: statistics
  name: motion_detected_animal_agos_15m
  entity_id: sensor.motion_detected_animal_agos
  sampling_size: 300
  max_age:
    minutes: 15

- platform: template
  sensors:
    motion_detected_animal_agos_15m_mean:
      friendly_name: '🐄 ∀⧖ μ 15m'
      icon_template: 'mdi:cow'
      unit_of_measurement: 's'
      value_template: >-
        {% if states('sensor.motion_detected_animal_agos_15m') != 'none' %}
          {{ states('sensor.motion_detected_animal_agos_15m')|float }}
        {%- else -%}null{%- endif -%}

# 30m

- platform: statistics
  name: motion_detected_animal_agos_30m
  entity_id: sensor.motion_detected_animal_agos
  sampling_size: 60
  max_age:
    minutes: 30

- platform: template
  sensors:
    motion_detected_animal_agos_30m_mean:
      friendly_name: '🐄 ∀⧖ μ 30m'
      icon_template: 'mdi:cow'
      unit_of_measurement: 's'
      value_template: >-
        {% if states('sensor.motion_detected_animal_agos_30m') != 'none' %}
          {{ states('sensor.motion_detected_animal_agos_30m')|float }}
        {%- else -%}null{%- endif -%}

## DELTA

- platform: template
  sensors:
    motion_detected_animal_agos_1m_delta:
      friendly_name: '🐄 ∀⧖ μ 1m Δ'
      icon_template: 'mdi:cow'
      unit_of_measurement: 's'
      value_template: >-
        {% set f = states('sensor.motion_detected_animal_agos_24h') %}
        {% set o = states('sensor.motion_detected_animal_agos_1m') %}
        {% if f|lower != 'none' and o|lower != 'none' %}
          {{ (f|float - o|float)|abs }}
        {%- else -%}null{%- endif %}

- platform: statistics
  name: motion_detected_animal_agos_1m_delta_statistics
  entity_id: sensor.motion_detected_animal_agos_1m_delta
  sampling_size: 100
  max_age:
    minutes: 5

- platform: template
  sensors:
    motion_detected_animal_agos_1m_delta_mean:
      friendly_name: '🐄 ∀⧖ 1m Δμ'
      icon_template: 'mdi:cow'
      unit_of_measurement: 's'
      value_template: >-
        {% if states('sensor.motion_detected_animal_agos_1m_delta_statistics')|lower != 'unknown' %}
          {{ states('sensor.motion_detected_animal_agos_1m_delta_statistics')|float }}
        {%- else -%}null{%- endif -%}

- platform: template
  sensors:
    motion_detected_animal_agos_5m_delta:
      friendly_name: '🐄 ∀⧖ μ 5m Δ'
      icon_template: 'mdi:cow'
      unit_of_measurement: 's'
      value_template: >-
        {% set f = states('sensor.motion_detected_animal_agos_5m') %}
        {% set o = states('sensor.motion_detected_animal_agos_1m') %}
        {% if f|lower != 'none' and o|lower != 'none' %}
          {{ (f|float - o|float)|abs }}
        {%- else -%}null{%- endif %}

- platform: statistics
  name: motion_detected_animal_agos_5m_delta_statistics
  entity_id: sensor.motion_detected_animal_agos_5m_delta
  sampling_size: 100
  max_age:
    minutes: 30

- platform: template
  sensors:
    motion_detected_animal_agos_5m_delta_mean:
      friendly_name: '🐄 ∀⧖ 5m Δμ'
      icon_template: 'mdi:cow'
      unit_of_measurement: 's'
      value_template: >-
        {% if states('sensor.motion_detected_animal_agos_5m_delta_statistics')|lower != 'unknown' %}
          {{ states('sensor.motion_detected_animal_agos_5m_delta_statistics')|float }}
        {%- else -%}null{%- endif -%}

- platform: template
  sensors:
    motion_detected_animal_agos_15m_delta:
      friendly_name: '🐄 ∀⧖ μ 15m Δ'
      icon_template: 'mdi:cow'
      unit_of_measurement: 's'
      value_template: >-
        {% set f = states('sensor.motion_detected_animal_agos_15m') %}
        {% set o = states('sensor.motion_detected_animal_agos_1m') %}
        {% if f|lower != 'none' and o|lower != 'none' %}
          {{ (f|float - o|float)|abs }}
        {%- else -%}null{%- endif %}

- platform: statistics
  name: motion_detected_animal_agos_15m_delta_statistics
  entity_id: sensor.motion_detected_animal_agos_15m_delta
  sampling_size: 100
  max_age:
    minutes: 90

- platform: template
  sensors:
    motion_detected_animal_agos_15m_delta_mean:
      friendly_name: '🐄 ∀⧖ 15m Δμ'
      icon_template: 'mdi:cow'
      unit_of_measurement: 's'
      value_template: >-
        {% if states('sensor.motion_detected_animal_agos_15m_delta_statistics')|lower != 'unknown' %}
          {{ states('sensor.motion_detected_animal_agos_15m_delta_statistics')|float }}
        {%- else -%}null{%- endif -%}

- platform: template
  sensors:
    motion_detected_animal_agos_30m_delta:
      friendly_name: '🐄 ∀⧖ μ 30m Δ'
      icon_template: 'mdi:cow'
      unit_of_measurement: 's'
      value_template: >-
        {% set f = states('sensor.motion_detected_animal_agos_30m') %}
        {% set o = states('sensor.motion_detected_animal_agos_1m') %}
        {% if f|lower != 'none' and o|lower != 'none' %}
          {{ (f|float - o|float)|abs }}
        {%- else -%}null{%- endif %}

- platform: statistics
  name: motion_detected_animal_agos_30m_delta_statistics
  entity_id: sensor.motion_detected_animal_agos_30m_delta
  sampling_size: 100
  max_age:
    minutes: 180

- platform: template
  sensors:
    motion_detected_animal_agos_30m_delta_mean:
      friendly_name: '🐄 ∀⧖ 30m Δμ'
      icon_template: 'mdi:cow'
      unit_of_measurement: 's'
      value_template: >-
        {% if states('sensor.motion_detected_animal_agos_30m_delta_statistics')|lower != 'unknown' %}
          {{ states('sensor.motion_detected_animal_agos_30m_delta_statistics')|float }}
        {%- else -%}null{%- endif -%}

## counters

- platform: template
  sensors:
    motion_detected_animal_counters:
      friendly_name: '🐄 all counters'
      icon_template: 'mdi:counter'
      value_template: >-
        {% set s = state_attr('input_select.motion_camera_name','options') %}
        {% if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|length > 0 %}
          [{% for i in s -%}
            {%- set j = states('sensor.motion_' + i + '_detected_animal_counter') -%}
            {%- if loop.first %}{% else %},{% endif %}
            {%- if j|int > 0 -%}{{- j|int -}}{% else %}0{%- endif -%}
          {%- endfor -%}]
        {%- else -%}null{%- endif -%}

- platform: template
  sensors:
    motion_detected_animal_counters_total:
      friendly_name: '🐄 counters total'
      icon_template: 'mdi:counter'
      value_template: >-
        {% set s = states('sensor.motion_detected_animal_counters') %}
        {% if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' %}
          {% set s = s|from_json %}
          {% if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s is iterable %}
            {{ s|sum }}
          {%- else -%}null{%- endif -%}
        {%- else -%}null{%- endif -%}

- platform: template
  sensors:
    motion_detected_animal_counter_max:
      friendly_name: '🐄 counters max'
      icon_template: 'mdi:cow'
      value_template: >-
        {% set s = states('sensor.motion_detected_animal_counters') %}
        {% if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' %}
          {{ s|from_json|max }}
        {%- else -%}null{%- endif -%}

- platform: template
  sensors:
    motion_detected_animal_percents:
      friendly_name: '🐄 counters %'
      icon_template: 'mdi:percent'
      value_template: >-
        {% set n = states('sensor.motion_detected_animal_counters_total') %}
        {% set s = states('sensor.motion_detected_animal_counters') %}
        {% if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' %}
          [{% for i in s|from_json -%}
            {%- if loop.first %}{% else %},{% endif %}
             {%- if i|int > 0 and n|int > 0 -%}{{ ((i|float / n|float)*100)|int }}{% else %}0{%- endif -%}
          {%- endfor -%}]
        {%- else -%}null{%- endif -%}

- platform: template
  sensors:
    motion_detected_animal_often:
      friendly_name: '🐄 min mean ago'
      icon_template: 'mdi:percent'
      value_template: >-
        {% set s = states('sensor.motion_detected_animal_ago_array') %}
        {% if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' %}
          {% set s = s|from_json %}
          {% if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' and s is iterable and s|length > 0 %}
            {% set s = s|select("number")|list %}
            {% if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s is iterable and s|length > 0 %}
              {{ s|min }}
            {%- else -%}null{%- endif -%}
          {%- else -%}null{%- endif -%}
        {%- else -%}null{%- endif -%}

- platform: template
  sensors:
    motion_detected_animal_camera_most:
      friendly_name: '🐄 most (Σ)'
      icon_template: 'mdi:map-marker'
      value_template: >-
        {%- set s = states('sensor.motion_detected_animal_camera_mosts') -%}
        {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' -%}
          {%- set s = states('sensor.motion_detected_animal_camera_mosts') -%}
          {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|length > 0 -%}
            {%- set s = s.split(' ')|list -%}
            {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|length > 0 -%}
              {%- set p = s|last -%}
            {%- endif -%}
          {%- endif -%}
        {%- endif -%}
        {%- if p is defined -%}{{- p -}}{%- else -%}Pending{%- endif -%}

- platform: template
  sensors:
    motion_detected_animal_camera_often:
      friendly_name: '🐄 often (⧖)'
      icon_template: 'mdi:map-marker'
      value_template: >-
        {%- set s = states('sensor.motion_detected_animal_camera_oftens') -%}
        {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' -%}
          {%- set s = states('sensor.motion_detected_animal_camera_oftens') -%}
          {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|length > 0 -%}
            {%- set s = s.split(' ')|list -%}
            {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|length > 0 -%}
              {%- set p = s|last -%}
            {%- endif -%}
          {%- endif -%}
        {%- endif -%}
        {%- if p is defined -%}{{- p -}}{%- else -%}Pending{%- endif -%}

- platform: template
  sensors:
    motion_detected_animal_camera_oftens:
      icon_template: 'mdi:cow'
      value_template: >-
        {%- set s = states('sensor.motion_detected_animal_percents') -%}
        {%- set m = states('sensor.motion_detected_animal_often') -%}
        {%- set o = state_attr('input_select.motion_camera_name','options') -%}
        {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null'
           and o|lower != 'none' and o|lower != 'unknown' and o|lower != 'unavailable' and o|length > 0
           and m|lower != 'none' and m|lower != 'unknown' and m|lower != 'unavailable' and m|int > 0 -%}
          {%- set s = s|from_json -%}
          {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' and s is iterable -%}
            {%- for p in s -%}
              {%- if p|lower != 'null' and p|int >= m|int -%}
                {{ o[loop.index0] }}
              {% endif -%}
            {%- endfor -%}
          {%- else -%}null{%- endif -%}
        {%- else -%}null{%- endif -%}

- platform: template
  sensors:
    motion_detected_animal_ago_often:
      friendly_name: '🐄 often ⧖'
      icon_template: 'mdi:cow'
      unit_of_measurement: 's'
      value_template: >-
        {%- set c = states('sensor.motion_detected_animal_camera_often') -%}
        {%- if c|lower != 'none' and c|lower != 'unknown' and c|lower != 'unavailable' and c|lower != 'null' %}
          {%- set p = states('sensor.motion_' + c + '_detected_animal_ago') %}
          {% if p|lower != 'none' and p|lower != 'unknown' and p|lower != 'unavailable' and p|lower != 'null' -%}
            {{ p }}
          {%- else -%}null{%- endif %}
        {%- else -%}null{%- endif %}

- platform: template
  sensors:
    motion_detected_animal_counter:
      unit_of_measurement: '🐄'
      value_template: >
        {{ states('counter.motion_detected_animal_counter') }}

- platform: template
  sensors:
    motion_detected_animal_ago_low_counter:
      friendly_name: '🐄 activity high counter'
      icon_template: 'mdi:counter'
      value_template: >
        {% if states('counter.motion_detected_animal_ago_low_counter')|lower != 'unavailable' %}
          {{ states('counter.motion_detected_animal_ago_low_counter')|int }}
        {%- else -%}null{%- endif -%}

- platform: template
  sensors:
    motion_detected_animal_ago_low_percent:
      friendly_name: '🐄 activity high percent'
      icon_template: 'mdi:percent'
      unit_of_measurement: '%'
      value_template: >
        {% set c = states('counter.motion_detected_animal_ago_low_counter') %}
        {% set ic = states('counter.motion_detected_animal_ago_counter') %}
        {% if ic|int > 0 %}
          {{ c|float / ic|float * 100.0 }}
        {%- else -%}null{%- endif -%}

- platform: template
  sensors:
    motion_detected_animal_ago_high_counter:
      friendly_name: '🐄 activity low counter'
      icon_template: 'mdi:counter'
      value_template: >
        {% if states('counter.motion_detected_animal_ago_high_counter')|lower != 'unavailable' %}
          {{ states('counter.motion_detected_animal_ago_high_counter')|int }}
        {%- else -%}null{%- endif -%}

- platform: template
  sensors:
    motion_detected_animal_ago_high_percent:
      friendly_name: '🐄 activity low percent'
      icon_template: 'mdi:percent'
      unit_of_measurement: '%'
      value_template: >
        {% set c = states('counter.motion_detected_animal_ago_high_counter') %}
        {% set ic = states('counter.motion_detected_animal_ago_counter') %}
        {% if ic|int > 0 %}
          {{ c|float / ic|float * 100.0 }}
        {%- else -%}null{%- endif -%}

## status
- platform: template
  sensors:
    motion_detected_animal_status:
      friendly_name: '🐄 (animal)'
      icon_template: 'mdi:cow'
      value_template: >-
        {% if is_state('binary_sensor.motion_detected_animal','on') %}
          {% set end = states('sensor.motion_detected_animal_date') %}
          {% set event = states('sensor.motion_detected_animal_event') %}
          {% set device = states('sensor.motion_detected_animal_device') %}
          {% set camera = states('sensor.motion_detected_animal_camera') %}
          {% set elapsed = states('sensor.motion_detected_animal_elapsed') %}
          {% set confidence = states('sensor.motion_detected_animal_confidence')|int %}
          {% set n = utcnow().timestamp()|int %}
          {% if n|int > 0 and end|int > 0 %}
            {% set when = end|int|timestamp_custom("%I:%M:%S %p on %m/%d") -%}
            {% set delay = n - end|int %}
          {% else %}
            {% set when = 'null' %}
            {% set delay = 'null' %}
          {% endif %}
          {{ device -}}/{{- camera -}}/{{- event }} at {{ when -}};
          β: {{ confidence }} ;
          Δ: {{ delay -}}s;
          δ: {{ elapsed -}}s
        {% else %}
          {% set s = states.sensor.motion_detected_animal_status.state %}
          {% if s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and s|lower != 'null' %}
            {{ s }}
          {% else %}Pending{% endif %}
        {% endif %}

## attributes
- platform: template
  sensors:
    motion_detected_animal_event:
      friendly_name: '🐄 event'
      value_template: >
        {% if is_state('binary_sensor.motion_detected_animal','on') %}
          {% set e = state_attr('sensor.motion_event_annotated','event') %}
          {% if e is mapping %}
            {% set s = e.event %}
            {% if s|lower != 'none' and s|lower != 'unknown' and s != 'null' and s|int > 0 %}
              {{ s|int }}
            {%- else -%}null{%- endif -%}
          {%- else -%}null{%- endif -%}
        {% elif is_state('binary_sensor.motion_detected_expired','off') %}
          {% set s = states('sensor.motion_detected_animal_event') %}
          {% if s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and s|lower != 'none' %}
            {{ s }}
          {% else %}null{% endif %}
        {% else %}null{% endif %}

- platform: template
  sensors:
    motion_detected_animal_device:
      friendly_name: '🐄 device'
      icon_template: 'mdi:raspberry-pi'
      value_template: >
        {% if is_state('binary_sensor.motion_detected_animal','on') %}
          {% set s = states('sensor.motion_detected_device') %}
          {% if s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and s|lower != 'null' %}
            {% set p = s %}
          {% endif %}
        {% endif %}
        {% if p is defined %}
          {{ p }}
        {% elif is_state('binary_sensor.motion_detected_animal_expired','off') %}
          {% set s = states.sensor.motion_detected_animal_device.state %}
          {% if s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and s|lower != 'null' %}
            {{ s }}
          {% else %}null{% endif %}
        {% else %}null{% endif %}

- platform: template
  sensors:
    motion_detected_animal_camera:
      friendly_name: '🐄 camera'
      icon_template: 'mdi:cow'
      value_template: >
        {% if is_state('binary_sensor.motion_detected_animal','on') %}
          {% set s = states('sensor.motion_detected_camera') %}
          {% if s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and s|lower != 'null' %}
            {% set p = s %}
          {% endif %}
        {% endif %}
        {% if p is defined %}
          {{ p }}
        {% elif is_state('binary_sensor.motion_detected_animal_expired','off') %}
          {% set s = states.sensor.motion_detected_animal_camera.state %}
          {% if s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and s|lower != 'null' %}
            {{ s }}
          {% else %}null{% endif %}
        {% else %}null{% endif %}

- platform: template
  sensors:
    motion_detected_animal_elapsed:
      friendly_name: '🐄 elapsed'
      icon_template: 'mdi:timer'
      value_template: >
        {% if is_state('binary_sensor.motion_detected_animal','on') %}
          {% set s = states('sensor.motion_detected_elapsed') %}
          {% if s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and s|lower != 'null' %}
            {% set p = s %}
          {% endif %}
        {% endif %}
        {% if p is defined %}
          {{ p }}
        {% elif is_state('binary_sensor.motion_detected_animal_expired','off') %}
          {% set s = states.sensor.motion_detected_animal_elapsed.state %}
          {% if s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and s|lower != 'null' %}
            {{ s }}
          {% else %}null{% endif %}
        {% else %}null{% endif %}

- platform: template
  sensors:
    motion_detected_animal_date:
      friendly_name: '🐄 date'
      value_template: >
        {% if is_state('binary_sensor.motion_detected_animal','on') %}
          {% set s = states('sensor.motion_detected_date') %}
          {% if s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and s|lower != 'null' %}
            {% set p = s %}
          {% endif %}
        {% endif %}
        {% if p is defined %}
          {{ p }}
        {% else %}
          {% set s = states.sensor.motion_detected_animal_date.state %}
          {% if s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and s|lower != 'null' %}
            {{ s }}
          {% else %}null{% endif %}
        {% endif %}

- platform: template
  sensors:
    motion_detected_animal_where:
      friendly_name: '🐄 where'
      icon_template: 'mdi:map-marker'
      value_template: >
        {% if is_state('binary_sensor.motion_detected_animal','on') %}
          {% set s = states('sensor.motion_detected_animal_camera') %}
          {% if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' %}
            {% set p = s %}
          {% endif %}
        {% endif %}
        {% if p is defined %}
          {{ p }}
        {% else %}
          {% set s = states('sensor.motion_detected_animal_where') %}
          {% if s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and s|lower != 'null' %}
            {{ s }}
          {% else %}Pending{% endif %}
        {% endif %}

- platform: template
  sensors:
    motion_detected_animal_when:
      friendly_name: '🐄 when'
      icon_template: 'mdi:calendar-clock'
      value_template: >
        {% if is_state('binary_sensor.motion_detected_animal','on') %}
          {% set s = states('sensor.motion_detected_animal_date') %}
          {% if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' %}
            {% set p = s|int|timestamp_custom("%a %b %d %I:%M:%S %p %Z") %}
          {% endif %}
        {% endif %}
        {% if p is defined %}
          {{ p }}
        {% else %}
          {% set s = states('sensor.motion_detected_animal_when') %}
          {% if s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and s|lower != 'null' %}
            {{ s }}
          {% else %}Pending{% endif %}
        {% endif %}

## live camera

- platform: template
  sensors:
    motion_camera_detected_animal_live:
      friendly_name: '🐄 camera live'
      value_template: >-
        {% set camera = states('sensor.motion_detected_animal_where') %}
        {% if camera|lower != 'none' and camera|lower != 'unavailable' and camera|lower != 'unknown' and camera|lower != 'null' and camera|length > 0 %}
          {% set ep = state_attr('camera.motion_' + camera + '_live','entity_picture') %}
          {% if ep|lower != 'none' and ep|lower != 'unknown' and ep|lower != 'unavailable' and ep|length > 0 %}
            {% set bu = states('sensor.motion_base_url') %}
            {% if bu|lower != 'none' and bu|lower != 'unknown' and bu|lower != 'unavailable' and bu|length > 0 %}
              {% set p = bu + ep %}
            {% endif %}
          {% endif %}
        {% endif %}
        {% if p is defined and p|lower != 'none' and p|lower != 'unavailable' and p|lower != 'unknown' and p|length > 0 %}
          {{ p }}
        {% else %}
          {{ states('sensor.motion_nosignal_animated') }}
        {% endif %}

## array of animal count

- platform: template
  sensors:
    motion_detected_animal_loop:
      value_template: >-
        {%- if is_state('binary_sensor.motion_detected_animal','on') -%}
          {%- set options = state_attr('input_select.motion_detect_animal','options') -%}
          {%- if options|lower != 'none' and options|lower != 'unknown' and options is iterable -%}
            {%- set array = state_attr('sensor.motion_event_annotated','results') -%}
            {%- if array|lower != 'none' and array|lower != 'unknown' and array is iterable -%}
              {%- set t = states('sensor.motion_detected_animal_threshold') -%}
              {%- if t|lower != 'none' and t|lower != 'unknown' and t|lower != 'null' -%}
                {%- for results in array -%}
                  {%- if loop.first -%}[{%- else -%},{%- endif -%}
                  {%- set e = results.entity -%}
                  {%- if e|lower != 'none' and e|lower != 'unknown' and e|lower != 'null' -%}
                    {%- set c = results.confidence -%}
                    {%- set x = results.center.x -%}
                    {%- set y = results.center.y -%}
                    {%- set w = results.width -%}
                    {%- set h = results.height -%}
                    {%- if e in options and c|float >= t|float -%}
                    {"e":"{{- e -}}","c":{{- c|int -}},"x":{{- x -}},"y":{{- y -}},"w":{{- w -}},"h":{{- h -}}}
                    {%- else -%}
                      null
                    {%- endif -%}
                  {%- else -%}
                    null
                  {%- endif -%}
                  {%- if loop.last -%}]{%- endif -%}
                {%- endfor -%}
              {%- else -%}null{%- endif -%}
            {%- else -%}null{%- endif -%}
          {%- else -%}null{%- endif -%}
        {%- else -%}
          {% set s = states.sensor.motion_detected_animal_loop.state %}
          {%- if s|lower != 'null' and s|lower != 'unavailable' and s|lower != 'unknown' and s|lower != 'none' -%}
            {{ s }}
          {%- else -%}null{%- endif -%}
        {%- endif -%}
    motion_detected_animal_list:
      icon_template: 'mdi:cow'
      friendly_name: '🐄 list'
      value_template: >-
        {%- if is_state('binary_sensor.motion_detected_animal','on') -%}
          {%- set s = states('sensor.motion_detected_animal_loop') -%}
          {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'null' and s|length > 0 -%}
            {%- set s = s|from_json -%}
            {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'null' and s|length > 0 -%}
              {%- set p = s|select("!=",none)|list -%}
            {%- endif -%}
          {%- endif -%}
        {%- endif -%}
        {%- if p is defined -%}
          {{ p }}
        {%- else -%}
          {%- set s = states.sensor.motion_detected_animal_list.state -%}
          {%- if s|lower != 'null' and s|lower != 'unavailable' and s|lower != 'unknown' and s|lower != 'none' -%}
            {{ s }}
          {%- else -%}null{%- endif -%}
        {%- endif -%}

## threshold

- platform: template
  sensors:
    motion_detected_animal_threshold_confirmed:
      friendly_name: '🐄✅ threshold (β)'
      unit_of_measurement: 'β'
      icon_template: 'mdi:beta'
      value_template: >-
        {% set s = states('sensor.motion_detected_animal_confidence_confirmed_mean') %}
        {% if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'null' and s|lower != 'unavailable' %}
          {{- s -}}
        {% else %}null{% endif %}
    motion_detected_animal_threshold:
      friendly_name: '🐄 threshold (β)'
      unit_of_measurement: 'β'
      icon_template: 'mdi:beta'
      value_template: >-
        {% set s = states('input_boolean.motion_detected_animal_tune') %}
        {% if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'null' and s|lower != 'unavailable' %}
          {% if is_state('input_boolean.motion_detected_animal_tune','off') %}
            {% set s = states('input_number.motion_detected_animal_percent') %}
          {% else %}
            {% set s = states('sensor.motion_detected_animal_threshold_tune') %}
          {% endif %}
          {% if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'null' and s|lower != 'unavailable' %}
            {{ s|int }}
          {% else %}null{% endif %}
        {% else %}null{% endif %}

- platform: template
  sensors:
    motion_detected_animal_threshold_deviation:
      friendly_name: '🐄 β deviation'
      unit_of_measurement: 'βσ'
      icon_template: 'mdi:sigma-lower'
      value_template: >-
        {% set p = states('input_number.motion_detected_animal_percent_deviation') %}
        {% if p|lower != 'none' and p|lower != 'unknown' and p|lower != 'null' and p|lower != 'unavailable' %}
          {{ p|float }}
        {% else %}null{% endif %}

- platform: template
  sensors:
    motion_detected_animal_threshold_tune:
      friendly_name: '🐄 auto-threshold'
      unit_of_measurement: 'β'
      icon_template: 'mdi:cow'
      value_template: >-
        {% set x = states('sensor.motion_detected_animal_confidence_max') %}
        {% set m = states('sensor.motion_detected_animal_confidence_mean') %}
        {% set c = states('sensor.motion_detected_animal_confidence_count') %}
        {% set d = states('sensor.motion_detected_animal_confidence_stdev') %}
        {% set p = states('sensor.motion_detected_animal_threshold_deviation') %}
        {% set z = states('input_number.motion_detected_animal_percent') %}
        {% if x|lower != 'none' and x|lower != 'unknown' and x|lower != 'unavailable' and x|lower != 'null'
            and m|lower != 'none' and m|lower != 'unknown' and m|lower != 'unavailable' and m|lower != 'null'
            and c|lower != 'none' and c|lower != 'unknown' and c|lower != 'unavailable' and c|lower != 'null'
            and d|lower != 'none' and d|lower != 'unknown' and d|lower != 'unavailable' and d|lower != 'null'
            and p|lower != 'none' and p|lower != 'unknown' and p|lower != 'unavailable' and p|lower != 'null'
            and z|lower != 'none' and z|lower != 'unknown' and z|lower != 'unavailable' and z|lower != 'null' 
            and c|int > 5 %}
          {% set n = m|float + 2 * d|float %}
          {% if n > x|float %}{% set n = x|float %}{% endif %}
          {% set n = n - p|float * d|float %}
          {# set n = x|float - p|float * d|float #}
          {% if n > z|float %}
            {{ n|int }}
          {% else %}
            {{ z|int }}
          {% endif %}
        {% else %}{{ z|int }}{% endif %}

## count

- platform: template
  sensors:
    motion_detected_animal_array:
      value_template: >-
        {%- set options = state_attr('input_select.motion_detect_animal','options') -%}
        {%- if options|lower != 'none' and options|lower != 'unknown' and options is iterable -%}
          {%- set option = states('input_select.motion_detect_animal') -%}
          {%- set array = state_attr('sensor.motion_event_annotated','results') -%}
          {%- if array|lower != 'none' and array|lower != 'unknown' and array is iterable -%}
            {%- set t = states('sensor.motion_detected_animal_threshold') -%}
            {%- if t|lower != 'none' and t|lower != 'unknown' and t|lower != 'null' -%}
              {%- for results in array -%}
                {%- if loop.first -%}[{%- else -%},{%- endif -%}
                {%- set e = results.entity -%}
                {%- if e|lower != 'none' and e|lower != 'unknown' and e|lower != 'null' -%}
                  {%- set c = results.confidence -%}
                  {%- if ( e in options ) and c|float >= t|float -%}
                    1
                  {%- else -%}
                    0
                  {%- endif -%}
                {%- else -%}
                  0
                {%- endif -%}
                {%- if loop.last -%}]{%- endif -%}
              {%- endfor -%}
            {%- else -%}null{%- endif -%}
          {%- else -%}null{%- endif -%}
        {%- else -%}null{%- endif -%}
    motion_detected_animal_count:
      friendly_name: '🐄 Count (Σ)'
      unit_of_measurement: '🐄'
      icon_template: 'mdi:cow'
      value_template: >
        {% if is_state('binary_sensor.motion_detected','on') %}
          {% set s = states('sensor.motion_detected_animal_array') %}
          {% if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'null' and s|length > 0 %}
            {% set s = s|from_json %}
            {% if s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'none' and s is iterable %}
              {% set p = s|sum|int %}
            {% endif %}
          {% endif %}
        {% endif %}
        {% if p is defined %}
          {{ p }}
        {% elif is_state('binary_sensor.motion_detected_animal_expired','off') %}
          {% set s = states.sensor.motion_detected_animal_count.state %}
          {% if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'null' %}
            {{ s }}
          {% else %}null{% endif %}
        {% else %}null{% endif %}

- platform: statistics
  name: motion_detected_animal_count_statistics
  entity_id: sensor.motion_detected_animal_count
  sampling_size: 100
  max_age:
    hours: 8

- platform: template
  sensors:
    motion_detected_animal_count_mean:
      friendly_name: '🐄 Σ μ'
      unit_of_measurement: '🐄'
      icon_template: 'mdi:cow'
      value_template: >-
        {% if states('sensor.motion_detected_animal_count_statistics')|lower != 'unknown' %}
          {{ states('sensor.motion_detected_animal_count_statistics')|float }}
        {%- else -%}null{%- endif -%}
    motion_detected_animal_count_min:
      friendly_name: '🐄 Σ min'
      unit_of_measurement: '🐄'
      icon_template: 'mdi:cow'
      value_template: >-
        {% if states('sensor.motion_detected_animal_count_statistics')|lower != 'unknown' %}
          {{ state_attr('sensor.motion_detected_animal_count_statistics','min_value')|int }}
        {%- else -%}null{%- endif -%}
    motion_detected_animal_count_max:
      friendly_name: '🐄 Σ max'
      unit_of_measurement: '🐄'
      icon_template: 'mdi:cow'
      value_template: >-
        {% if states('sensor.motion_detected_animal_count_statistics')|lower != 'unknown' %}
          {{ state_attr('sensor.motion_detected_animal_count_statistics','max_value')|int }}
        {%- else -%}null{%- endif -%}
    motion_detected_animal_count_stdev:
      friendly_name: '🐄 Σ σ'
      unit_of_measurement: '🐄'
      icon_template: 'mdi:cow'
      value_template: >
        {% if states('sensor.motion_detected_animal_count_statistics')|lower != 'unknown' %}
          {{ state_attr('sensor.motion_detected_animal_count_statistics','standard_deviation')|float }}
        {%- else -%}null{%- endif -%}
    motion_detected_animal_count_change:
      friendly_name: '🐄 Σ Δ'
      unit_of_measurement: '🐄'
      icon_template: 'mdi:cow'
      value_template: >
        {% if states('sensor.motion_detected_animal_count_statistics')|lower != 'unknown' %}
          {{ state_attr('sensor.motion_detected_animal_count_statistics','change')|float }}
        {%- else -%}null{%- endif -%}

- platform: statistics
  name: motion_detected_animal_count_stdev_statistics
  entity_id: sensor.motion_detected_animal_count_stdev
  sampling_size: 100
  max_age:
    hours: 24

- platform: template
  sensors:
    motion_detected_animal_count_stdev_mean:
      friendly_name: '🐄 Σ σ μ'
      unit_of_measurement: '🐄'
      value_template: >
        {% if states.sensor.motion_detected_animal_count_stdev_statistics|lower != 'unknown' %}
          {{ states('sensor.motion_detected_animal_count_stdev_statistics')|float }}
        {%- else -%}null{%- endif -%}
    motion_detected_animal_count_stdev_change:
      friendly_name: '🐄 Σ σ Δ'
      unit_of_measurement: '🐄'
      value_template: >
        {% if states.sensor.motion_detected_animal_count_stdev_statistics|lower != 'unknown' %}
          {{ state_attr('sensor.motion_detected_animal_count_stdev_statistics','change')|float }}
        {%- else -%}null{%- endif -%}
    motion_detected_animal_count_stdev_stdev:
      friendly_name: '🐄 Σ σ σ'
      unit_of_measurement: '🐄'
      value_template: >
        {% if states.sensor.motion_detected_animal_count_stdev_statistics|lower != 'unknown' %}
          {{ state_attr('sensor.motion_detected_animal_count_stdev_statistics','standard_deviation')|float }}
        {%- else -%}null{%- endif -%}

- platform: statistics
  name: motion_detected_animal_count_change_statistics
  entity_id: sensor.motion_detected_animal_count_change
  sampling_size: 100
  max_age:
    hours: 24

- platform: template
  sensors:
    motion_detected_animal_count_change_mean:
      friendly_name: '🐄 Σ Δ μ'
      unit_of_measurement: '🐄'
      value_template: >
        {% if states.sensor.motion_detected_animal_count_change_statistics|lower != 'unknown' %}
          {{ states('sensor.motion_detected_animal_count_change_statistics')|float }}
        {%- else -%}null{%- endif -%}
    motion_detected_animal_count_change_stdev:
      friendly_name: '🐄 Σ Δ σ'
      unit_of_measurement: '🐄'
      value_template: >
        {% if states.sensor.motion_detected_animal_count_change_statistics|lower != 'unknown' %}
          {{ state_attr('sensor.motion_detected_animal_count_change_statistics','standard_deviation')|float }}
        {%- else -%}null{%- endif -%}
    motion_detected_animal_count_change_change:
      friendly_name: '🐄 Σ Δ Δ'
      unit_of_measurement: '🐄'
      value_template: >
        {% if states.sensor.motion_detected_animal_count_change_statistics|lower != 'unknown' %}
          {{ state_attr('sensor.motion_detected_animal_count_change_statistics','change')|float }}
        {%- else -%}null{%- endif -%}

## confidence

- platform: template
  sensors:
    motion_detected_animal_confidence:
      friendly_name: '🐄 confidence (β)'
      icon_template: 'mdi:cow'
      unit_of_measurement: 'β'
      value_template: >
        {% set mc = '' %}
        {% if is_state('binary_sensor.motion_' + mc + 'detected','on') %}
          {% set results = state_attr('sensor.motion_' + mc + 'event_annotated','results') %}
          {% if results|lower != 'none' and results|lower != 'unavailable' and results|lower != 'unknown' and results|length > 0 %}
            {% set option = states('input_select.motion_detect_animal') %}
            {% if option|lower == 'all' or option|lower == 'animal' %}
              {% set options = state_attr('input_select.motion_detect_animal','options') %}
              {% if options|lower != 'none' and options|lower != 'unknown' and options|length > 0 %}
                {% set l = results|selectattr('entity','in',options)|map(attribute='confidence')|list %}
              {% endif %}
            {% else %}
              {% set options = state_attr('input_select.motion_detect_animal','options') %}
              {% if options|lower != 'none' and options|lower != 'unknown' and options|length > 0 %}
                {% set l = results|selectattr('entity','==',option)|map(attribute='confidence')|list %}
              {% endif %}
            {% endif %}
            {% if l|lower != 'none' and l|length > 0 %}
              {% set p = l|max|int %}
            {% endif %}
          {% endif %}
        {% endif %}
        {% if p is defined %}
          {{ p }}
        {%- elif is_state('binary_sensor.motion_' + mc + 'detected_animal_expired','off') -%}
          {% set s = states('sensor.motion_' + mc + 'detected_animal_confidence.state') %}
          {% if s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and s|lower != 'null' %}
            {{ s }}
          {% else %}null{% endif %}
        {% else %}null{% endif %}
    motion_detected_animal_confidence_confirmed:
      friendly_name: '🐄✅ (β)'
      icon_template: 'mdi:cow'
      unit_of_measurement: 'β'
      value_template: >
        {% if is_state('binary_sensor.motion_detected_animal','on') %}
          {% set s = states('sensor.motion_detected_animal_confidence') %}
          {% if s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and s|lower != 'null' and s|int <= 100 %}
            {% set p = s|int %}
          {% endif %}
        {% endif %}
        {% if p is defined %}
          {{ p }}
        {% else %}
          {% set s = states.sensor.motion_detected_animal_confidence_confirmed.state %}
          {% if s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and s|lower != 'null' %}
            {{ s }}
          {% else %}null{% endif %}
        {% endif %}
    motion_detected_animal_confirmed_percent:
      friendly_name: '🐄✅ (%)'
      icon_template: 'mdi:percent'
      unit_of_measurement: '%'
      value_template: >
        {% set cc = states('sensor.motion_detected_animal_confidence_confirmed_count') %}
        {% if cc|lower != 'none' and cc|lower != 'unavailable' and cc|lower != 'unknown' and cc|lower != 'null' %}
          {% set c = states('sensor.motion_detected_animal_confidence_count') %}
          {% if c|lower != 'none' and c|lower != 'unavailable' and c|lower != 'unknown' and c|lower != 'null' and c|int > 0 %}
            {{ '%0.2f' | format(cc|int / c|int) }}
          {% else %}null{% endif %}
        {% else %}null{% endif %}

- platform: statistics
  name: motion_detected_animal_confirmed_percent_statistics
  entity_id: sensor.motion_detected_animal_confirmed_percent
  sampling_size: 1000
  max_age:
    hours: 72

- platform: template
  sensors:
    motion_detected_animal_confirmed_percent_count:
      friendly_name: '🐄✅ % Σ'
      unit_of_measurement: '%'
      icon_template: 'mdi:cow'
      value_template: >-
        {% if states('sensor.motion_detected_animal_confidence_statistics')|lower != 'unknown' %}
          {{ state_attr('sensor.motion_detected_animal_confirmed_percent_statistics','count')|int }}
        {%- else -%}null{%- endif -%}
    motion_detected_animal_confirmed_percent_mean:
      friendly_name: '🐄✅ % μ'
      icon_template: 'mdi:cow'
      unit_of_measurement: '%'
      value_template: >-
        {% if states('sensor.motion_detected_animal_confirmed_percent_statistics')|lower != 'unknown' %}
          {{ states('sensor.motion_detected_animal_confirmed_percent_statistics')|float }}
        {%- else -%}null{%- endif -%}
    motion_detected_animal_confirmed_percent_min:
      friendly_name: '🐄✅ % min'
      unit_of_measurement: '%'
      icon_template: 'mdi:cow'
      value_template: >-
        {% if states('sensor.motion_detected_animal_confirmed_percent_statistics')|lower != 'unknown' %}
          {{ state_attr('sensor.motion_detected_animal_confirmed_percent_statistics','min_value')|int }}
        {%- else -%}null{%- endif -%}
    motion_detected_animal_confirmed_percent_max:
      friendly_name: '🐄✅ % max'
      unit_of_measurement: '%'
      icon_template: 'mdi:cow'
      value_template: >-
        {% if states('sensor.motion_detected_animal_confirmed_percent_statistics')|lower != 'unknown' %}
          {{ state_attr('sensor.motion_detected_animal_confirmed_percent_statistics','max_value')|int }}
        {%- else -%}null{%- endif -%}
    motion_detected_animal_confirmed_percent_stdev:
      friendly_name: '🐄✅ % σ'
      unit_of_measurement: '%'
      icon_template: 'mdi:cow'
      value_template: >
        {% if states('sensor.motion_detected_animal_confirmed_percent_statistics')|lower != 'unknown' %}
          {{ state_attr('sensor.motion_detected_animal_confirmed_percent_statistics','standard_deviation')|float }}
        {%- else -%}null{%- endif -%}

- platform: statistics
  name: motion_detected_animal_confidence_confirmed_statistics
  entity_id: sensor.motion_detected_animal_confidence_confirmed
  sampling_size: 1000
  max_age:
    hours: 72

- platform: template
  sensors:
    motion_detected_animal_confidence_confirmed_count:
      friendly_name: '🐄✅ β Σ'
      unit_of_measurement: 'β'
      icon_template: 'mdi:cow'
      value_template: >-
        {% if states('sensor.motion_detected_animal_confidence_confirmed_statistics')|lower != 'unknown' %}
          {{ state_attr('sensor.motion_detected_animal_confidence_confirmed_statistics','count')|int }}
        {%- else -%}null{%- endif -%}
    motion_detected_animal_confidence_confirmed_mean:
      friendly_name: '🐄✅ β μ'
      icon_template: 'mdi:cow'
      unit_of_measurement: 'β'
      value_template: >-
        {% if states('sensor.motion_detected_animal_confidence_confirmed_statistics')|lower != 'unknown' %}
          {{ states('sensor.motion_detected_animal_confidence_confirmed_statistics')|float }}
        {%- else -%}null{%- endif -%}
    motion_detected_animal_confidence_confirmed_min:
      friendly_name: '🐄✅ β min'
      unit_of_measurement: 'β'
      icon_template: 'mdi:cow'
      value_template: >-
        {% if states('sensor.motion_detected_animal_confidence_confirmed_statistics')|lower != 'unknown' %}
          {{ state_attr('sensor.motion_detected_animal_confidence_confirmed_statistics','min_value')|int }}
        {%- else -%}null{%- endif -%}
    motion_detected_animal_confidence_confirmed_max:
      friendly_name: '🐄✅ β max'
      unit_of_measurement: 'β'
      icon_template: 'mdi:cow'
      value_template: >-
        {% if states('sensor.motion_detected_animal_confidence_confirmed_statistics')|lower != 'unknown' %}
          {{ state_attr('sensor.motion_detected_animal_confidence_confirmed_statistics','max_value')|int }}
        {%- else -%}null{%- endif -%}
    motion_detected_animal_confidence_confirmed_stdev:
      friendly_name: '🐄✅ β σ'
      unit_of_measurement: 'β'
      icon_template: 'mdi:cow'
      value_template: >
        {% if states('sensor.motion_detected_animal_confidence_confirmed_statistics')|lower != 'unknown' %}
          {{ state_attr('sensor.motion_detected_animal_confidence_confirmed_statistics','standard_deviation')|float }}
        {%- else -%}null{%- endif -%}

- platform: statistics
  name: motion_detected_animal_confidence_statistics
  entity_id: sensor.motion_detected_animal_confidence
  sampling_size: 1000
  max_age:
    hours: 72

- platform: template
  sensors:
    motion_detected_animal_confidence_count:
      friendly_name: '🐄 β Σ'
      icon_template: 'mdi:counter'
      unit_of_measurement: '🐄β'
      value_template: >-
        {% if states('sensor.motion_detected_animal_confidence_statistics')|lower != 'unknown' %}
          {{ state_attr('sensor.motion_detected_animal_confidence_statistics','count')|int }}
        {%- else -%}null{%- endif -%}
    motion_detected_animal_confidence_mean:
      friendly_name: '🐄 β μ'
      icon_template: 'mdi:cow'
      unit_of_measurement: 'β'
      value_template: >-
        {% if states('sensor.motion_detected_animal_confidence_statistics')|lower != 'unknown' %}
          {{ states('sensor.motion_detected_animal_confidence_statistics')|float }}
        {%- else -%}null{%- endif -%}
    motion_detected_animal_confidence_min:
      friendly_name: '🐄 β min'
      unit_of_measurement: 'β'
      icon_template: 'mdi:cow'
      value_template: >-
        {% if states('sensor.motion_detected_animal_confidence_statistics')|lower != 'unknown' %}
          {{ state_attr('sensor.motion_detected_animal_confidence_statistics','min_value')|int }}
        {%- else -%}null{%- endif -%}
    motion_detected_animal_confidence_max:
      friendly_name: '🐄 β max'
      unit_of_measurement: 'β'
      icon_template: 'mdi:cow'
      value_template: >-
        {% if states('sensor.motion_detected_animal_confidence_statistics')|lower != 'unknown' %}
          {{ state_attr('sensor.motion_detected_animal_confidence_statistics','max_value')|int }}
        {%- else -%}null{%- endif -%}
    motion_detected_animal_confidence_stdev:
      friendly_name: '🐄 β σ'
      unit_of_measurement: 'β'
      icon_template: 'mdi:cow'
      value_template: >
        {% if states('sensor.motion_detected_animal_confidence_statistics')|lower != 'unknown' %}
          {{ state_attr('sensor.motion_detected_animal_confidence_statistics','standard_deviation')|float }}
        {%- else -%}null{%- endif -%}
    motion_detected_animal_confidence_change:
      friendly_name: '🐄 β Δ'
      unit_of_measurement: 'β'
      icon_template: 'mdi:cow'
      value_template: >
        {% if states('sensor.motion_detected_animal_confidence_statistics')|lower != 'unknown' %}
          {{ state_attr('sensor.motion_detected_animal_confidence_statistics','change')|float }}
        {%- else -%}null{%- endif -%}

- platform: statistics
  name: motion_detected_animal_confidence_stdev_statistics
  entity_id: sensor.motion_detected_animal_confidence_stdev
  sampling_size: 1000
  max_age:
    hours: 72

- platform: template
  sensors:
    motion_detected_animal_confidence_stdev_mean:
      friendly_name: '🐄 β σ μ'
      unit_of_measurement: 'β'
      value_template: >
        {% if states.sensor.motion_detected_animal_confidence_stdev_statistics|lower != 'unknown' %}
          {{ states('sensor.motion_detected_animal_confidence_stdev_statistics')|float }}
        {%- else -%}null{%- endif -%}
    motion_detected_animal_confidence_stdev_change:
      friendly_name: '🐄 β σ Δ'
      unit_of_measurement: 'β'
      value_template: >
        {% if states.sensor.motion_detected_animal_confidence_stdev_statistics|lower != 'unknown' %}
          {{ state_attr('sensor.motion_detected_animal_confidence_stdev_statistics','change')|float }}
        {%- else -%}null{%- endif -%}
    motion_detected_animal_confidence_stdev_stdev:
      friendly_name: '🐄 β σ σ'
      unit_of_measurement: 'β'
      value_template: >
        {% if states.sensor.motion_detected_animal_confidence_stdev_statistics|lower != 'unknown' %}
          {{ state_attr('sensor.motion_detected_animal_confidence_stdev_statistics','standard_deviation')|float }}
        {%- else -%}null{%- endif -%}

- platform: statistics
  name: motion_detected_animal_confidence_change_statistics
  entity_id: sensor.motion_detected_animal_confidence_change
  sampling_size: 1000
  max_age:
    hours: 72

- platform: template
  sensors:
    motion_detected_animal_confidence_change_mean:
      friendly_name: '🐄 β Δ μ'
      unit_of_measurement: 'β'
      value_template: >
        {% if states.sensor.motion_detected_animal_confidence_change_statistics|lower != 'unknown' %}
          {{ states('sensor.motion_detected_animal_confidence_change_statistics')|float }}
        {%- else -%}null{%- endif -%}
    motion_detected_animal_confidence_change_stdev:
      friendly_name: '🐄 β Δ σ'
      unit_of_measurement: 'β'
      value_template: >
        {% if states.sensor.motion_detected_animal_confidence_change_statistics|lower != 'unknown' %}
          {{ state_attr('sensor.motion_detected_animal_confidence_change_statistics','standard_deviation')|float }}
        {%- else -%}null{%- endif -%}
    motion_detected_animal_confidence_change_change:
      friendly_name: '🐄 β Δ Δ'
      unit_of_measurement: 'β'
      value_template: >
        {% if states.sensor.motion_detected_animal_confidence_change_statistics|lower != 'unknown' %}
          {{ state_attr('sensor.motion_detected_animal_confidence_change_statistics','change')|float }}
        {%- else -%}null{%- endif -%}

## ago

- platform: template
  sensors:
    motion_detected_animal_ago_relative:
      friendly_name: '🐄 ago'
      icon_template: 'mdi:cow'
      value_template: >
        {% set s = states('sensor.motion_detected_animal_ago')|int %}
        {% if s > 0 %}
          {% set d = (s/86400)|int %}
          {% set h = ((s-(d*86400))/3600)|int %}
          {% if d < 1 %}
            {% set m = ((s-(d*86400)-(h*3600))/60)|int %}
            {% set s = (s % 60)|int %}
            {% if h < 1 %}
              {% if m < 1 %}
                {{ s -}}s
              {% else %}
                {{ m -}}m; {{ s -}}s
              {% endif %}
            {% else %}
              {{ h -}}h; {{ m -}}m
            {% endif %}
          {% else %}
            {{ d -}}d; {{ h -}}h
          {% endif %}
        {%- else -%}Pending{%- endif -%}

- platform: template
  sensors:
    motion_detected_animal_ago:
      friendly_name: '🐄 ago (⧖)'
      icon_template: 'mdi:camera-timer'
      unit_of_measurement: 's'
      value_template: >
        {%- set s = states('sensor.motion_detected_animal_date') -%}
        {%- if s|lower != 'unavailable' and s|lower != 'unknown' and s|lower != 'none' and s|lower != 'null' -%}
          {%- set s = utcnow().timestamp()|int - s|int -%}
          {{- s -}}
        {%- else -%}null{%- endif -%}

- platform: statistics
  name: motion_detected_animal_ago_statistics
  entity_id: sensor.motion_detected_animal_ago
  sampling_size: 100
  max_age:
    hours: 8

# 1m
- platform: statistics
  name: motion_detected_animal_ago_1m
  entity_id: sensor.motion_detected_animal_ago
  sampling_size: 20
  max_age:
    minutes: 1

- platform: template
  sensors:
    motion_detected_animal_ago_1m_mean:
      friendly_name: '🐄 ⧖ μ 1m'
      icon_template: 'mdi:acago'
      unit_of_measurement: 's'
      value_template: >-
        {% if states('sensor.motion_detected_animal_ago_1m') != 'none' %}
          {{ states('sensor.motion_detected_animal_ago_1m')|float }}
        {%- else -%}null{%- endif -%}

# 5m
- platform: statistics
  name: motion_detected_animal_ago_5m
  entity_id: sensor.motion_detected_animal_ago
  sampling_size: 100
  max_age:
    minutes: 5

- platform: template
  sensors:
    motion_detected_animal_ago_5m_mean:
      friendly_name: '🐄 ⧖ μ 5m'
      icon_template: 'mdi:acago'
      unit_of_measurement: 's'
      value_template: >-
        {% if states('sensor.motion_detected_animal_ago_5m') != 'none' %}
          {{ states('sensor.motion_detected_animal_ago_5m')|float }}
        {%- else -%}null{%- endif -%}

# 15m
- platform: statistics
  name: motion_detected_animal_ago_15m
  entity_id: sensor.motion_detected_animal_ago
  sampling_size: 300
  max_age:
    minutes: 15

- platform: template
  sensors:
    motion_detected_animal_ago_15m_mean:
      friendly_name: '🐄 ⧖ μ 15m'
      icon_template: 'mdi:acago'
      unit_of_measurement: 's'
      value_template: >-
        {% if states('sensor.motion_detected_animal_ago_15m') != 'none' %}
          {{ states('sensor.motion_detected_animal_ago_15m')|float }}
        {%- else -%}null{%- endif -%}

# 30m
- platform: statistics
  name: motion_detected_animal_ago_30m
  entity_id: sensor.motion_detected_animal_ago
  sampling_size: 600
  max_age:
    minutes: 30

- platform: template
  sensors:
    motion_detected_animal_ago_30m_mean:
      friendly_name: '🐄 ⧖ μ 30m'
      icon_template: 'mdi:acago'
      unit_of_measurement: 's'
      value_template: >-
        {% if states('sensor.motion_detected_animal_ago_30m') != 'none' %}
          {{ states('sensor.motion_detected_animal_ago_30m')|float }}
        {%- else -%}null{%- endif -%}

- platform: template
  sensors:
    motion_detected_animal_ago_mean:
      friendly_name: '🐄 ⧖ μ'
      unit_of_measurement: 's'
      value_template: >-
        {% if states('sensor.motion_detected_animal_ago_statistics')|lower != 'unknown' %}
          {{ states('sensor.motion_detected_animal_ago_statistics')|float }}
        {%- else -%}null{%- endif -%}
    motion_detected_animal_ago_min:
      friendly_name: '🐄 ⧖ min'
      unit_of_measurement: 's'
      value_template: >-
        {% if states('sensor.motion_detected_animal_ago_statistics')|lower != 'unknown' %}
          {{ state_attr('sensor.motion_detected_animal_ago_statistics','min_value')|int }}
        {%- else -%}null{%- endif -%}
    motion_detected_animal_ago_max:
      friendly_name: '🐄 ⧖ max'
      unit_of_measurement: 's'
      value_template: >-
        {% if states('sensor.motion_detected_animal_ago_statistics')|lower != 'unknown' %}
          {{ state_attr('sensor.motion_detected_animal_ago_statistics','max_value')|int }}
        {%- else -%}null{%- endif -%}
    motion_detected_animal_ago_stdev:
      friendly_name: '🐄 ⧖ σ'
      unit_of_measurement: 's'
      value_template: >
        {% if states('sensor.motion_detected_animal_ago_statistics')|lower != 'unknown' %}
          {{ state_attr('sensor.motion_detected_animal_ago_statistics','standard_deviation')|float }}
        {%- else -%}null{%- endif -%}
    motion_detected_animal_ago_change:
      friendly_name: '🐄 ⧖ Δ'
      unit_of_measurement: 's'
      value_template: >
        {% if states('sensor.motion_detected_animal_ago_statistics')|lower != 'unknown' %}
          {{ state_attr('sensor.motion_detected_animal_ago_statistics','change')|float }}
        {%- else -%}null{%- endif -%}

- platform: statistics
  name: motion_detected_animal_ago_stdev_statistics
  entity_id: sensor.motion_detected_animal_ago_stdev
  sampling_size: 100
  max_age:
    hours: 24

- platform: template
  sensors:
    motion_detected_animal_ago_stdev_mean:
      friendly_name: '🐄 ⧖ σ μ'
      unit_of_measurement: 's'
      value_template: >
        {% if states.sensor.motion_detected_animal_ago_stdev_statistics|lower != 'unknown' %}
          {{ states('sensor.motion_detected_animal_ago_stdev_statistics')|float }}
        {%- else -%}null{%- endif -%}
    motion_detected_animal_ago_stdev_change:
      friendly_name: '🐄 ⧖ σ Δ'
      unit_of_measurement: 's'
      value_template: >
        {% if states.sensor.motion_detected_animal_ago_stdev_statistics|lower != 'unknown' %}
          {{ state_attr('sensor.motion_detected_animal_ago_stdev_statistics','change')|float }}
        {%- else -%}null{%- endif -%}
    motion_detected_animal_ago_stdev_stdev:
      friendly_name: '🐄 ⧖ σ σ'
      unit_of_measurement: 's'
      value_template: >
        {% if states.sensor.motion_detected_animal_ago_stdev_statistics|lower != 'unknown' %}
          {{ state_attr('sensor.motion_detected_animal_ago_stdev_statistics','standard_deviation')|float }}
        {%- else -%}null{%- endif -%}

- platform: statistics
  name: motion_detected_animal_ago_change_statistics
  entity_id: sensor.motion_detected_animal_ago_change
  sampling_size: 100
  max_age:
    hours: 24

- platform: template
  sensors:
    motion_detected_animal_ago_change_mean:
      friendly_name: '🐄 ⧖ Δ μ'
      unit_of_measurement: 's'
      value_template: >
        {% if states.sensor.motion_detected_animal_ago_change_statistics|lower != 'unknown' %}
          {{ states('sensor.motion_detected_animal_ago_change_statistics')|float }}
        {%- else -%}null{%- endif -%}
    motion_detected_animal_ago_change_stdev:
      friendly_name: '🐄 ⧖ Δ σ'
      unit_of_measurement: 's'
      value_template: >
        {% if states.sensor.motion_detected_animal_ago_change_statistics|lower != 'unknown' %}
          {{ state_attr('sensor.motion_detected_animal_ago_change_statistics','standard_deviation')|float }}
        {%- else -%}null{%- endif -%}
    motion_detected_animal_ago_change_change:
      friendly_name: '🐄 ⧖ Δ Δ'
      unit_of_measurement: 's'
      value_template: >
        {% if states.sensor.motion_detected_animal_ago_change_statistics|lower != 'unknown' %}
          {{ state_attr('sensor.motion_detected_animal_ago_change_statistics','change')|float }}
        {%- else -%}null{%- endif -%}
