###
# homeassistant/sensor/motion/person.yaml
###

- platform: template
  sensors:
    motion_person_online_array:
      value_template: >-
        [{%- for state in states.person -%}
          {%- if loop.first -%}{%- else -%},{%- endif -%}
          {%- set s = state.state_with_unit|lower -%}
          {%- if s != 'unknown' and s != 'unavailable' and s != 'null' and s != 'none' -%}1{%- else -%}0{%- endif -%}
        {%- endfor -%}]
    motion_person_list:
      value_template: >-
        [{%- for state in states.person -%}
          {%- if loop.first -%}{%- else -%},{%- endif -%}
          {%- set s = state.entity_id|replace ('person.','') -%}
          "{{- s -}}"
        {%- endfor -%}]

- platform: template
  sensors:
    motion_person_agos:
      value_template: >-
        [{%- for state in states.person -%}
          {%- if loop.first -%}{%- else -%},{%- endif -%}
          {%- set s = state.entity_id|replace ('person.','') -%}
          {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|length > 0 %}
            {{- (utcnow().timestamp() - as_timestamp(states.person[s].last_updated))|int -}}
          {% else %}null{% endif %}
        {%- endfor -%}]
    motion_persons:
      value_template: >-
        {% set s = states('sensor.motion_person_list') %}
        {% if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' %}
          {% set s = s|from_json %}
          {% if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' and s is iterable and s|count > 0 %}
            {%- for r in s if r|lower != 'null' -%}
              {%- if loop.first -%}{%- else -%},{%- endif -%}
              {{- r -}}
            {%- endfor -%}
          {% else %}None{% endif %}
        {% else %}Pending{% endif %}
    motion_persons_ago:
      value_template: >-
        {% set s = states('sensor.motion_person_agos') %}
        {% if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' %}
          {% set s = s|from_json %}
          {% if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' and s is iterable and s|count > 0 %}
            {%- for r in s -%}
              {%- if loop.first -%}{%- else -%},{%- endif -%}
              {{- r -}}
            {%- endfor -%}
          {% else %}None{% endif %}
        {% else %}Pending{% endif %}

- platform: template
  sensors:
    motion_person_device_ago:
      unique_id: motion_person_device_ago
      friendly_name: '📱 ago'
      icon_template: 'mdi:av-timer'
      unit_of_measurement: 's'
      value_template: >-
        {%- set s = states('sensor.motion_person_device') -%}
        {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' and s|length > 0 -%}
          {{- (utcnow().timestamp() - as_timestamp(states.device_tracker[s].last_updated))|int -}}
        {%- else -%}null{%- endif -%}
    motion_person_ago:
      unique_id: motion_person_ago
      friendly_name: '👱 ago'
      icon_template: 'mdi:av-timer'
      unit_of_measurement: 's'
      value_template: >
        {%- set s = states('sensor.motion_person') -%}
        {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' and s|length > 0 -%}
          {%- set s = states.person[s].last_updated -%}
          {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' -%}
            {{- utcnow().timestamp()|int - as_timestamp(s)|int -}}
          {%- else -%}null{%- endif -%}
        {%- else -%}null{%- endif -%}
 
- platform: template
  sensors:
    motion_person_weeks:
      value_template: >
        {%- set s = states('input_number.motion_person_weeks') -%}
        {%- if s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'none' and s|lower != 'null' -%}
          {{ s|int -}}
        {%- else -%}{{- 1 -}}{%- endif -%}
    motion_person_weeks_timestamp:
      value_template: >
        {%- set s = states('sensor.motion_person_weeks') -%}
        {%- if s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'none' and s|lower != 'null' -%}
          {{ s|int * 7 * 24 * 60 * 60 -}}
        {%- else -%}{{- 604800 -}}{%- endif -%}

- platform: template
  sensors:
    motion_person_selected:
      value_template: >-
        {%- set s = states('input_select.motion_person') -%}
        {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' and s|lower != 'auto' -%}
          {{- s -}}
        {%- else -%}
          {%- set s = states.sensor.motion_person_selected.state -%}
          {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' -%}
            {{- s -}}
          {%- else -%}none{%- endif -%}
        {%- endif -%}
    motion_person:
      unique_id: motion_person
      friendly_name: 'Person (👱)'
      icon_template: 'mdi:account'
      attribute_templates:
        entity_picture: >-
          {{ state_attr('person.' + states('sensor.motion_person'),'entity_picture')|default('/local/images/genie.png',true)  }}
      value_template: >-
        {%- set s = states('sensor.motion_person_calculator') -%}
        {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' and s|string|length > 0 -%}
          {{- s -}}
        {%- else -%}none{%- endif -%}
    motion_person_calculator:
      value_template: >-
        {%- set s = states('input_text.motion_person') -%}
        {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' -%}
          {%- set p = s -%}
        {%- endif -%}
        {%- set s = states('input_select.motion_person') -%}
        {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' and s|lower != 'auto' -%}
          {%- set p = s -%}
        {%- endif -%}
        {%- set s = states.sensor.motion_person.state -%}
        {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' and s|lower != 'auto' -%}
          {%- set p = s -%}
        {%- endif -%}
        {%- set s = states('sensor.motion_person_selected') -%}
        {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' and s|lower != 'auto' -%}
          {%- set p = s -%}
        {%- endif -%}
        {%- if p is defined %}
          {{- p -}}
        {%- else -%}
          {%- set s = states('sensor.motion_person_list') -%}
          {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' -%}
            {%- set s = s|from_json -%}
            {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' and s is iterable and s|length > 0 -%}
              {{- s|first -}}
            {%- else -%}none{%- endif -%}
          {%- else -%}none{%- endif -%}
        {%- endif -%}

- platform: template
  sensors:
    motion_person_device_selected:
      value_template: >-
        {%- set s = states('input_select.motion_person_device') -%}
        {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' and s|lower != 'auto' -%}
          {{- s -}}
        {%- else -%}
          {%- set s = states.sensor.motion_person_device_selected.state -%}
          {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' -%}
            {{- s -}}
          {%- else -%}none{%- endif -%}
        {%- endif -%}
    motion_person_device:
      unique_id: motion_person_device
      friendly_name: 'Device (📱)'
      icon_template: 'mdi:cellphone'
      value_template: >-
        {%- set s = states('sensor.motion_person_device_calculator') -%}
        {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' and s|string|length > 0 -%}
          {{- s -}}
        {%- else -%}none{%- endif -%}
    motion_person_device_calculator:
      value_template: >-
        {%- set s = states('sensor.motion_person_device_calculator_match') -%}
        {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' %}
          {% if s|string|length > 0 -%}
            {{- s -}}
          {%- else -%}
            {%- for state in states.device_tracker if states(state.entity_id)|lower != 'unavailable' and state_attr(state.entity_id,'source_type')|lower == 'gps' %}
              {% if loop.first %}{{- state.entity_id|replace ('device_tracker.','') -}}{% endif %}
            {%- endfor -%}
          {%- endif -%}
        {%- else -%}none{%- endif -%}
    motion_person_device_calculator_match:
      value_template: >-
        {%- set s = states('input_text.motion_person_device') -%}
        {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' and s|string|length > 0 -%}
          {%- set p = s -%}
        {%- endif -%}
        {%- set s = states('input_select.motion_person_device') -%}
        {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' and s|lower != 'auto' and s|string|length > 0 -%}
          {%- set p = s -%}
        {%- endif -%}
        {%- set s = states.sensor.motion_person_device.state -%}
        {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' and s|lower != 'auto' and s|string|length > 0 -%}
          {%- set p = s -%}
        {%- endif -%}
        {%- set s = states('sensor.motion_person_device_selected') -%}
        {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' and s|lower != 'auto' and s|string|length > 0 -%}
          {%- set p = s -%}
        {%- endif -%}
        {%- if p is defined %}
          {{- p -}}
        {%- else -%}
          {%- set s = states('sensor.motion_person') -%}
          {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' and s|string|length > 0 -%}
            {%- for state in states.device_tracker if s in state.entity_id and states(state.entity_id)|lower != 'unavailable' and state_attr(state.entity_id,'source_type')|lower == 'gps' %}
              {% if loop.first %}{{- state.entity_id|replace ('device_tracker.','') -}}{% endif %}
            {%- endfor -%}
          {%- else -%}none{%- endif -%}
        {%- endif -%}

## home

- platform: template
  sensors:
    motion_person_home:
      unique_id: motion_person_home
      friendly_name: '👱 home'
      icon_template: >-
        {% if is_state('binary_sensor.motion_person_home','on') %}
          {{- 'mdi:home-variant' -}}
        {%- else %}
          {{- 'mdi:home-variant-outline' -}}
        {%- endif %}
      value_template: >-
        {%- if is_state('binary_sensor.motion_person_home','on') -%}Home{%- else -%}Away{%- endif -%}

- platform: template
  sensors:
    motion_person_zone:
      friendly_name: '👱 zone'
      icon_template: >-
        {{- 'mdi:map-marker-radius' -}}
      attribute_templates:
        entity_picture: >-
          {% if is_state('binary_sensor.motion_person_zones','on') -%}
            {% set s = state_attr('sensor.motion_person_location','zone') %}
            {% if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'null' and 'entity_id' in s %}
              {% set s = s|from_json %}
              {% set s = state_attr(s.entity_id,'entity_picture') %}
              {% if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'null' %}
                {% set p = s %}
              {%- endif -%}
            {%- endif -%}
          {%- endif -%}
          {{- p|default('/local/images/map-marker-radius.png',true) -}}
        entity_id: >-
          {% if is_state('binary_sensor.motion_person_zones','on') -%}
            {% set s = state_attr('sensor.motion_person_location','zone') %}
            {% if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'null' and 'entity_id' in s %}
              {% set s = s|from_json %}
              {{ s.entity_id }}
            {%- else -%}none{%- endif -%}
          {%- else -%}unknown{%- endif -%}
        name: >-
          {% if is_state('binary_sensor.motion_person_zones','on') -%}
            {% set s = state_attr('sensor.motion_person_location','zone') %}
            {% if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'null' and 'entity_id' in s %}
              {% set s = s|from_json %}
              {{ state_attr(s.entity_id,'friendly_name') }}
            {%- else -%}none{%- endif -%}
          {%- else -%}unknown{%- endif -%}
        radius: >-
          {% if is_state('binary_sensor.motion_person_zones','on') -%}
            {% set s = state_attr('sensor.motion_person_location','zone') %}
            {% if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'null' and 'entity_id' in s %}
              {% set s = s|from_json %}
              {{ state_attr(s.entity_id,'radius') }}
            {%- else -%}none{%- endif -%}
          {%- else -%}unknown{%- endif -%}
        latitude: >-
          {% if is_state('binary_sensor.motion_person_zones','on') -%}
            {% set s = state_attr('sensor.motion_person_location','zone') %}
            {% if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'null' and 'entity_id' in s %}
              {% set s = s|from_json %}
              {{ state_attr(s.entity_id,'latitude') }}
            {%- else -%}none{%- endif -%}
          {%- else -%}unknown{%- endif -%}
        longitude: >-
          {% if is_state('binary_sensor.motion_person_zones','on') -%}
            {% set s = state_attr('sensor.motion_person_location','zone') %}
            {% if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'null' and 'entity_id' in s %}
              {% set s = s|from_json %}
              {{ state_attr(s.entity_id,'longitude') }}
            {%- else -%}none{%- endif -%}
          {%- else -%}unknown{%- endif -%}
        status: >-
          {% if is_state('binary_sensor.motion_person_zones','on') -%}
            {%- set zone = state_attr('sensor.motion_person_location','zone') -%}
            {%- if zone|lower != 'unknown' and zone|lower != 'none' and zone|lower != 'unavailable' and zone|lower != 'null' and zone|length > 0 -%}
              {{ state_attr('person.' + states('sensor.motion_person'),'friendly_name')|string + ' is ' }}
              {%- set zone = zone|from_json -%}
              {%- if zone|lower != 'unknown' and zone|lower != 'none' and zone|lower != 'unavailable' and zone|lower != 'null' and zone|length > 0 -%}
                {%- if 'inside' in zone and zone.inside|lower == 'true' -%}
                  {{- 'at ' -}}
                {%- elif 'distance' in zone -%}
                  {%- set m = zone.distance -%}
                  {%- set unit = 'm' -%}
                  {%- if m|int > 1000 -%}
                    {%- set m = '%0.2f'|format(m|float / 1000) -%}
                    {%- set unit = 'km' -%}
                  {%- else -%}
                    {%- set m = '%0.f'|format(m) -%}
                  {%- endif -%}
                  {{- 'closest (' + m|string + unit + ') to ' -}}
                {%- else -%}
                  {{- 'not found for ' -}}
                {%- endif -%}
                {%- if 'entity_id' in zone -%}
                  {{- state_attr(zone.entity_id,'friendly_name') -}}
                {%- else -%}null{%- endif -%}
              {%- else -%}
                {{- 'not available' -}}
              {%- endif -%}
            {%- else -%}none{%- endif -%}
          {%- else -%}unknown{%- endif %}
      value_template: >-
        {% if is_state('binary_sensor.motion_person_zones','on') -%}
          {% set s = state_attr('sensor.motion_person_location','zone') %}
          {% if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'null' and 'entity_id' in s %}
            {% set s = s|from_json %}
            {{ s.entity_id|replace('zone.','') }}
          {%- else -%}none{%- endif -%}
        {%- else -%}unknown{%- endif -%}

- platform: template
  sensors:
    motion_person_device_activity:
      unique_id: motion_person_device_activity
      friendly_name: '📱 activity'
      icon_template: >-
        {%- set s = states('sensor.motion_person_device') -%}
        {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' -%}
          {% set s = states('sensor.' + s + '_activity')|lower %}
          {% if s == 'stationary' %}{{- 'mdi:sofa-single' -}}
          {% elif s == 'walking' %}{{- 'mdi:walk' -}}
          {% elif s == 'running' %}{{- 'mdi:run' -}}
          {% elif s == 'automotive' %}{{- 'mdi:car' -}}
          {% elif s == 'cycling' %}{{- 'mdi:bike' -}}
          {% else %}{{- 'mdi:crosshairs-question' -}}{% endif %}
        {% else %}{{- 'mdi:crosshairs-question' -}}{% endif %}
      value_template: >-
        {%- set s = states('sensor.motion_person_device') -%}
        {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' -%}
          {{ states('sensor.' + states('sensor.motion_person_device') + '_activity') }}
        {%- else -%}null{%- endif -%}

- platform: template
  sensors:
    motion_person_device_battery:
      unique_id: motion_person_device_battery
      friendly_name: 'Battery'
      icon_template: >-
        {%- if states('sensor.motion_person_device_battery_state')|lower == 'charging' %}
          {{- 'mdi:battery-charging' -}}
        {%- else %}
          {%- set s = states('sensor.motion_person_device_battery_level') %}
          {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' -%}
            {%- if s|int >= 75 %}
              {{- 'mdi:battery-high' -}}
            {%- elif s|int >= 50 %}
              {{- 'mdi:battery-medium' -}}
            {%- elif s|int >= 25 %}
              {{- 'mdi:battery-low' -}}
            {%- else %}
              {{- 'mdi:battery-outline' -}}
            {% endif %}
          {%- else %}
            {{- 'mdi:battery' -}}
          {% endif %}
        {% endif %}
      value_template: >-
        {%- if is_state('binary_sensor.motion_person_smartphone_powersave','on') -%}
            {{- 'Power-save mode' -}}
        {%- else -%}
          {%- set s = states('sensor.motion_person_device_battery_state') -%}
          {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' -%}
            {{- s -}}
          {%- else -%}
            {{- 'Pending' -}}
          {%- endif -%}
        {%- endif -%}
        {%- set s = states('sensor.motion_person_device_battery_level') -%}
        {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' -%}
          {{- '; ' + s + '%' -}}
        {%- endif -%}

- platform: template
  sensors:
    motion_person_device_location:
      unique_id: motion_person_device_location
      friendly_name: '📱 location'
      icon_template: 'mdi:map'
      value_template: >-
        {%- set s = states('sensor.motion_person_device') -%}
        {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' -%}
          {{ '%0.4f' | format(state_attr('device_tracker.' + s,'latitude')|float) -}},
          {{- '%0.4f' | format(state_attr('device_tracker.' + s,'longitude')|float) -}}
          {{- ' ±' -}}
          {{- state_attr('device_tracker.' + s,'gps_accuracy') -}}m
          ({{- state_attr('device_tracker.' + s,'source_type')|upper -}});
          {{ state_attr('device_tracker.' + s,'altitude')|int -}}
          {{- '±' -}}
          {{- state_attr('device_tracker.' + s,'vertical_accuracy') -}}m
        {%- else -%}Pending{%- endif -%}

- platform: template
  sensors:
    motion_person_device_bssid:
      unique_id: motion_person_device_bssid
      friendly_name: '📱 BSSID'
      icon_template: 'mdi:signal-cellular-outline'
      value_template: >-
        {%- set s = states('sensor.motion_person_device') -%}
        {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' -%}
          {{ states('sensor.' + states('sensor.motion_person_device') + '_basid') }}
        {%- else -%}null{%- endif -%}
    motion_person_device_battery_state:
      unique_id: motion_person_device_battery_state
      friendly_name: 'Battery'
      icon_template: 'mdi:battery-outline'
      value_template: >-
        {%- set s = states('sensor.motion_person_device') -%}
        {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' -%}
          {{ states('sensor.' + states('sensor.motion_person_device') + '_battery_state') }}
        {%- else -%}null{%- endif -%}
    motion_person_device_connection_type:
      unique_id: motion_person_device_connection_type
      friendly_name: 'Network connection'
      icon_template: 'mdi:cellphone-wireless'
      value_template: >-
        {%- set s = states('sensor.motion_person_device') -%}
        {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' -%}
          {{ states('sensor.' + states('sensor.motion_person_device') + '_connection_type') }}
        {%- else -%}null{%- endif -%}
    motion_person_device_geocoded_location:
      unique_id: motion_person_device_geocoded_location
      friendly_name: 'Location'
      icon_template: 'mdi:map-marker'
      value_template: >-
        {%- set s = states('sensor.motion_person_device') -%}
        {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' -%}
          {{ states('sensor.' + states('sensor.motion_person_device') + '_geocoded_location') }}
        {%- else -%}null{%- endif -%}
    motion_person_device_last_update_trigger:
      unique_id: motion_person_device_last_update_trigger
      friendly_name: 'Last update'
      icon_template: 'mdi:alarm-check'
      value_template: >-
        {%- set s = states('sensor.motion_person_device') -%}
        {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' -%}
          {{ states('sensor.' + states('sensor.motion_person_device') + '_last_update_trigger') }}
        {%- else -%}null{%- endif -%}
    motion_person_device_ssid:
      unique_id: motion_person_device_ssid
      friendly_name: 'WiFi SSID'
      icon_template: 'mdi:wifi'
      value_template: >-
        {%- set s = states('sensor.motion_person_device') -%}
        {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' -%}
          {{ states('sensor.' + states('sensor.motion_person_device') + '_ssid') }}
        {%- else -%}null{%- endif -%}
    motion_person_device_storage:
      unique_id: motion_person_device_storage
      friendly_name: 'Storage available'
      icon_template: 'mdi:database'
      unit_of_measurement: '℀'
      value_template: >-
        {%- set s = states('sensor.motion_person_device') -%}
        {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' -%}
          {{ states('sensor.' + s + '_storage') }}
        {%- else -%}null{%- endif -%}
    motion_person_device_status:
      unique_id: motion_person_device_status
      friendly_name: '📱  status'
      icon_template: 'mdi:cellphone'
      value_template: >-
        {%- set s = states('sensor.motion_person_device') -%}
        {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' -%}
          {{- states('device_tracker.' + s) -}}
        {%- else -%}Pending{%- endif -%}
    motion_person_device_battery_level:
      unique_id: motion_person_device_battery_level
      friendly_name: 'Battery level'
      icon_template: 'mdi:battery'
      unit_of_measurement: '🔋'
      value_template: >-
        {%- set s = states('sensor.motion_person_device') -%}
        {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' -%}
          {{- state_attr('device_tracker.' + s,'battery_level') -}}
        {%- else -%}null{%- endif -%}

- platform: template
  sensors:
    motion_person_device_source_type:
      unique_id: motion_person_device_source_type
      friendly_name: '🗺 source'
      icon_template: 'mdi:map'
      value_template: >-
        {%- set s = states('sensor.motion_person_device') -%}
        {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' -%}
          {{- state_attr('device_tracker.' + s,'source_type') -}}
        {%- else -%}null{%- endif -%}
    motion_person_device_latitude:
      unique_id: motion_person_device_latitude
      friendly_name: '🗺 latitude'
      icon_template: 'mdi:latitude'
      value_template: >-
        {%- set s = states('sensor.motion_person_device') -%}
        {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' -%}
          {{- state_attr('device_tracker.' + s,'latitude') -}}
        {%- else -%}null{%- endif -%}
    motion_person_device_longitude:
      unique_id: motion_person_device_longitude
      friendly_name: '🗺 longitude'
      icon_template: 'mdi:longitude'
      value_template: >-
        {%- set s = states('sensor.motion_person_device') -%}
        {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' -%}
          {{- state_attr('device_tracker.' + s,'longitude') -}}
        {%- else -%}null{%- endif -%}
    motion_person_device_gps_accuracy:
      unique_id: motion_person_device_gps_accuracy
      friendly_name: '🗺 accuracy (x,y)'
      icon_template: 'mdi:human'
      unit_of_measurement: 'm'
      value_template: >-
        {%- set s = states('sensor.motion_person_device') -%}
        {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' -%}
          {{- state_attr('device_tracker.' + s,'gps_accuracy') -}}
        {%- else -%}null{%- endif -%}
    motion_person_device_altitude:
      unique_id: motion_person_device_altitude
      friendly_name: '🗺 altitude'
      icon_template: 'mdi:altimeter'
      unit_of_measurement: 'm'
      value_template: >-
        {%- set s = states('sensor.motion_person_device') -%}
        {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' -%}
          {{- state_attr('device_tracker.' + s,'altitude') -}}
        {%- else -%}null{%- endif -%}
    motion_person_device_vertical_accuracy:
      unique_id: motion_person_device_vertical_accuracy
      friendly_name: '🗺 accuracy (z)'
      icon_template: 'mdi:human'
      unit_of_measurement: 'm'
      value_template: >-
        {%- set s = states('sensor.motion_person_device') -%}
        {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' -%}
          {{- state_attr('device_tracker.' + s,'vertical_accuracy') -}}
        {%- else -%}null{%- endif -%}
    motion_person_steps:
      unique_id: motion_person_steps
      friendly_name: 'Steps'
      icon_template: 'mdi:foot-print'
      unit_of_measurement: '🦶'
      value_template: >-
        {%- set s = states('sensor.motion_person_device') -%}
        {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' -%}
          {%- set s = states('sensor.' + s + '_steps') -%}
          {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' -%}
            {{- s -}}
          {%- else -%}null{%- endif -%}
        {%- else -%}null{%- endif -%}
    motion_person_pedometer_pace:
      unique_id: motion_person_pedometer_pace
      friendly_name: 'Pedometer pace'
      unit_of_measurement: 'm/s'
      icon_template: 'mdi:shoe-print'
      value_template: >-
        {%- set s = states('sensor.motion_person_device') -%}
        {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' -%}
          {{ states('sensor.' + s + '_average_active_pace') }}
        {%- else -%}null{%- endif -%}
    motion_person_floors:
      unique_id: motion_person_floors
      friendly_name: 'Floors total'
      unit_of_measurement: '⬆⬇'
      icon_template: 'mdi:stairs'
      value_template: >-
        {{ states('sensor.motion_person_floors_ascended')|int + states('sensor.motion_person_floors_descended')|int }}
    motion_person_floors_ascended:
      unique_id: motion_person_floors_ascended
      friendly_name: 'Floors up'
      icon_template: 'mdi:stairs'
      unit_of_measurement: '⬆'
      value_template: >-
        {%- set s = states('sensor.motion_person_device') -%}
        {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' -%}
          {%- set s = states('sensor.' + s + '_floors_ascended') -%}
          {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' -%}
            {{- s -}}
          {%- else -%}null{%- endif -%}
        {%- else -%}null{%- endif -%}
    motion_person_floors_descended:
      unique_id: motion_person_floors_descended
      friendly_name: 'Floors down'
      unit_of_measurement: '⬇'
      icon_template: 'mdi:stairs'
      value_template: >-
        {%- set s = states('sensor.motion_person_device') -%}
        {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' -%}
          {%- set s = states('sensor.' + s + '_floors_descended') -%}
          {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' -%}
            {{- s -}}
          {%- else -%}null{%- endif -%}
        {%- else -%}null{%- endif -%}
    motion_smartphone_distance:
      unique_id: motion_smartphone_distance
      friendly_name: '📱 distance'
      icon_template: 'mdi:walk'
      unit_of_measurement: 'm'
      value_template: >-
        {%- set s = states('sensor.motion_person_device') -%}
        {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' -%}
          {%- set s = states('sensor.' + s + '_distance') -%}
          {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' -%}
            {{- s -}}
          {%- else -%}null{%- endif -%}
        {%- else -%}null{%- endif -%}
    motion_person_frontmost_app:
      unique_id: motion_person_frontmost_app
      friendly_name: 'Current app'
      icon_template: 'mdi:cellphone-play'
      value_template: >-
        {%- set s = states('sensor.motion_person_device') -%}
        {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' -%}
          {%- set s = states('sensor.' + s + '_frontmost_app') -%}
          {%- if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' and s|lower != 'null' -%}
            {{- s -}}
          {%- else -%}null{%- endif -%}
        {%- else -%}null{%- endif -%}

## smartphone distance

- platform: statistics
  name: motion_smartphone_distance_statistics_1d
  entity_id: sensor.motion_smartphone_distance
  sampling_size: 100
  max_age:
    hours: 24

- platform: statistics
  name: motion_smartphone_distance_statistics_1w
  entity_id: sensor.motion_smartphone_distance
  sampling_size: 700
  max_age:
    hours: 168

- platform: template
  sensors:
    motion_smartphone_distance_mean_1d:
      friendly_name: '📱 distance μ (1d)'
      unit_of_measurement: 'm'
      icon_template: 'mdi:hike'
      value_template: >-
        {% if states('sensor.motion_smartphone_distance_statistics_1d') != 'unknown' %}
          {{ states.sensor.motion_smartphone_distance_statistics_1d.state|float }}
        {%- else -%}null{%- endif -%}
    motion_smartphone_distance_min_1d:
      friendly_name: '📱 distance min (1d)'
      unit_of_measurement: 'm'
      icon_template: 'mdi:hike'
      value_template: >-
        {% if states('sensor.motion_smartphone_distance_statistics_1d') != 'unknown' %}
          {{ state_attr('sensor.motion_smartphone_distance_statistics_1d','min_value')|int }}
        {%- else -%}null{%- endif -%}
    motion_smartphone_distance_max_1d:
      friendly_name: '📱 distance max (1d)'
      unit_of_measurement: 'm'
      icon_template: 'mdi:hike'
      value_template: >-
        {% if states('sensor.motion_smartphone_distance_statistics_1d') != 'unknown' %}
          {{ state_attr('sensor.motion_smartphone_distance_statistics_1d','max_value')|int }}
        {%- else -%}null{%- endif -%}
    motion_smartphone_distance_stdev_1d:
      friendly_name: '📱 distance stdev (1d)'
      unit_of_measurement: 'm'
      icon_template: 'mdi:sigma-lower'
      value_template: >
        {% if states('sensor.motion_smartphone_distance_statistics_1d') != 'unknown' %}
          {{ state_attr('sensor.motion_smartphone_distance_statistics_1d','standard_deviation')|float }}
        {%- else -%}null{%- endif -%}
    motion_smartphone_distance_change_1d:
      friendly_name: '📱 distance change (1d)'
      unit_of_measurement: 'm'
      icon_template: 'mdi:hike'
      value_template: >
        {% if states('sensor.motion_smartphone_distance_statistics_1d') != 'unknown' %}
          {{ state_attr('sensor.motion_smartphone_distance_statistics_1d','change')|float }}
        {%- else -%}null{%- endif -%}
    motion_smartphone_distance_mean_1w:
      friendly_name: '📱 distance μ (1w)'
      unit_of_measurement: 'm'
      icon_template: 'mdi:hike'
      value_template: >-
        {% if states('sensor.motion_smartphone_distance_statistics_1w') != 'unknown' %}
          {{ states.sensor.motion_smartphone_distance_statistics_1w.state|float }}
        {%- else -%}null{%- endif -%}
    motion_smartphone_distance_min_1w:
      friendly_name: '📱 distance min (1w)'
      unit_of_measurement: 'm'
      icon_template: 'mdi:hike'
      value_template: >-
        {% if states('sensor.motion_smartphone_distance_statistics_1w') != 'unknown' %}
          {{ state_attr('sensor.motion_smartphone_distance_statistics_1w','min_value')|int }}
        {%- else -%}null{%- endif -%}
    motion_smartphone_distance_max_1w:
      friendly_name: '📱 distance max (1w)'
      unit_of_measurement: 'm'
      icon_template: 'mdi:hike'
      value_template: >-
        {% if states('sensor.motion_smartphone_distance_statistics_1w') != 'unknown' %}
          {{ state_attr('sensor.motion_smartphone_distance_statistics_1w','max_value')|int }}
        {%- else -%}null{%- endif -%}
    motion_smartphone_distance_stdev_1w:
      friendly_name: '📱 distance stdev (1w)'
      unit_of_measurement: 'm'
      icon_template: 'mdi:sigma-lower'
      value_template: >
        {% if states('sensor.motion_smartphone_distance_statistics_1w') != 'unknown' %}
          {{ state_attr('sensor.motion_smartphone_distance_statistics_1w','standard_deviation')|float }}
        {%- else -%}null{%- endif -%}
    motion_smartphone_distance_change_1w:
      friendly_name: '📱 distance change (1w)'
      unit_of_measurement: 'm'
      icon_template: 'mdi:hike'
      value_template: >
        {% if states('sensor.motion_smartphone_distance_statistics_1w') != 'unknown' %}
          {{ state_attr('sensor.motion_smartphone_distance_statistics_1w','change')|float }}
        {%- else -%}null{%- endif -%}

## ids

- platform: template
  sensors:
    motion_person_ids:
      value_template: >-
        [{%- for state in states.person -%}
          {%- if loop.first -%}{%- else -%},{%- endif -%}
          {%- set s = state.entity_id|replace ('person.','') -%}
          {{- state_attr('person[s]','id') -}}
        {%- endfor -%}]

- platform: template
  sensors:
    motion_person_count:
      unique_id: motion_person_count
      friendly_name: Person count
      icon_template: 'mdi:counter'
      unit_of_measurement: '👱'
      value_template: >-
        {{ states('sensor.motion_person_online_array')|from_json|length }}

- platform: template
  sensors:
    motion_person_count_online:
      unique_id: motion_person_count_online
      friendly_name: Person on-line count
      icon_template: 'mdi:counter'
      unit_of_measurement: '👱'
      value_template: >-
        {{ states('sensor.motion_person_online_array')|from_json|sum }}

- platform: template
  sensors:
    motion_person_count_offline:
      unique_id: motion_person_count_offline
      friendly_name: Person off-line count
      icon_template: 'mdi:counter'
      unit_of_measurement: '👱'
      value_template: >-
        {{ states('sensor.motion_person_count')|int - states('sensor.motion_person_count_online')|int  }}

- platform: template
  sensors:
    motion_person_online_percent:
      unique_id: motion_person_online_percent
      friendly_name: Person on-line percent
      icon_template: 'mdi:account'
      unit_of_measurement: '%'
      value_template: >-
        {% set c = states('sensor.motion_person_count')|int %}
        {% if c > 0 %}
          {% set o = states('sensor.motion_person_count_online')|int %}
          {{ (o / c * 100)|int }}
        {% else %}null{% endif %}

- platform: template
  sensors:
    motion_person_online_status:
      unique_id: motion_person_online_status
      friendly_name: Person on-line status 
      icon_template: 'mdi:account'
      value_template: >-
        {{ states('sensor.motion_person_count_online') -}}/{{- states('sensor.motion_person_count') }}👱;
        {{ states('sensor.motion_person_online_percent') -}}%

###
# motion_person_device_ago_mean
###

# 1d

- platform: statistics
  name: motion_person_device_ago_mean_1d
  entity_id: sensor.motion_person_device_ago
  state_characteristic: mean
  sampling_size: 100
  max_age:
    hours: 24

- platform: statistics
  name: motion_person_device_ago_min_1d
  entity_id: sensor.motion_person_device_ago
  state_characteristic: value_min
  sampling_size: 100
  max_age:
    hours: 24

- platform: statistics
  name: motion_person_device_ago_max_1d
  entity_id: sensor.motion_person_device_ago
  state_characteristic: value_max
  sampling_size: 100
  max_age:
    hours: 24

- platform: statistics
  name: motion_person_device_ago_stdev_1d
  entity_id: sensor.motion_person_device_ago
  state_characteristic: standard_deviation
  sampling_size: 100
  max_age:
    hours: 24

# 1w

- platform: statistics
  name: motion_person_device_ago_mean_1w
  entity_id: sensor.motion_person_device_ago
  state_characteristic: mean
  sampling_size: 500
  max_age:
    hours: 168

- platform: statistics
  name: motion_person_device_ago_min_1w
  entity_id: sensor.motion_person_device_ago
  state_characteristic: value_min
  sampling_size: 500
  max_age:
    hours: 168

- platform: statistics
  name: motion_person_device_ago_max_1w
  entity_id: sensor.motion_person_device_ago
  state_characteristic: value_max
  sampling_size: 500
  max_age:
    hours: 168

- platform: statistics
  name: motion_person_device_ago_stdev_1w
  entity_id: sensor.motion_person_device_ago
  state_characteristic: standard_deviation
  sampling_size: 500
  max_age:
    hours: 168
