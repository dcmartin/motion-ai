###
# homeassistant/sensor/motion/notification.yaml
###

- platform: template
  sensors:
    motion_notification_count:
      unit_of_measurement: 'count'
      friendly_name: Notification count
      icon_template: 'mdi:counter'
      value_template: >-
        {% set s = state_attr('binary_sensor.motion_notification','count') %}
        {% if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' %}
          {{ s|int }}
        {% else %}null{% endif %}

# count statistics

- platform: statistics
  name: motion_notification_count_statistics
  entity_id: sensor.motion_notifications_count
  sampling_size: 100
  max_age:
    hours: 72

- platform: template
  sensors:
    motion_notification_count_mean:
      friendly_name: 'Notification Σ'
      unit_of_measurement: '📓'
      icon_template: 'mdi:book'
      value_template: >-
        {% if states('sensor.motion_notification_count_statistics') != 'unknown' %}
          {{ states.sensor.motion_notification_count_statistics.state|float }}
        {%- else -%}null{%- endif -%}
    motion_notification_count_min:
      friendly_name: 'Notification Σ min'
      unit_of_measurement: '📓'
      icon_template: 'mdi:book'
      value_template: >-
        {% if states('sensor.motion_notification_count_statistics') != 'unknown' %}
          {{ states.sensor.motion_notification_count_statistics.attributes.min_value|int }}
        {%- else -%}null{%- endif -%}
    motion_notification_count_max:
      friendly_name: 'Notification Σ max'
      unit_of_measurement: '📓'
      icon_template: 'mdi:book'
      value_template: >-
        {% if states('sensor.motion_notification_count_statistics') != 'unknown' %}
          {{ states.sensor.motion_notification_count_statistics.attributes.max_value|int }}
        {%- else -%}null{%- endif -%}
    motion_notification_count_stdev:
      friendly_name: 'Notification Σ σ'
      icon_template: 'mdi:book'
      unit_of_measurement: '📓'
      value_template: >
        {% if states('sensor.motion_notification_count_statistics') != 'unknown' %}
          {{ states.sensor.motion_notification_count_statistics.attributes.standard_deviation|float }}
        {%- else -%}null{%- endif -%}
    motion_notification_count_change:
      friendly_name: 'Notification Σ Δ'
      unit_of_measurement: '📓'
      icon_template: 'mdi:book'
      value_template: >
        {% if states('sensor.motion_notification_count_statistics') != 'unknown' %}
          {{ states.sensor.motion_notification_count_statistics.attributes.change|float }}
        {%- else -%}null{%- endif -%}
