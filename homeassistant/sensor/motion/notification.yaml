###
# homeassistant/sensor/motion/notification.yaml
###

## general/app

- platform: template
  sensors:
    motion_notification_count:
      unit_of_measurement: '📯'
      friendly_name: Notification count
      icon_template: 'mdi:counter'
      attribute_templates:
        message: >-
          {{- '**' + state_attr('binary_sensor.motion_notification','title')|string + '**<br>' + state_attr('binary_sensor.motion_notification','message')|string }}
      value_template: >-
        {% set s = state_attr('binary_sensor.motion_notification','count') %}
        {% if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' %}
          {{ s|int }}
        {% else %}null{% endif %}

## system

- platform: template
  sensors:
    motion_notification_system_count:
      unit_of_measurement: '📯'
      friendly_name: System notification count
      icon_template: 'mdi:counter'
      attribute_templates:
        message: >-
          {{- '**' + state_attr('binary_sensor.motion_notification_system','title')|string + '**<br>' + state_attr('binary_sensor.motion_notification_system','message')|string }}
      value_template: >-
        {% set s = state_attr('binary_sensor.motion_notification_system','count') %}
        {% if s|lower != 'none' and s|lower != 'unknown' and s|lower != 'unavailable' %}
          {{ s|int }}
        {% else %}null{% endif %}

# count statistics

- platform: statistics
  name: motion_notification_count_statistics
  entity_id: sensor.motion_notifications_count
  sampling_size: 100
  max_age:
    hours: 72

- platform: template
  sensors:
    motion_notification_count_mean:
      friendly_name: 'Notification Σ'
      unit_of_measurement: '📯'
      icon_template: 'mdi:air-horn'
      value_template: >-
        {% if states('sensor.motion_notification_count_statistics') != 'unknown' %}
          {{ states('sensor.motion_notification_count_statistics')|float }}
        {%- else -%}null{%- endif -%}
    motion_notification_count_min:
      friendly_name: 'Notification Σ min'
      unit_of_measurement: '📯'
      icon_template: 'mdi:air-horn'
      value_template: >-
        {% if states('sensor.motion_notification_count_statistics') != 'unknown' %}
          {{ state_attr('sensor.motion_notification_count_statistics','min_value')|int }}
        {%- else -%}null{%- endif -%}
    motion_notification_count_max:
      friendly_name: 'Notification Σ max'
      unit_of_measurement: '📯'
      icon_template: 'mdi:air-horn'
      value_template: >-
        {% if states('sensor.motion_notification_count_statistics') != 'unknown' %}
          {{ state_attr('sensor.motion_notification_count_statistics','max_value')|int }}
        {%- else -%}null{%- endif -%}
    motion_notification_count_stdev:
      friendly_name: 'Notification Σ σ'
      icon_template: 'mdi:air-horn'
      unit_of_measurement: '📯'
      value_template: >
        {% if states('sensor.motion_notification_count_statistics') != 'unknown' %}
          {{ state_attr('sensor.motion_notification_count_statistics','standard_deviation')|float }}
        {%- else -%}null{%- endif -%}
    motion_notification_count_change:
      friendly_name: 'Notification Σ Δ'
      unit_of_measurement: '📯'
      icon_template: 'mdi:air-horn'
      value_template: >
        {% if states('sensor.motion_notification_count_statistics') != 'unknown' %}
          {{ state_attr('sensor.motion_notification_count_statistics','change')|float }}
        {%- else -%}null{%- endif -%}

