###
## sensor/motion/person_wakeup.yaml
###

- platform: template
  sensors:
    motion_person_wakeup:
      friendly_name: 'Person wakeup'
      icon_template: 'mdi:alarm'
      unit_of_measurement: 'hours'
      attribute_templates:
        begin: >-
          {# default wakeup begin specified in seconds from midnight #}
          {{ states('input_number.motion_schedule_wakeup_begin') }}
        end: >-
          {# default wakeup end specified in seconds from midnight #}
          {{ states('input_number.motion_schedule_wakeup_end') }}
      value_template: >-
        {{ states('sensor.motion_person_wakeup_measurement') }}
    motion_person_day:
      friendly_name: 'Person day'
      icon_template: 'mdi:alarm'
      unit_of_measurement: 'boolean'
      attribute_templates:
        begin: >-
          {# calculated history_stats day begin timestamp #}
          {% set s = state_attr('sensor.motion_person_wakeup','begin') %}
          {{ s|int(0) + utcnow().replace(hour=0).replace(minute=0).replace(second=0)|as_timestamp }}
        end: >-
          {# default history_stats day end timestamp #}
          {% set s = state_attr('sensor.motion_person_wakeup','end') %}
          {{ s|int(0) + utcnow().replace(hour=0).replace(minute=0).replace(second=0)|as_timestamp + 86400 }}
      value_template: >-
        {{ state_attr('binary_sensor.motion_person_day','end')|int(state_attr('sensor.motion_person_day','end')|int(0)) 
           > utcnow().timestamp()|int 
           > state_attr('binary_sensor.motion_person_day','begin')|int(state_attr('sensor.motion_person_day','begin')|int(0)) }}
    motion_person_wakeup_begin:
      value_template: >-
        {{ state_attr('sensor.motion_schedule_wakeup','begin') }}
    motion_person_wakeup_end:
      value_template: >-
        {{ state_attr('sensor.motion_schedule_wakeup','end') }}
    motion_person_wakeup_week:
      friendly_name: 'Person wakeup (1w)'
      icon_template: 'mdi:calendar-week'
      attribute_templates:
        count: >-
          {{ states('sensor.motion_person_wakeup_count_1w') }}
        coverage: >-
          {{ state_attr('sensor.motion_person_wakeup_oldest_1w','age_coverage_ratio')|float(0) * 100 }}
        usage: >-
          {{ state_attr('sensor.motion_person_wakeup_oldest_1w','buffer_usage_ratio')|float(0) * 100 }}
        duration: >-
          {% set s = states('sensor.motion_person_wakeup_oldest_1w') %}
          {% if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'null' and s|string|length > 0 %}
            {{ utcnow().timestamp() - s|as_timestamp }}
          {%- else -%}null{%- endif -%}
        oldest: >-
          {% set s = states('sensor.motion_person_wakeup_oldest_1w') %}
          {%- if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'null' and s|string|length > 0 -%}
            {{ s|as_datetime|relative_time }}
          {%- else -%}Pending{%- endif -%}
      value_template: >-
        {% set s = states('sensor.motion_person_wakeup_mean_1w')|float(-1) %}
        {%- if s > 0 -%}{{ s }}{% else %}null{% endif %}
    motion_person_wakeup_month:
      friendly_name: 'Person bedtime (1m)'
      icon_template: 'mdi:calendar-month'
      attribute_templates:
        count: >-
          {{ states('sensor.motion_person_wakeup_count_1m') }}
        coverage: >-
          {{ state_attr('sensor.motion_person_wakeup_oldest_1m','age_coverage_ratio')|float(0) * 100 }}
        usage: >-
          {{ state_attr('sensor.motion_person_wakeup_oldest_1m','buffer_usage_ratio')|float(0) * 100 }}
        duration: >-
          {% set s = states('sensor.motion_person_wakeup_oldest_1m') %}
          {% if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'null' and s|string|length > 0 %}
            {{ utcnow().timestamp() - s|as_timestamp }}
          {%- else -%}null{%- endif -%}
        oldest: >-
          {% set s = states('sensor.motion_person_wakeup_oldest_1m') %}
          {%- if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'null' and s|string|length > 0 -%}
            {{ s|as_datetime|relative_time }}
          {%- else -%}Pending{%- endif -%}
      value_template: >-
        {% set s = states('sensor.motion_person_wakeup_mean_1m')|float(-1) %}
        {%- if s > 0 -%}{{ s }}{% else %}null{% endif %}

# history

- platform: history_stats
  name: motion_person_active_count_day
  entity_id: binary_sensor.motion_person_active
  state: 'on'
  type: count
  start: >-
    {{ state_attr('sensor.motion_person_day','begin')|int(utcnow().replace(hour=0).replace(minute=0).replace(second=0)|as_timestamp) %}
  end: >-
    {{ state_attr('sensor.motion_person_day','end')|int(utcnow().replace(hour=0).replace(minute=0).replace(second=0)|as_timestamp + 86400) %}

- platform: history_stats
  name: motion_person_active_time_day
  entity_id: binary_sensor.motion_person_active
  state: 'on'
  type: time
  start: >-
    {{ state_attr('sensor.motion_person_day','begin')|int(utcnow().replace(hour=0).replace(minute=0).replace(second=0)|as_timestamp) %}
  end: >-
    {{ state_attr('sensor.motion_person_day','end')|int(utcnow().replace(hour=0).replace(minute=0).replace(second=0)|as_timestamp + 86400) %}

# 1w

- platform: statistics
  name: motion_person_wakeup_mean_1w
  entity_id: sensor.motion_person_wakeup
  state_characteristic: mean
  sampling_size: 10
  max_age:
    days: 7

- platform: statistics
  name: motion_person_wakeup_count_1w
  entity_id: sensor.motion_person_wakeup
  state_characteristic: count
  sampling_size: 10
  max_age:
    days: 7

- platform: statistics
  name: motion_person_wakeup_oldest_1w
  entity_id: sensor.motion_person_wakeup
  state_characteristic: datetime_oldest
  sampling_size: 10
  max_age:
    days: 7

- platform: statistics
  name: motion_person_wakeup_min_1w
  entity_id: sensor.motion_person_wakeup
  state_characteristic: value_min
  sampling_size: 10
  max_age:
    days: 7

- platform: statistics
  name: motion_person_wakeup_max_1w
  entity_id: sensor.motion_person_wakeup
  state_characteristic: value_max
  sampling_size: 10
  max_age:
    days: 7

- platform: statistics
  name: motion_person_wakeup_stdev_1w
  entity_id: sensor.motion_person_wakeup
  state_characteristic: standard_deviation
  sampling_size: 10
  max_age:
    days: 7

# 1m

- platform: statistics
  name: motion_person_wakeup_mean_1m
  entity_id: sensor.motion_person_wakeup
  state_characteristic: mean
  sampling_size: 40
  max_age:
    days: 30

- platform: statistics
  name: motion_person_wakeup_count_1m
  entity_id: sensor.motion_person_wakeup
  state_characteristic: count
  sampling_size: 40
  max_age:
    days: 30

- platform: statistics
  name: motion_person_wakeup_oldest_1m
  entity_id: sensor.motion_person_wakeup
  state_characteristic: datetime_oldest
  sampling_size: 40
  max_age:
    days: 30

- platform: statistics
  name: motion_person_wakeup_min_1m
  entity_id: sensor.motion_person_wakeup
  state_characteristic: value_min
  sampling_size: 40
  max_age:
    days: 30

- platform: statistics
  name: motion_person_wakeup_max_1m
  entity_id: sensor.motion_person_wakeup
  state_characteristic: value_max
  sampling_size: 40
  max_age:
    days: 30

- platform: statistics
  name: motion_person_wakeup_stdev_1m
  entity_id: sensor.motion_person_wakeup
  state_characteristic: standard_deviation
  sampling_size: 40
  max_age:
    days: 30
