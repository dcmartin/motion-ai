###
# homeassistant/template/notification.yaml
###

- unique_id: motion_notification_count_measurement
  trigger:
    - platform: state
      entity_id: binary_sensor.motion_notification
#    - platform: template
#      value_template: >-
#        {{ states.persistent_notification|count > 0 }}
#    - platform: event
#      event_type: service_registered
#      domain: persistent.notification
#      service: create
#    - platform: event
#      event_type: service_registered
#      domain: persistent.notification
#      service: dismiss
  sensor:
    - name: 'motion_notification_count_measurement'
      state_class: 'measurement'
      device_class: 'pressure'
      unit_of_measurement: 'psi'
      state: >-
        {{ state_attr('binary_sensor.motion_notification','count')|int }}
