###
# homeassistant/template/device/class/voltage.yaml
###

- unique_id: motion_device_voltage_high_spike
  trigger:
    - platform: state
      entity_id: sensor.motion_device_voltage_1w_max
      for:
        seconds: 30
  sensor:
    - name: 'motion_device_voltage_high_spike'
      state_class: 'measurement'
      device_class: 'power_factor'
      unit_of_measurement: '%'
      attributes:
        limit: >-
          {% set mean = state_attr('sensor.motion_device_voltage_max','mean') %}
          {% set stdev = state_attr('sensor.motion_device_voltage_max','stdev') %}
          {%- if mean|lower != 'none' and mean is number and stdev|lower != 'none' and stdev is number -%}
            {% set max = state_attr('sensor.motion_device_voltage_max','max') %}
            {% set s = mean|float(0) + stdev|float(0) * states('input_number.motion_device_voltage_stdev')|float(1) %}
            {%- if s > max|float(s) -%}{{ max }}{%- else -%}{{ s }}{%- endif -%}
          {%- else -%}none{%- endif -%}
        factor: >-
          {% set mean = state_attr('sensor.motion_device_voltage_max','mean') %}
          {% set stdev = state_attr('sensor.motion_device_voltage_max','stdev') %}
          {%- if mean|lower != 'none' and mean is number and stdev|lower != 'none' and stdev is number and mean|float(0) > 0 -%}
            {{ (mean|float(0) + stdev|float(0)) / mean|float(1) * 100 }}
          {%- else -%}none{%- endif -%}
        spike: >-
         {{ states.sensor.motion_device_voltage_max.attributes }}
      state: >-
        {{- state_attr('sensor.motion_device_voltage','max') -}}

- unique_id: motion_device_voltage_low_spike
  trigger:
    - platform: state
      entity_id: sensor.motion_device_voltage_1w_min
      for:
        seconds: 30
  sensor:
    - name: 'motion_device_voltage_low_spike'
      state_class: 'measurement'
      device_class: 'power_factor'
      unit_of_measurement: '%'
      attributes:
        limit: >-
          {% set mean = state_attr('sensor.motion_device_voltage_min','mean') %}
          {% set stdev = state_attr('sensor.motion_device_voltage_min','stdev') %}
          {%- if mean|lower != 'none' and mean is number and stdev|lower != 'none' and stdev is number -%}
            {% set min = state_attr('sensor.motion_device_voltage_min','min') %}
            {% set s = mean|float(0) + stdev|float(0) * states('input_number.motion_device_voltage_stdev')|float(1) %}
            {%- if s < min|float(s) -%}{{ min }}{%- else -%}{{ s }}{%- endif -%}
          {%- else -%}none{%- endif -%}
        factor: >-
          {% set mean = state_attr('sensor.motion_device_voltage_min','mean') %}
          {% set stdev = state_attr('sensor.motion_device_voltage_min','stdev') %}
          {%- if mean|lower != 'none' and mean is number and stdev|lower != 'none' and stdev is number and mean|float(0) > 0 -%}
            {{ (mean|float(0) - stdev|float(0)) / mean|float(1) * 100 }}
          {%- else -%}none{%- endif -%}
        spike: >-
         {{ states.sensor.motion_device_voltage_min.attributes }}
      state: >-
        {{- state_attr('sensor.motion_device_voltage','min') -}}

- unique_id: motion_device_voltage_alarm
  trigger:
    - platform: state
      entity_id: group.motion_device_voltage_alarm
    - platform: state
      entity_id: binary_sensor.motion_device_voltage_exist
      attribute: 'alarm'
    - platform: state
      entity_id: binary_sensor.motion_device_class
      attribute: 'alarms'
  binary_sensor:
    - name: 'motion_device_voltage_alarm'
      device_class: problem
      attributes:
        status: >-
          {{ '{' -}}
          {%- for i in state_attr('binary_sensor.motion_device_class','alarms') -%}
            {%- if not loop.first -%},{%- endif -%}
            {{- '"' + i|string + '":"' ~ states('binary_sensor.motion_device_voltage_' + i) ~ '"' -}}
          {%- endfor -%}
          {{- '}' }}
        markdown: >-
          {%- for i in state_attr('binary_sensor.motion_device_class','alarms') if is_state('binary_sensor.motion_device_voltage_' + i|string,'on') -%}
            {%- if loop.first -%}<ul>{%- endif -%}
            {%- if states('binary_sensor.motion_device_voltage_' + i|string) -%}
              {%- set s = state_attr('binary_sensor.motion_device_voltage_' + i|string,'markdown') -%}
               <li>The voltage alarm is: <b>{{- i|upper -}}</b>{%- if s|lower != 'none' -%}<br>{{- s -}}</br>{%- endif -%}</li>
            {%- endif -%}
            {%- if loop.last -%}</ul>{%- endif -%}
          {%- endfor -%}
      state: >-
        {%- set s = state_attr('binary_sensor.motion_device_class','alarms') -%}
        {%- if s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
          {%- for i in s if states('binary_sensor.motion_device_voltage_' + i)|lower == 'on' -%}
            {%- if loop.first -%}on{%- endif -%}
          {% else %}off{%- endfor -%}
        {%- else -%}off{%- endif -%}

- unique_id: motion_device_voltage_alarm_exist
  trigger:
    - platform: state
      entity_id:
        - binary_sensor.motion_device_voltage
        - binary_sensor.motion_device_voltage_alarm
        - binary_sensor.motion_device_voltage_exist
        - binary_sensor.motion_device_voltage_good
        - binary_sensor.motion_device_voltage_bad
        - binary_sensor.motion_device_voltage_low
        - binary_sensor.motion_device_voltage_high
        - binary_sensor.motion_device_voltage_low_spike
        - binary_sensor.motion_device_voltage_high_spike
        - binary_sensor.motion_device_voltage_missing
        - binary_sensor.motion_device_voltage_unavailable
      for:
        seconds: 30
  binary_sensor:
    - name: motion_device_voltage_alarm_exist
      device_class: 'problem'
      attributes:
        friendly_name: 'Alarm exists: voltage'
        actions: >-
          {%- set group = 'device-voltage' -%}
          {%- set interval = 300 -%}
          {%- set all = [] -%}
          {#- bad -#}
          {%- set id = group|string + '-bad' -%}
          {%- set responses = state_attr('binary_sensor.motion_device_voltage_bad','responses') -%}
          {%- set actions = state_attr('binary_sensor.motion_device_voltage_bad','actions') -%}
          {%- set all = all + [{'id': id,'group': group,'interval': interval,'responses': responses,'actions': actions}] -%}
          {#- good -#}
          {%- set id = group|string + '-good' -%}
          {%- set responses = state_attr('binary_sensor.motion_device_voltage_good','responses') -%}
          {%- set actions = state_attr('binary_sensor.motion_device_voltage_good','actions') -%}
          {%- set all = all + [{'id': id,'group': group,'interval': interval,'responses': responses,'actions': actions}] -%}
          {#- high -#}
          {%- set id = group|string + '-high' -%}
          {%- set responses = state_attr('binary_sensor.motion_device_voltage_high','responses') -%}
          {%- set actions = state_attr('binary_sensor.motion_device_voltage_high','actions') -%}
          {%- set all = all + [{'id': id,'group': group,'interval': interval,'responses': responses,'actions': actions}] -%}
          {#- low -#}
          {%- set id = group|string + '-low' -%}
          {%- set responses = state_attr('binary_sensor.motion_device_voltage_low','responses') -%}
          {%- set actions = state_attr('binary_sensor.motion_device_voltage_low','actions') -%}
          {%- set all = all + [{'id': id,'group': group,'interval': interval,'responses': responses,'actions': actions}] -%}
          {#- high-spike -#}
          {%- set id = group|string + '-high_spike' -%}
          {%- set responses = state_attr('binary_sensor.motion_device_voltage_high_spike','responses') -%}
          {%- set actions = state_attr('binary_sensor.motion_device_voltage_high_spike','actions') -%}
          {%- set all = all + [{'id': id,'group': group,'interval': interval,'responses': responses,'actions': actions}] -%}
          {#- low-spike -#}
          {%- set id = group|string + '-low_spike' -%}
          {%- set responses = state_attr('binary_sensor.motion_device_voltage_low_spike','responses') -%}
          {%- set actions = state_attr('binary_sensor.motion_device_voltage_low_spike','actions') -%}
          {%- set all = all + [{'id': id,'group': group,'interval': interval,'responses': responses,'actions': actions}] -%}
          {#- missing -#}
          {%- set id = group|string + '-missing' -%}
          {%- set responses = state_attr('binary_sensor.motion_device_voltage_missing','responses') -%}
          {%- set actions = state_attr('binary_sensor.motion_device_voltage_missing','actions') -%}
          {%- set all = all + [{'id': id,'group': group,'interval': interval,'responses': responses,'actions': actions}] -%}
          {#- unavailable -#}
          {%- set id = group|string + '-unavailable' -%}
          {%- set responses = state_attr('binary_sensor.motion_device_voltage_unavailable','responses') -%}
          {%- set actions = state_attr('binary_sensor.motion_device_voltage_unavailable','actions') -%}
          {%- set all = all + [{'id': id,'group': group,'interval': interval,'responses': responses,'actions': actions}] -%}
          {#- activity -#}
          {%- set id = group|string + '-activity' -%}
          {%- set responses = state_attr('binary_sensor.motion_device_voltage_activity','responses') -%}
          {%- set actions = state_attr('binary_sensor.motion_device_voltage_activity','actions') -%}
          {%- set all = all + [{'id': id,'group': group,'interval': interval,'responses': responses,'actions': actions}] -%}
          {#- found -#}
          {%- set id = group|string + '-found' -%}
          {%- set responses = state_attr('binary_sensor.motion_device_voltage_found','responses') -%}
          {%- set actions = state_attr('binary_sensor.motion_device_voltage_found','actions') -%}
          {%- set all = all + [{'id': id,'group': group,'interval': interval,'responses': responses,'actions': actions}] -%}
          {{- all -}}
        good: >-
          {{ not is_state_attr('binary_sensor.motion_device_voltage_exist','good','none') and is_state('binary_sensor.motion_device_voltage_good','on') }}
        bad: >-
          {{ not is_state_attr('binary_sensor.motion_device_voltage_exist','bad','none') and is_state('binary_sensor.motion_device_voltage_bad','on') }}
        activity: >-
          {{ not is_state_attr('binary_sensor.motion_device_voltage_exist','activity','none') and is_state('binary_sensor.motion_device_voltage_activity','on') }}
        low: >-
          {{ not is_state_attr('binary_sensor.motion_device_voltage_exist','low','none') and is_state('binary_sensor.motion_device_voltage_low','on') }}
        high: >-
          {{ not is_state_attr('binary_sensor.motion_device_voltage_exist','high','none') and is_state('binary_sensor.motion_device_voltage_high','on') }}
        low_spike: >-
          {{ not is_state_attr('binary_sensor.motion_device_voltage_exist','low_spike','none') and is_state('binary_sensor.motion_device_voltage_low_spike','on') }}
        high_spike: >-
          {{ not is_state_attr('binary_sensor.motion_device_voltage_exist','high_spike','none') and is_state('binary_sensor.motion_device_voltage_high_spike','on') }}
        missing: >-
          {{ not is_state_attr('binary_sensor.motion_device_voltage_exist','required','none') and not is_state('binary_sensor.motion_device_voltage_missing','off') }}
        unavailable: >-
          {{ is_state('binary_sensor.motion_device_voltage_unavailable','on') }}
        found: >-
          {{ is_state('binary_sensor.motion_device_voltage_exist','on') }}
        active: >-
          {%- set s = state_attr('binary_sensor.motion_device_voltage_alarm','status') -%}
          {% if s|lower != 'none' and not s is string and s is iterable and s|count > 0 %}
            {%- if s.values()|reject('==','off')|list|count > 0 -%}on{%- else -%}off{%- endif -%}
          {% else %}unknown{% endif %}
        markdown: >-
          {%- set s = states('binary_sensor.motion_device_voltage_alarm') -%}
          <h2>The <a href="/notify-devices/voltage">voltage</a> alarm is: <i>{{- s|upper -}}</i></h2>
          {%- if s|lower == 'on' -%}
            {{- state_attr('binary_sensor.motion_device_voltage_alarm','markdown') -}}
          {%- endif -%}
      state: >-
        {{ is_state_attr('binary_sensor.motion_device_voltage_exist','alarm','on') and is_state('binary_sensor.motion_device_voltage_alarm','on') }}

- unique_id: motion_device_voltage_status
  trigger:
    - platform: state
      entity_id: binary_sensor.motion_device_voltage
      attribute: 'status'
      for:
        seconds: 30
  binary_sensor:
    - name: motion_device_voltage_status
      attributes:
        markdown: >-
          {%- set s = state_attr('binary_sensor.motion_device_voltage','status') %}
          {%- if s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
            {%- set s = s|selectattr('entity_id','search','binary_sensor\.')|list -%}
            {%- if s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
            {{- '<h2>Status</h2><ul>' -}}
            {%- for i in s if 'entity_id' in  i -%}
              {%- set u = device_id(i.entity_id) -%}
              {{- '<li>' -}}
              {%- if u|lower != 'none' -%}
                <a href="/config/devices/device/{{- u -}}"><b>{{- i.name -}}</b></a>: {{ states(i.entity_id) }}
              {%- else -%}
                <b>{{- i.name -}}</b>: {{ states(i.entity_id) -}}
              {%- endif -%}
              {{- '</li>' -}}
            {%- endfor -%}
            {{- '</ul>' -}}
            {%- else -%}<h2>No status</h2>{%- endif -%}
          {%- else -%}<h2>No status</h2>{%- endif -%}
        found: >-
          {%- set s = state_attr('binary_sensor.motion_device_voltage','status') %}
          {%- if s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
            {%- set s = s|selectattr('entity_id','search','binary_sensor\.')|list -%}
            {{- s|lower != 'none' and not s is string and s is iterable and s|count > 0 -}}
          {% else %}false{% endif %}
      state: >-
        {% set s = state_attr('binary_sensor.motion_device_voltage_exist','status') %}
        {% if s|lower != 'null' and s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' %}
          {{ s }}
        {% else %}unknown{% endif %}


- unique_id: motion_device_voltage_measurement
  trigger:
    - platform: state
      entity_id: sensor.motion_device_voltage
      attribute: 'measurement'
      for:
        seconds: 30
  binary_sensor:
    - name: motion_device_voltage_measurement
      attributes:
        markdown: >-
          {%- set class = 'voltage' %}
          {%- set s = state_attr('sensor.motion_device_' + class,'measurement') %}
          {%- if s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
            {%- set s = s|rejectattr('entity_id','search','binary_sensor\.')|selectattr('entity_id','search','sensor\.')|list -%}
            {%- if s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
              {{- '<h2>Measurements</h2><ul>' -}}
              {%- for i in s if 'entity_id' in i -%}
                {%- set u = device_id(i.entity_id) -%}
                {%- set v = states(i.entity_id) -%}
                {%- if v|lower != 'none' and v|lower != 'unknown' and v|lower != 'unavailable' and v|lower != 'null' and v|string|count > 0 -%}
                  {%- set uom = state_attr(i.entity_id,'unit_of_measurement') -%}
                  {%- if uom|lower != 'none' and uom|lower != 'unknown' and uom|lower != 'unavailable' and uom|lower != 'null' and uom|string|count > 0 -%}
                    {%- set v = v|string + uom|string -%}
                  {%- endif -%}
                {%- endif -%}
                {{- '<li>' -}}
                {%- if u|lower != 'none' -%}
                  <a href="/config/devices/device/{{- u -}}"><b>{{- i.name -}}</b></a>: {{ v }}
                {%- else -%}
                  <b>{{- i.name -}}</b>: {{ v }}
                {%- endif -%}
                {{- '</li>' -}}
              {%- endfor -%}
              {{- '</ul>' -}}
            {%- else -%}<h2>No measurements</h2>{%- endif -%}
          {%- else -%}<h2>No measurements</h2>{%- endif -%}
        found: >-
          {%- set class = 'voltage' %}
          {%- set s = state_attr('binary_sensor.motion_device_' + class,'status') %}
          {%- if s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
            {%- set s = s|rejectattr('entity_id','search','binary_sensor\.')|selectattr('entity_id','search','sensor\.')|list -%}
            {{- s|lower != 'none' and not s is string and s is iterable and s|count > 0 -}}
          {% else %}false{% endif %}
      state: >-
        {%- set class = 'voltage' %}
        {% set s = state_attr('binary_sensor.motion_device_' + class + '_exist','measurement') %}
        {% if s|lower != 'null' and s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' %}
          {{ s }}
        {% else %}unknown{% endif %}


- unique_id: motion_device_voltage_statistics
  trigger:
    - platform: state
      entity_id: sensor.motion_device_voltage
      attribute: 'statistics'
      for:
        seconds: 30
  binary_sensor:
    - name: motion_device_voltage_statistics
      attributes:
        status: >-
          {%- set s = state_attr('sensor.motion_device_voltage','statistics') %}
          {%- if s|lower != 'null' and s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and s is iterable and s|count > 0 -%}
            [{%- for i in s if 'entity_id' in i -%}
            {%- if not loop.first -%},{%- endif -%}
              {'{{- i.name -}}':['{{- states(i.entity_id) }} {{ state_attr(i.entity_id,'unit_of_measurement') -}}','{{- states[i.entity_id].last_updated|relative_time }}']}
            {%- endfor -%}]
          {%- else -%}none{%- endif -%}
        markdown: >-
          {%- set s = state_attr('sensor.motion_device_voltage','statistics') %}
          {%- if s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
            {{- '<h2>Statistics</h2><ul>' -}}
            {%- for i in s if 'entity_id' in i -%}
              {%- set u = device_id(i.entity_id) -%}
              {%- set v = states(i.entity_id) -%}
              {%- if v|lower != 'none' and v|lower != 'unknown' and v|lower != 'unavailable' and v|lower != 'null' and v|string|count > 0 -%}
                {%- set uom = state_attr(i.entity_id,'unit_of_measurement') -%}
                {%- if uom|lower != 'none' and uom|lower != 'unknown' and uom|lower != 'unavailable' and uom|lower != 'null' and uom|string|count > 0 -%}
                  {%- set v = v|string + uom|string -%}
                {%- endif -%}
              {%- endif -%}
              {{- '<li>' -}}
              {%- if u|lower != 'none' -%}
                <a href="/config/devices/device/{{- u -}}"><b>{{- i.name -}}</b></a>: {{ v }}
              {%- else -%}
                <b>{{- i.name -}}</b>: {{ v -}}
              {%- endif -%}
              {{- '</li>' -}}
            {%- endfor -%}
            {{- '</ul>' -}}
          {%- else -%}<h2>No statistics</h2>{%- endif -%}
      state: >-
        {% set s = state_attr('sensor.motion_device_voltage','statistics') %}
        {% if s|lower != 'null' and s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and s is iterable %}
          {{ s|count > 0 }}
        {% else %}off{% endif %}
