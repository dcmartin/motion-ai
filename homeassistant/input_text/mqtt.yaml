motion_mqtt_broker:
  name: motion_mqtt_broker
  initial: !secret mqtt-broker
  max: 255
  mode: text

motion_mqtt_port:
  name: motion_mqtt_port
  initial: !secret mqtt-port
  max: 255
  mode: text

motion_mqtt_username:
  name: motion_mqtt_username
  initial: !secret mqtt-username
  max: 255
  mode: text

motion_mqtt_password:
  name: motion_mqtt_password
  initial: !secret mqtt-password
  max: 255
  mode: text
