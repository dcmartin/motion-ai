motion_devices_config:
  name: motion_devices_config
  initial: 'none'
  max: 1024
  mode: text

motion_app:
  name: motion_app
  initial: !secret motion-app
  mode: text

motion_group:
  name: motion_group
  initial: !secret motion-group
  mode: text
  max: 50

motion_hub:
  name: motion_hub
  initial: !secret motion-device
  mode: text
  max: 50

motion_client:
  name: motion_client
  initial: !secret motion-client
  mode: text
  max: 50

motion_url:
  name: motion_url
  initial: !secret ha-url
  mode: text

motion_name:
  name: motion_name
  initial: !secret ha-name
  mode: text

motion_site:
  name: motion_site
  initial: !secret motion-site
  mode: text

