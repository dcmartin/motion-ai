###
## homeassistant/motion/input_text/config.yaml
###

motion_group:
  name: motion_group
  initial: !secret motion-group
  mode: text
  max: 50

motion_device:
  name: motion_device
  initial: !secret motion-device
  mode: text
  max: 50

motion_client:
  name: motion_client
  initial: !secret motion-client
  mode: text
  max: 50

motion_yolo_ip:
  name: YOLO IP
  icon: mdi:eye
  initial: !secret yolo-ip
  mode: text
  max: 128
