###
## AUTO-GENERATED from homeassistant/motion/input_text.config.yaml.tmpl
##
## - MOTION_GROUP: ${MOTION_GROUP}
## - MOTION_DEVICE: ${MOTION_DEVICE}
## - MOTION_CLIENT: ${MOTION_CLIENT}
##
###

motion_group:
  name: motion_group
  initial: !secret motion-group
  mode: text
  max: 50

motion_device:
  name: motion_device
  initial: !secret motion-device
  mode: text
  max: 50

motion_client:
  name: motion_client
  initial: !secret motion-client
  mode: text
  max: 50

motion_overview_apikey:
  name: Overview APIKEY
  initial: !secret motion-overview-apikey
  mode: text
  max: 128

motion_overview_image:
  name: Overview image
  icon: mdi:file-image-outline
  initial: !secret motion-overview-image
  mode: text
  max: 128

motion_yolo_ip:
  name: YOLO IP
  icon: mdi:eye
  initial: !secret yolo-ip
  mode: text
  max: 128
