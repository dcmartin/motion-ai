###
# homeassistant/input_text/motion/person.yaml
###

## accounts/names

motion_person:
  name: motion_person
  initial: !secret motion-user
  mode: text
  max: 64

motion_primary:
  name: motion_primary
  initial: !secret motion-primary
  mode: text
  max: 64

motion_secondary:
  name: motion_secondary
  initial: !secret motion-secondary
  mode: text
  max: 64

motion_tertiary:
  name: motion_tertiary
  initial: !secret motion-tertiary
  mode: text
  max: 64

## device/contact

motion_person_device:
  name: motion_person_device
  initial: !secret motion-person-device
  mode: text
  max: 64

motion_primary_device:
  name: motion_primary_device
  initial: !secret motion-primary-device
  mode: text
  max: 64

motion_secondary_device:
  name: motion_secondary_device
  initial: !secret motion-secondary-device
  mode: text
  max: 64

motion_tertiary_device:
  name: motion_tertiary_device
  initial: !secret motion-tertiary-device
  mode: text
  max: 64

