###
# homeassistant/template/detected_animal.yaml
###

- unique_id: motion_${MOTION_CAMERA}_animal_count_measurement
  trigger:
    - platform: state
      entity_id: 'sensor.motion_${MOTION_CAMERA}_detected_animal_count'
  sensor:
    - name: 'motion_${MOTION_CAMERA}_animal_count_measurement'
      state_class: 'measurement'
      device_class: 'carbon_dioxide'
      unit_of_measurement: 'ppm'
      state: >-
        {% set s = states('sensor.motion_${MOTION_CAMERA}_detected_animal_count') %}
        {% if s|lower != 'none' and s|lower != 'uknown' and s|lower != 'unavailable' and s|lower != 'null' %}
          {{ s|int }}
        {% else %}null{% endif %}

- unique_id: motion_${MOTION_CAMERA}_animal_ago_measurement
  trigger:
    - platform: state
      entity_id: 'sensor.motion_${MOTION_CAMERA}_detected_animal_ago'
  sensor:
    - name: 'motion_${MOTION_CAMERA}_animal_ago_measurement'
      state_class: 'measurement'
      device_class: 'pressure'
      unit_of_measurement: 'psi'
      state: >-
        {% set s = states('sensor.motion_${MOTION_CAMERA}_detected_animal_ago') %}
        {% if s|lower != 'none' and s|lower != 'uknown' and s|lower != 'unavailable' and s|lower != 'null' %}
          {{ s|int }}
        {% else %}null{% endif %}
