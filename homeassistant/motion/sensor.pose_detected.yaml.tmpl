###
## AUTO-GENERATED from homeassistant/motion/sensor.pose.yaml.tmpl
##
## - MOTION_GROUP: ${MOTION_GROUP}
## - MOTION_CLIENT: ${MOTION_CLIENT}
## - MOTION_CAMERA: ${MOTION_CAMERA}
###

###
## pose
###

## event_pose
- platform: mqtt
  name: motion_${MOTION_CAMERA}_event_pose
  state_topic: '${MOTION_GROUP}/${MOTION_CLIENT}/${MOTION_CAMERA}/event/pose/+'
  json_attributes_topic: '${MOTION_GROUP}/${MOTION_CLIENT}/${MOTION_CAMERA}/event/pose/+'
  expire_after: !secret motion-pose-expire-after
  force_update: !secret motion-pose-force-update
  value_template: >
    {% if value_json is defined %}True{% else %}False{% endif %}

## status
- platform: template
  sensors:
    motion_${MOTION_CAMERA}_pose_status:
      friendly_name: '${MOTION_CAMERA} POSE'
      value_template: >-
        {% if states('binary_sensor.motion_${MOTION_CAMERA}_pose')|lower == 'on' %}
          {% set e = state_attr('sensor.motion_${MOTION_CAMERA}_event_pose','event') %}
          {% if e|lower != 'none' and e|lower != 'unknown' and e|lower != 'unavailable' and e|lower != 'null' and e is mapping %}
            {% set n = utcnow().timestamp()|int %}
            {% set end = e.end|int %}
            {% set ago = n - end %}
            {% set event = e.event %}
            {% set device = e.device %}
            {% set camera = e.camera %}
            {% set elapsed = e.elapsed|int %}
            {% set what = state_attr('sensor.motion_${MOTION_CAMERA}_event_pose','detected')|map(attribute='entity')|list %}
            {% set count = state_attr('sensor.motion_${MOTION_CAMERA}_event_pose','count') %}
            {% set when = end|timestamp_custom("%a %b %d %I:%M:%S %p %Z") %}
            {% set delay = n - end %}
            POSE: {{ count }} {{ what }};
            from {{ device -}}/{{- camera -}}/{{- event -}};
            at {{ when }};
            ago: {{ ago }};
            delay: {{ delay }};
            duration: {{ elapsed }};
            counter: {{ states('counter.motion_${MOTION_CAMERA}_pose_counter') -}}/{{- states('sensor.motion_pose_counter') -}}
          {% else %}Pending{% endif %}
        {% else %}
          {% set s = states('sensor.motion_${MOTION_CAMERA}_pose_status') %}
          {% if s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and s|lower != 'none' %}
            {{ s }}
          {% else %}Pending{% endif %}
        {% endif %}

## counter
- platform: template
  sensors:
    motion_${MOTION_CAMERA}_pose_counter:
      friendly_name: '${MOTION_CAMERA} pose counter'
      unit_of_measurement: count
      value_template: >
        {{ states('counter.motion_${MOTION_CAMERA}_pose_counter')|int }}

## date
- platform: template
  sensors:
    # date
    motion_${MOTION_CAMERA}_pose_date:
      unit_of_measurement: 's'
      value_template: >-
        {% set e = state_attr('sensor.motion_${MOTION_CAMERA}_event_pose','event') %}
        {% if e|lower != 'none' and e|lower != 'unknown' and e|lower != 'unavailable' and e|lower != 'null' and e is mapping %}
          {% set s = e.end %}
          {% if s|lower != 'none' and s|lower != 'unknown' and s != 'null' and s|int > 0 %}
            {{ s|int }}
          {%- else -%}null{%- endif -%}
        {%- else -%}null{%- endif -%}

## when
- platform: template
  sensors:
    motion_${MOTION_CAMERA}_pose_when:
      friendly_name: '${MOTION_CAMERA} pose when'
      value_template: >-
        {% set s = states('sensor.motion_${MOTION_CAMERA}_pose_date')|int %}
        {% if s > 0 %}
          {% set n = utcnow().timestamp() %}
          {{ s|int|timestamp_custom("%a %b %d %I:%M:%S %p %Z") -}}; {{ (n|int-s)|int }} lag
        {% else %}Pending{% endif %}

## count 

- platform: template
  sensors:
    motion_${MOTION_CAMERA}_pose_count:
      friendly_name: '${MOTION_CAMERA} pose count'
      unit_of_measurement: pose
      value_template: >
        {% if is_state('binary_sensor.motion_${MOTION_CAMERA}_pose','on') or is_state('binary_sensor.motion_${MOTION_CAMERA}_pose_expired','off') %}
          {% set count = state_attr('sensor.motion_${MOTION_CAMERA}_event_pose','count') %}
          {% if count|lower != 'none' and count|lower != 'unknown' and count != 'null' %}
            {{ count|int }}
          {% else %}null{% endif %}
        {% else %}null{% endif %}

#- platform: template
#  sensors:
#    motion_${MOTION_CAMERA}_pose_count_mean:
#      friendly_name: '${MOTION_CAMERA} count mean'
#      unit_of_measurement: poses
#      value_template: >-
#        {% if states('sensor.motion_${MOTION_CAMERA}_pose_count_statistics') != 'unknown' %}
#          {{ states.sensor.motion_${MOTION_CAMERA}_pose_count_statistics.state|float }}
#        {%- else -%}null{%- endif -%}
#    motion_${MOTION_CAMERA}_pose_count_min:
#      friendly_name: '${MOTION_CAMERA} count min'
#      unit_of_measurement: poses
#      value_template: >-
#        {% if states('sensor.motion_${MOTION_CAMERA}_pose_count_statistics') != 'unknown' %}
#          {{ states.sensor.motion_${MOTION_CAMERA}_pose_count_statistics.attributes.min_value|int }}
#        {%- else -%}null{%- endif -%}
#    motion_${MOTION_CAMERA}_pose_count_max:
#      friendly_name: '${MOTION_CAMERA} count max'
#      unit_of_measurement: poses
#      value_template: >-
#        {% if states('sensor.motion_${MOTION_CAMERA}_pose_count_statistics') != 'unknown' %}
#          {{ states.sensor.motion_${MOTION_CAMERA}_pose_count_statistics.attributes.max_value|int }}
#        {%- else -%}null{%- endif -%}
#    motion_${MOTION_CAMERA}_pose_count_stdev:
#      friendly_name: '${MOTION_CAMERA} count stdev'
#      unit_of_measurement: poses
#      value_template: >
#        {% if states('sensor.motion_${MOTION_CAMERA}_pose_count_statistics') != 'unknown' %}
#          {{ states.sensor.motion_${MOTION_CAMERA}_pose_count_statistics.attributes.standard_deviation|float }}
#        {%- else -%}null{%- endif -%}
#
#- platform: statistics
#  name: motion_${MOTION_CAMERA}_pose_count_statistics
#  entity_id: sensor.motion_${MOTION_CAMERA}_pose_count
#  sampling_size: 100
#  max_age:
#    hours: 72

## ago

- platform: template
  sensors:
    motion_${MOTION_CAMERA}_pose_ago:
      friendly_name: '${MOTION_CAMERA} pose ago'
      unit_of_measurement: 's'
      value_template: >
        {%- set s = states('sensor.motion_${MOTION_CAMERA}_pose_date') -%}
        {%- if s|lower != 'unavailable' and s|lower != 'unknown' and s|lower != 'none' and s|lower != 'null' -%}
          {%- set s = utcnow().timestamp()|int - s|int -%}
          {{- s -}}
        {%- else -%}null{%- endif -%}

#- platform: template
#  sensors:
#    motion_${MOTION_CAMERA}_pose_ago_mean:
#      friendly_name: '${MOTION_CAMERA} pose ago mean'
#      unit_of_measurement: 's'
#      value_template: >-
#        {% if states.sensor.motion_${MOTION_CAMERA}_pose_ago_statistics|lower != 'none' %}
#          {{ states.sensor.motion_${MOTION_CAMERA}_pose_ago_statistics.state|float }}
#        {%- else -%}null{%- endif -%}
#    motion_${MOTION_CAMERA}_pose_ago_min:
#      friendly_name: '${MOTION_CAMERA} pose min'
#      unit_of_measurement: 's'
#      value_template: >-
#        {% if states.sensor.motion_${MOTION_CAMERA}_pose_ago_statistics|lower != 'none' %}
#          {{ states.sensor.motion_${MOTION_CAMERA}_pose_ago_statistics.attributes.min_value|int }}
#        {%- else -%}null{%- endif -%}
#    motion_${MOTION_CAMERA}_pose_ago_max:
#      friendly_name: '${MOTION_CAMERA} pose max'
#      unit_of_measurement: 's'
#      value_template: >-
#        {% if states.sensor.motion_${MOTION_CAMERA}_pose_ago_statistics|lower != 'none' %}
#          {{ states.sensor.motion_${MOTION_CAMERA}_pose_ago_statistics.attributes.max_value|int }}
#        {%- else -%}null{%- endif -%}
#    motion_${MOTION_CAMERA}_pose_ago_stdev:
#      friendly_name: '${MOTION_CAMERA} pose stdev'
#      unit_of_measurement: 's'
#      value_template: >
#        {% if states.sensor.motion_${MOTION_CAMERA}_pose_ago_statistics|lower != 'none' %}
#          {{ states.sensor.motion_${MOTION_CAMERA}_pose_ago_statistics.attributes.standard_deviation|float }}
#        {%- else -%}null{%- endif -%}
#
#- platform: statistics
#  name: motion_${MOTION_CAMERA}_pose_ago_statistics
#  entity_id: sensor.motion_${MOTION_CAMERA}_pose_ago
#  sampling_size: 100
#  max_age:
#    hours: 72
#

## percent

- platform: template
  sensors:
    motion_${MOTION_CAMERA}_pose_percent:
      friendly_name: '${MOTION_CAMERA} pose %'
      unit_of_measurement: '%'
      value_template: >-
        {% if states('sensor.motion_${MOTION_CAMERA}_pose_counter')|int > 0 and states('sensor.motion_end_counter')|int > 0 %}
          {% set value = states.sensor.motion_${MOTION_CAMERA}_pose_counter.state|int / states.sensor.motion_end_counter.state|int * 100.0 %}
          {{ "%.2f"|format(value|float) }}
        {%- else -%}null{%- endif -%}

#- platform: template
#  sensors:
#    motion_${MOTION_CAMERA}_pose_percent_mean:
#      friendly_name: '${MOTION_CAMERA} pose % mean'
#      unit_of_measurement: '%'
#      value_template: >-
#        {% if states('sensor.motion_${MOTION_CAMERA}_pose_percent_statistics') != 'unknown' %}
#          {{ states.sensor.motion_${MOTION_CAMERA}_pose_percent_statistics.state|float }}
#        {%- else -%}null{%- endif -%}
#    motion_${MOTION_CAMERA}_pose_percent_min:
#      friendly_name: '${MOTION_CAMERA} pose % min'
#      unit_of_measurement: '%'
#      value_template: >-
#        {% if states('sensor.motion_${MOTION_CAMERA}_pose_percent_statistics') != 'unknown' %}
#          {{ states.sensor.motion_${MOTION_CAMERA}_pose_percent_statistics.attributes.min_value|float }}
#        {%- else -%}null{%- endif -%}
#    motion_${MOTION_CAMERA}_pose_percent_max:
#      friendly_name: '${MOTION_CAMERA} pose % max'
#      unit_of_measurement: '%'
#      value_template: >-
#        {% if states('sensor.motion_${MOTION_CAMERA}_pose_percent_statistics') != 'unknown' %}
#          {{ states.sensor.motion_${MOTION_CAMERA}_pose_percent_statistics.attributes.max_value|float }}
#        {%- else -%}null{%- endif -%}
#    motion_${MOTION_CAMERA}_pose_percent_stdev:
#      friendly_name: '${MOTION_CAMERA} pose % stdev'
#      unit_of_measurement: '%'
#      value_template: >
#        {% if states('sensor.motion_${MOTION_CAMERA}_pose_percent_statistics') != 'unknown' %}
#          {{ states.sensor.motion_${MOTION_CAMERA}_pose_percent_statistics.attributes.standard_deviation|float }}
#        {%- else -%}null{%- endif -%}
#
#- platform: statistics
#  name: motion_${MOTION_CAMERA}_pose_percent_statistics
#  entity_id: sensor.motion_${MOTION_CAMERA}_pose_percent
#  sampling_size: 100
#  max_age:
#    hours: 72
#

## delay 

- platform: template
  sensors:
    motion_${MOTION_CAMERA}_pose_delay:
      friendly_name: '${MOTION_CAMERA} POSE delay'
      unit_of_measurement: 's'
      value_template: >
        {%- set s = state_attr('sensor.motion_${MOTION_CAMERA}_event_pose','timestamp') -%}
        {%- if s|lower != 'unavailable' and s|lower != 'none' and s|lower != 'unknown' and s != null -%}
          {%- set e = state_attr('sensor.motion_${MOTION_CAMERA}_event_pose','event') -%}
          {%- if e|lower != 'unavailable' and e|lower != 'none' and e|lower != 'unknown' and e != null and e is iterable %}
            {%- set t = e.timestamp.end -%}
            {%- if t|lower != 'unavailable' and t|lower != 'none' and t|lower != 'unknown' and t != null and t|length > 0 %}
              {{- as_timestamp(s)|int - as_timestamp(t)|int }}
            {% else %}null{% endif %}
          {% else %}null{% endif %}
        {% else %}null{% endif %}

- platform: template
  sensors:
    motion_${MOTION_CAMERA}_pose_delay_mean:
      friendly_name: '${MOTION_CAMERA} pose delay mean'
      unit_of_measurement: 's'
      value_template: >-
        {% if states('sensor.motion_${MOTION_CAMERA}_pose_delay_statistics') != 'unknown' %}
          {{ states.sensor.motion_${MOTION_CAMERA}_pose_delay_statistics.state|float }}
        {%- else -%}null{%- endif -%}
    motion_${MOTION_CAMERA}_pose_delay_min:
      friendly_name: '${MOTION_CAMERA} pose delay min'
      unit_of_measurement: 's'
      value_template: >-
        {% if states('sensor.motion_${MOTION_CAMERA}_pose_delay_statistics') != 'unknown' %}
          {{ states.sensor.motion_${MOTION_CAMERA}_pose_delay_statistics.attributes.min_value|int }}
        {%- else -%}null{%- endif -%}
    motion_${MOTION_CAMERA}_pose_delay_max:
      friendly_name: '${MOTION_CAMERA} pose delay max'
      unit_of_measurement: 's'
      value_template: >-
        {% if states('sensor.motion_${MOTION_CAMERA}_pose_delay_statistics') != 'unknown' %}
          {{ states.sensor.motion_${MOTION_CAMERA}_pose_delay_statistics.attributes.max_value|int }}
        {%- else -%}null{%- endif -%}
    motion_${MOTION_CAMERA}_pose_delay_stdev:
      friendly_name: '${MOTION_CAMERA} pose delay stdev'
      unit_of_measurement: 's'
      value_template: >
        {% if states('sensor.motion_${MOTION_CAMERA}_pose_delay_statistics') != 'unknown' %}
          {{ states.sensor.motion_${MOTION_CAMERA}_pose_delay_statistics.attributes.standard_deviation|float }}
        {%- else -%}null{%- endif -%}

- platform: statistics
  name: motion_${MOTION_CAMERA}_pose_delay_statistics
  entity_id: sensor.motion_${MOTION_CAMERA}_pose_delay
  sampling_size: 100
  max_age:
    hours: 72

###
## pose_detected
###

## human readable status
- platform: template
  sensors:
    motion_${MOTION_CAMERA}_pose_detected_status:
      friendly_name: '${MOTION_CAMERA} POSE detected status'
      value_template: >-
        {% if states('binary_sensor.motion_${MOTION_CAMERA}_pose_detected')|lower == 'on' %}
          {% set e = state_attr('sensor.motion_${MOTION_CAMERA}_event_pose','event') %}
          {% if e|lower != 'none' and e|lower != 'unknown' and e|lower != 'unavailable' and e|lower != 'null' and e is mapping %}
            {% set n = utcnow().timestamp()|int %}
            {% set end = e.end|int %}
            {% set ago = n - end %}
            {% set event = e.event %}
            {% set device = e.device %}
            {% set camera = e.camera %}
            {% set elapsed = e.elapsed|int %}
            {% set what = state_attr('sensor.motion_${MOTION_CAMERA}_event_pose','detected')|list %}
            {% set count = state_attr('sensor.motion_${MOTION_CAMERA}_event_pose','count')|int %}
            {% set when = end|timestamp_custom("%a %b %d %I:%M:%S %p %Z") %}
            {% set delay = n - end %}
            POSE: {{ count }} {{ what }};
            from {{ device -}}/{{- camera -}}/{{- event -}};
            at {{ when }};
            ago: {{ ago }};
            delay: {{ delay }};
            duration: {{ elapsed }};
            counter: {{ states('counter.motion_${MOTION_CAMERA}_pose_detected_counter') -}}/{{- states('sensor.motion_pose_detected_counter') -}}
          {% else %}Pending{% endif %}
        {% else %}
          {% set s = states('sensor.motion_${MOTION_CAMERA}_pose_detected_status') %}
          {% if s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and s != 'null' %}
            {{ s }}
          {% else %}Pending{% endif %}
        {% endif %}

## count_entity
- platform: template
  sensors:
    motion_${MOTION_CAMERA}_pose_detected_count_entity:
      friendly_name: '${MOTION_CAMERA} POSE detected count entity'
      value_template: >-
        {% if is_state('binary_sensor.motion_${MOTION_CAMERA}_pose_detected','on') or is_state('binary_sensor.motion_${MOTION_CAMERA}_pose_detected_expired','off') %}
          {% set s = state_attr('sensor.motion_${MOTION_CAMERA}_event_annotated','detected')|selectattr('entity','==',states('sensor.motion_${MOTION_CAMERA}_detect_entity'))|map(attribute='count')|sum %}
          {% if s|lower != 'none' and s|lower != 'unknown' and s != 'null' %}
            {{ s|int }}
          {% else %}null{% endif %}
        {% else %}null{% endif %}

## counter
- platform: template
  sensors:
    motion_${MOTION_CAMERA}_pose_detected_counter:
      friendly_name: '${MOTION_CAMERA} POSE detected counter'
      unit_of_measurement: count
      value_template: >
        {{ states('counter.motion_${MOTION_CAMERA}_pose_detected_counter')|int }}

## date
- platform: template
  sensors:
    motion_${MOTION_CAMERA}_pose_detected_date:
      friendly_name: '${MOTION_CAMERA} POSE detected date'
      unit_of_measurement: 's'
      value_template: >-
        {% if is_state('binary_sensor.motion_${MOTION_CAMERA}_pose_detected','on') or is_state('binary_sensor.motion_${MOTION_CAMERA}_pose_detected_expired','off') %}
          {% set date = states('sensor.motion_${MOTION_CAMERA}_annotated_date')|int %}
          {% if date > 0 %}{{ date }}{% else %}null{% endif %}
        {% else %}
          {% set s = states('sensor.motion_${MOTION_CAMERA}_pose_detected_date') %}
          {% if s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and s != 'null' %}
            {{ s }}
          {% else %}null{% endif %}
        {% endif %}

## when
- platform: template
  sensors:
    motion_${MOTION_CAMERA}_pose_detected_when:
      friendly_name: '${MOTION_CAMERA} POSE detected when'
      value_template: >-
        {% set s = states('sensor.motion_${MOTION_CAMERA}_pose_detected_date')|int %}
        {% if s > 0 %}
          {% set n = utcnow().timestamp() %}
            {{ s|int|timestamp_custom("%a %b %d %I:%M:%S %p %Z") -}}
        {% else %}Pending{% endif %}

## ago

- platform: template
  sensors:
    motion_${MOTION_CAMERA}_pose_detected_ago:
      friendly_name: '${MOTION_CAMERA} POSE detected ago'
      unit_of_measurement: 's'
      value_template: >
        {%- set s = states('sensor.motion_${MOTION_CAMERA}_pose_detected_date') -%}
        {%- if s|lower != 'unavailable' and s|lower != 'unknown' and s|lower != 'none' and s|lower != 'null' -%}
          {%- set s = utcnow().timestamp()|int - s|int -%}
          {{- s -}}
        {%- else -%}null{%- endif -%}

- platform: template
  sensors:
    motion_${MOTION_CAMERA}_pose_detected_ago_mean:
      friendly_name: '${MOTION_CAMERA} POSE detected ago mean'
      unit_of_measurement: 's'
      value_template: >-
        {% if states.sensor.motion_${MOTION_CAMERA}_pose_detected_ago_statistics|lower != 'none' %}
          {% if states.sensor.motion_${MOTION_CAMERA}_pose_detected_ago_statistics.state|int is number %}
            {{ states.sensor.motion_${MOTION_CAMERA}_pose_detected_ago_statistics.state|float }}
          {%- else -%}null{%- endif -%}
        {%- else -%}null{%- endif -%}
    motion_${MOTION_CAMERA}_pose_detected_ago_min:
      friendly_name: '${MOTION_CAMERA} POSE detected ago min'
      unit_of_measurement: 's'
      value_template: >-
        {% if states.sensor.motion_${MOTION_CAMERA}_pose_detected_ago_statistics|lower != 'none' %}
          {{ states.sensor.motion_${MOTION_CAMERA}_pose_detected_ago_statistics.attributes.min_value|int }}
        {%- else -%}null{%- endif -%}
    motion_${MOTION_CAMERA}_pose_detected_ago_max:
      friendly_name: '${MOTION_CAMERA} POSE detected ago max'
      unit_of_measurement: 's'
      value_template: >-
        {% if states.sensor.motion_${MOTION_CAMERA}_pose_detected_ago_statistics|lower != 'none' %}
          {{ states.sensor.motion_${MOTION_CAMERA}_pose_detected_ago_statistics.attributes.max_value|int }}
        {%- else -%}null{%- endif -%}
    motion_${MOTION_CAMERA}_pose_detected_ago_stdev:
      friendly_name: '${MOTION_CAMERA} POSE detected ago stdev'
      unit_of_measurement: 's'
      value_template: >
        {% if states.sensor.motion_${MOTION_CAMERA}_pose_detected_ago_statistics|lower != 'none' %}
          {{ states.sensor.motion_${MOTION_CAMERA}_pose_detected_ago_statistics.attributes.standard_deviation|float }}
        {%- else -%}null{%- endif -%}

- platform: statistics
  name: motion_${MOTION_CAMERA}_pose_detected_ago_statistics
  entity_id: sensor.motion_${MOTION_CAMERA}_pose_detected_ago
  sampling_size: 100
  max_age:
    hours: 72

## count

- platform: template
  sensors:
    motion_${MOTION_CAMERA}_pose_detected_count:
      friendly_name: '${MOTION_CAMERA} POSE detected count'
      unit_of_measurement: pose
      value_template: >
        {% if is_state('binary_sensor.motion_${MOTION_CAMERA}_pose_detected','on') or is_state('binary_sensor.motion_${MOTION_CAMERA}_pose_detected_expired','off') %}
          {% set s = states('sensor.motion_${MOTION_CAMERA}_annotated_count') %}
          {% if s|lower != 'none' and s|lower != 'unknown' and s != 'null' %}
            {{ s|int }}
          {% else %}null{% endif %}
        {% else %}null{% endif %}

- platform: template
  sensors:
    motion_${MOTION_CAMERA}_pose_detected_count_mean:
      friendly_name: '${MOTION_CAMERA} POSE detected count mean'
      unit_of_measurement: pose
      value_template: >-
        {% if states('sensor.motion_${MOTION_CAMERA}_pose_detected_count_statistics') != 'unknown' %}
          {{ states.sensor.motion_${MOTION_CAMERA}_pose_detected_count_statistics.state|float }}
        {%- else -%}null{%- endif -%}
    motion_${MOTION_CAMERA}_pose_detected_count_min:
      friendly_name: '${MOTION_CAMERA} POSE detected count min'
      unit_of_measurement: pose
      value_template: >-
        {% if states('sensor.motion_${MOTION_CAMERA}_pose_detected_count_statistics') != 'unknown' %}
          {{ states.sensor.motion_${MOTION_CAMERA}_pose_detected_count_statistics.attributes.min_value|int }}
        {%- else -%}null{%- endif -%}
    motion_${MOTION_CAMERA}_pose_detected_count_max:
      friendly_name: '${MOTION_CAMERA} POSE detected count max'
      unit_of_measurement: pose
      value_template: >-
        {% if states('sensor.motion_${MOTION_CAMERA}_pose_detected_count_statistics') != 'unknown' %}
          {{ states.sensor.motion_${MOTION_CAMERA}_pose_detected_count_statistics.attributes.max_value|int }}
        {%- else -%}null{%- endif -%}
    motion_${MOTION_CAMERA}_pose_detected_count_stdev:
      friendly_name: '${MOTION_CAMERA} POSE detected count stdev'
      unit_of_measurement: pose
      value_template: >
        {% if states('sensor.motion_${MOTION_CAMERA}_pose_detected_count_statistics') != 'unknown' %}
          {{ states.sensor.motion_${MOTION_CAMERA}_pose_detected_count_statistics.attributes.standard_deviation|float }}
        {%- else -%}null{%- endif -%}

- platform: statistics
  name: motion_${MOTION_CAMERA}_pose_detected_count_statistics
  entity_id: sensor.motion_${MOTION_CAMERA}_pose_detected_count
  sampling_size: 100
  max_age:
    hours: 72

## percent

- platform: template
  sensors:
    motion_${MOTION_CAMERA}_pose_detected_percent:
      friendly_name: '${MOTION_CAMERA} POSE detected %'
      unit_of_measurement: '%'
      value_template: >-
        {% if states('sensor.motion_${MOTION_CAMERA}_pose_detected_counter')|int > 0 and states('sensor.motion_annotated_counter')|int > 0 %}
          {% set value = states.sensor.motion_${MOTION_CAMERA}_pose_detected_counter.state|int / states.sensor.motion_annotated_counter.state|int * 100.0 %}
          {{ "%.2f"|format(value|float) }}
        {%- else -%}null{%- endif -%}

- platform: template
  sensors:
    motion_${MOTION_CAMERA}_pose_detected_percent_mean:
      friendly_name: '${MOTION_CAMERA} POSE detected % mean'
      unit_of_measurement: '%'
      value_template: >-
        {% if states('sensor.motion_${MOTION_CAMERA}_pose_detected_percent_statistics') != 'unknown' %}
          {{ states.sensor.motion_${MOTION_CAMERA}_pose_detected_percent_statistics.state|float }}
        {%- else -%}null{%- endif -%}
    motion_${MOTION_CAMERA}_pose_detected_percent_min:
      friendly_name: '${MOTION_CAMERA} POSE detected % min'
      unit_of_measurement: '%'
      value_template: >-
        {% if states('sensor.motion_${MOTION_CAMERA}_pose_detected_percent_statistics') != 'unknown' %}
          {{ states.sensor.motion_${MOTION_CAMERA}_pose_detected_percent_statistics.attributes.min_value|float }}
        {%- else -%}null{%- endif -%}
    motion_${MOTION_CAMERA}_pose_detected_percent_max:
      friendly_name: '${MOTION_CAMERA} POSE detected % max'
      unit_of_measurement: '%'
      value_template: >-
        {% if states('sensor.motion_${MOTION_CAMERA}_pose_detected_percent_statistics') != 'unknown' %}
          {{ states.sensor.motion_${MOTION_CAMERA}_pose_detected_percent_statistics.attributes.max_value|float }}
        {%- else -%}null{%- endif -%}
    motion_${MOTION_CAMERA}_pose_detected_percent_stdev:
      friendly_name: '${MOTION_CAMERA} POSE detected % stdev'
      unit_of_measurement: '%'
      value_template: >
        {% if states('sensor.motion_${MOTION_CAMERA}_pose_detected_percent_statistics') != 'unknown' %}
          {{ states.sensor.motion_${MOTION_CAMERA}_pose_detected_percent_statistics.attributes.standard_deviation|float }}
        {%- else -%}null{%- endif -%}

- platform: statistics
  name: motion_${MOTION_CAMERA}_pose_detected_percent_statistics
  entity_id: sensor.motion_${MOTION_CAMERA}_pose_detected_percent
  sampling_size: 100
  max_age:
    hours: 72
