###
## AUTO-GENERATED from homeassistant/motion/sensor.device.yaml.tmpl
##
## - MOTION_GROUP: ${MOTION_GROUP}
## - MOTION_DEVICE: ${MOTION_DEVICE}
###

###
# apache
###

- platform: template
  sensors:
    motion_apache_htdocs:
      friendly_name: Apache root directory
      value_template: >-
        {% set s = states('input_text.motion_apache_htdocs') %}
        {% if s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and s != null %}
          {{ s }}
        {% else %}null{% endif %}
    motion_apache_host_${MOTION_DEVICE}:
      friendly_name: Apache root directory
      value_template: >-
        {% set s = states('input_text.motion_apache_host') %}
        {% if s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and s != null %}
          {{ s }}
        {% else %}null{% endif %}
    motion_apache_port_${MOTION_DEVICE}:
      friendly_name: Apache root directory
      value_template: >-
        {% set s = states('input_text.motion_apache_port') %}
        {% if s|lower != 'none'  and s|lower != 'unavailable' and s|lower != 'unknown' and s != null %}
          {{ s }}
        {% else %}null{% endif %}

###
# config
###

- platform: template
  sensors:
    motion_config_url_${MOTION_DEVICE}:
      friendly_name: Motion configuration URL
      value_template: >-
        {% set s = states('sensor.motion_apache_host_${MOTION_DEVICE}') %}
        {% if s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and s != null %}
          {% set host = s %}
          {% set s = states('sensor.motion_apache_port_${MOTION_DEVICE}') %}
          {% if s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and s != null %}
            {% set port = s %}
            {{ 'http://' + host + ':' + port + '/cgi-bin/config' }}
          {% else %}null{% endif %}
        {% else %}null{% endif %}

- platform: rest
  name: motion_config_${MOTION_DEVICE}
  json_attributes:
    - timestamp
    - config
  force_update: true
  resource_template: >-
    {% set s = states('sensor.motion_config_url_${MOTION_DEVICE}') %}
    {% if s|lower !='none' and s|lower != 'unavailable' and s|lower != 'unknown' and s != null %}
      {{- s -}}
    {% else %}http://127.0.0.1:7999/cgi-bin/config{% endif %}
  value_template: >-
    {% if value_json is defined %}True{% else %}False{% endif %}

# count
- platform: template
  sensors:
    motion_config_camera_count_${MOTION_DEVICE}:
      friendly_name: Add-on ${MOTION_DEVICE} config camera count
      icon_template: 'mdi:cctv'
      value_template: >-
        {% set s = state_attr('sensor.motion_config_${MOTION_DEVICE}','config') %}
        {% if s is not none and s|lower != 'unavailable' and s|lower != 'unknown' and s != null %}
          {% set s = s.cameras %}
          {% if s is not none and s|lower != 'unavailable' and s|lower != 'unknown' and s|list is iterable %}
            {{ s|list|length }}
          {% else %}null{% endif %}
        {% else %}null{% endif %}
    motion_config_camera_count_netcam_${MOTION_DEVICE}:
      friendly_name: Add-on ${MOTION_DEVICE} netcam count
      icon_template: 'mdi:webcam'
      value_template: >-
        {% set s = state_attr('sensor.motion_config_${MOTION_DEVICE}','config') %}
        {% if s is not none and s|lower != 'unavailable' and s|lower != 'unknown' and s != null %}
          {{ s.cameras|selectattr('type','==','netcam')|list|length }}
        {% else %}null{% endif %}
    motion_config_camera_count_mqtt_${MOTION_DEVICE}:
      friendly_name: Add-on ${MOTION_DEVICE} mqtt count
      icon_template: 'mdi:message'
      value_template: >-
        {% set s = state_attr('sensor.motion_config_${MOTION_DEVICE}','config') %}
        {% if s is not none and s|lower != 'unavailable' and s|lower != 'unknown' and s != null %}
          {{ s.cameras|selectattr('type','==','mqtt')|list|length }}
        {% else %}null{% endif %}
    motion_config_camera_count_local_${MOTION_DEVICE}:
      friendly_name: Add-on ${MOTION_DEVICE} local count
      icon_template: 'mdi:camera'
      value_template: >-
        {% set s = state_attr('sensor.motion_config_${MOTION_DEVICE}','config') %}
        {% if s is not none and s|lower != 'unavailable' and s|lower != 'unknown' and s != null %}
          {{ s.cameras|selectattr('type','==','local')|list|length }}
        {% else %}null{% endif %}
    motion_config_camera_count_ftpd_${MOTION_DEVICE}:
      friendly_name: Add-on ${MOTION_DEVICE} ftpd count
      icon_template: 'mdi:download'
      value_template: >-
        {% set s = state_attr('sensor.motion_config_${MOTION_DEVICE}','config') %}
        {% if s is not none and s|lower != 'unavailable' and s|lower != 'unknown' and s != null %}
          {{ s.cameras|selectattr('type','==','ftpd')|list|length }}
        {% else %}null{% endif %}
    motion_config_camera_count_direct_${MOTION_DEVICE}:
      friendly_name: Add-on ${MOTION_DEVICE} direct count
      icon_template: 'mdi:camera-outline'
      value_template: >-
        {% set l = states('sensor.motion_config_camera_count_local_${MOTION_DEVICE}') %}
        {% set n = states('sensor.motion_config_camera_count_netcam_${MOTION_DEVICE}') %}
        {{ l|int + n|int }}
    motion_config_camera_count_remote_${MOTION_DEVICE}:
      friendly_name: Add-on ${MOTION_DEVICE} remote count
      icon_template: 'mdi:camera-wireless-outline'
      value_template: >-
        {% set l = states('sensor.motion_config_camera_count_${MOTION_DEVICE}') %}
        {% set n = states('sensor.motion_config_camera_count_direct_${MOTION_DEVICE}') %}
        {{ l|int - n|int }}

# mqtt
- platform: template
  sensors:
    motion_config_mqtt_broker_${MOTION_DEVICE}:
      friendly_name: Add-on ${MOTION_DEVICE} MQTT broker
      icon_template: 'mdi:message-settings'
      value_template: >-
        {% set s = state_attr('sensor.motion_config_${MOTION_DEVICE}','config') %}
        {% if s is not none and s|lower != 'unavailable' and s|lower != 'unknown' and s != null %}
          {% set s = s.mqtt %}
          {% if s is not none and s|lower != 'unavailable' and s|lower != 'unknown' %}
            {{ s.host }}
          {% else %}null{% endif %}
        {% else %}null{% endif %}
    motion_config_mqtt_port_${MOTION_DEVICE}:
      friendly_name: Add-on ${MOTION_DEVICE} MQTT port
      icon_template: 'mdi:message-settings'
      value_template: >-
        {% set s = state_attr('sensor.motion_config_${MOTION_DEVICE}','config') %}
        {% if s is not none and s|lower != 'unavailable' and s|lower != 'unknown' and s != null %}
          {% set s = s.mqtt %}
          {% if s is not none and s|lower != 'unavailable' and s|lower != 'unknown' %}
            {{ s.port }}
          {% else %}null{% endif %}
        {% else %}null{% endif %}
    motion_config_mqtt_username_${MOTION_DEVICE}:
      friendly_name: Add-on ${MOTION_DEVICE} MQTT username
      icon_template: 'mdi:message-settings'
      value_template: >-
        {% set s = state_attr('sensor.motion_config_${MOTION_DEVICE}','config') %}
        {% if s is not none and s|lower != 'unavailable' and s|lower != 'unknown' and s != null %}
          {% set s = s.mqtt %}
          {% if s is not none and s|lower != 'unavailable' and s|lower != 'unknown' %}
            {{ s.username }}
          {% else %}null{% endif %}
        {% else %}null{% endif %}
    motion_config_mqtt_password_${MOTION_DEVICE}:
      friendly_name: Add-on ${MOTION_DEVICE} MQTT password
      icon_template: 'mdi:message-settings'
      value_template: >-
        {% set s = state_attr('sensor.motion_config_${MOTION_DEVICE}','config') %}
        {% if s is not none and s|lower != 'unavailable' and s|lower != 'unknown' and s != null %}
          {% set s = s.mqtt %}
          {% if s is not none and s|lower != 'unavailable' and s|lower != 'unknown' %}
            {{ s.password }}
          {% else %}null{% endif %}
        {% else %}null{% endif %}

###
# storage
###

- platform: template
  sensors:
    motion_storage_url_${MOTION_DEVICE}:
      friendly_name: Add-on ${MOTION_DEVICE} storage URL
      value_template: >-
        {% set s = states('sensor.motion_apache_host_${MOTION_DEVICE}') %}
        {% if s is not none and s|lower != 'unavailable' and s|lower != 'unknown' and s != null %}
          {% set host = s %}
          {% set s = states('sensor.motion_apache_port_${MOTION_DEVICE}') %}
          {% if s is not none and s|lower != 'unavailable' and s|lower != 'unknown' and s != null %}
            {% set port = s %}
            {{ 'http://' + host + ':' + port + '/cgi-bin/storage' }}
          {% else %}null{% endif %}
        {% else %}null{% endif %}

- platform: rest
  name: motion_storage_${MOTION_DEVICE}
  json_attributes:
    - blocks
    - used
    - available
    - directory
    - cameras
  force_update: true
  resource_template: >
    {% set s = states('sensor.motion_storage_url_${MOTION_DEVICE}') %}
    {% if s is not none and s|lower != 'unavailable' and s|lower != 'unknown' and s != null %}
      {{  s }}
    {% else %}http://127.0.0.1:7999/cgi-bin/storage{% endif %}
  value_template: >
    {% if value_json is defined %}True{% else %}False{% endif %}
   
- platform: template
  sensors:
    motion_storage_used_${MOTION_DEVICE}:
      friendly_name: Add-on ${MOTION_DEVICE} storage used 
      icon_template: 'mdi:database'
      unit_of_measurement: '%'
      value_template: >-
        {% set s = states('sensor.motion_storage_${MOTION_DEVICE}') %}
        {% if s is not none and s|lower != 'unavailable' and s|lower != 'unknown' and s != null %}
          {% set used = state_attr('sensor.motion_storage_${MOTION_DEVICE}','used') %}
          {% set blocks = state_attr('sensor.motion_storage_${MOTION_DEVICE}','blocks') %}
          {% if used is not none and used|lower != 'unavailable' and used|lower != 'unknown' and used != null
               and blocks is not none and blocks|lower != 'unavailable' and blocks|lower != 'unknown' and blocks != null %}
            {{ (used|float / blocks|float * 100.0)|int }}
          {% else %}null{% endif %}
        {% else %}null{% endif %}
   
- platform: template
  sensors:
    motion_storage_available_${MOTION_DEVICE}:
      friendly_name: Add-on ${MOTION_DEVICE} storage available
      icon_template: 'mdi:database'
      unit_of_measurement: '%'
      value_template: >-
        {% set s = states('sensor.motion_storage_${MOTION_DEVICE}') %}
        {% if s is not none and s|lower != 'unavailable' and s|lower != 'unknown' and s != null %}
          {% set available = state_attr('sensor.motion_storage_${MOTION_DEVICE}','available') %}
          {% set blocks = state_attr('sensor.motion_storage_${MOTION_DEVICE}','blocks') %}
          {% if available is not none and available|lower != 'unavailable' and available|lower != 'unknown' and available != null
               and blocks is not none and blocks|lower != 'unavailable' and blocks|lower != 'unknown' and blocks != null %}
            {{ (available|float / blocks|float * 100.0)|int }}
          {% else %}null{% endif %}
        {% else %}null{% endif %}

###
# status
###

- platform: template
  sensors:
    motion_status_url_${MOTION_DEVICE}:
      friendly_name: Motion ${MOTION_DEVICE} status URL
      value_template: >-
        {% set s = states('sensor.motion_apache_host_${MOTION_DEVICE}') %}
        {% if s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and s != null %}
          {% set host = s %}
          {% set s = states('sensor.motion_apache_port_${MOTION_DEVICE}') %}
          {% if s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and s != null %}
            {% set port = s %}
            {{ 'http://' + host + ':' + port + '/cgi-bin/status' }}
          {% else %}null{% endif %}
        {% else %}null{% endif %}

- platform: rest
  name: motion_status_${MOTION_DEVICE}
  json_attributes:
    - host
    - port
    - cameras
  force_update: true
  resource_template: >-
    {% set s = states('sensor.motion_status_url_${MOTION_DEVICE}') %}
    {% if s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and s != null %}
      {{- s -}}
    {% else %}http://127.0.0.1:7999/cgi-bin/status{% endif %}
  value_template: >-
    {% if value_json is defined %}True{% else %}False{% endif %}

- platform: template
  sensors:
    motion_status_host_${MOTION_DEVICE}:
      friendly_name: Add-on ${MOTION_DEVICE} host
      icon_template: 'mdi:cctv'
      value_template: >-
        {% set s = state_attr('sensor.motion_status_${MOTION_DEVICE}','host') %}
        {% if s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and s != null %}
          {{ s }}
        {% else %}null{% endif %}
    motion_status_port_${MOTION_DEVICE}:
      friendly_name: Add-on ${MOTION_DEVICE} port
      icon_template: 'mdi:cctv'
      value_template: >-
        {% set s = state_attr('sensor.motion_status_${MOTION_DEVICE}','port') %}
        {% if s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and s != null %}
          {{ s }}
        {% else %}null{% endif %}
    motion_status_camera_status_array_${MOTION_DEVICE}:
      friendly_name: Add-on ${MOTION_DEVICE} status array
      icon_template: 'mdi:cctv'
      value_template: >-
        {% set s = state_attr('sensor.motion_status_${MOTION_DEVICE}','cameras') %}
        {% if s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and s != null %}
          [{%- for state in s -%}
            {%- if loop.first -%}{%- else -%},{%- endif -%}
            {%- set s = state.status|lower -%}
            {%- if s == 'true' -%}1{%- else -%}0{%- endif -%}
          {%- endfor -%}]
        {% else %}null{% endif %}
    motion_status_camera_count_${MOTION_DEVICE}:
      friendly_name: 'Add-on ${MOTION_DEVICE} camera status count'
      icon_template: 'mdi:camera-outline'
      value_template: >-
        {% set s = states('sensor.motion_status_camera_status_array_${MOTION_DEVICE}') %}
        {% if s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and s != null and s|from_json is iterable %}
          {{ s|from_json|length }}
        {% else %}null{% endif %}
    motion_status_camera_count_online_${MOTION_DEVICE}:
      friendly_name: 'Add-on ${MOTION_DEVICE} direct on-line'
      icon_template: 'mdi:camera-outline'
      value_template: >-
        {% set s = states('sensor.motion_status_camera_status_array_${MOTION_DEVICE}') %}
        {% if s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and s != null and s|from_json is iterable %}
          {{ s|from_json|sum }}
        {% else %}null{% endif %}
    motion_status_camera_offline_${MOTION_DEVICE}:
      friendly_name: Add-on ${MOTION_DEVICE} off-line camera(s)
      icon_template: 'mdi:camera-outline'
      value_template: >-
        {% set s = state_attr('sensor.motion_status_${MOTION_DEVICE}','cameras') %}
        {% if s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and s != null %}
          {{ s|selectattr('status','==',false)|map(attribute='camera')|list }}
        {% else %}null{% endif %}
    motion_status_camera_count_offline_${MOTION_DEVICE}:
      friendly_name: 'Add-on ${MOTION_DEVICE} direct off-line'
      icon_template: 'mdi:camera-outline'
      value_template: >-
        {{ states('sensor.motion_status_camera_count_${MOTION_DEVICE}')|int - states('sensor.motion_status_camera_count_online_${MOTION_DEVICE}')|int  }}
    motion_status_camera_offline_percent_${MOTION_DEVICE}:
      friendly_name: 'Add-on ${MOTION_DEVICE} direct off-line %'
      icon_template: 'mdi:percent'
      unit_of_measurement: '%'
      value_template: >-
        {% set c = states('sensor.motion_status_camera_count_${MOTION_DEVICE}')|int %}
        {% if c > 0 %}
          {% set o = states('sensor.motion_status_camera_count_offline_${MOTION_DEVICE}')|int %}
          {{ (o / c * 100)|int }}
        {% else %}0{% endif %}
    motion_status_camera_status_${MOTION_DEVICE}:
      friendly_name: 'Camera status: ${MOTION_DEVICE}'
      icon_template: 'mdi:light-camera'
      value_template: >-
        {{ states('sensor.motion_status_camera_count_online_${MOTION_DEVICE}') -}}/{{- states('sensor.motion_status_camera_count_${MOTION_DEVICE}') -}};
        {{ states('sensor.motion_status_camera_offline_percent_${MOTION_DEVICE}') -}}%

# percent statistics

- platform: statistics
  name: motion_status_camera_offline_percent_${MOTION_DEVICE}_statistics
  entity_id: sensor.motion_status_camera_offline_percent_${MOTION_DEVICE}
  sampling_size: 100
  max_age:
    hours: 72

- platform: template
  sensors:
    motion_status_camera_offline_percent_${MOTION_DEVICE}_mean:
      friendly_name: 'Add-on ${MOTION_DEVICE} direct off-line mean %'
      unit_of_measurement: '%'
      icon_template: 'mdi:camera-outline'
      value_template: >-
        {% if states('sensor.motion_status_camera_offline_percent_${MOTION_DEVICE}_statistics') != 'unknown' %}
          {{ states.sensor.motion_status_camera_offline_percent_${MOTION_DEVICE}_statistics.state|float }}
        {%- else -%}null{%- endif -%}
    motion_status_camera_offline_percent_${MOTION_DEVICE}_min_value:
      friendly_name: 'Add-on ${MOTION_DEVICE} direct off-line min'
      unit_of_measurement: '%'
      icon_template: 'mdi:camera-outline'
      value_template: >-
        {% if states('sensor.motion_status_camera_offline_percent_${MOTION_DEVICE}_statistics') != 'unknown' %}
          {{ states.sensor.motion_status_camera_offline_percent_${MOTION_DEVICE}_statistics.attributes.min_value|int }}
        {%- else -%}null{%- endif -%}
    motion_status_camera_offline_percent_${MOTION_DEVICE}_max_value:
      friendly_name: 'Add-on ${MOTION_DEVICE} direct off-line max'
      unit_of_measurement: '%'
      icon_template: 'mdi:camera-outline'
      value_template: >-
        {% if states('sensor.motion_status_camera_offline_percent_${MOTION_DEVICE}_statistics') != 'unknown' %}
          {{ states.sensor.motion_status_camera_offline_percent_${MOTION_DEVICE}_statistics.attributes.max_value|int }}
        {%- else -%}null{%- endif -%}
    motion_status_camera_offline_percent_${MOTION_DEVICE}_stdev:
      friendly_name: 'Add-on ${MOTION_DEVICE} direct off-line stdev'
      icon_template: 'mdi:sigma-lower'
      unit_of_measurement: '%'
      value_template: >
        {% if states('sensor.motion_status_camera_offline_percent_${MOTION_DEVICE}_statistics') != 'unknown' %}
          {{ states.sensor.motion_status_camera_offline_percent_${MOTION_DEVICE}_statistics.attributes.standard_deviation|float }}
        {%- else -%}null{%- endif -%}
    motion_status_camera_offline_percent_${MOTION_DEVICE}_change:
      friendly_name: 'Add-on ${MOTION_DEVICE} direct off-line Δ'
      icon_template: 'mdi:delta'
      unit_of_measurement: '%'
      value_template: >
        {% if states('sensor.motion_status_camera_offline_percent_${MOTION_DEVICE}_statistics') != 'unknown' %}
          {{ states.sensor.motion_status_camera_offline_percent_${MOTION_DEVICE}_statistics.attributes.change|float }}
        {%- else -%}null{%- endif -%}

