###
# homeassistant/binary_sensor/device/opening.yaml
###

- platform: template
  sensors:
    motion_device_opening_alarm:
      friendly_name: 'Alarm status: opening'
      attribute_templates:
        markdown: >-
          {{ '<h2>No opening alarm information</h2>' }}
        status: >-
          {{ 'unknown' }}
      value_template: >-
        {{ 'unknown' }}

- platform: template
  sensors:
    motion_device_opening_alarm_exist:
      friendly_name: 'Alarm exists: opening'
      attribute_templates:
        good: >-
          {{ is_state_attr('binary_sensor.motion_device_opening_exist','good','on') and is_state('binary_sensor.motion_device_opening_good','on') }}
        bad: >-
          {{ is_state_attr('binary_sensor.motion_device_opening_exist','bad','on') and is_state('binary_sensor.motion_device_opening_bad','on') }}
        activity: >-
          {{ is_state_attr('binary_sensor.motion_device_opening_exist','activity','on') and is_state('binary_sensor.motion_device_opening_activity','on') }}
        low: >-
          {{ is_state_attr('binary_sensor.motion_device_opening_exist','low','on') and is_state('binary_sensor.motion_device_opening_low','on') }}
        high: >-
          {{ is_state_attr('binary_sensor.motion_device_opening_exist','high','on') and is_state('binary_sensor.motion_device_opening_high','on') }}
        missing: >-
          {{ is_state_attr('binary_sensor.motion_device_opening_exist','required','on') and is_state('binary_sensor.motion_device_opening_missing','on') }}
        unavailable: >-
          {{ is_state('binary_sensor.motion_device_opening_unavailable','on') }}
        spike: >-
          {"min":{{ is_state('binary_sensor.motion_device_opening_min_spike','on') }},"max":{{ is_state('binary_sensor.motion_device_opening_max_spike','on') }}}
        notify: >-
          {%- set id = 'opening-alarm-notification' -%}
          {% set g = 'home-environment' -%}
          {%- set i = 30 -%}
          {%- set r = ['approve','deny'] -%}
          {%- set p = state_attr('sensor.motion_role_primary','friendly_name') -%}
          {%- set a = [
            {'action': id|string + '-approve','icon':'sfsymbols:hand.thumbsup','title':'Approve opening increase','destructive':false},
            {'action': id|string + '-wait','icon':'sfsymbols:timer','title':'Ask again in ' + i|string + ' minutes','destructive':false},
            {'action': id|string + '-deny','icon':'sfsymbols:hand.thumbsdown.fill','title':'Notify ' + p|string,'destructive':true}
          ] -%}
          {"id":"{{- id -}}","group":"{{- g -}}","interval":{{- i -}},"responses":{{- r -}},"primary":"{{- p -}}","action":{{ a }}}
      value_template: >-
        {{ is_state_attr('binary_sensor.motion_device_opening_exist','alarm','on') and is_state('binary_sensor.motion_device_opening_alarm','on') }}

- platform: template
  sensors:
    motion_device_opening_exist:
      friendly_name: Existing opening sensor(s)
      attribute_templates:
        good: >-
          {% set g = state_attr('binary_sensor.motion_device_class','good') -%}
          {%- set b = state_attr('binary_sensor.motion_device_class','bad') -%}
          {%- if g|lower != 'none' and b|lower != 'none' -%}
            {%- if 'opening' in g -%}on{%- elif 'opening' in b -%}off{%- else -%}none{%- endif -%}
          {%- else -%}none{%- endif %}
        bad: >-
          {% set g = state_attr('binary_sensor.motion_device_class','good') -%}
          {%- set b = state_attr('binary_sensor.motion_device_class','bad') -%}
          {%- if g|lower != 'none' and b|lower != 'none' -%}
            {%- if 'opening' in b -%}on{%- elif 'opening' in g -%}off{%- else -%}none{%- endif -%}
          {%- else -%}none{%- endif %}
        alarm: >-
          {% set s = state_attr('binary_sensor.motion_device_class','alarm') -%}
          {%- if s|lower != 'none' -%}
            {%- if 'opening' in s -%}on{%- else -%}none{%- endif -%}
          {%- else -%}none{%- endif %}
        activity: >-
          {% set s = state_attr('binary_sensor.motion_device_class','activity') -%}
          {%- if s|lower != 'none' -%}
            {%- if 'opening' in s -%}on{%- else -%}none{%- endif -%}
          {%- else -%}none{%- endif %}
        required: >-
          {% set s = state_attr('binary_sensor.motion_device_class','sensors') -%}
          {% set b = state_attr('binary_sensor.motion_device_class','binary_sensors') -%}
          {%- if s|lower != 'none' and b|lower != 'none' -%}
            {% set required = (s|list + b|list)|sort|unique %}
            {%- if 'opening' in required -%}on{%- else -%}none{%- endif -%}
          {%- else -%}none{%- endif %}
        low: >-
          {% set s = state_attr('binary_sensor.motion_device_class','low') -%}
          {%- if s|lower != 'none' -%}
            {%- if 'opening' in s -%}
              {{ states('input_number.motion_device_opening_low') }}
            {%- else -%}none{%- endif -%}
          {%- else -%}none{%- endif %}
        high: >-
          {% set s = state_attr('binary_sensor.motion_device_class','high') -%}
          {%- if s|lower != 'none' -%}
            {%- if 'opening' in s -%}
              {{ states('input_number.motion_device_opening_high') }}
            {%- else -%}none{%- endif -%}
          {%- else -%}none{%- endif %}
        measurement: >-
          {% set s = state_attr('binary_sensor.motion_device_class','sensor_list') -%}
          {%- if s|lower != 'none' -%}
            {%- if 'opening' in s -%}on{%- else -%}none{%- endif -%}
          {%- else -%}none{%- endif %}
        status: >-
          {% set s = state_attr('binary_sensor.motion_device_class','binary_sensor_list') -%}
          {%- if s|lower != 'none' -%}
            {%- if 'opening' in s -%}on{%- else -%}none{%- endif -%}
          {%- else -%}none{%- endif %}
        markdown: >-
          {% set c = 'opening' -%}
          <h2><a href="/notify-devices/{{- c -}}">{{ c|upper }}</a> device class</h2>
          {%- for i in ['good','bad','alarm','activity','required','low','high'] if not is_state_attr('binary_sensor.motion_device_opening_exist',i,'none') -%}
            {%- if loop.first -%}<ul>{%- endif -%}
            <li>{{- i|upper -}}: {{ state_attr('binary_sensor.motion_device_opening_exist',i) -}}</li>
            {%- if loop.last -%}</ul>{%- endif -%}
          {%- endfor -%}
          {%- set s = state_attr('binary_sensor.motion_device_opening_status','markdown') -%} {%- if s|lower != 'none' -%}{{- s -}}{%- endif -%}
          {%- set s = state_attr('binary_sensor.motion_device_opening_measurement','markdown') -%} {%- if s|lower != 'none' -%}{{- s -}}{%- endif -%}
          {%- set s = state_attr('binary_sensor.motion_device_opening_statistics','markdown') -%} {%- if s|lower != 'none' -%}{{- s -}}{%- endif -%}
          {%- set s = state_attr('binary_sensor.motion_device_opening_controls','markdown') -%} {%- if s|lower != 'none' -%}{{- s -}}{%- endif -%}
          {%- set s = state_attr('binary_sensor.motion_device_opening_activity','markdown') -%} {%- if s|lower != 'none' -%}{{- s -}}{%- endif -%}
          {%- set s = state_attr('binary_sensor.motion_device_opening_unavailable','markdown') -%} {%- if s|lower != 'none' -%}{{- s -}}{%- endif -%}
      value_template: >-
        {% set s = state_attr('binary_sensor.motion_device_opening','total') %}
        {% if s|lower != 'null' and s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' %}
          {{ s|int(0) > 0 }}
        {% else %}unknown{% endif %}

- platform: template
  sensors:
    motion_device_opening_status:
      friendly_name: Status opening
      attribute_templates:
        markdown: >-
          {%- set s = state_attr('binary_sensor.motion_device_opening','status') %}
          {%- if s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
            {%- set s = s|selectattr('entity_id','search','binary_sensor.')|list -%}
            {%- if s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
            {{- '<h2>Status</h2><ul>' -}}
            {%- for i in s -%}
              {%- set u = device_id(i.entity_id) -%}
              {{- '<li>' -}}
              {%- if u|lower != 'none' -%}
                <a href="/config/devices/device/{{- u -}}"><b>{{- i.name -}}</b></a>: {{ states(i.entity_id) }}
              {%- else -%}
                <b>{{- i.name -}}</b>: {{ states(i.entity_id) -}}
              {%- endif -%}
              {{- '</li>' -}}
            {%- endfor -%}
            {{- '</ul>' -}}
            {%- else -%}<h2>No status</h2>{%- endif -%}
          {%- else -%}<h2>No status</h2>{%- endif -%}
      value_template: >-
        {% set s = state_attr('binary_sensor.motion_device_opening_exist','status') %}
        {% if s|lower != 'null' and s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' %}
          {{ s }}
        {% else %}unknown{% endif %}
    motion_device_opening_measurement:
      friendly_name: Measurement opening
      attribute_templates:
        markdown: >-
          {%- set s = state_attr('binary_sensor.motion_device_opening','status') %}
          {%- if s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
            {%- set s = s|rejectattr('entity_id','search','binary_sensor.')|list -%}
            {%- if s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
              {{- '<h2>Measurements</h2><ul>' -}}
              {%- for i in s -%}
                {%- set u = device_id(i.entity_id) -%}
                {%- set v = states(i.entity_id) -%}
                {%- if v|lower != 'none' and v|lower != 'unknown' and v|lower != 'unavailable' and v|lower != 'null' and v|string|count > 0 -%}
                  {%- set uom = state_attr(i.entity_id,'unit_of_measurement') -%}
                  {%- if uom|lower != 'none' and uom|lower != 'unknown' and uom|lower != 'unavailable' and uom|lower != 'null' and uom|string|count > 0 -%}
                    {%- set v = v|string + uom|string -%}
                  {%- endif -%}
                {%- endif -%}
                {{- '<li>' -}}
                {%- if u|lower != 'none' -%}
                  <a href="/config/devices/device/{{- u -}}"><b>{{- i.name -}}</b></a>: {{ v }}
                {%- else -%}
                  <b>{{- i.name -}}</b>: {{ v }}
                {%- endif -%}
                {{- '</li>' -}}
              {%- endfor -%}
              {{- '</ul>' -}}
            {%- else -%}<h2>No measurements</h2>{%- endif -%}
          {%- else -%}<h2>No measurements</h2>{%- endif -%}
      value_template: >-
        {% set s = state_attr('binary_sensor.motion_device_opening_exist','measurement') %}
        {% if s|lower != 'null' and s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' %}
          {{ s }}
        {% else %}unknown{% endif %}
    motion_device_opening_statistics:
      friendly_name: Statistics opening
      attribute_templates:
        status: >-
          {%- set s = state_attr('binary_sensor.motion_device_opening','statistics') %}
          {%- if s|lower != 'null' and s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and s is iterable and s|count > 0 -%}
            [{%- for i in s if 'entity_id' in i -%}
            {%- if not loop.first -%},{%- endif -%}
              {"{{- i.name -}}":["{{- states(i.entity_id) }} {{ state_attr(i.entity_id,'unit_of_measurement') -}}","{{- states[i.entity_id].last_updated|relative_time }}"]}
            {%- endfor -%}]
          {%- else -%}none{%- endif -%}
        markdown: >-
          {%- set s = state_attr('binary_sensor.motion_device_opening','statistics') %}
          {%- if s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
            {{- '<h2>Statistics</h2><ul>' -}}
            {%- for i in s -%}
              {%- set u = device_id(i.entity_id) -%}
              {%- set v = states(i.entity_id) -%}
              {%- if v|lower != 'none' and v|lower != 'unknown' and v|lower != 'unavailable' and v|lower != 'null' and v|string|count > 0 -%}
                {%- set uom = state_attr(i.entity_id,'unit_of_measurement') -%}
                {%- if uom|lower != 'none' and uom|lower != 'unknown' and uom|lower != 'unavailable' and uom|lower != 'null' and uom|string|count > 0 -%}
                  {%- set v = v|string + uom|string -%}
                {%- endif -%}
              {%- endif -%}
              {{- '<li>' -}}
              {%- if u|lower != 'none' -%}
                <a href="/config/devices/device/{{- u -}}"><b>{{- i.name -}}</b></a>: {{ v }}
              {%- else -%}
                <b>{{- i.name -}}</b>: {{ v -}}
              {%- endif -%}
              {{- '</li>' -}}
            {%- endfor -%}
            {{- '</ul>' -}}
          {%- else -%}<h2>No statistics</h2>{%- endif -%}
      value_template: >-
        {% set s = state_attr('binary_sensor.motion_device_opening_statistics','status') %}
        {% if s|lower != 'null' and s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and s is iterable %}
          {{ s|count > 0 }}
        {% else %}unknown{% endif %}
    motion_device_opening_controls:
      friendly_name: Control opening
      attribute_templates:
        status: >-
          {%- set s = state_attr('binary_sensor.motion_device_opening','controls') %}
          {%- if s|lower != 'null' and s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and s is iterable -%}
            [{%- for i in s if 'entity_id' in i and not 'button.' in i.entity_id -%}
              {%- if not loop.first -%},{%- endif -%}
              {"{{- i.name -}}":["{{ states(i.entity_id) -}}","{{- states[i.entity_id].last_updated|relative_time }}"]}
            {%- endfor -%}]
          {%- else -%}none{%- endif -%}
        markdown: >-
          {%- set s = state_attr('binary_sensor.motion_device_opening','controls') %}
          {%- if s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
            {{- '<h2>Controls</h2>' -}}
            {%- for i in s if 'entity_id' in i and not 'button.' in i.entity_id -%}
              {%- if loop.first -%}{{- '<ul>' -}}{%- endif -%}
              {%- set u = device_id(i.entity_id) -%}
              {{- '<li>' -}}
              {%- if u|lower != 'none' -%}
                <a href="/config/devices/device/{{- u -}}"><b>{{- i.name -}}</b></a>: {{ states(i.entity_id) -}}
              {%- else -%}
                <b>{{- i.name -}}</b>: {{ states(i.entity_id) -}}
              {%- endif -%}
              {{- '</li>' -}}
              {%- if loop.last -%}{{- '</ul>' -}}{%- endif -%}
            {%- endfor -%}
          {%- else -%}
            {{- '<h2>No controls</h2>' -}}
          {%- endif -%}
      value_template: >-
        {% set s = state_attr('binary_sensor.motion_device_opening_controls','status') %}
        {% if s|lower != 'null' and s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and s is iterable %}
          {{ s|count > 0 }}
        {% else %}unknown{% endif %}
    motion_device_opening_required:
      value_template: >-
        {% set s = state_attr('binary_sensor.motion_device_opening_exist','required') %}
        {% if s|lower != 'null' and s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' %}
          {{ s }}
        {% else %}unknown{% endif %}
    motion_device_opening_self_test:
      icon_template: 'mdi:gesture-tap-button'
      attribute_templates:
        entity_ids: >-
          {% set s = state_attr('binary_sensor.motion_device_opening','controls') %}
          {%- if s|lower != 'unknown' and s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
            [{%- for i in s if '_self_test' in i.entity_id -%}
              {%- if not loop.first -%},{%- endif -%}
              "{{- i.entity_id -}}"
            {%- endfor -%}]
          {%- else -%}none{%- endif %}
      value_template: >-
        {% set s = state_attr('binary_sensor.motion_device_opening_self_test','entity_ids') %}
        {% if s|lower != 'none' and  s|lower != 'unknown' and not s is string and s is iterable %}
          {{- s|count|int(0) > 0 -}}
        {% else %}unknown{% endif %}
    motion_device_opening_turn_off:
      icon_template: 'mdi:turn_off'
      attribute_templates:
        entity_ids: >-
          {% set s = state_attr('binary_sensor.motion_device_opening','controls') %}
          {%- if s|lower != 'unknown' and s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
            [{%- for i in s if 'switch.' in i.entity_id -%}
              {%- if not loop.first -%},{%- endif -%}
              "{{- i.entity_id -}}"
            {%- endfor -%}]
          {%- else -%}none{%- endif %}
      value_template: >-
        {% set s = state_attr('binary_sensor.motion_device_opening_turn_off','entity_ids') %}
        {% if s|lower != 'none' and  s|lower != 'unknown' and not s is string and s is iterable %}
          {{- s|count|int(0) > 0 -}}
        {% else %}unknown{% endif %}
    motion_device_opening_turn_on:
      icon_template: 'mdi:turn_on'
      attribute_templates:
        entity_ids: >-
          {% set s = state_attr('binary_sensor.motion_device_opening','controls') %}
          {%- if s|lower != 'unknown' and s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
            [{%- for i in s if 'switch.' in i.entity_id -%}
              {%- if not loop.first -%},{%- endif -%}
              "{{- i.entity_id -}}"
            {%- endfor -%}]
          {%- else -%}none{%- endif %}
      value_template: >-
        {% set s = state_attr('binary_sensor.motion_device_opening_turn_on','entity_ids') %}
        {% if s|lower != 'none' and  s|lower != 'unknown' and not s is string and s is iterable %}
          {{- s|count|int(0) > 0 -}}
        {% else %}unknown{% endif %}
    motion_device_opening_toggle:
      icon_template: 'mdi:toggle'
      attribute_templates:
        entity_ids: >-
          {% set s = state_attr('binary_sensor.motion_device_opening','controls') %}
          {%- if s|lower != 'unknown' and s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
            [{%- for i in s if 'switch.' in i.entity_id -%}
              {%- if not loop.first -%},{%- endif -%}
              "{{- i.entity_id -}}"
            {%- endfor -%}]
          {%- else -%}none{%- endif %}
      value_template: >-
        {% set s = state_attr('binary_sensor.motion_device_opening_toggle','entity_ids') %}
        {% if s|lower != 'none' and  s|lower != 'unknown' and not s is string and s is iterable %}
          {{- s|count|int(0) > 0 -}}
        {% else %}unknown{% endif %}
    motion_device_opening_mute:
      icon_template: 'mdi:gesture-tap-button'
      attribute_templates:
        entity_ids: >-
          {% set s = state_attr('binary_sensor.motion_device_opening','controls') %}
          {%- if s|lower != 'unknown' and s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
            [{%- for i in s if '_mute' in i.entity_id -%}
              {%- if not loop.first -%},{%- endif -%}
              "{{- i.entity_id -}}"
            {%- endfor -%}]
          {%- else -%}none{%- endif %}
      value_template: >-
        {% set s = state_attr('binary_sensor.motion_device_opening_mute','entity_ids') %}
        {% if s|lower != 'none' and  s|lower != 'unknown' and not s is string and s is iterable %}
          {{- s|count|int(0) > 0 -}}
        {% else %}unknown{% endif %}
    motion_device_opening_unmute:
      icon_template: 'mdi:gesture-tap-button'
      attribute_templates:
        entity_ids: >-
          {% set s = state_attr('binary_sensor.motion_device_opening','controls') %}
          {%- if s|lower != 'unknown' and s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
            [{%- for i in s if '_unmute' in i.entity_id -%}
              {%- if not loop.first -%},{%- endif -%}
              "{{- i.entity_id -}}"
            {%- endfor -%}]
          {%- else -%}none{%- endif %}
      value_template: >-
        {% set s = state_attr('binary_sensor.motion_device_opening_unmute','entity_ids') %}
        {% if s|lower != 'none' and  s|lower != 'unknown' and not s is string and s is iterable %}
          {{- s|count|int(0) > 0 -}}
        {% else %}unknown{% endif %}
    motion_device_opening_update:
      icon_template: 'mdi:update'
      attribute_templates:
        entity_ids: >-
          {% set s = state_attr('binary_sensor.motion_device_opening','update') %}
          {% if s|lower != 'none' and  s|lower != 'unknown' and not s is string and s is iterable and s|count > 0 %}
            [{%- for i in s -%}
              {%- if not loop.first -%},{%- endif -%}
              "{{- i.entity_id -}}"
            {%- endfor -%}]
          {% else %}none{% endif %}
      value_template: >-
        {% set s = state_attr('binary_sensor.motion_device_opening','update') %}
        {% if s|lower != 'none' and  s|lower != 'unknown' and not s is string and s is iterable %}
          {{- s|count|int(0) > 0 -}}
        {% else %}unknown{% endif %}
    motion_device_opening_restart:
      icon_template: 'mdi:restart'
      attribute_templates:
        entity_ids: >-
          {% set s = state_attr('binary_sensor.motion_device_opening','restart') %}
          {% if s|lower != 'none' and  s|lower != 'unknown' and not s is string and s is iterable and s|count > 0 %}
            [{%- for i in s -%}
              {%- if not loop.first -%},{%- endif -%}
              "{{- i.entity_id -}}"
            {%- endfor -%}]
          {% else %}none{% endif %}
      value_template: >-
        {% set s = state_attr('binary_sensor.motion_device_opening','restart') %}
        {% if s|lower != 'none' and  s|lower != 'unknown' and not s is string and s is iterable %}
          {{- s|count|int(0) > 0 -}}
        {% else %}unknown{% endif %}
    motion_device_opening_test:
      device_class: 'problem'
      icon_template: 'mdi:alert-circle'
      attribute_templates:
        values: >-
          {% set s = state_attr('binary_sensor.motion_device_opening','test') %}
          {% if s|lower != 'none' and  s|lower != 'unknown' and not s is string and s is iterable and s|count > 0 %}
            [{%- for i in s  -%}
              {%- if not loop.first -%},{%- endif -%}
              {% if i.state|lower == 'normal' %}0{% else %}1{% endif %}
            {%- endfor -%}]
          {% else %}none{% endif %}
      value_template: >-
        {% set s = state_attr('binary_sensor.motion_device_opening_test','values') %}
        {% if s|lower != 'none' and  s|lower != 'unknown' and not s is string and s is iterable and s|count > 0 %}
          {{- s|sum|int(0) > 0 -}}
        {% else %}unknown{% endif %}
    motion_device_opening:
      friendly_name: Status opening sensor(s)
      attribute_templates:
        last: >-
          {{- none -}}
        ids: >-
          {{- 'unknown' -}}
        total: >-
          {{- 'unknown' -}}
        update: >-
          {{- 'unknown' -}}
        restart: >-
          {{- 'unknown' -}}
        controls: >-
          {{- 'unknown' -}}
        test: >-
          {{- 'unknown' -}}
        status: >-
          {{- 'unknown' -}}
        high: >-
          {{- 'unknown' -}}
        low: >-
          {{- 'unknown' -}}
        good: >-
          {{- 'unknown' -}}
        bad: >-
          {{- 'unknown' -}}
        activity: >-
          {{- 'unknown' -}}
        unavailable: >-
          {{- 'unknown' -}}
      value_template: >-
        {# set through automation #}
        {{- 'unknown' -}}

###

- platform: template
  sensors:
    motion_device_opening_bad:
      friendly_name: Bad opening
      attribute_templates:
        last: >-
          {{ 'unknown' }}
        details: >-
          {{ 'unknown' }}
      value_template: >-
        {{ 'unknown' }}
    motion_device_opening_good:
      friendly_name: Good opening
      attribute_templates:
        last: >-
          {{ 'unknown' }}
        details: >-
          {{ 'unknown' }}
      value_template: >-
        {{ 'unknown' }}
    motion_device_opening_low:
      friendly_name: Low opening
      attribute_templates:
        last: >-
          {{ 'unknown' }}
        details: >-
          {{ 'unknown' }}
      value_template: >-
        {{ 'unknown' }}
    motion_device_opening_high:
      friendly_name: High opening
      attribute_templates:
        last: >-
          {{ 'unknown' }}
        details: >-
          {{ 'unknown' }}
      value_template: >-
        {{ 'unknown' }}
    motion_device_opening_activity:
      friendly_name: Activity opening
      attribute_templates:
        last: >-
          {{ 'unknown' }}
        ids: >-
          {{ 'unknown' }}
        details: >-
          {{ 'unknown' }}
      value_template: >-
        {{ 'unknown' }}
    motion_device_opening_unavailable:
      friendly_name: Unavailable opening
      device_class: 'connectivity'
      attribute_templates:
        last: >-
          {{ 'unknown' }}
        ids: >-
          {{ 'unknown' }}
        details: >-
          {{ 'unknown' }}
      value_template: >-
        {{ 'unknown' }}
    motion_device_opening_missing:
      attribute_templates:
        last: >-
          {{ 'unknown' }}
        ids: >-
          {{ 'unknown' }}
      value_template: >-
        {{ 'unknown' }}
