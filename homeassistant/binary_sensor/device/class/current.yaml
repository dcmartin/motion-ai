###
# homeassistant/binary_sensor/device/current.yaml
###

- platform: template
  sensors:
    motion_device_current:
      friendly_name: Status current sensor(s)
      attribute_templates:
        last: >-
          {{- none -}}
        condition: >-
          {{- 'none' -}}
        response: >-
          {{- 'none' -}}
        actions: >-
          {{- 'none' -}}
        ids: >-
          {{- 'none' -}}
        total: >-
          {{- 'none' -}}
        update: >-
          {{- 'none' -}}
        restart: >-
          {{- 'none' -}}
        controls: >-
          {{- 'none' -}}
        test: >-
          {{- 'none' -}}
        status: >-
          {{- 'none' -}}
        high: >-
          {{- 'none' -}}
        low: >-
          {{- 'none' -}}
        good: >-
          {{- 'none' -}}
        bad: >-
          {{- 'none' -}}
        activity: >-
          {{- 'none' -}}
        unavailable: >-
          {{- 'none' -}}
      value_template: >-
        {# set through automation #}
        {{- 'unknown' -}}

- platform: template
  sensors:
    motion_device_current_expired:
      attribute_templates:
        last: >-
          {{- 'none' -}}
      value_template: >-
        {# set through automation #}
        {{- 'unknown' -}}

- platform: template
  sensors:
    motion_device_current_required:
      value_template: >-
        {% set s = state_attr('binary_sensor.motion_device_current_exist','required') %}
        {% if s|lower != 'null' and s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' %}
          {{ s }}
        {% else %}unknown{% endif %}

- platform: template
  sensors:
    motion_device_current_rescan:
      value_template: >-
        {{ states.sensor.motion_device_current_count.last_updated|as_timestamp(0) >
           states.automation.motion_device_current_rescan.attributes.last_triggered|as_timestamp(0) }}

- platform: template
  sensors:
    motion_device_current_status:
      friendly_name: Status current
      attribute_templates:
        markdown: >-
          {%- set s = state_attr('binary_sensor.motion_device_current','status') %}
          {%- if s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
            {%- set s = s|selectattr('entity_id','search','binary_sensor.')|list -%}
            {%- if s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
            {{- '<h2>Status</h2><ul>' -}}
            {%- for i in s if 'entity_id' in  i -%}
              {%- set u = device_id(i.entity_id) -%}
              {{- '<li>' -}}
              {%- if u|lower != 'none' -%}
                <a href="/config/devices/device/{{- u -}}"><b>{{- i.name -}}</b></a>: {{ states(i.entity_id) }}
              {%- else -%}
                <b>{{- i.name -}}</b>: {{ states(i.entity_id) -}}
              {%- endif -%}
              {{- '</li>' -}}
            {%- endfor -%}
            {{- '</ul>' -}}
            {%- else -%}<h2>No status</h2>{%- endif -%}
          {%- else -%}<h2>No status</h2>{%- endif -%}
      value_template: >-
        {% set s = state_attr('binary_sensor.motion_device_current_exist','status') %}
        {% if s|lower != 'null' and s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' %}
          {{ s }}
        {% else %}unknown{% endif %}
    motion_device_current_measurement:
      friendly_name: Measurement current
      attribute_templates:
        markdown: >-
          {%- set s = state_attr('binary_sensor.motion_device_current','status') %}
          {%- if s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
            {%- set s = s|rejectattr('entity_id','search','binary_sensor.')|selectattr('entity_id','search','sensor')|list -%}
            {%- if s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
              {{- '<h2>Measurements</h2><ul>' -}}
              {%- for i in s if 'entity_id' in i -%}
                {%- set u = device_id(i.entity_id) -%}
                {%- set v = states(i.entity_id) -%}
                {%- if v|lower != 'none' and v|lower != 'unknown' and v|lower != 'unavailable' and v|lower != 'null' and v|string|count > 0 -%}
                  {%- set uom = state_attr(i.entity_id,'unit_of_measurement') -%}
                  {%- if uom|lower != 'none' and uom|lower != 'unknown' and uom|lower != 'unavailable' and uom|lower != 'null' and uom|string|count > 0 -%}
                    {%- set v = v|string + uom|string -%}
                  {%- endif -%}
                {%- endif -%}
                {{- '<li>' -}}
                {%- if u|lower != 'none' -%}
                  <a href="/config/devices/device/{{- u -}}"><b>{{- i.name -}}</b></a>: {{ v }}
                {%- else -%}
                  <b>{{- i.name -}}</b>: {{ v }}
                {%- endif -%}
                {{- '</li>' -}}
              {%- endfor -%}
              {{- '</ul>' -}}
            {%- else -%}<h2>No measurements</h2>{%- endif -%}
          {%- else -%}<h2>No measurements</h2>{%- endif -%}
      value_template: >-
        {% set s = state_attr('binary_sensor.motion_device_current_exist','measurement') %}
        {% if s|lower != 'null' and s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' %}
          {{ s }}
        {% else %}unknown{% endif %}
    motion_device_current_statistics:
      friendly_name: Statistics current
      attribute_templates:
        status: >-
          {%- set s = state_attr('binary_sensor.motion_device_current','statistics') %}
          {%- if s|lower != 'null' and s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and s is iterable and s|count > 0 -%}
            [{%- for i in s if 'entity_id' in i -%}
            {%- if not loop.first -%},{%- endif -%}
              {'{{- i.name -}}':['{{- states(i.entity_id) }} {{ state_attr(i.entity_id,'unit_of_measurement') -}}','{{- states[i.entity_id].last_updated|relative_time }}']}
            {%- endfor -%}]
          {%- else -%}none{%- endif -%}
        markdown: >-
          {%- set s = state_attr('binary_sensor.motion_device_current','statistics') %}
          {%- if s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
            {{- '<h2>Statistics</h2><ul>' -}}
            {%- for i in s if 'entity_id' in i -%}
              {%- set u = device_id(i.entity_id) -%}
              {%- set v = states(i.entity_id) -%}
              {%- if v|lower != 'none' and v|lower != 'unknown' and v|lower != 'unavailable' and v|lower != 'null' and v|string|count > 0 -%}
                {%- set uom = state_attr(i.entity_id,'unit_of_measurement') -%}
                {%- if uom|lower != 'none' and uom|lower != 'unknown' and uom|lower != 'unavailable' and uom|lower != 'null' and uom|string|count > 0 -%}
                  {%- set v = v|string + uom|string -%}
                {%- endif -%}
              {%- endif -%}
              {{- '<li>' -}}
              {%- if u|lower != 'none' -%}
                <a href="/config/devices/device/{{- u -}}"><b>{{- i.name -}}</b></a>: {{ v }}
              {%- else -%}
                <b>{{- i.name -}}</b>: {{ v -}}
              {%- endif -%}
              {{- '</li>' -}}
            {%- endfor -%}
            {{- '</ul>' -}}
          {%- else -%}<h2>No statistics</h2>{%- endif -%}
      value_template: >-
        {% set s = state_attr('binary_sensor.motion_device_current_statistics','status') %}
        {% if s|lower != 'null' and s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and s is iterable %}
          {{ s|count > 0 }}
        {% else %}unknown{% endif %}
    motion_device_current_controls:
      friendly_name: Control current
      attribute_templates:
        status: >-
          {%- set s = state_attr('binary_sensor.motion_device_current','controls') %}
          {%- if s|lower != 'null' and s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and s is iterable -%}
            [{%- for i in s if 'entity_id' in i and not 'button.' in i.entity_id -%}
              {%- if not loop.first -%},{%- endif -%}
              {'{{- i.name -}}':['{{ states(i.entity_id) -}}','{{- states[i.entity_id].last_updated|relative_time }}']}
            {%- endfor -%}]
          {%- else -%}none{%- endif -%}
        markdown: >-
          {%- set s = state_attr('binary_sensor.motion_device_current','controls') %}
          {%- if s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
            {{- '<h2>Controls</h2>' -}}
            {%- for i in s if 'entity_id' in i and not 'button.' in i.entity_id -%}
              {%- if loop.first -%}{{- '<ul>' -}}{%- endif -%}
              {%- set u = device_id(i.entity_id) -%}
              {{- '<li>' -}}
              {%- if u|lower != 'none' -%}
                <a href="/config/devices/device/{{- u -}}"><b>{{- i.name -}}</b></a>: {{ states(i.entity_id) -}}
              {%- else -%}
                <b>{{- i.name -}}</b>: {{ states(i.entity_id) -}}
              {%- endif -%}
              {{- '</li>' -}}
              {%- if loop.last -%}{{- '</ul>' -}}{%- endif -%}
            {%- endfor -%}
          {%- else -%}
            {{- '<h2>No controls</h2>' -}}
          {%- endif -%}
      value_template: >-
        {% set s = state_attr('binary_sensor.motion_device_current_controls','status') %}
        {% if s|lower != 'null' and s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and s is iterable %}
          {{ s|count > 0 }}
        {% else %}unknown{% endif %}
    motion_device_current_self_test:
      icon_template: 'mdi:gesture-tap-button'
      attribute_templates:
        entity_ids: >-
          {% set s = state_attr('binary_sensor.motion_device_current','controls') %}
          {%- if s|lower != 'unknown' and s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
            [{%- for i in s if '_self_test' in i.entity_id -%}
              {%- if not loop.first -%},{%- endif -%}
              '{{- i.entity_id -}}'
            {%- endfor -%}]
          {%- else -%}none{%- endif %}
      value_template: >-
        {% set s = state_attr('binary_sensor.motion_device_current_self_test','entity_ids') %}
        {% if s|lower != 'none' and  s|lower != 'unknown' and not s is string and s is iterable %}
          {{- s|count|int(0) > 0 -}}
        {% else %}unknown{% endif %}
    motion_device_current_turn_off:
      icon_template: 'mdi:turn_off'
      attribute_templates:
        entity_ids: >-
          {% set s = state_attr('binary_sensor.motion_device_current','controls') %}
          {%- if s|lower != 'unknown' and s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
            [{%- for i in s if 'switch.' in i.entity_id -%}
              {%- if not loop.first -%},{%- endif -%}
              '{{- i.entity_id -}}'
            {%- endfor -%}]
          {%- else -%}none{%- endif %}
      value_template: >-
        {% set s = state_attr('binary_sensor.motion_device_current_turn_off','entity_ids') %}
        {% if s|lower != 'none' and  s|lower != 'unknown' and not s is string and s is iterable %}
          {{- s|count|int(0) > 0 -}}
        {% else %}unknown{% endif %}
    motion_device_current_turn_on:
      icon_template: 'mdi:turn_on'
      attribute_templates:
        entity_ids: >-
          {% set s = state_attr('binary_sensor.motion_device_current','controls') %}
          {%- if s|lower != 'unknown' and s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
            [{%- for i in s if 'switch.' in i.entity_id -%}
              {%- if not loop.first -%},{%- endif -%}
              '{{- i.entity_id -}}'
            {%- endfor -%}]
          {%- else -%}none{%- endif %}
      value_template: >-
        {% set s = state_attr('binary_sensor.motion_device_current_turn_on','entity_ids') %}
        {% if s|lower != 'none' and  s|lower != 'unknown' and not s is string and s is iterable %}
          {{- s|count|int(0) > 0 -}}
        {% else %}unknown{% endif %}
    motion_device_current_toggle:
      icon_template: 'mdi:toggle'
      attribute_templates:
        entity_ids: >-
          {% set s = state_attr('binary_sensor.motion_device_current','controls') %}
          {%- if s|lower != 'unknown' and s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
            [{%- for i in s if 'switch.' in i.entity_id -%}
              {%- if not loop.first -%},{%- endif -%}
              '{{- i.entity_id -}}'
            {%- endfor -%}]
          {%- else -%}none{%- endif %}
      value_template: >-
        {% set s = state_attr('binary_sensor.motion_device_current_toggle','entity_ids') %}
        {% if s|lower != 'none' and  s|lower != 'unknown' and not s is string and s is iterable %}
          {{- s|count|int(0) > 0 -}}
        {% else %}unknown{% endif %}
    motion_device_current_mute:
      icon_template: 'mdi:gesture-tap-button'
      attribute_templates:
        entity_ids: >-
          {% set s = state_attr('binary_sensor.motion_device_current','controls') %}
          {%- if s|lower != 'unknown' and s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
            [{%- for i in s if '_mute' in i.entity_id -%}
              {%- if not loop.first -%},{%- endif -%}
              '{{- i.entity_id -}}'
            {%- endfor -%}]
          {%- else -%}none{%- endif %}
      value_template: >-
        {% set s = state_attr('binary_sensor.motion_device_current_mute','entity_ids') %}
        {% if s|lower != 'none' and  s|lower != 'unknown' and not s is string and s is iterable %}
          {{- s|count|int(0) > 0 -}}
        {% else %}unknown{% endif %}
    motion_device_current_unmute:
      icon_template: 'mdi:gesture-tap-button'
      attribute_templates:
        entity_ids: >-
          {% set s = state_attr('binary_sensor.motion_device_current','controls') %}
          {%- if s|lower != 'unknown' and s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
            [{%- for i in s if '_unmute' in i.entity_id -%}
              {%- if not loop.first -%},{%- endif -%}
              '{{- i.entity_id -}}'
            {%- endfor -%}]
          {%- else -%}none{%- endif %}
      value_template: >-
        {% set s = state_attr('binary_sensor.motion_device_current_unmute','entity_ids') %}
        {% if s|lower != 'none' and  s|lower != 'unknown' and not s is string and s is iterable %}
          {{- s|count|int(0) > 0 -}}
        {% else %}unknown{% endif %}
    motion_device_current_update:
      icon_template: 'mdi:update'
      attribute_templates:
        entity_ids: >-
          {% set s = state_attr('binary_sensor.motion_device_current','update') %}
          {% if s|lower != 'none' and  s|lower != 'unknown' and not s is string and s is iterable and s|count > 0 %}
            [{%- for i in s -%}
              {%- if not loop.first -%},{%- endif -%}
              '{{- i.entity_id -}}'
            {%- endfor -%}]
          {% else %}none{% endif %}
      value_template: >-
        {% set s = state_attr('binary_sensor.motion_device_current','update') %}
        {% if s|lower != 'none' and  s|lower != 'unknown' and not s is string and s is iterable %}
          {{- s|count|int(0) > 0 -}}
        {% else %}unknown{% endif %}
    motion_device_current_restart:
      icon_template: 'mdi:restart'
      attribute_templates:
        entity_ids: >-
          {% set s = state_attr('binary_sensor.motion_device_current','restart') %}
          {% if s|lower != 'none' and  s|lower != 'unknown' and not s is string and s is iterable and s|count > 0 %}
            [{%- for i in s -%}
              {%- if not loop.first -%},{%- endif -%}
              '{{- i.entity_id -}}'
            {%- endfor -%}]
          {% else %}none{% endif %}
      value_template: >-
        {% set s = state_attr('binary_sensor.motion_device_current','restart') %}
        {% if s|lower != 'none' and  s|lower != 'unknown' and not s is string and s is iterable %}
          {{- s|count|int(0) > 0 -}}
        {% else %}unknown{% endif %}
    motion_device_current_test:
      device_class: 'problem'
      icon_template: 'mdi:alert-circle'
      attribute_templates:
        values: >-
          {% set s = state_attr('binary_sensor.motion_device_current','test') %}
          {% if s|lower != 'none' and  s|lower != 'unknown' and not s is string and s is iterable and s|count > 0 %}
            [{%- for i in s  -%}
              {%- if not loop.first -%},{%- endif -%}
              {% if i.state|lower == 'normal' %}0{% else %}1{% endif %}
            {%- endfor -%}]
          {% else %}none{% endif %}
      value_template: >-
        {% set s = state_attr('binary_sensor.motion_device_current_test','values') %}
        {% if s|lower != 'none' and  s|lower != 'unknown' and not s is string and s is iterable and s|count > 0 %}
          {{- s|sum|int(0) > 0 -}}
        {% else %}unknown{% endif %}

### missing

- platform: template
  sensors:
    motion_device_current_missing:
      friendly_name: Missing current
      device_class: 'problem'
      attribute_templates:
        last: >-
          {{ 'unknown' }}
        details: >-
          {{ 'unknown' }}
        responses: >-
          {%- set responses = ['deny','wait','help'] -%}
          {{- responses -}}
        actions: >-
          {%- set id = 'device-current-missing' -%}
          {%- set group = 'device-current' -%}
          {%- set interval = 300 -%}
          {%- set primary = state_attr('sensor.motion_role_primary','friendly_name') -%}
          {%- set approve = {'id':'approve','action':id|string + '-approve','title':'I\'m Okay','icon':'mdi:thumb-up','symbol':'sfsymbols:hand.thumbsup','destructive':false,'trigger':'automation.motion_device_current_missing_okay'} -%}
          {%- set deny = {'id':'deny','action':id|string + '-deny','title':'Missing alarm off','icon':'mdi:bell-cancel','symbol':'sfsymbols:hand.thumbsdown.fill','destructive':true,'trigger':'automation.motion_device_current_missing_off'} -%}
          {%- set wait = {'id':'wait','action':id|string + '-wait','title':'Ignore','icon':'mdi:bell-sleep','symbol':'sfsymbols:timer','destructive':false,'trigger':'automation.motion_device_current_missing_dismiss'} -%}
          {%- set help = {'id':'help','action':id|string + '-help','title':'Notify ' + primary|string,'icon':'mdi:message-alert','symbol':'sfsymbols:message','destructive':false,'trigger':'automation.motion_notification_current_missing'} -%}
          [{{ approve }},{{ deny }},{{ wait }},{{ help }}]
        markdown: >-
          No <a href="/lovelace-primary/current">current</a> devices found; missing: <a href="/notify-devices/current"><b>{{- state_attr('binary_sensor.motion_device_current_exist','missing') -}}</b></a>
          <br><i>Last updated</i>: {{ utcnow().timestamp()|timestamp_custom("%a %b %d %I:%M %p %Z",true,'unknown') }}
      value_template: >-
        {% set s = state_attr('binary_sensor.motion_device_class_missing','ids') %}
        {{ s|lower != 'none' and s is iterable and 'current' in s }}

### bad,good

- platform: template
  sensors:
    motion_device_current_bad:
      friendly_name: Bad current
      attribute_templates:
        last: >-
          {{ 'unknown' }}
        details: >-
          {{ 'unknown' }}
        responses: >-
          {%- set responses = ['deny','wait','help'] -%}
          {{- responses -}}
        actions: >-
          {%- set id = 'device-current-bad' -%}
          {%- set group = 'device-current' -%}
          {%- set interval = 300 -%}
          {%- set primary = state_attr('sensor.motion_role_primary','friendly_name') -%}
          {%- set approve = {'id':'approve','action':id|string + '-approve','title':'I\'m Okay','icon':'mdi:thumb-up','symbol':'sfsymbols:hand.thumbsup','destructive':false,'trigger':'automation.motion_device_current_bad_okay'} -%}
          {%- set deny = {'id':'deny','action':id|string + '-deny','title':'Bad alarm off','icon':'mdi:bell-cancel','symbol':'sfsymbols:hand.thumbsdown.fill','destructive':true,'trigger':'automation.motion_device_current_bad_off'} -%}
          {%- set wait = {'id':'wait','action':id|string + '-wait','title':'Ignore','icon':'mdi:bell-sleep','symbol':'sfsymbols:timer','destructive':false,'trigger':'automation.motion_device_current_bad_dismiss'} -%}
          {%- set help = {'id':'help','action':id|string + '-help','title':'Notify ' + primary|string,'icon':'mdi:message-alert','symbol':'sfsymbols:message','destructive':false,'trigger':'automation.motion_notification_current_bad'} -%}
          [{{ approve }},{{ deny }},{{ wait }},{{ help }}]
      value_template: >-
        {{ 'unknown' }}
    motion_device_current_good:
      friendly_name: Good current
      attribute_templates:
        last: >-
          {{ 'unknown' }}
        details: >-
          {{ 'unknown' }}
        responses: >-
          {%- set responses = ['deny','wait','help'] -%}
          {{- responses -}}
        actions: >-
          {%- set id = 'device-current-good' -%}
          {%- set group = 'device-current' -%}
          {%- set interval = 300 -%}
          {%- set primary = state_attr('sensor.motion_role_primary','friendly_name') -%}
          {%- set approve = {'id':'approve','action':id|string + '-approve','title':'None','icon':'mdi:thumb-up','symbol':'sfsymbols:nosign','destructive':false,'trigger':'none'} -%}
          {%- set deny = {'id':'deny','action':id|string + '-deny','title':'Good alarm off','icon':'mdi:bell-off','symbol':'sfsymbols:hand.thumbsdown.fill','destructive':true,'trigger':'automation.motion_device_current_good_off'} -%}
          {%- set wait = {'id':'wait','action':id|string + '-wait','title':'Ignore','icon':'mdi:reminder','symbol':'sfsymbols:timer','destructive':false,'trigger':'automation.motion_device_current_good_dismiss'} -%}
          {%- set help = {'id':'help','action':id|string + '-help','title':'Notify ' + primary|string,'icon':'mdi:message-alert','symbol':'sfsymbols:message','destructive':false,'trigger':'automation.motion_notification_current_good'} -%}
          [{{ approve }},{{ deny }},{{ wait }},{{ help }}]
      value_template: >-
        {{ 'unknown' }}

### low,high

- platform: template
  sensors:
    motion_device_current_low:
      friendly_name: Low current
      attribute_templates:
        last: >-
          {{ 'unknown' }}
        details: >-
          {{ 'unknown' }}
        responses: >-
          {%- set responses = ['approve','deny','wait','help'] -%}
          {{- responses -}}
        actions: >-
          {%- set id = 'device-current-low' -%}
          {%- set group = 'device-current' -%}
          {%- set interval = 300 -%}
          {%- set primary = state_attr('sensor.motion_role_primary','friendly_name') -%}
          {%- set approve = {'id':'approve','action':id|string + '-approve','title':'I\'m okay','icon':'mdi:thumb-up','symbol':'sfsymbols:thumsup','destructive':false,'trigger':'automation.motion_device_current_low_okay'} -%}
          {%- set deny = {'id':'deny','action':id|string + '-deny','title':'Low alarm off','icon':'mdi:bell-cancel','symbol':'sfsymbols:hand.thumbsdown.fill','destructive':true,'trigger':'automation.motion_device_current_low_off'} -%}
          {%- set wait = {'id':'wait','action':id|string + '-wait','title':'Decrease limit','icon':'mdi:arrow-expand-down','symbol':'sfsymbols:arrowtriangle.down.circle','destructive':false,'trigger':'automation.motion_device_current_low_limit'} -%}
          {%- set help = {'id':'help','action':id|string + '-help','title':'Notify ' + primary|string,'icon':'mdi:message-alert','symbol':'sfsymbols:message','destructive':false,'trigger':'automation.motion_notification_current_low'} -%}
          [{{ approve }},{{ deny }},{{ wait }},{{ help }}]
      value_template: >-
        {{ 'unknown' }}
    motion_device_current_high:
      friendly_name: High current
      attribute_templates:
        last: >-
          {{ 'unknown' }}
        details: >-
          {{ 'unknown' }}
        responses: >-
          {%- set responses = ['approve','deny','wait','help'] -%}
          {{- responses -}}
        actions: >-
          {%- set id = 'device-current-high' -%}
          {%- set group = 'device-current' -%}
          {%- set interval = 300 -%}
          {%- set primary = state_attr('sensor.motion_role_primary','friendly_name') -%}
          {%- set approve = {'id':'approve','action':id|string + '-approve','title':'I\'m okay','icon':'mdi:thumb-up','symbol':'sfsymbols:thumsup','destructive':false,'trigger':'automation.motion_notification_current_high_okay'} -%}
          {%- set deny = {'id':'deny','action':id|string + '-deny','title':'High alarm off','icon':'mdi:bell-cancel','symbol':'sfsymbols:hand.thumbsdown.fill','destructive':true,'trigger':'automation.motion_device_current_high_off'} -%}
          {%- set wait = {'id':'wait','action':id|string + '-wait','title':'Increase limit','icon':'mdi:arrow-expand-up','symbol':'sfsymbols:arrowtriangle.up.circle','destructive':false,'trigger':'automation.motion_device_current_high_limit'} -%}
          {%- set help = {'id':'help','action':id|string + '-help','title':'Notify ' + primary|string,'icon':'mdi:message-alert','symbol':'sfsymbols:message','destructive':false,'trigger':'automation.motion_notification_current_high'} -%}
          [{{ approve }},{{ deny }},{{ wait }},{{ help }}]
      value_template: >-
        {{ 'unknown' }}
    motion_device_current_low_spike:
      friendly_name: Low current spike
      attribute_templates:
        last: >-
          {{ 'unknown' }}
        details: >-
          {{ 'unknown' }}
        responses: >-
          {{ 'none' }}
        actions: >-
          {{ 'none' }}
      value_template: >-
        {{ 'unknown' }}
    motion_device_current_high_spike:
      friendly_name: High current spike
      attribute_templates:
        last: >-
          {{ 'unknown' }}
        details: >-
          {{ 'unknown' }}
        responses: >-
          {{ 'none' }}
        actions: >-
          {{ 'none' }}
      value_template: >-
        {{ 'unknown' }}
    motion_device_current_activity:
      friendly_name: Activity current
      attribute_templates:
        last: >-
          {{ 'unknown' }}
        ids: >-
          {{ 'unknown' }}
        dids: >-
          {{ 'unknown' }}
        details: >-
          {{ 'unknown' }}
        responses: >-
          {{ 'none' }}
        actions: >-
          {{ 'none' }}
      value_template: >-
        {{ 'unknown' }}
    motion_device_current_unavailable:
      friendly_name: Unavailable current
      device_class: 'connectivity'
      attribute_templates:
        last: >-
          {{ 'unknown' }}
        ids: >-
          {{ 'unknown' }}
        dids: >-
          {{ 'unknown' }}
        details: >-
          {{ 'unknown' }}
        responses: >-
          {{ 'none' }}
        actions: >-
          {{ 'none' }}
      value_template: >-
        {{ 'unknown' }}
    motion_device_current_found:
      attribute_templates:
        last: >-
          {{ 'unknown' }}
        dids: >-
          {{ 'unknown' }}
        ids: >-
          {{ 'unknown' }}
        responses: >-
          {{ 'none' }}
        actions: >-
          {{ 'none' }}
      value_template: >-
        {{ 'unknown' }}
