###
# homeassistant/binary_sensor/device/smoke.yaml
###

- platform: template
  sensors:
    motion_device_smoke_alarm_notify:
      attribute_templates:
        activity: >-
          {%- if state_attr('binary_sensor.motion_device_smoke_alarm_exist','activity')|lower == 'true' -%}
            {%- set responses = state_attr('binary_sensor.motion_device_smoke_activity','responses') -%}
            {%- set approve = state_attr('binary_sensor.motion_device_smoke_activity','approve') -%}
            {%- set deny = state_attr('binary_sensor.motion_device_smoke_activity','deny') -%}
            {%- set wait = state_attr('binary_sensor.motion_device_smoke_activity','wait') -%}
            {%- set help = state_attr('binary_sensor.motion_device_smoke_activity','help') -%}
            {%- set p = state_attr('sensor.motion_role_primary','friendly_name') -%}
            {%- set id = 'smoke-alarm-activity' -%}
            {%- set g = 'home-activity' -%}
            {%- set i = 30 -%}
            {%- set a = [
              {'action': id|string + '-approve','icon': approve.icon|string,'title': approve.title|string,'destructive':false},
              {'action': id|string + '-deny','icon': deny.icon|string,'title': deny.title|string,'destructive':true},
              {'action': id|string + '-wait','icon': wait.icon|string,'title': wait.title|string,'destructive':false},
              {'action': id|string + '-help','icon': help.icon|string,'title': help.title|string,'destructive':false}
              ] -%}
            {"id":"{{- id -}}","group":"{{- g -}}","interval":{{- i -}},"responses":{{- responses -}},"primary":"{{- p -}}","actions":{{ a }}}
          {% else %}none{% endif %}
        missing: >-
          {%- if state_attr('binary_sensor.motion_device_smoke_alarm_exist','missing')|lower == 'true' -%}
            {%- set responses = state_attr('binary_sensor.motion_device_smoke_missing','responses') -%}
            {%- set approve = state_attr('binary_sensor.motion_device_smoke_missing','approve') -%}
            {%- set deny = state_attr('binary_sensor.motion_device_smoke_missing','deny') -%}
            {%- set wait = state_attr('binary_sensor.motion_device_smoke_missing','wait') -%}
            {%- set help = state_attr('binary_sensor.motion_device_smoke_missing','help') -%}
            {%- set p = state_attr('sensor.motion_role_primary','friendly_name') -%}
            {%- set id = 'smoke-alarm-missing' -%}
            {%- set g = 'home-device' -%}
            {%- set i = 30 -%}
            {%- set a = [
              {'action': id|string + '-approve','icon': approve.icon|string,'title': approve.title|string,'destructive':false},
              {'action': id|string + '-deny','icon': deny.icon|string,'title': deny.title|string,'destructive':true},
              {'action': id|string + '-wait','icon': wait.icon|string,'title': wait.title|string,'destructive':false},
              {'action': id|string + '-help','icon': help.icon|string,'title': help.title|string,'destructive':false}
              ] -%}
            {"id":"{{- id -}}","group":"{{- g -}}","interval":{{- i -}},"responses":{{- responses -}},"primary":"{{- p -}}","actions":{{ a }}}
          {% else %}none{% endif %}
        unavailable: >-
          {%- if state_attr('binary_sensor.motion_device_smoke_alarm_exist','unavailable')|lower == 'true' -%}
            {%- set responses = state_attr('binary_sensor.motion_device_smoke_unavailable','responses') -%}
            {%- set approve = state_attr('binary_sensor.motion_device_smoke_unavailable','approve') -%}
            {%- set deny = state_attr('binary_sensor.motion_device_smoke_unavailable','deny') -%}
            {%- set wait = state_attr('binary_sensor.motion_device_smoke_unavailable','wait') -%}
            {%- set help = state_attr('binary_sensor.motion_device_smoke_unavailable','help') -%}
            {%- set p = state_attr('sensor.motion_role_primary','friendly_name') -%}
            {%- set id = 'smoke-alarm-unavailable' -%}
            {%- set g = 'home-device' -%}
            {%- set i = 30 -%}
            {%- set a = [
              {'action': id|string + '-approve','icon': approve.icon|string,'title': approve.title|string,'destructive':false},
              {'action': id|string + '-deny','icon': deny.icon|string,'title': deny.title|string,'destructive':true},
              {'action': id|string + '-wait','icon': wait.icon|string,'title': wait.title|string,'destructive':false},
              {'action': id|string + '-help','icon': help.icon|string,'title': help.title|string,'destructive':false}
              ] -%}
            {"id":"{{- id -}}","group":"{{- g -}}","interval":{{- i -}},"responses":{{- responses -}},"primary":"{{- p -}}","actions":{{ a }}}
          {% else %}none{% endif %}
      value_template: >-
        {{ state_attr('binary_sensor.motion_device_smoke_alarm_exist','activity') }}

- platform: template
  sensors:
    motion_device_smoke_required:
      value_template: >-
        {% set s = state_attr('binary_sensor.motion_device_smoke_exist','required') %}
        {% if s|lower != 'null' and s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' %}
          {{ s }}
        {% else %}unknown{% endif %}

- platform: template
  sensors:
    motion_device_smoke_alarm_exist:
      friendly_name: 'Alarm exists: smoke'
      attribute_templates:
        good: >-
          {{ not is_state_attr('binary_sensor.motion_device_smoke_exist','good','none') and is_state('binary_sensor.motion_device_smoke_good','on') }}
        bad: >-
          {{ not is_state_attr('binary_sensor.motion_device_smoke_exist','bad','none') and is_state('binary_sensor.motion_device_smoke_bad','on') }}
        activity: >-
          {{ not is_state_attr('binary_sensor.motion_device_smoke_exist','activity','none') and is_state('binary_sensor.motion_device_smoke_activity','on') }}
        low: >-
          {{ not is_state_attr('binary_sensor.motion_device_smoke_exist','low','none') and is_state('binary_sensor.motion_device_smoke_low','on') }}
        high: >-
          {{ not is_state_attr('binary_sensor.motion_device_smoke_exist','high','none') and is_state('binary_sensor.motion_device_smoke_high','on') }}
        low_spike: >-
          {{ not is_state_attr('binary_sensor.motion_device_smoke_exist','low_spike','none') and is_state('binary_sensor.motion_device_smoke_low_spike','on') }}
        high_spike: >-
          {{ not is_state_attr('binary_sensor.motion_device_smoke_exist','high_spike','none') and is_state('binary_sensor.motion_device_smoke_high_spike','on') }}
        missing: >-
          {{ not is_state_attr('binary_sensor.motion_device_smoke_exist','required','none') and is_state('binary_sensor.motion_device_smoke_missing','on') }}
        unavailable: >-
          {{ is_state('binary_sensor.motion_device_smoke_unavailable','on') }}
        found: >-
          {{ is_state('binary_sensor.motion_device_smoke_exist','on') }}
        active: >-
          {%- set s = state_attr('binary_sensor.motion_device_smoke_alarm','status') -%}
          {% if s|lower != 'none' and not s is string and s is iterable and s|count > 0 %}
            {%- if s.values()|reject('==','off')|list|count > 0 -%}on{%- else -%}off{%- endif -%}
          {% else %}unknown{% endif %}
        markdown: >-
          {%- set s = states('binary_sensor.motion_device_smoke_alarm') -%}
          <h2>The <a href="/notify-devices/smoke">smoke</a> alarm is: <i>{{- s|upper -}}</i></h2>
          {%- if s|lower == 'on' -%}
            {{- state_attr('binary_sensor.motion_device_smoke_alarm','markdown') -}}
          {%- endif -%}
      value_template: >-
        {{ is_state_attr('binary_sensor.motion_device_smoke_exist','alarm','on') and is_state('binary_sensor.motion_device_smoke_alarm','on') }}

- platform: template
  sensors:
    motion_device_smoke_exist:
      friendly_name: Existing smoke sensor(s)
      attribute_templates:
        good: >-
          {% set g = state_attr('binary_sensor.motion_device_class','good') -%}
          {%- set b = state_attr('binary_sensor.motion_device_class','bad') -%}
          {%- if g|lower != 'none' and b|lower != 'none' -%}
            {%- if 'smoke' in g -%}on{%- elif 'smoke' in b -%}off{%- else -%}none{%- endif -%}
          {%- else -%}none{%- endif %}
        bad: >-
          {% set g = state_attr('binary_sensor.motion_device_class','good') -%}
          {%- set b = state_attr('binary_sensor.motion_device_class','bad') -%}
          {%- if g|lower != 'none' and b|lower != 'none' -%}
            {%- if 'smoke' in b -%}on{%- elif 'smoke' in g -%}off{%- else -%}none{%- endif -%}
          {%- else -%}none{%- endif %}
        alarm: >-
          {% set s = state_attr('binary_sensor.motion_device_class','alarm') -%}
          {%- if s|lower != 'none' -%}
            {%- if 'smoke' in s -%}on{%- else -%}none{%- endif -%}
          {%- else -%}none{%- endif %}
        activity: >-
          {% set s = state_attr('binary_sensor.motion_device_class','activity') -%}
          {%- if s|lower != 'none' -%}
            {%- if 'smoke' in s -%}on{%- else -%}none{%- endif -%}
          {%- else -%}none{%- endif %}
        required: >-
          {% set s = state_attr('binary_sensor.motion_device_class','sensors') -%}
          {% set b = state_attr('binary_sensor.motion_device_class','binary_sensors') -%}
          {%- if s|lower != 'none' and b|lower != 'none' -%}
            {% set required = (s|list + b|list)|sort|unique %}
            {%- if 'smoke' in required -%}on{%- else -%}none{%- endif -%}
          {%- else -%}none{%- endif %}
        low: >-
          {% set s = state_attr('binary_sensor.motion_device_class','low') -%}
          {%- if s|lower != 'none' -%}
            {%- if 'smoke' in s -%}
              {{ states('input_number.motion_device_smoke_low') }}
            {%- else -%}none{%- endif -%}
          {%- else -%}none{%- endif %}
        high: >-
          {% set s = state_attr('binary_sensor.motion_device_class','high') -%}
          {%- if s|lower != 'none' -%}
            {%- if 'smoke' in s -%}
              {{ states('input_number.motion_device_smoke_high') }}
            {%- else -%}none{%- endif -%}
          {%- else -%}none{%- endif %}
        low_spike: >-
          {% set s = state_attr('binary_sensor.motion_device_class','low_spike') -%}
          {%- if s|lower != 'none' -%}
            {%- if 'smoke' in s -%}
              {%- set s = state_attr('sensor.motion_device_smoke_low_spike','limit') -%}
              {%- if s|lower != 'none' and s is number -%}
                {{ '%0.2f'|format(s) }}
              {%- else -%}none{%- endif -%}
            {%- else -%}none{%- endif -%}
          {%- else -%}none{%- endif %}
        high_spike: >-
          {% set s = state_attr('binary_sensor.motion_device_class','high_spike') -%}
          {%- if s|lower != 'none' -%}
            {%- if 'smoke' in s -%}
              {%- set s = state_attr('sensor.motion_device_smoke_high_spike','limit') -%}
              {%- if s|lower != 'none' and s is number -%}
                {{ '%0.2f'|format(s) }}
              {%- else -%}none{%- endif -%}
            {%- else -%}none{%- endif -%}
          {%- else -%}none{%- endif %}
        measurement: >-
          {% set s = state_attr('binary_sensor.motion_device_class','sensor_list') -%}
          {%- if s|lower != 'none' -%}
            {%- if 'smoke' in s -%}on{%- else -%}none{%- endif -%}
          {%- else -%}none{%- endif %}
        status: >-
          {% set s = state_attr('binary_sensor.motion_device_class','binary_sensor_list') -%}
          {%- if s|lower != 'none' -%}
            {%- if 'smoke' in s -%}on{%- else -%}none{%- endif -%}
          {%- else -%}none{%- endif %}
        markdown: >-
          {% set c = 'smoke' -%}
          {%- set alarms = ['required','alarm'] -%}
          <h1>{{ c|upper }}</h1>
          {%- set s = state_attr('binary_sensor.motion_device_class','alarms') -%}
          {%- if s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
            {%- set alarms = alarms + s|sort|unique|list -%}
          {%- endif -%}
          {%- for i in alarms if state_attr('binary_sensor.motion_device_smoke_exist',i)|lower != 'none' -%}
            {%- if loop.first -%}<ul>{%- endif -%}
            <li>{{- i|upper -}}: {{ state_attr('binary_sensor.motion_device_smoke_exist',i) -}}</li>
            {%- if loop.last -%}</ul>{%- endif -%}
          {%- endfor -%}
          {%- set s = state_attr('binary_sensor.motion_device_smoke_status','markdown') -%} {%- if s|lower != 'none' -%}{{- s -}}{%- endif -%}
          {%- set s = state_attr('binary_sensor.motion_device_smoke_measurement','markdown') -%} {%- if s|lower != 'none' -%}{{- s -}}{%- endif -%}
          {%- set s = state_attr('binary_sensor.motion_device_smoke_statistics','markdown') -%} {%- if s|lower != 'none' -%}{{- s -}}{%- endif -%}
          {%- set s = state_attr('binary_sensor.motion_device_smoke_controls','markdown') -%} {%- if s|lower != 'none' -%}{{- s -}}{%- endif -%}
          {%- set s = state_attr('binary_sensor.motion_device_smoke_activity','markdown') -%} {%- if s|lower != 'none' -%}{{- s -}}{%- endif -%}
          {%- set s = state_attr('binary_sensor.motion_device_smoke_unavailable','markdown') -%} {%- if s|lower != 'none' -%}{{- s -}}{%- endif -%}
      value_template: >-
        {% set s = state_attr('binary_sensor.motion_device_smoke','total') %}
        {% if s|lower != 'null' and s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' %}
          {{ s|int(0) > 0 }}
        {% else %}unknown{% endif %}

- platform: template
  sensors:
    motion_device_smoke_status:
      friendly_name: Status smoke
      attribute_templates:
        markdown: >-
          {%- set s = state_attr('binary_sensor.motion_device_smoke','status') %}
          {%- if s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
            {%- set s = s|selectattr('entity_id','search','binary_sensor.')|list -%}
            {%- if s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
            {{- '<h2>Status</h2><ul>' -}}
            {%- for i in s -%}
              {%- set u = device_id(i.entity_id) -%}
              {{- '<li>' -}}
              {%- if u|lower != 'none' -%}
                <a href="/config/devices/device/{{- u -}}"><b>{{- i.name -}}</b></a>: {{ states(i.entity_id) }}
              {%- else -%}
                <b>{{- i.name -}}</b>: {{ states(i.entity_id) -}}
              {%- endif -%}
              {{- '</li>' -}}
            {%- endfor -%}
            {{- '</ul>' -}}
            {%- else -%}<h2>No status</h2>{%- endif -%}
          {%- else -%}<h2>No status</h2>{%- endif -%}
      value_template: >-
        {% set s = state_attr('binary_sensor.motion_device_smoke_exist','status') %}
        {% if s|lower != 'null' and s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' %}
          {{ s }}
        {% else %}unknown{% endif %}
    motion_device_smoke_measurement:
      friendly_name: Measurement smoke
      attribute_templates:
        markdown: >-
          {%- set s = state_attr('binary_sensor.motion_device_smoke','status') %}
          {%- if s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
            {%- set s = s|rejectattr('entity_id','search','binary_sensor.')|list -%}
            {%- if s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
              {{- '<h2>Measurements</h2><ul>' -}}
              {%- for i in s -%}
                {%- set u = device_id(i.entity_id) -%}
                {%- set v = states(i.entity_id) -%}
                {%- if v|lower != 'none' and v|lower != 'unknown' and v|lower != 'unavailable' and v|lower != 'null' and v|string|count > 0 -%}
                  {%- set uom = state_attr(i.entity_id,'unit_of_measurement') -%}
                  {%- if uom|lower != 'none' and uom|lower != 'unknown' and uom|lower != 'unavailable' and uom|lower != 'null' and uom|string|count > 0 -%}
                    {%- set v = v|string + uom|string -%}
                  {%- endif -%}
                {%- endif -%}
                {{- '<li>' -}}
                {%- if u|lower != 'none' -%}
                  <a href="/config/devices/device/{{- u -}}"><b>{{- i.name -}}</b></a>: {{ v }}
                {%- else -%}
                  <b>{{- i.name -}}</b>: {{ v }}
                {%- endif -%}
                {{- '</li>' -}}
              {%- endfor -%}
              {{- '</ul>' -}}
            {%- else -%}<h2>No measurements</h2>{%- endif -%}
          {%- else -%}<h2>No measurements</h2>{%- endif -%}
      value_template: >-
        {% set s = state_attr('binary_sensor.motion_device_smoke_exist','measurement') %}
        {% if s|lower != 'null' and s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' %}
          {{ s }}
        {% else %}unknown{% endif %}
    motion_device_smoke_statistics:
      friendly_name: Statistics smoke
      attribute_templates:
        status: >-
          {%- set s = state_attr('binary_sensor.motion_device_smoke','statistics') %}
          {%- if s|lower != 'null' and s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and s is iterable and s|count > 0 -%}
            [{%- for i in s if 'entity_id' in i -%}
            {%- if not loop.first -%},{%- endif -%}
              {"{{- i.name -}}":["{{- states(i.entity_id) }} {{ state_attr(i.entity_id,'unit_of_measurement') -}}","{{- states[i.entity_id].last_updated|relative_time }}"]}
            {%- endfor -%}]
          {%- else -%}none{%- endif -%}
        markdown: >-
          {%- set s = state_attr('binary_sensor.motion_device_smoke','statistics') %}
          {%- if s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
            {{- '<h2>Statistics</h2><ul>' -}}
            {%- for i in s -%}
              {%- set u = device_id(i.entity_id) -%}
              {%- set v = states(i.entity_id) -%}
              {%- if v|lower != 'none' and v|lower != 'unknown' and v|lower != 'unavailable' and v|lower != 'null' and v|string|count > 0 -%}
                {%- set uom = state_attr(i.entity_id,'unit_of_measurement') -%}
                {%- if uom|lower != 'none' and uom|lower != 'unknown' and uom|lower != 'unavailable' and uom|lower != 'null' and uom|string|count > 0 -%}
                  {%- set v = v|string + uom|string -%}
                {%- endif -%}
              {%- endif -%}
              {{- '<li>' -}}
              {%- if u|lower != 'none' -%}
                <a href="/config/devices/device/{{- u -}}"><b>{{- i.name -}}</b></a>: {{ v }}
              {%- else -%}
                <b>{{- i.name -}}</b>: {{ v -}}
              {%- endif -%}
              {{- '</li>' -}}
            {%- endfor -%}
            {{- '</ul>' -}}
          {%- else -%}<h2>No statistics</h2>{%- endif -%}
      value_template: >-
        {% set s = state_attr('binary_sensor.motion_device_smoke_statistics','status') %}
        {% if s|lower != 'null' and s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and s is iterable %}
          {{ s|count > 0 }}
        {% else %}unknown{% endif %}
    motion_device_smoke_controls:
      friendly_name: Control smoke
      attribute_templates:
        status: >-
          {%- set s = state_attr('binary_sensor.motion_device_smoke','controls') %}
          {%- if s|lower != 'null' and s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and s is iterable -%}
            [{%- for i in s if 'entity_id' in i and not 'button.' in i.entity_id -%}
              {%- if not loop.first -%},{%- endif -%}
              {"{{- i.name -}}":["{{ states(i.entity_id) -}}","{{- states[i.entity_id].last_updated|relative_time }}"]}
            {%- endfor -%}]
          {%- else -%}none{%- endif -%}
        markdown: >-
          {%- set s = state_attr('binary_sensor.motion_device_smoke','controls') %}
          {%- if s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
            {{- '<h2>Controls</h2>' -}}
            {%- for i in s if 'entity_id' in i and not 'button.' in i.entity_id -%}
              {%- if loop.first -%}{{- '<ul>' -}}{%- endif -%}
              {%- set u = device_id(i.entity_id) -%}
              {{- '<li>' -}}
              {%- if u|lower != 'none' -%}
                <a href="/config/devices/device/{{- u -}}"><b>{{- i.name -}}</b></a>: {{ states(i.entity_id) -}}
              {%- else -%}
                <b>{{- i.name -}}</b>: {{ states(i.entity_id) -}}
              {%- endif -%}
              {{- '</li>' -}}
              {%- if loop.last -%}{{- '</ul>' -}}{%- endif -%}
            {%- endfor -%}
          {%- else -%}
            {{- '<h2>No controls</h2>' -}}
          {%- endif -%}
      value_template: >-
        {% set s = state_attr('binary_sensor.motion_device_smoke_controls','status') %}
        {% if s|lower != 'null' and s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' and s is iterable %}
          {{ s|count > 0 }}
        {% else %}unknown{% endif %}
    motion_device_smoke_self_test:
      icon_template: 'mdi:gesture-tap-button'
      attribute_templates:
        entity_ids: >-
          {% set s = state_attr('binary_sensor.motion_device_smoke','controls') %}
          {%- if s|lower != 'unknown' and s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
            [{%- for i in s if '_self_test' in i.entity_id -%}
              {%- if not loop.first -%},{%- endif -%}
              "{{- i.entity_id -}}"
            {%- endfor -%}]
          {%- else -%}none{%- endif %}
      value_template: >-
        {% set s = state_attr('binary_sensor.motion_device_smoke_self_test','entity_ids') %}
        {% if s|lower != 'none' and  s|lower != 'unknown' and not s is string and s is iterable %}
          {{- s|count|int(0) > 0 -}}
        {% else %}unknown{% endif %}
    motion_device_smoke_turn_off:
      icon_template: 'mdi:turn_off'
      attribute_templates:
        entity_ids: >-
          {% set s = state_attr('binary_sensor.motion_device_smoke','controls') %}
          {%- if s|lower != 'unknown' and s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
            [{%- for i in s if 'switch.' in i.entity_id -%}
              {%- if not loop.first -%},{%- endif -%}
              "{{- i.entity_id -}}"
            {%- endfor -%}]
          {%- else -%}none{%- endif %}
      value_template: >-
        {% set s = state_attr('binary_sensor.motion_device_smoke_turn_off','entity_ids') %}
        {% if s|lower != 'none' and  s|lower != 'unknown' and not s is string and s is iterable %}
          {{- s|count|int(0) > 0 -}}
        {% else %}unknown{% endif %}
    motion_device_smoke_turn_on:
      icon_template: 'mdi:turn_on'
      attribute_templates:
        entity_ids: >-
          {% set s = state_attr('binary_sensor.motion_device_smoke','controls') %}
          {%- if s|lower != 'unknown' and s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
            [{%- for i in s if 'switch.' in i.entity_id -%}
              {%- if not loop.first -%},{%- endif -%}
              "{{- i.entity_id -}}"
            {%- endfor -%}]
          {%- else -%}none{%- endif %}
      value_template: >-
        {% set s = state_attr('binary_sensor.motion_device_smoke_turn_on','entity_ids') %}
        {% if s|lower != 'none' and  s|lower != 'unknown' and not s is string and s is iterable %}
          {{- s|count|int(0) > 0 -}}
        {% else %}unknown{% endif %}
    motion_device_smoke_toggle:
      icon_template: 'mdi:toggle'
      attribute_templates:
        entity_ids: >-
          {% set s = state_attr('binary_sensor.motion_device_smoke','controls') %}
          {%- if s|lower != 'unknown' and s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
            [{%- for i in s if 'switch.' in i.entity_id -%}
              {%- if not loop.first -%},{%- endif -%}
              "{{- i.entity_id -}}"
            {%- endfor -%}]
          {%- else -%}none{%- endif %}
      value_template: >-
        {% set s = state_attr('binary_sensor.motion_device_smoke_toggle','entity_ids') %}
        {% if s|lower != 'none' and  s|lower != 'unknown' and not s is string and s is iterable %}
          {{- s|count|int(0) > 0 -}}
        {% else %}unknown{% endif %}
    motion_device_smoke_mute:
      icon_template: 'mdi:gesture-tap-button'
      attribute_templates:
        entity_ids: >-
          {% set s = state_attr('binary_sensor.motion_device_smoke','controls') %}
          {%- if s|lower != 'unknown' and s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
            [{%- for i in s if '_mute' in i.entity_id -%}
              {%- if not loop.first -%},{%- endif -%}
              "{{- i.entity_id -}}"
            {%- endfor -%}]
          {%- else -%}none{%- endif %}
      value_template: >-
        {% set s = state_attr('binary_sensor.motion_device_smoke_mute','entity_ids') %}
        {% if s|lower != 'none' and  s|lower != 'unknown' and not s is string and s is iterable %}
          {{- s|count|int(0) > 0 -}}
        {% else %}unknown{% endif %}
    motion_device_smoke_unmute:
      icon_template: 'mdi:gesture-tap-button'
      attribute_templates:
        entity_ids: >-
          {% set s = state_attr('binary_sensor.motion_device_smoke','controls') %}
          {%- if s|lower != 'unknown' and s|lower != 'none' and not s is string and s is iterable and s|count > 0 -%}
            [{%- for i in s if '_unmute' in i.entity_id -%}
              {%- if not loop.first -%},{%- endif -%}
              "{{- i.entity_id -}}"
            {%- endfor -%}]
          {%- else -%}none{%- endif %}
      value_template: >-
        {% set s = state_attr('binary_sensor.motion_device_smoke_unmute','entity_ids') %}
        {% if s|lower != 'none' and  s|lower != 'unknown' and not s is string and s is iterable %}
          {{- s|count|int(0) > 0 -}}
        {% else %}unknown{% endif %}
    motion_device_smoke_update:
      icon_template: 'mdi:update'
      attribute_templates:
        entity_ids: >-
          {% set s = state_attr('binary_sensor.motion_device_smoke','update') %}
          {% if s|lower != 'none' and  s|lower != 'unknown' and not s is string and s is iterable and s|count > 0 %}
            [{%- for i in s -%}
              {%- if not loop.first -%},{%- endif -%}
              "{{- i.entity_id -}}"
            {%- endfor -%}]
          {% else %}none{% endif %}
      value_template: >-
        {% set s = state_attr('binary_sensor.motion_device_smoke','update') %}
        {% if s|lower != 'none' and  s|lower != 'unknown' and not s is string and s is iterable %}
          {{- s|count|int(0) > 0 -}}
        {% else %}unknown{% endif %}
    motion_device_smoke_restart:
      icon_template: 'mdi:restart'
      attribute_templates:
        entity_ids: >-
          {% set s = state_attr('binary_sensor.motion_device_smoke','restart') %}
          {% if s|lower != 'none' and  s|lower != 'unknown' and not s is string and s is iterable and s|count > 0 %}
            [{%- for i in s -%}
              {%- if not loop.first -%},{%- endif -%}
              "{{- i.entity_id -}}"
            {%- endfor -%}]
          {% else %}none{% endif %}
      value_template: >-
        {% set s = state_attr('binary_sensor.motion_device_smoke','restart') %}
        {% if s|lower != 'none' and  s|lower != 'unknown' and not s is string and s is iterable %}
          {{- s|count|int(0) > 0 -}}
        {% else %}unknown{% endif %}
    motion_device_smoke_test:
      device_class: 'problem'
      icon_template: 'mdi:alert-circle'
      attribute_templates:
        values: >-
          {% set s = state_attr('binary_sensor.motion_device_smoke','test') %}
          {% if s|lower != 'none' and  s|lower != 'unknown' and not s is string and s is iterable and s|count > 0 %}
            [{%- for i in s  -%}
              {%- if not loop.first -%},{%- endif -%}
              {% if i.state|lower == 'normal' %}0{% else %}1{% endif %}
            {%- endfor -%}]
          {% else %}none{% endif %}
      value_template: >-
        {% set s = state_attr('binary_sensor.motion_device_smoke_test','values') %}
        {% if s|lower != 'none' and  s|lower != 'unknown' and not s is string and s is iterable and s|count > 0 %}
          {{- s|sum|int(0) > 0 -}}
        {% else %}unknown{% endif %}
    motion_device_smoke:
      friendly_name: Status smoke sensor(s)
      attribute_templates:
        last: >-
          {{- none -}}
        ids: >-
          {{- 'unknown' -}}
        total: >-
          {{- 'unknown' -}}
        update: >-
          {{- 'unknown' -}}
        restart: >-
          {{- 'unknown' -}}
        controls: >-
          {{- 'unknown' -}}
        test: >-
          {{- 'unknown' -}}
        status: >-
          {{- 'unknown' -}}
        high: >-
          {{- 'unknown' -}}
        low: >-
          {{- 'unknown' -}}
        good: >-
          {{- 'unknown' -}}
        bad: >-
          {{- 'unknown' -}}
        activity: >-
          {{- 'unknown' -}}
        unavailable: >-
          {{- 'unknown' -}}
      value_template: >-
        {# set through automation #}
        {{- 'unknown' -}}

###

- platform: template
  sensors:
    motion_device_smoke_bad:
      friendly_name: Bad smoke
      attribute_templates:
        last: >-
          {{ 'unknown' }}
        details: >-
          {{ 'unknown' }}
      value_template: >-
        {{ 'unknown' }}
    motion_device_smoke_good:
      friendly_name: Good smoke
      attribute_templates:
        last: >-
          {{ 'unknown' }}
        details: >-
          {{ 'unknown' }}
      value_template: >-
        {{ 'unknown' }}
    motion_device_smoke_low:
      friendly_name: Low smoke
      attribute_templates:
        last: >-
          {{ 'unknown' }}
        details: >-
          {{ 'unknown' }}
      value_template: >-
        {{ 'unknown' }}
    motion_device_smoke_high:
      friendly_name: High smoke
      attribute_templates:
        last: >-
          {{ 'unknown' }}
        details: >-
          {{ 'unknown' }}
      value_template: >-
        {{ 'unknown' }}
    motion_device_smoke_low_spike:
      friendly_name: Low smoke spike
      attribute_templates:
        last: >-
          {{ 'unknown' }}
        details: >-
          {{ 'unknown' }}
      value_template: >-
        {{ 'unknown' }}
    motion_device_smoke_high_spike:
      friendly_name: High smoke spike
      attribute_templates:
        last: >-
          {{ 'unknown' }}
        details: >-
          {{ 'unknown' }}
      value_template: >-
        {{ 'unknown' }}
    motion_device_smoke_activity:
      friendly_name: Activity smoke
      attribute_templates:
        last: >-
          {{ 'unknown' }}
        ids: >-
          {{ 'unknown' }}
        details: >-
          {{ 'unknown' }}
      value_template: >-
        {{ 'unknown' }}
    motion_device_smoke_unavailable:
      friendly_name: Unavailable smoke
      device_class: 'connectivity'
      attribute_templates:
        last: >-
          {{ 'unknown' }}
        ids: >-
          {{ 'unknown' }}
        details: >-
          {{ 'unknown' }}
      value_template: >-
        {{ 'unknown' }}
    motion_device_smoke_found:
      attribute_templates:
        last: >-
          {{ 'unknown' }}
        ids: >-
          {{ 'unknown' }}
      value_template: >-
        {{ 'unknown' }}
    motion_device_smoke_missing:
      attribute_templates:
        last: >-
          {{ 'unknown' }}
        ids: >-
          {{ 'unknown' }}
      value_template: >-
        {{ 'unknown' }}
    motion_device_smoke_actions:
      attribute_templates:
        missing: >-
          {%- set primary = state_attr('sensor.motion_role_primary','friendly_name') -%}
          {%- set responses = ['approve','deny','wait','help'] -%}
          {%- set approve = {'title':'Rescan','icon':'sfsymbols:hand.thumbsup'} -%}
          {%- set deny = {'title':'Remove','icon':'sfsymbols:hand.thumbsdown.fill'} -%}
          {%- set wait = {'title':'Remind me','icon':'sfsymbols:timer'} -%}
          {%- set help = {'title':'Ask ' + primary|string,'icon':'sfsymbols:message'} -%}
          {"responses":{{ responses|to_json }},"actions":{"approve":{{ approve|to_json }},"deny":{{ deny|to_json }},"wait":{{ wait|to_json }},"help":{{ help|to_json }}}}
        activity: >-
          {%- set primary = state_attr('sensor.motion_role_primary','friendly_name') -%}
          {%- set responses = ['approve','deny','wait','help'] -%}
          {%- set approve = {'title':'Yup!','icon':'sfsymbols:hand.thumbsup'} -%}
          {%- set deny = {'title':'WRONG!','icon':'sfsymbols:hand.thumbsdown.fill'} -%}
          {%- set wait = {'title':'Ignore','icon':'sfsymbols:timer'} -%}
          {%- set help = {'title':'Notify ' + primary|string,'icon':'sfsymbols:message'} -%}
          {"responses":{{ responses|to_json }},"actions":{"approve":{{ approve|to_json }},"deny":{{ deny|to_json }},"wait":{{ wait|to_json }},"help":{{ help|to_json }}}}
        unavailable: >-
          {%- set primary = state_attr('sensor.motion_role_primary','friendly_name') -%}
          {%- set responses = ['approve','deny','wait','help'] -%}
          {%- set approve = {'title':'Rescan','icon':'sfsymbols:hand.thumbsup'} -%}
          {%- set deny = {'title':'Remove','icon':'sfsymbols:hand.thumbsdown.fill'} -%}
          {%- set wait = {'title':'Remind me','icon':'sfsymbols:timer'} -%}
          {%- set help = {'title':'Update ' + primary|string,'icon':'sfsymbols:message'} -%}
          {"responses":{{ responses|to_json }},"actions":{"approve":{{ approve|to_json }},"deny":{{ deny|to_json }},"wait":{{ wait|to_json }},"help":{{ help|to_json }}}}
        found: >-
          {%- set primary = state_attr('sensor.motion_role_primary','friendly_name') -%}
          {%- set responses = ['approve','deny','wait','help'] -%}
          {%- set approve = {'title':'Okay!','icon':'sfsymbols:hand.thumbsup'} -%}
          {%- set deny = {'title':'Remove','icon':'sfsymbols:hand.thumbsdown.fill'} -%}
          {%- set wait = {'title':'Ignore','icon':'sfsymbols:timer'} -%}
          {%- set help = {'title':'Inform ' + primary|string,'icon':'sfsymbols:message'} -%}
          {"responses":{{ responses|to_json }},"actions":{"approve":{{ approve|to_json }},"deny":{{ deny|to_json }},"wait":{{ wait|to_json }},"help":{{ help|to_json }}}}
      value_template: >-
        {{ is_state('binary_sensor.motion_device_smoke_exist','on') }}
