###
# homeassistant/binary_sensor/alarm/class/light.yaml
###

- platform: template
  sensors:
    motion_alarm_light:
      friendly_name: 'Alarm: light'
      attribute_templates:
        bad: >-
          {%- set s = state_attr('binary_sensor.motion_alarm','bad') -%}
          {%- if s|lower != 'none' and not s is string and s is iterable -%}
          {%- for i in s if i|count > 3 and 'light' in i[2] -%}
            {{ i[3] }}
          {%- else -%}<h2>No light <i>bad</i> alarm{%- endfor -%}
          {%- else -%}<h2>No light <i>bad</i> alarm information{%- endif -%}
        good: >-
          {%- set s = state_attr('binary_sensor.motion_alarm','good') -%}
          {%- if s|lower != 'none' and not s is string and s is iterable -%}
          {%- for i in s if i|count > 3 and 'light' in i[2] -%}
            {{ i[3] }}
          {%- else -%}<h2>No light <i>good</i> alarm{%- endfor -%}
          {%- else -%}<h2>No light <i>good</i> alarm information{%- endif -%}
        low: >-
          {%- set s = state_attr('binary_sensor.motion_alarm','low') -%}
          {%- if s|lower != 'none' and not s is string and s is iterable -%}
          {%- for i in s if i|count > 3 and 'light' in i[2] -%}
            {{ i[3] }}
          {%- else -%}<h2>No light <i>low</i> alarm{%- endfor -%}
          {%- else -%}<h2>No light <i>low</i> alarm information{%- endif -%}
        high: >-
          {%- set s = state_attr('binary_sensor.motion_alarm','high') -%}
          {%- if s|lower != 'none' and not s is string and s is iterable -%}
          {%- for i in s if i|count > 3 and 'light' in i[2] -%}
            {{ i[3] }}
          {%- else -%}<h2>No light <i>high</i> alarm{%- endfor -%}
          {%- else -%}<h2>No light <i>high</i> alarm information{%- endif -%}
        activity: >-
          {%- set s = state_attr('binary_sensor.motion_alarm','activity') -%}
          {%- if s|lower != 'none' and not s is string and s is iterable -%}
          {%- for i in s if i|count > 3 and 'light' in i[2] -%}
            {{ i[3] }}
          {%- else -%}<h2>No light <i>activity</i> alarm{%- endfor -%}
          {%- else -%}<h2>No light <i>activity</i> alarm information{%- endif -%}
        missing: >-
          {%- set s = state_attr('binary_sensor.motion_alarm','missing') -%}
          {%- if s|lower != 'none' and not s is string and s is iterable -%}
          {%- for i in s if i|count > 3 and 'light' in i[2] -%}
            {{ i[3] }}
          {%- else -%}<h2>No light <i>missing</i> alarm{%- endfor -%}
          {%- else -%}<h2>No light <i>missing</i> alarm information{%- endif -%}
        unavailable: >-
          {%- set s = state_attr('binary_sensor.motion_alarm','unavailble') -%}
          {%- if s|lower != 'none' and not s is string and s is iterable -%}
          {%- for i in s if i|count > 3 and 'light' in i[2] -%}
            {{ i[3] }}
          {%- else -%}<h2>No light <i>unavailable</i> alarm{%- endfor -%}
          {%- else -%}<h2>No light <i>unavailable</i> alarm information{%- endif -%}
        found: >-
          {%- set s = state_attr('binary_sensor.motion_alarm','found') -%}
          {%- if s|lower != 'none' and not s is string and s is iterable -%}
          {%- for i in s if i|count > 3 and 'light' in i[2] -%}
            {{ i[3] }}
          {%- else -%}<h2>No light <i>found</i> alarm{%- endfor -%}
          {%- else -%}<h2>No light <i>found</i> alarm information{%- endif -%}
      value_template: >-
        {%- set s = state_attr('binary_sensor.motion_alarm','all') -%}
        {%- if s|lower != 'none' and not s is string and s is iterable -%}
          {%- for i in s if i|count > 0 and 'light' in i[0] -%}
            {%- if loop.first -%}{{- 'on' -}}{%- endif -%}
          {%- else -%}{{- 'off' -}}{%- endfor -%}
        {%- else -%}{{- 'unknown' -}}{%- endif -%}
