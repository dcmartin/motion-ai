###
# homeassistant/binary_sensor/alarm/class/vibration.yaml
###

- platform: template
  sensors:
    motion_alarm_vibration:
      friendly_name: 'Alarm: vibration'
      attribute_templates:
        bad: >-
          {%- set s = state_attr('binary_sensor.motion_alarm','bad') -%}
          {%- if s|lower != 'none' and not s is string and s is iterable -%}
          {%- for i in s if i|count > 3 and 'vibration' in i[2] -%}
            {{ i[3] }}
          {%- else -%}<h2>No vibration <i>bad</i> alarm{%- endfor -%}
          {%- else -%}<h2>No vibration <i>bad</i> alarm information{%- endif -%}
        good: >-
          {%- set s = state_attr('binary_sensor.motion_alarm','good') -%}
          {%- if s|lower != 'none' and not s is string and s is iterable -%}
          {%- for i in s if i|count > 3 and 'vibration' in i[2] -%}
            {{ i[3] }}
          {%- else -%}<h2>No vibration <i>good</i> alarm{%- endfor -%}
          {%- else -%}<h2>No vibration <i>good</i> alarm information{%- endif -%}
        low: >-
          {%- set s = state_attr('binary_sensor.motion_alarm','low') -%}
          {%- if s|lower != 'none' and not s is string and s is iterable -%}
          {%- for i in s if i|count > 3 and 'vibration' in i[2] -%}
            {{ i[3] }}
          {%- else -%}<h2>No vibration <i>low</i> alarm{%- endfor -%}
          {%- else -%}<h2>No vibration <i>low</i> alarm information{%- endif -%}
        high: >-
          {%- set s = state_attr('binary_sensor.motion_alarm','high') -%}
          {%- if s|lower != 'none' and not s is string and s is iterable -%}
          {%- for i in s if i|count > 3 and 'vibration' in i[2] -%}
            {{ i[3] }}
          {%- else -%}<h2>No vibration <i>high</i> alarm{%- endfor -%}
          {%- else -%}<h2>No vibration <i>high</i> alarm information{%- endif -%}
        activity: >-
          {%- set s = state_attr('binary_sensor.motion_alarm','activity') -%}
          {%- if s|lower != 'none' and not s is string and s is iterable -%}
          {%- for i in s if i|count > 3 and 'vibration' in i[2] -%}
            {{ i[3] }}
          {%- else -%}<h2>No vibration <i>activity</i> alarm{%- endfor -%}
          {%- else -%}<h2>No vibration <i>activity</i> alarm information{%- endif -%}
        missing: >-
          {%- set s = state_attr('binary_sensor.motion_alarm','missing') -%}
          {%- if s|lower != 'none' and not s is string and s is iterable -%}
          {%- for i in s if i|count > 3 and 'vibration' in i[2] -%}
            {{ i[3] }}
          {%- else -%}<h2>No vibration <i>missing</i> alarm{%- endfor -%}
          {%- else -%}<h2>No vibration <i>missing</i> alarm information{%- endif -%}
        unavailable: >-
          {%- set s = state_attr('binary_sensor.motion_alarm','unavailble') -%}
          {%- if s|lower != 'none' and not s is string and s is iterable -%}
          {%- for i in s if i|count > 3 and 'vibration' in i[2] -%}
            {{ i[3] }}
          {%- else -%}<h2>No vibration <i>unavailable</i> alarm{%- endfor -%}
          {%- else -%}<h2>No vibration <i>unavailable</i> alarm information{%- endif -%}
        found: >-
          {%- set s = state_attr('binary_sensor.motion_alarm','found') -%}
          {%- if s|lower != 'none' and not s is string and s is iterable -%}
          {%- for i in s if i|count > 3 and 'vibration' in i[2] -%}
            {{ i[3] }}
          {%- else -%}<h2>No vibration <i>found</i> alarm{%- endfor -%}
          {%- else -%}<h2>No vibration <i>found</i> alarm information{%- endif -%}
      value_template: >-
        {%- set s = state_attr('binary_sensor.motion_alarm','all') -%}
        {%- if s|lower != 'none' and not s is string and s is iterable -%}
          {%- for i in s if i|count > 0 and 'vibration' in i[0] -%}
            {%- if loop.first -%}{{- 'on' -}}{%- endif -%}
          {%- else -%}{{- 'off' -}}{%- endfor -%}
        {%- else -%}{{- 'unknown' -}}{%- endif -%}
