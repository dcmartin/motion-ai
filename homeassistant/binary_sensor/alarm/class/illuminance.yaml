###
# homeassistant/binary_sensor/alarm/class/illuminance.yaml
###

- platform: template
  sensors:
    motion_alarm_illuminance:
      friendly_name: 'Alarm: illuminance'
      attribute_templates:
        bad: >-
          {%- set s = state_attr('binary_sensor.motion_alarm','bad') -%}
          {%- if s|lower != 'none' and not s is string and s is iterable -%}
          {%- for i in s if i|count > 3 and 'illuminance' in i[2] -%}
            {{ i[3] }}
          {%- else -%}No illuminance <i>bad</i> alarm{%- endfor -%}
          {%- else -%}No illuminance <i>bad</i> alarm information{%- endif -%}
        good: >-
          {%- set s = state_attr('binary_sensor.motion_alarm','good') -%}
          {%- if s|lower != 'none' and not s is string and s is iterable -%}
          {%- for i in s if i|count > 3 and 'illuminance' in i[2] -%}
            {{ i[3] }}
          {%- else -%}No illuminance <i>good</i> alarm{%- endfor -%}
          {%- else -%}No illuminance <i>good</i> alarm information{%- endif -%}
        low: >-
          {%- set s = state_attr('binary_sensor.motion_alarm','low') -%}
          {%- if s|lower != 'none' and not s is string and s is iterable -%}
          {%- for i in s if i|count > 3 and 'illuminance' in i[2] -%}
            {{ i[3] }}
          {%- else -%}No illuminance <i>low</i> alarm{%- endfor -%}
          {%- else -%}No illuminance <i>low</i> alarm information{%- endif -%}
        high: >-
          {%- set s = state_attr('binary_sensor.motion_alarm','high') -%}
          {%- if s|lower != 'none' and not s is string and s is iterable -%}
          {%- for i in s if i|count > 3 and 'illuminance' in i[2] -%}
            {{ i[3] }}
          {%- else -%}No illuminance <i>high</i> alarm{%- endfor -%}
          {%- else -%}No illuminance <i>high</i> alarm information{%- endif -%}
        activity: >-
          {%- set s = state_attr('binary_sensor.motion_alarm','activity') -%}
          {%- if s|lower != 'none' and not s is string and s is iterable -%}
          {%- for i in s if i|count > 3 and 'illuminance' in i[2] -%}
            {{ i[3] }}
          {%- else -%}No illuminance <i>activity</i> alarm{%- endfor -%}
          {%- else -%}No illuminance <i>activity</i> alarm information{%- endif -%}
        missing: >-
          {%- set s = state_attr('binary_sensor.motion_alarm','missing') -%}
          {%- if s|lower != 'none' and not s is string and s is iterable -%}
          {%- for i in s if i|count > 3 and 'illuminance' in i[2] -%}
            {{ i[3] }}
          {%- else -%}No illuminance <i>missing</i> alarm{%- endfor -%}
          {%- else -%}No illuminance <i>missing</i> alarm information{%- endif -%}
        unavailable: >-
          {%- set s = state_attr('binary_sensor.motion_alarm','unavailble') -%}
          {%- if s|lower != 'none' and not s is string and s is iterable -%}
          {%- for i in s if i|count > 3 and 'illuminance' in i[2] -%}
            {{ i[3] }}
          {%- else -%}No illuminance <i>unavailable</i> alarm{%- endfor -%}
          {%- else -%}No illuminance <i>unavailable</i> alarm information{%- endif -%}
        found: >-
          {%- set s = state_attr('binary_sensor.motion_alarm','found') -%}
          {%- if s|lower != 'none' and not s is string and s is iterable -%}
          {%- for i in s if i|count > 3 and 'illuminance' in i[2] -%}
            {{ i[3] }}
          {%- else -%}No illuminance <i>found</i> alarm{%- endfor -%}
          {%- else -%}No illuminance <i>found</i> alarm information{%- endif -%}
      value_template: >-
        {%- set s = state_attr('binary_sensor.motion_alarm','all') -%}
        {%- if s|lower != 'none' and not s is string and s is iterable -%}
          {%- for i in s if i|count > 0 and 'illuminance' in i[0] -%}
            {%- if loop.first -%}{{- 'on' -}}{%- endif -%}
          {%- else -%}{{- 'off' -}}{%- endfor -%}
        {%- else -%}{{- 'unknown' -}}{%- endif -%}
