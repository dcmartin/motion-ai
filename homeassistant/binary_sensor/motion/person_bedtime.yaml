###
# binary_sensor/motion/person_bedtime.yaml
###

- platform: template
  sensors:
    motion_person_bedtime:
      attribute_templates:
        timestamp: >-
          {# timestamp of last person_active during bedtime interval; set through automation #}
          {{ 'none' }}
        begin: >-
          {# timestamp of bedtime interval begin; set through automation #}
          {{ 'none' }}
        end: >-
          {# timestamp of bedtime interval end; set through automation #}
          {{ 'none' }}
      value_template: >-
        {# person_active during bedtime interval; set through automation #}
        {{ 'unknown' }}
    motion_person_night:
      friendly_name: 'Person night'
      icon_template: 'mdi:clock'
      attribute_templates:
        start: >-
          {# history_stats start timestamp; set through automation #}
          {{ 'none' }}
        end: >-
          {# history_stats end timestamp; set through automation #}
          {{ 'none' }}
        count: >-
          {# history_stats count at end; set through automation #}
          {{ 'none' }}
        time: >-
          {# history_stats time at end; set through automation #}
          {{ 'none' }}
      value_template: >-
        {# set through automation #}
        {{ 'unknown' }}
