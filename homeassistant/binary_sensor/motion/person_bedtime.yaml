###
# binary_sensor/motion/person_bedtime.yaml
###

- platform: template
  sensors:
    motion_person_bedtime:
      attribute_templates:
        timestamp: >-
          {# set through automation #}
          {{ 'none' }}
        today: >-
          {# set through automation #}
          {{ 'none' }}
        begin: >-
          {# set through automation #}
          {{ 'none' }}
        end: >-
          {# set through automation #}
          {{ 'none' }}
      value_template: >-
        {# set through automation #}
        {{ 'unknown' }}
    motion_person_night:
      friendly_name: 'Person night'
      icon_template: 'mdi:clock'
      attribute_templates:
        start: >-
          {# set through automation #}
          {{ 'none' }}
        end: >-
          {# set through automation #}
          {{ 'none' }}
        count: >-
          {# set through automation #}
          {{ 'none' }}
        time: >-
          {# set through automation #}
          {{ 'none' }}
    value_template: >-
      {# set through automation #}
      {{ 'unknown' }}
