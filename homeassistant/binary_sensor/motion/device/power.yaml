###
# homeassistant/binary_sensor/power.yaml
###

- platform: template
  sensors:
    motion_device_powers:
      value_template: >-
        {% set s = state_attr('binary_sensor.motion_device_power','total') %}
        {% if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'null' %}
          {{ s|int(0) > 0 }}
        {% else %}unknown{% endif %}
    motion_device_power:
      unique_id: motion_device_power
      friendly_name: Power status
      device_class: 'power'
      attribute_templates:
        ids: >-
          {{- 'unknown' -}}
        total: >-
          {{- 'unknown' -}}
        measurement: >-
          {{- 'unknown' -}}
        status: >-
          {{- 'unknown' -}}
        update: >-
          {{- 'unknown' -}}
        restart: >-
          {{- 'unknown' -}}
        controls: >-
          {{- 'unknown' -}}
        values: >-
          {{- 'unknown' -}}
        positive: >-
          {{- 'unknown' -}}
        negative: >-
          {{- 'unknown' -}}
      value_template: >-
        {{- 'unknown' -}}
