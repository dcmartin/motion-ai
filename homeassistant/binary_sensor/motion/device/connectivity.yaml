###
# homeassistant/binary_sensor/connectivity.yaml
###

- platform: template
  sensors:
    motion_device_connectivitys:
      value_template: >-
        {% set s = state_attr('binary_sensor.motion_device_connectivity','total') %}
        {% if s|lower != 'null' and s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'unknown' %}
          {{ s|int(0) > 0 }}
        {% else %}unknown{% endif %}
    motion_device_connectivity:
      friendly_name: Connectivity status
      device_class: 'connectivity'
      attribute_templates:
        ids: >-
          {{- 'unknown' -}}
        total: >-
          {{- 'unknown' -}}
        status: >-
          {{- 'unknown' -}}
        update: >-
          {{- 'unknown' -}}
        restart: >-
          {{- 'unknown' -}}
        controls: >-
          {{- 'unknown' -}}
        values: >-
          {{- 'unknown' -}}
        positive: >-
          {{- 'unknown' -}}
        negative: >-
          {{- 'unknown' -}}
      value_template: >-
        {{- 'unknown' -}}
