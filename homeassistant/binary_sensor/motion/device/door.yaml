###
# homeassistant/binary_sensor/door.yaml
###

- platform: template
  sensors:
    motion_device_doors:
      value_template: >-
        {% set s = state_attr('binary_sensor.motion_device_door','total') %}
        {% if s|lower != 'unknown' and s|lower != 'none' and s|lower != 'unavailable' and s|lower != 'null' %}
          {{ s|int(0) > 0 }}
        {% else %}unknown{% endif %}
    motion_device_door:
      unique_id: motion_device_door
      friendly_name: Door status
      device_class: 'motion'
      attribute_templates:
        ids: >-
          {{- 'unknown' -}}
        total: >-
          {{- 'unknown' -}}
        update: >-
          {{- 'unknown' -}}
        restart: >-
          {{- 'unknown' -}}
        controls: >-
          {{- 'unknown' -}}
        status: >-
          {{- 'unknown' -}}
        values: >-
          {{- 'unknown' -}}
        positive: >-
          {{- 'unknown' -}}
        negative: >-
          {{- 'unknown' -}}
      value_template: >-
        {# set through automation #}
        {{- 'unknown' -}}
