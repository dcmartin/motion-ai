
O
###
# binary_sensor/motion/person_wakeup.yaml
###

- platform: template
  sensors:
    motion_person_wakeup:
      attribute_templates:
        timestamp: >-
          {# set through automation #}
          {{ 'none' }}
        today: >-
          {# set through automation #}
          {{ 'none' }}
      value_template: >-
        {# set through automation #}
        {{ 'unknown' }}
    motion_person_day:
      friendly_name: 'Person day'
      icon_template: 'mdi:clock'
      attribute_templates:
        start: >-
          {# set through automation #}
          {{ 'none' }}
        end: >-
          {# set through automation #}
          {{ 'none' }}
        count: >-
          {# set through automation #}
          {{ 'none' }}
        time: >-
          {# set through automation #}
          {{ 'none' }}
    value_template: >-
      {# set through automation #}
      {{ 'unknown' }}
