###
# binary_sensor/motion/person_wakeup.yaml
###

- platform: template
  sensors:
    motion_person_wakeup:
      attribute_templates:
        timestamp: >-
          {# timestamp of first person_active during wakeup interval; set through automation #}
          {{ 'none' }}
        begin: >-
          {# timestamp of wakeup interval begin; set through automation #}
          {{ 'none' }}
        end: >-
          {# timestamp of wakeup interval end; set through automation #}
          {{ 'none' }}
      value_template: >-
        {# person_active during wakeup interval; set through automation #}
        {{ 'unknown' }}
    motion_person_day:
      friendly_name: 'Person day'
      icon_template: 'mdi:clock'
      attribute_templates:
        start: >-
          {# history_stats start timestamp; set through automation #}
          {{ 'none' }}
        end: >-
          {# history_stats end timestamp; set through automation #}
          {{ 'none' }}
        count: >-
          {# history_stats count at end; set through automation #}
          {{ 'none' }}
        time: >-
          {# history_stats time at end; set through automation #}
          {{ 'none' }}
      value_template: >-
        {# set through automation #}
        {{ 'unknown' }}
