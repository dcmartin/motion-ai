###
# binary_sensor/motion/person_night.yaml
###

- platform: template
  sensors:
    motion_person_night:
      friendly_name: 'Person night'
      icon_template: 'mdi:clock'
      attribute_templates:
        begin: >-
          {# set through automation #}
          {{ 'null' }}
        end: >-
          {# set through automation #}
          {{ 'null' }}
        count: >-
          {# set through automation #}
          {{ 'null' }}
        time: >-
          {# set through automation #}
          {{ 'null' }}
      value_template: >-
        {# set through automation #}
        {{ 'unknown' }}
