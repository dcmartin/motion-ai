###
# homeassistant/shell_command/reload.yaml
###

motion_reset: >-
  cd /config/.storage && rm -f core.entity_registry core.restore_state core.device_registry core.area_registry && echo true || echo false
