###
### startup SENSOR
###

- platform: mqtt
  qos: 1
  name: startup_event
  state_topic: 'startup'
  force_update: true
  expire_after: 30
  json_attributes:
    - startup
  value_template: >
    {%- if value_json is defined -%}true{%- else -%}false{%- endif -%}

## status 
- platform: template
  sensors:
    startup_status:
      value_template: >-
        {% if states.sensor.startup_event != none
              and state_attr('sensor.startup_event','startup') != none
              and state_attr('sensor.startup_event','startup').date|int(0) > 0 %}
          From {{ states.sensor.startup_latest_id.state }}
        {% else %}None{% endif %}

## counting
- platform: template
  sensors:
    startup_counter:
      unit_of_measurement: count
      value_template: >
        {% if states.counter.startup_counter != none %}
          {{ states.counter.startup_counter.state|int(0) }}
        {%- else -%}None{%- endif -%}

# counter
- platform: statistics
  name: startup_counter_statistics
  entity_id: sensor.startup_counter
  sampling_size: 100
  max_age:
    hours: 2

## timestamp
- platform: template
  sensors:
    startup_timestamp:
      value_template: >-
        {% if states.sensor.startup_event != none and state_attr('sensor.startup_event','startup') != none %}
          {{ state_attr('sensor.startup_event','startup').timestamp }}
        {% else %} null {% endif %}

## timing
- platform: template
  sensors:
    startup_date:
      unit_of_measurement: 's'
      value_template: >
        {% if states.sensor.startup_event != none and state_attr('sensor.startup_event','startup') != none %}
          {{ state_attr('sensor.startup_event','startup').date|int(0) }}
        {% else %} null {% endif %}
    startup_when:
      value_template: >
        {% if states.sensor.startup_date != none and states.sensor.startup_date.state != 'none' %}
          {{ states.sensor.startup_date.state|int|timestamp_custom("%a %b %d ~ %I:%M %p %Z",true,'unknown') }}
        {% else %}None{% endif %}

## ago
- platform: template
  sensors:
    startup_ago:
      unit_of_measurement: 's'
      value_template: >
        {% set t = states('sensor.time') %}
        {% if states.sensor.startup_event != none
              and state_attr('sensor.startup_event','startup') != none %}
          {{ (now().timestamp()|int(0)) - (state_attr('sensor.startup_event','startup').date|int(0)) }}
        {%- else -%}None{%- endif -%}
    # mean ago
    startup_ago_mean:
      unit_of_measurement: 's'
      value_template: >
        {% if states.sensor.startup_ago_statistics != none %}
          {{ states.sensor.startup_ago_statistics.state|float(0.0) }}
        {%- else -%}None{%- endif -%}
    startup_ago_min:
      unit_of_measurement: 's'
      value_template: >
        {% if states.sensor.startup_ago_statistics != none %}
          {{ state_attr('sensor.startup_ago_statistics','min_value')|int(0) }}
        {%- else -%}None{%- endif -%}
    startup_ago_max:
      unit_of_measurement: 's'
      value_template: >
        {% if states.sensor.startup_ago_statistics != none %}
          {{ state_attr('sensor.startup_ago_statistics','max_value')|int(0) }}
        {%- else -%}None{%- endif -%}

# ago
- platform: statistics
  name: startup_ago_statistics
  entity_id: sensor.startup_ago
  sampling_size: 100
  max_age:
    hours: 2

## location
- platform: template
  sensors:
    startup_latitude:
      unit_of_measurement: degrees
      value_template: >
        {% if states.sensor.startup_event != none
              and state_attr('sensor.startup_event','startup') != none
              and state_attr('sensor.startup_event','startup').activity|length > 0 %}
          {{ (state_attr('sensor.startup_event','startup').activity|first).latitude|float(0.0) }}
        {%- else -%}None{%- endif -%}
    startup_longitude:
      unit_of_measurement: degrees
      value_template: >
        {% if states.sensor.startup_event != none
              and state_attr('sensor.startup_event','startup') != none
              and state_attr('sensor.startup_event','startup').activity|length > 0 %}
          {{ (state_attr('sensor.startup_event','startup').activity|first).longitude|float(0.0) }}
        {%- else -%}None{%- endif -%}

- platform: template
  sensors:
    startup_period:
      unit_of_measurement: 's'
      value_template: >
        {% if states.sensor.startup_event != none
              and state_attr('sensor.startup_event','startup') != none %}
          {{ state_attr('sensor.startup_event','startup').period }}
        {% else %} null {% endif %}

## latest activity details
- platform: template
  sensors:
    startup_latest_id:
      value_template: >
        {% if states.sensor.startup_event != none
              and state_attr('sensor.startup_event','startup') != none
              and state_attr('sensor.startup_event','startup').activity|length > 0 %}
          {{ (state_attr('sensor.startup_event','startup').activity|first).id }}
        {%- else -%}None{%- endif -%}
    startup_latest_date:
      unit_of_measurement: 's'
      value_template: >
        {% if states.sensor.startup_event != none
              and state_attr('sensor.startup_event','startup') != none
              and state_attr('sensor.startup_event','startup').activity|length > 0 %}
          {{ (state_attr('sensor.startup_event','startup').activity|first).date }}
        {%- else -%}None{%- endif -%}
    startup_latest_measurement:
      unit_of_measurement: count
      value_template: >
        {% if states.sensor.startup_event != none
              and state_attr('sensor.startup_event','startup') != none
              and state_attr('sensor.startup_event','startup').activity|length > 0 %}
          {{ (state_attr('sensor.startup_event','startup').activity|first).count }}
        {%- else -%}None{%- endif -%}
    startup_latest_interval:
      unit_of_measurement: 's'
      value_template: >
        {% if states.sensor.startup_event != none
              and state_attr('sensor.startup_event','startup') != none
              and state_attr('sensor.startup_event','startup').activity|length > 0 %}
          {{ (state_attr('sensor.startup_event','startup').activity|first).interval }}
        {%- else -%}None{%- endif -%}
    startup_latest_timestamp:
      value_template: >
        {% if states.sensor.startup_event != none
              and state_attr('sensor.startup_event','startup') != none
              and state_attr('sensor.startup_event','startup').activity|length > 0 %}
          {{ (state_attr('sensor.startup_event','startup').activity|first).timestamp }}
        {%- else -%}None{%- endif -%}
    startup_latest_ago:
      unit_of_measurement: 's'
      value_template: >
        {% set t = states('sensor.time') %}
        {% if states.sensor.startup_event != none
              and state_attr('sensor.startup_event','startup') != none
              and state_attr('sensor.startup_event','startup').activity|length > 0 %}
          {{ now().timestamp()|int(0) - as_timestamp(strptime((state_attr('sensor.startup_event','startup').activity|first).timestamp, "%FT%TZ"))|int(0) }}
        {%- else -%}None{%- endif -%}

- platform: template
  sensors:
    startup_latest_average:
      unit_of_measurement: count
      value_template: >
        {% if states.sensor.startup_event != none
              and state_attr('sensor.startup_event','startup') != none
              and state_attr('sensor.startup_event','startup').activity|length > 0 %}
          {{ (state_attr('sensor.startup_event','startup').activity|first).average }}
        {%- else -%}None{%- endif -%}

# client containers (average)
- platform: statistics
  name: startup_container_average_statistics
  entity_id: sensor.startup_latest_average
  sampling_size: 100
  max_age:
    hours: 24
- platform: statistics
  name: startup_container_average_stdev_statistics
  entity_id: sensor.startup_container_average_stdev
  sampling_size: 100
  max_age:
    hours: 2

# ratio of machines whose container count is average

## product from hal
- platform: template
  sensors:
    startup_latest_product:
      value_template: >
        {% if states.sensor.startup_event != none
              and state_attr('sensor.startup_event','startup').activity|length > 0 %}
          {{ (state_attr('sensor.startup_event','startup').activity|first).product }}
        {%- else -%}None{%- endif -%}
    startup_latest_product_type:
      unit_of_measurement: product_type
      value_template: >
        {% if states.sensor.startup_latest_product != none %}
          {% if "Raspberry Pi 3 Model B Plus" in states.sensor.startup_latest_product.state %}
            {{ 'raspberrypi-3bp' }}
          {% elif "Raspberry Pi 3 Model B" in states.sensor.startup_latest_product.state %}
            {{ 'raspberrypi-3b' }}
          {% elif "jetson-nano" in states.sensor.startup_latest_product.state %}
            {{ 'jetson-nano' }}
          {% elif "quill" in states.sensor.startup_latest_product.state %}
            {{ 'jetson-tx2' }}
          {% elif "VirtualBox" in states.sensor.startup_latest_product.state %}
            {{ 'virtualbox' }}
          {% elif "HVM" in states.sensor.startup_latest_product.state %}
            {{ 'cloudvm' }}
          {% else %}
            {{ 'other' }}
          {% endif %}
        {%- else -%}None{%- endif -%}

## client count
- platform: template
  sensors:
    startup_client_count:
      unit_of_measurement: count
      value_template: >
        {% if states.sensor.startup_event != none
              and state_attr('sensor.startup_event','startup') != none %}
          {{ state_attr('sensor.startup_event','startup').activity|length }}
        {%- else -%}None{%- endif -%}
    # statistics
    startup_client_count_mean:
      unit_of_measurement: count
      value_template: >
        {% if states.sensor.startup_client_count_statistics != none %}
          {{ states.sensor.startup_client_count_statistics.state }}
        {%- else -%}None{%- endif -%}
    startup_client_count_min:
      unit_of_measurement: count
      value_template: >
        {% if states.sensor.startup_client_count_statistics != none %}
          {{ state_attr('sensor.startup_client_count_statistics','min_value') }}
        {%- else -%}None{%- endif -%}
    startup_client_count_max:
      unit_of_measurement: count
      value_template: >
        {% if states.sensor.startup_client_count_statistics != none %}
          {{ state_attr('sensor.startup_client_count_statistics','max_value') }}
        {%- else -%}None{%- endif -%}
    startup_client_count_stdev:
      unit_of_measurement: count
      value_template: >
        {% if states.sensor.startup_client_count_statistics != none %}
          {% if state_attr('sensor.startup_client_count_statistics','standard_deviation') != 'unknown' %}
            {{ state_attr('sensor.startup_client_count_statistics','standard_deviation') }}
          {%- else -%}None{%- endif -%}
        {%- else -%}None{%- endif -%}

# client count
- platform: statistics
  name: startup_client_count_statistics
  entity_id: sensor.startup_client_count
  sampling_size: 100
  max_age:
    hours: 24
- platform: statistics
  name: startup_client_count_stdev_statistics
  entity_id: sensor.startup_client_count_stdev
  sampling_size: 100
  max_age:
    hours: 24

## client percent
- platform: template
  sensors:
    startup_client_percent:
      unit_of_measurement: '%'
      value_template: >-
        {% if states.sensor.startup_client_count != none
              and states.sensor.horizon_organization_nodes_count != none
              and states.sensor.horizon_organization_nodes_count.state != null
              and states.sensor.horizon_organization_nodes_count != none
              and states.sensor.horizon_organization_nodes_count.state|int(0) > 0 %}
          {{ '%0.2f' | format(states.sensor.startup_client_count.state|float(0.0) / states.sensor.horizon_organization_nodes_count.state|float(0.0) * 100.0 ) }}
        {% else %} null {% endif %}
    # statistics
    startup_client_percent_mean:
      unit_of_measurement: '%'
      value_template: >
        {% if states.sensor.startup_client_percent_statistics != none %}
          {{ states.sensor.startup_client_percent_statistics.state }}
        {%- else -%}None{%- endif -%}
    startup_client_percent_min:
      unit_of_measurement: '%'
      value_template: >
        {% if states.sensor.startup_client_percent_statistics != none %}
          {{ state_attr('sensor.startup_client_percent_statistics','min_value') }}
        {%- else -%}None{%- endif -%}
    startup_client_percent_max:
      unit_of_measurement: '%'
      value_template: >
        {% if states.sensor.startup_client_percent_statistics != none %}
          {{ state_attr('sensor.startup_client_percent_statistics','max_value') }}
        {%- else -%}None{%- endif -%}
    startup_client_percent_stdev:
      unit_of_measurement: '%'
      value_template: >
        {% if states.sensor.startup_client_percent_statistics != none %}
          {{ state_attr('sensor.startup_client_percent_statistics','standard_deviation') }}
        {%- else -%}None{%- endif -%}

# client percent
- platform: statistics
  name: startup_client_percent_stdev_statistics
  entity_id: sensor.startup_client_percent_stdev
  sampling_size: 100
  max_age:
    hours: 24
- platform: statistics
  name: startup_client_percent_statistics
  entity_id: sensor.startup_client_percent
  sampling_size: 100
  max_age:
    hours: 24


## containers average
- platform: template
  sensors:
    startup_container_average_mean:
      unit_of_measurement: count
      value_template: >
        {% if states.sensor.startup_container_average_statistics != none %}
          {{ states.sensor.startup_container_average_statistics.state }}
        {%- else -%}None{%- endif -%}
    startup_container_average_min:
      unit_of_measurement: count
      value_template: >
        {% if states.sensor.startup_container_average_statistics != none %}
          {{ state_attr('sensor.startup_container_average_statistics','min_value')|float(0.0) }}
        {%- else -%}None{%- endif -%}
    startup_container_average_max:
      unit_of_measurement: count
      value_template: >
        {% if states.sensor.startup_container_average_statistics != none %}
          {{ state_attr('sensor.startup_container_average_statistics','max_value')|float(0.0) }}
        {%- else -%}None{%- endif -%}
    startup_container_average_stdev:
      unit_of_measurement: count
      value_template: >
        {% if states.sensor.startup_container_average_statistics != none %}
          {% if state_attr('sensor.startup_container_average_statistics','standard_deviation') != 'unknown' %}
            {{ state_attr('sensor.startup_container_average_statistics','standard_deviation') }}
          {%- else -%}None{%- endif -%}
        {%- else -%}None{%- endif -%}

## download speed from wan
- platform: template
  sensors:
    startup_latest_download:
      unit_of_measurement: Mbps
      value_template: >
        {% if states.sensor.startup_event != none
              and state_attr('sensor.startup_event','startup') != none
              and state_attr('sensor.startup_event','startup').activity|length > 0 %}
           {% set result = (state_attr('sensor.startup_event','startup').activity|first).download %}
           {% if result != null %}
              {{ '%0.2f' | format(result|float(0.0) / 1000000.0) }}
           {% else %}
              {{ '%0.2f' | format(0.0 / 1000000.0) }}
           {% endif %}
        {%- else -%}None{%- endif -%}
    # statistics
    startup_download_mean:
      unit_of_measurement: Mbps
      value_template: >
        {% if states.sensor.startup_download_statistics != none %}
          {{ states.sensor.startup_download_statistics.state }}
        {%- else -%}None{%- endif -%}
    startup_download_min:
      unit_of_measurement: Mbps
      value_template: >
        {% if states.sensor.startup_download_statistics != none %}
          {{ state_attr('sensor.startup_download_statistics','min_value')|float(0.0) }}
        {%- else -%}None{%- endif -%}
    startup_download_max:
      unit_of_measurement: Mbps
      value_template: >
        {% if states.sensor.startup_download_statistics != none %}
          {{ state_attr('sensor.startup_download_statistics','max_value')|float(0.0) }}
        {%- else -%}None{%- endif -%}
    startup_download_stdev:
      unit_of_measurement: Mbps
      value_template: >
        {% if states.sensor.startup_download_statistics != none %}
          {% if state_attr('sensor.startup_download_statistics','standard_deviation') != 'unknown' %}
            {{ state_attr('sensor.startup_download_statistics','standard_deviation') }}
          {%- else -%}None{%- endif -%}
        {%- else -%}None{%- endif -%}

# client download
- platform: statistics
  name: startup_download_statistics
  entity_id: sensor.startup_latest_download
  sampling_size: 100
  max_age:
    hours: 24
- platform: statistics
  name: startup_download_stdev_statistics
  entity_id: sensor.startup_download_stdev
  sampling_size: 100
  max_age:
    hours: 24

## measurements average
- platform: template
  sensors:
    startup_measurement_mean:
      unit_of_measurement: count
      value_template: >
        {% if states.sensor.startup_measurement_statistics != none %}
          {{ states.sensor.startup_measurement_statistics.state }}
        {%- else -%}None{%- endif -%}
    startup_measurement_min:
      unit_of_measurement: count
      value_template: >
        {% if states.sensor.startup_measurement_statistics != none %}
          {{ state_attr('sensor.startup_measurement_statistics','min_value')|float(0.0) }}
        {%- else -%}None{%- endif -%}
    startup_measurement_max:
      unit_of_measurement: count
      value_template: >
        {% if states.sensor.startup_measurement_statistics != none %}
          {{ state_attr('sensor.startup_measurement_statistics','max_value')|float(0.0) }}
        {%- else -%}None{%- endif -%}
    startup_measurement_stdev:
      unit_of_measurement: count
      value_template: >
        {% if states.sensor.startup_measurement_statistics != none %}
          {{ state_attr('sensor.startup_measurement_statistics','standard_deviation') }}
        {%- else -%}None{%- endif -%}

# client measurement
- platform: statistics
  name: startup_measurement_statistics
  entity_id: sensor.startup_latest_measurement
  sampling_size: 100
  max_age:
    hours: 24

## percent from cpu
- platform: template
  sensors:
    startup_latest_percent:
      unit_of_measurement: '%'
      value_template: >
        {% if states.sensor.startup_event != none
              and state_attr('sensor.startup_event','startup') != none
              and state_attr('sensor.startup_event','startup').activity|length > 0 %}
          {{ '%0.2f' | format((state_attr('sensor.startup_event','startup').activity|first).percent) }}
        {%- else -%}None{%- endif -%}
    startup_percent_mean:
      unit_of_measurement: '%'
      value_template: >
        {% if states.sensor.startup_percent_statistics is defined %}
          {{ states.sensor.startup_percent_statistics.state }}
        {%- else -%}None{%- endif -%}
    startup_percent_min:
      unit_of_measurement: '%'
      value_template: >
        {% if states.sensor.startup_percent_statistics is defined %}
          {{ state_attr('sensor.startup_percent_statistics','min_value')|float(0.0) }}
        {%- else -%}None{%- endif -%}
    startup_percent_max:
      unit_of_measurement: '%'
      value_template: >
        {% if states.sensor.startup_percent_statistics is defined %}
          {{ state_attr('sensor.startup_percent_statistics','max_value')|float(0.0) }}
        {%- else -%}None{%- endif -%}
    startup_percent_stdev:
      unit_of_measurement: '%'
      value_template: >
        {% if states.sensor.startup_percent_statistics is defined %}
          {{ state_attr('sensor.startup_percent_statistics','standard_deviation') }}
        {%- else -%}None{%- endif -%}
# client percent
- platform: statistics
  name: startup_percent_statistics
  entity_id: sensor.startup_latest_percent
  sampling_size: 100
  max_age:
    hours: 24

# hal product types

- platform: history_stats
  name: startup_product_ratio_rpi3bp
  entity_id: sensor.startup_latest_product_type
  state: 'raspberrypi-3bp'
  type: ratio
  start: '{{ now().replace(hour=0).replace(minute=0).replace(second=0) }}'
  end: '{{ now() }}'
- platform: history_stats
  name: startup_product_ratio_rpi3b
  entity_id: sensor.startup_latest_product_type
  state: 'raspberrypi-3b'
  type: ratio
  start: '{{ now().replace(hour=0).replace(minute=0).replace(second=0) }}'
  end: '{{ now() }}'
- platform: history_stats
  name: startup_product_ratio_nano
  entity_id: sensor.startup_latest_product_type
  state: 'jetson-nano'
  type: ratio
  start: '{{ now().replace(hour=0).replace(minute=0).replace(second=0) }}'
  end: '{{ now() }}'
- platform: history_stats
  name: startup_product_ratio_tx2
  entity_id: sensor.startup_latest_product_type
  state: 'jetson-tx2'
  type: ratio
  start: '{{ now().replace(hour=0).replace(minute=0).replace(second=0) }}'
  end: '{{ now() }}'
- platform: history_stats
  name: startup_product_ratio_virtualbox
  entity_id: sensor.startup_latest_product_type
  state: 'virtualbox'
  type: ratio
  start: '{{ now().replace(hour=0).replace(minute=0).replace(second=0) }}'
  end: '{{ now() }}'
- platform: history_stats
  name: startup_product_ratio_cloudvm
  entity_id: sensor.startup_latest_product_type
  state: 'cloudvm'
  type: ratio
  start: '{{ now().replace(hour=0).replace(minute=0).replace(second=0) }}'
  end: '{{ now() }}'
- platform: history_stats
  name: startup_product_ratio_other
  entity_id: sensor.startup_latest_product_type
  state: 'other'
  type: ratio
  start: '{{ now().replace(hour=0).replace(minute=0).replace(second=0) }}'
  end: '{{ now() }}'

## history

#- platform: history_stats
#  name: startup_containers_history
#  entity_id: binary_sensor.startup_containers_average
#  state: on
#  type: ratio 
#  start: '{{ now().replace(hour=0).replace(minute=0).replace(second=0) }}'
#  end: '{{ now() }}'
